# Backlog — 2026-05-17

## 목표

- 배포 패키지의 보안 기본값/환경변수 강제 정책을 정리한다.
- 외부 DB/번들 DB(local-db profile) 모드 모두에서 compose 검증이 일관되게 동작하는지 확인한다.
- main 최신 반영 상태에서 packaging 브랜치 후속 작업 기반을 정리한다.

## 작업 항목

- `TASK-DEPLOY-SECURE-DEFAULTS` (done)
  - 상태: `done`
  - 설명: deploy compose/IdP/runtime-config 보안 기본값 강화
  - 산출물:
    - `docker-compose.deploy.yml`
    - `infra/idp/hydra.deploy.yaml`
    - `frontend/app/api/runtime-config/route.ts`

- `TASK-DEPLOY-DB-MODE-VALIDATION` (done)
  - 상태: `done`
  - 설명: 외부 DB/번들 DB 모드 compose config 검증
  - 산출물: 검증 로그(세션 커맨드 기록)

- `TASK-MAIN-SYNC-CHECK` (done)
  - 상태: `done`
  - 설명: main 최신 상태 확인 후 packaging 브랜치에서 작업 재개
  - 산출물: session/handoff/state 갱신

## 검증 메모

- `docker compose -f docker-compose.deploy.yml config` (필수 env 누락 시 실패) 확인
- 외부 DB 모드 env 주입 후 `docker compose ... config -q` 통과
- `--profile local-db` + `@db` DSN env 주입 후 `docker compose ... config -q` 통과

## 메모

- `DB_URL`, `HYDRA_DSN`, `KRATOS_DSN`을 deploy compose에서 필수화(`:?set`)했다.
- `DEVHUB_AUTH_DEV_FALLBACK` 기본값은 `0` 유지.
- Hydra `system secret`는 하드코딩 제거 후 env 주입(`HYDRA_SYSTEM_SECRET`)으로 전환.
