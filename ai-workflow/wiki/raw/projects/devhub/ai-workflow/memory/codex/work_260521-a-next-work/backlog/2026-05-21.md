# Backlog — 2026-05-21

- 목적: 단일 포트(23000) 기준 외부 접속 URL/포트와 내부 서비스 포트 분리 구조에서 OIDC redirect 불일치가 재발하지 않도록 패키징 설정을 정리한다.
- 상태: done

## 오늘 완료

- [x] `DEVHUB_PUBLIC_BASE_URL` 기반으로 runtime redirect fallback 정합화
- [x] `NEXT_PUBLIC_APP_ORIGIN` 도입으로 브라우저 fallback redirect 생성 일관화
- [x] deploy compose/env/preflight 정합 강화
- [x] tailscale `http://100.90.113.29:23000` 기준 E2E 실행 (`51 passed`)
- [x] nginx/keycloak 로그에서 localhost redirect 흔적 미발견 확인

## 다음 액션

- [ ] 변경분 커밋/푸시 후 PR 생성

## 메모

- 외부 접속 포트(`23000`)와 내부 frontend 포트(`3000`)가 달라도 redirect URL은 public base URL 기준으로 유지된다.
