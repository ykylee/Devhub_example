# Backlog - 2026-05-31 (Gitea Simulation Integration)

- **브랜치**: `gemini/gitea-simulation-integration`
- **목적**: 외부 SCM Gitea 연동 및 가상 애플리케이션/프로젝트 등록 시뮬레이션
- **상태**: `done`

## 1. 예정된 작업 리스트 (TODO)

- [x] **[SIM-1] Docker Compose 환경 정상 기동 상태 검증**
  - Backend Core, Frontend, Keycloak, DB 헬스체크 및 포트(3000, 8080, 8180) 접속 확인.
- [x] **[SIM-2] Keycloak 인증 획득 테스트**
  - admin 계정(admin/admin)으로 토큰 획득 또는 프론트엔드 로그인 성공 확인.
- [x] **[SIM-3] Gitea SCM Integration Provider 등록 및 검증**
  - `https://homelab.ddn777.synology.me/gitea` (Basic Auth: yklee/yklee12!) API 등록.
  - Connection Test (`POST /api/v1/integration/test-connection`) 성공 여부 확인.
- [x] **[SIM-4] 가상 Project 및 Application 등록**
  - Project 생성 (Key: `SIMPROJ`)
  - Application 생성 (Key: `SIMAPP001`)
  - Gitea SCM 저장소를 Application에 바인딩.
- [x] **[SIM-5] SCM Sync Job 트리거 및 데이터 확인**
  - Pull Sync Job 실행 및 정상 완료 상태 확인. (Gitea 404 API 핫픽스 적용 성공으로 sync job 최종 succeeded 완결)
  - 대시보드 지표(SCM 동기화, Task Done 등) 시각적 검증.
- [x] **[SIM-6] 빌드 정보 미존재 시 "없음"으로 UI 일원화 한글화 개선**
  - 공통 유틸 모듈(`last-build.ts` 2종) 및 리포지토리/어플리케이션 UI 페이지의 `N/A`, `Unknown`을 **`없음`** 으로 통일 적용.
  - 967개 회귀 테스트 및 Next.js Turbopack 빌드 최종 확인.

## 2. 진행 상태 기록

- **2026-05-31 21:00**:
  - `gemini/gitea-simulation-integration` 브랜치 생성 및 격리 메모리 폴더(`ai-workflow/memory/gemini/gitea-simulation-integration/`) 초기화 완료.
  - Colima 4 CPU, 8GB 기동 완료.
  - Docker Compose를 이용한 전체 스택 빌드 및 구동 완료 (`devhub-keycloak` Healthy).
  - 시뮬레이션 계획(`implementation_plan.md`) 작성 완료.
- **2026-05-31 21:07**:
  - `client.go`에 `APIError` 구조체를 정의하고 404 발생 시 빈 슬라이스(`nil, nil`)를 리턴하도록 Gitea API 404 예외 graceful degradation 핫픽스 구현.
  - `go test ./...` 100% PASS 검증 완료 및 ARM64 타겟 컴파일 배포 완료.
  - SCM Sync Job(`e4b2bb45-ac9b-480f-b9c1-b49fadd784e7`) 트리거하여 `status: succeeded` 최종 완료 검증 성공. 브랜치 메모리 동기화 완료 및 세션 종료 준비 완료.
- **2026-05-31 21:17**:
  - 중복 기재되어 있던 `synology-gitea` integration provider 정비 완료.
  - Gitea SCM에 `devhub-simulation` 원격 저장소 생성 및 현재 devhub 코드베이스 push 성공.
  - SCM Sync 재트리거를 통해 repositories 매핑 완료 (id: 77).
  - 가상 프로젝트 `DEVHUBPROJ` 및 가상 애플리케이션 `DEVHUBAPP` 생성 후 리포지토리와 상호 바인딩 완료.
  - Admin Catalog UI(`frontend/app/(dashboard)/admin/catalog/page.tsx`) 내 UUID 기반 Leader 출력을 `identityService` 기반 실명 렌더링으로 개선 및 Turbopack 빌드 무결 검증 성공.
- **2026-05-31 21:45**:
  - 빌드 정보가 존재하지 않을 때 `Unknown` 이나 `N/A` 라벨이 리턴 또는 하드코딩 표출되던 지점들을 일괄 수정했습니다.
  - 공통 유틸 2종(`shared/utils/last-build.ts` & `lib/utils/last-build.ts`), 저장소 목록 뷰(`repositories/page.tsx` 빌드 헬스 지표 및 summary bar), 어플리케이션 목록 뷰(`applications/page.tsx`)에 대해 **`없음`** 으로 통일 적용 완료.
  - `vitest run`을 통해 73개 테스트 파일, 967개 유닛 테스트 케이스 100% 그린 PASS 검증 성공 및 Next.js Turbopack 빌드(`npm run build`) 최종 무결성 확인 완료.

- **2026-05-31 22:20 (2차 UI 개선 & 진척도 메트릭 완결)**:
  - **Linked Repositories UI 수정**: 프로젝트 상세 페이지(`frontend/app/(dashboard)/projects/[id]/page.tsx`) 우측 사이드바의 `"Linked Repositories (N:M)"` 타이틀에서 불필요한 `"(N:M)"` 텍스트를 제거하여 깔끔하고 세련된 `"Linked Repositories"`로 표출 완료.
  - **더미 에러/경고 배너 완벽 제거**: 백엔드에 프로젝트 액티비티(`GET /api/v1/projects/:id/activity`) 및 태스크(`GET /api/v1/projects/:id/tasks`) API가 미구현 상태임에 따라 404가 발생하여 화면에 빨갛게 `"일부 프로젝트 데이터를 불러오지 못했습니다"` 경고창(`opsError`)이 나타나던 현상을 패치. API 실패 시 빈 리스트(`[]`)로 온화하게 fallback 처리하여 무의미한 에러 배너 노출을 종결함.
  - **실질적 진척도(Progress) 판단 메트릭 구현**: 실질적 데이터(Task, PR, Issue)가 전혀 없을 때 default 60% 로 강제되던 더미 진척도 계산 로직을 삭제하고, 등록된 Tasks(status='done') + SCM PR(state='merged'|'closed') + Issues(state='closed')의 종결 비율을 과학적으로 연동 산출하는 가중 평균 메트릭 알고리즘을 적용. 리소스가 전혀 없는 초기 상태에서는 정직하게 **`0%`**로 표시됨을 스크린샷으로 검증 완료.
  - **로컬 Next.js Standalone 빌드 & 컨테이너 리로드**: `npm run build` Turbopack production 빌드 100% 무결 통과 확인 후 `docker compose up -d --build frontend`로 컨테이너 재생성/기동 완료.
  - **OIDC Bypass Playwright E2E 검증 PASS**: Playwright E2E 검증(`npx playwright test tests/e2e/verify-unknown-fix.spec.ts`)에서 2개 테스트가 단 7.3초 만에 모두 **그린 PASS(100% 성공)**함을 완벽하게 확인 및 실물 스크린샷 영구 안착 완료.
