# Backlog — 2026-06-10 (feat/work_260610-v1-1-sprint-a-followup)

- **sprint branch**: `feat/work_260610-v1-1-sprint-a-followup` (PR #539, MERGED main @ `87e6c1f5`)
- **session**: OpenCode (Sisyphus, `MiniMax-M3`)
- **당일 작업 시간대**: 2026-06-10 (single session, ~2h)

## 작업 시간순 요약

1. **sprint -a follow-up scope 확인** — 사용자 결정 2026-06-10: 본 PR = stub + main wiring (Recommended). real adapter 별도 PR.
2. **saovae_stub 작성** — `backend-core/internal/sso-integrations/keycloak/saovae_stub.go` (105 lines NEW). 4 port (BearerTokenVerifier + IdentityAdmin + OIDCLogoutClient + KeycloakEventPort) + webhook handler. `go build ./...` PASS.
3. **main.go wiring** — `var (idpAdmin httpapi.IdentityAdmin; oidcLogout httpapi.OIDCLogoutClient; keycloakEventPort integration.KeycloakEventPort)` + `DEVHUB_BUILD_TIER` env var 분기. default (사외) = saovae_stub; `=internal` 시 real KeycloakAdminClient.
4. **ports.go alias 통합** — `KeycloakUserEvent` / `KeycloakAdminEvent` mirror struct → `httpapi.KeycloakUserEvent` / `httpapi.KeycloakAdminEvent` alias. `*KeycloakAdminClient` 가 `KeycloakEventPort` 충족 위해 필수.
5. **view/ deprecation comment** — `view/auth.go:59` BearerTokenVerifier + `view/handler.go:27` IdentityAdmin + `view/handler.go:197` OIDCLogoutClient. canonical = `integration/`.
6. **build / test / fmt / lint 검증** — `go build ./...` PASS, `go test ./internal/domain/auth-session/...` PASS, `gofmt -l` clean, `bash scripts/check-openapi-yaml-lint.sh` PASS, `bash scripts/check-tier-separation.sh` no changes.
7. **commit + push + PR** — commit `a00793bc` (5 files, +238 -97), push `feat/work_260610-v1-1-sprint-a-followup` → origin, PR #539 생성.
8. **CI 모니터링** — 7/7 PASS (Backend Integration 1m15s, Backend Unit 1m8s, E2E Build 1m49s, E2E Playwright 1/3 4m0s, 2/3 4m2s, 3/3 5m30s, Detect Changed Paths 11s, Migration Prefix 7s, OpenAPI Lint 9s, Workflow Lint 13s, Frontend Unit skip).
9. **merge** — squash merge → main HEAD `87e6c1f5`. branch delete.
10. **memory 갱신** — 본 디렉터리 (state.json + session_handoff.md + work_backlog.md + backlog/2026-06-10.md) 작성.

## 사용자 결정 사항

- **PR scope**: sprint -a follow-up 본 PR = stub + main wiring (Recommended). real adapter 별도 PR.
- **Build 정책**: `//go:build` tag 미사용, runtime injection (단일 binary, `DEVHUB_BUILD_TIER` env var).
- **Default = 사외 (saovae_stub)**: env var 미설정 시 saovae_stub 자동 사용.
- **`=internal` 시 real KeycloakAdminClient**: 사내 staging/prod-smoke 검증.
- **Type alias**: ports.go 의 mirror struct 를 httpapi alias 로 통합. distinct type → alias.

## commit / PR

- **commit**: `a00793bc` (sprint -a follow-up, 5 files changed, +238 -97)
- **PR**: https://github.com/ykylee/Devhub_example/pull/539
- **merge**: main HEAD `87e6c1f5` (squash, branch delete)

## 후속 작업 (carry-over)

1. **C-a** real adapter: `sso-integrations/keycloak/{verifier,admin_client}.go` (P0)
2. **C-b** main.go event listener type assertion 정리 (P0)
3. **C-c** `_ = keycloakEventPort` placeholder 제거 (P0, C-b 완료 시)
4. **C-d** v1.0 mirror struct 제거: `httpapi.KeycloakUserEvent` / `httpapi.KeycloakAdminEvent` (P1)
5. **C-e** audit-ops 의 mirror 와 통합 (P1)
6. **C-f** `infra/idp/_archive_2026-06-10/` immutable archive (P1)
7. **C-g** traceability report.md IMPL-30/31/32 row 갱신 (P2)
8. **C-h** ADR-0030 §5 timeline 갱신 (P2)
9. **C-i** E2E test saovae_stub + real adapter CI matrix (P2)
10. **C-j** build tag 정책 재검토 (P3)
