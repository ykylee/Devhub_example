# Backlog — 2026-06-11 (N-13 implementation PR A)

## 오늘 완료

- N-13 backend foundation PR A. PR #547 (N-13 housekeeping, ID slot 9 row 발급) 의 코드 변경 본. PR A-2 (routing + voc_handler + openapi) 는 별도 sprint.
- 9 file / +382 -37줄:
  - `backend-core/migrations/000007_platform_inbound_source.{up,down}.sql` (신규)
  - `backend-core/internal/domain/application.go` (Platform 2 field)
  - `backend-core/internal/domain/application-lifecycle/repository/applications.go` (Scan/Insert/Update 정합 + 2 method)
  - `backend-core/internal/domain/application-lifecycle/view/handler.go` (PlatformStore interface +2 method)
  - `backend-core/internal/domain/application-lifecycle/view/applications.go` (UpdatePlatform inbound_source 별도 처리 + platformResponse echo)
  - `backend-core/internal/domain/application-lifecycle/view/applications_handler_test.go` (4 UT)
  - `backend-core/internal/domain/application-lifecycle/view/fake_store_test.go` (fake 2 method + json.Valid)
  - `backend-core/internal/httpapi/applications_test.go` (memoryPlatformStore 2 method + json import)

## 정공법 핵심

- **inbound_source 격리**: `inboundTouched` 일 때 `UpdatePlatform` skip + `UpdatePlatformInboundSource` 단독 호출. 부분 실패 격리.
- **routePermissionTable 변경 불요**: 기존 PATCH /platforms entry 가 `ResourcePlatforms + ActionEdit` 으로 inbound_source cover.
- **migration 000007 CHECK**: type='' 일 때 config NULL/'{}' 만 (consistency) + type whitelist `''|gitea|jira|other`.

## 검증

- `go build ./...` PASS
- `go test ./...` PASS (모든 package ok)
- 4 UT 신규 (GiteaOK / InvalidType400 / InvalidConfig400 / DisableEmpty) PASS

## 잔여 (PR A-2, 별도 sprint)

- routing/auto_route.go 신규 (pattern matcher + auto route)
- voc_handler.createOrGetVoc 자동 routing 호출
- backend IT
- openapi.yaml 정합
- ADR-0028 §6 amendment

## 다음 액션

- PR 발행 (push + gh pr create)
- 사용자 confirm 후 PR 머지
- main flat memory 3 file finalize (post-merge sync)
