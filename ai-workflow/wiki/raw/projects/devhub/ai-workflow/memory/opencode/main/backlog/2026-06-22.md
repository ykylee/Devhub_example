# Daily Backlog — 2026-06-22 (main branch)

- Branch: `main`
- Agent: opencode (Sisyphus, MiniMax-M3)
- Session: 2026-06-22T00:00:00+09:00 ~ 2026-06-22T00:10:00+09:00 (10 min)
- Status: main 정합 완료 (origin/main = `196d8593`)

## 오늘 한 일 (Done)

### feat/260619-v0-2-frontend-svelte 브랜치 정리

- 본 브랜치의 4 commit (scaffold / layout+dashboard / concepts / admin 5 page) 조사
  - **발견**: PR #661 frontend PoC (M-v0.2.0) + fix 2 commit (`f49078eb` / `43f7bad5`) 이 main 에 이미 머지
  - 본 feat 브랜치 = PR #661 의 이전 버전 코드, 동일 scope 의 redundant
- 사용자 결정: rebase main 우선 (`-X ours`) + .gitignore 만 별도 보존
- `git rebase -X ours origin/main` → 4 commit 모두 **"patch contents already upstream"** auto-drop (충돌 0)

### .gitignore 변경 분리 + PR

- `/src/var/` backend-knowledge runtime data ignore 추가
- 신규 브랜치 `chore/260621-gitignore-src-var` 생성 (frontend 브랜치명 부적합)
- cherry-pick: `7183d7c6` (feat) → `006284ae` (chore)
- PR #662 생성: 사외 tier, 추적성 N/A, label `worker/opencode`
- 4/9 CI pass (path-filtered skip 5), mergeStateStatus CLEAN
- self-merge (regular merge commit `196d8593`)

### 브랜치 폐기

- feat/260619-v0-2-frontend-svelte local + origin 삭제
- chore/260621-gitignore-src-var 자동삭제 (PR --delete-branch)
- local main → origin/main fast-forward (34 file / +5568 / -984)

### 메모리 갱신

- `ai-workflow/memory/opencode/main/` 디렉터리 신규 생성 (이전엔 부재)
- `state.json` + `session_handoff.md` + `work_backlog.md` 작성
- 직전 sprint (`chore/260619-v0-2-poc-followup`) 와 cross-reference

## 오늘 배운 것 / 다음 세션 참고

- **main branch memory directory**: AGENTS.md 의 per-branch memory 경로 (opencode/main/) 가 부재 상태였음. main 정합 작업 시점에 디렉터리 생성 + 초기화
- **PR #661 + fix 가 완전 상위 호환**: 본 feat 브랜치의 frontend 작업은 main 에 이미 reflect. redundant 작업의 rebase 시 `-X ours` 가 auto-drop 으로 깔끔하게 처리됨
- **PR merge 방식 2종 공존**: umbrella + release notes + FR = squash / trivial chore (.gitignore 등) = regular merge. 둘 다 허용, 사용자 정책 정합
- **force-with-lease vs push -u**: 원격 ref 부재 시 `--force-with-lease` 가 stale info 거부 → `git push -u` 로 신규 push 가 force-push 와 동등 효과

## 다음 작업 (사용자 결정 대기)

| # | scope | 우선순위 |
| --- | --- | --- |
| A | M-v0.2.2 backend-ai 폐기 (umbrella §1.2 G2) | M-v0.2.2 |
| B | §2.5 Tier 분리 정공법 (직전 sprint P1 잔여) | M-v0.2.1+ |
| C | §17.2 known gaps 자연 해소 (직전 sprint P1 잔여) | M-v0.2.1+ |
| D | v1.x P2 backlog (issue #222 / #218) | v1.1 |
| E | ai-workflow 메모리 정합 (housekeeping) | low |
