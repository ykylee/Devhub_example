# Backlog — 2026-06-12 (sprint `fix/work_260612-3-n13-followup-b-test2-rebase`)

- 문서 목적: 본 sprint 의 2026-06-12 작업 로그.
- 범위: 5 file (docs only) + 메모리 4 file 동기화.
- 상태: **2026-06-12 작업 진행**

## 1. 작업 흐름 (5 step)

| Step | 작업 | 결과 |
| --- | --- | --- |
| 1 | **PR #574 머지 후 main flat memory finalize** | `state.json` M-v1.0 notes 의 `phase2_7th_chunk_n13_followup_b_test2_rebase` 추가. main HEAD `896d9018` (PR #574 squash) 정합. |
| 2 | **Test 2 Sign-out timeout root cause 분석** | (1) `screenshots.spec.ts:66` 의 30s default timeout; (2) logout flow network race (backend 204/401/502 분기 + frontend `window.location.assign('/login')` 강제 redirect). PR #550 + PR #574 적용 후 자동 해결 가정. |
| 3 | **새 브랜치 생성** | `git checkout -b fix/work_260612-3-n13-followup-b-test2-rebase`. main HEAD `896d9018` 기반. |
| 4 | **verification report 작성** (5 file 변경) | `docs/validation/2026-06-12-n13-test2-rebase-verification.md` (NEW) + `state.json` + N-10 follow-up cross-ref (optional) + 브랜치 메모리 4 file (NEW). |
| 5 | **PR 발행** (사용자 confirm 대기) | branch push + PR 발행 + squash merge + main flat memory finalize + e2e CI 결과 확인. |

## 2. root cause 분석

**Test 2 증상**: `Test timeout of 30000ms exceeded` (Sign-out flow, N-8 race 유사)

**Root cause 후보**:
- (1) `frontend/tests/e2e/screenshots.spec.ts:66` — 30s default timeout 으로 logout flow 가 fail. 이미 180s 로 확장.
- (2) logout flow 의 network race (backend 204/401/502 분기 + frontend `window.location.assign('/login')` 강제 redirect) — N-8 race 와 유사 패턴.

**자동 해결 가정**:
- PR #550 (E2E spec timing fix, MERGED 2026-06-11) — spec waitForURL / waitForSelector timeout buffer 추가
- PR #574 (N-13 follow-up A, MERGED 2026-06-12) — strict mode violation bypass

## 3. follow-up 잔여 (사용자 결정 영역)

| Branch | 대상 | 결정 |
| --- | --- | --- |
| 1 | **구현 follow-up** | v1.1 milestone 진입 시점 별도 sprint (rebase main + PR #550 fix + 본 fix 종합 + 자동 재실행) |

## 4. 변경 이력

| 일자 | 변경 |
| --- | --- |
| 2026-06-12 | 1차 작성 — N-13 follow-up B (Test 2 Sign-out timeout rebase + 자동 재실행 검증). 5 file 변경 (docs only) + 메모리 4 file 동기화. |
