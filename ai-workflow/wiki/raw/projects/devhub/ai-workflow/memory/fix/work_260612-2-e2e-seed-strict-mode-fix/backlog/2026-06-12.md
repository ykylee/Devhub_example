# Backlog — 2026-06-12 (sprint `fix/work_260612-2-e2e-seed-strict-mode-fix`)

- 문서 목적: 본 sprint 의 2026-06-12 작업 로그.
- 범위: 1 file (frontend e2e spec) + 메모리 4 file 동기화.
- 상태: **2026-06-12 작업 진행**

## 1. 작업 흐름 (5 step)

| Step | 작업 | 결과 |
| --- | --- | --- |
| 1 | **PR #573 머지 후 main flat memory finalize** | `state.json` M-v1.0 notes 의 `phase2_5th_chunk_n13_housekeeping_followup` 에 main HEAD `5fb9ae75` (PR #573 squash) 정합 추가. |
| 2 | **Test 1 e2e seed 중복 strict mode violation 분석** | `frontend/tests/e2e/repositories-ui.spec.ts:5, 7` 의 `repoALink` / `repoBLink` matcher 가 `.first()` 미적용. E2E shard 실행 중 다른 spec 잔재 (예: `e2e-repo-a3xd7` 와 같이 random suffix 가 붙은 publish spec 의 잔재) 가 동일 이름 link 를 2+ 개 만들면 strict mode violation. 기존 `repository-dashboard.spec.ts:71, 117` 의 동일 패턴 정합. |
| 3 | **새 브랜치 생성** | `git checkout -b fix/work_260612-2-e2e-seed-strict-mode-fix`. main HEAD `5fb9ae75` 기반. |
| 4 | **1 file 변경** (frontend e2e spec) | `repositories-ui.spec.ts:5, 7` `.first()` 추가 + 코멘트 1 line. diff +3/-2. |
| 5 | **PR 발행** (사용자 confirm 대기) | branch push + PR 발행 + squash merge + main flat memory finalize. |

## 2. e2e spec 변경

**`frontend/tests/e2e/repositories-ui.spec.ts`** (line 1~11):

```ts
import { appPath, expect, loginAs, SEEDED, test } from "./fixtures";

test.describe("/repositories — repository list/detail UI", () => {
  // N-13 follow-up A: strict mode bypass via .first() (prior residue e.g. e2e-repo-a3xd7 may produce duplicate links)
  const repoALink = (page: import("@playwright/test").Page) =>
    page.getByRole("link", { name: "e2e-repo-a", exact: true }).first();
  const repoBLink = (page: import("@playwright/test").Page) =>
    page.getByRole("link", { name: "e2e-repo-b", exact: true }).first();
```

## 3. follow-up 잔여 (사용자 결정 영역)

| Branch | 대상 | 결정 |
| --- | --- | --- |
| 1 | **Test 2 Sign-out timeout** | main rebase + 자동 재실행 검증 별도 (PR #550 spec timing fix 가 해결 가능성 ↑) |
| 2 | **구현 follow-up** | v1.1 milestone 진입 시점 별도 sprint (rebase main + PR #550 fix + 본 fix 종합 + 자동 재실행) |

## 4. 변경 이력

| 일자 | 변경 |
| --- | --- |
| 2026-06-12 | 1차 작성 — N-13 follow-up A (Test 1 e2e seed 중복 strict mode violation fix). 1 file 변경 (frontend/tests/e2e/repositories-ui.spec.ts). |
