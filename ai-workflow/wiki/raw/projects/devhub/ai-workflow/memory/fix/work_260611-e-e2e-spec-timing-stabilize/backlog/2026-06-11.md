# Backlog — 2026-06-11 (E2E spec timing 안정화)

## 오늘 완료

- PR #548 (N-13 backend foundation, OPEN) 의 E2E Internal 3 spec fail (repositories-ui:41 + repository-dashboard:115 + signout:80) 자체 보완.
- 4 file / +11 -6줄, frontend only, 백엔드 무영향.

## 보완 정공법

1. **repositories-ui.spec.ts:41**: `getByText("devhub/e2e-repo-a", exact: false)` timeout 15_000 → `getByText(/devhub\/e2e-repo-a/i)` timeout 20_000
2. **repository-dashboard.spec.ts:115**: `getByText("e2e-repo-a", exact: false)` → `getByText(/e2e-repo-a/i).first()` timeout 20_000
3. **signout.spec.ts:59, 80, 109**: `waitForSignInForm` 옵션에 `timeoutMs: 45_000` 추가 (CI race 환경 30s 도 fail 가능)
4. **fixtures.ts**: `WaitForSignInFormOptions` 에 `timeoutMs` 옵션 신규 (default 30_000, opt-in 45_000)

## 검증

- ESLint PASS (1 warning = 기존 OIDC_CLIENT_ID, 본 PR scope 외)
- TypeScript typecheck: 기존 63 diagnostics (12 file, 본 PR scope 외)
- 4 file 모두 frontend only, 백엔드 무영향

## 다음 액션

- PR 발행 (push + gh pr create)
- 사용자 confirm 후 PR 머지
- PR #548 (N-13 backend foundation) 머지 결정 (E2E Internal 1 fail 안정화 검증 후)
