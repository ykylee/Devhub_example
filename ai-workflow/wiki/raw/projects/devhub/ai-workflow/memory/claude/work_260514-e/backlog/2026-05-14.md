# Backlog — 2026-05-14 (sprint claude/work_260514-e)

## 진입 컨텍스트
- Base: main @ `7822a91` (PR #108 머지 직후)
- 직전 sprint: claude/work_260514-d (PR #108 hotfix).
- 본 sprint scope: postgres integration test 도입 (work_260514-c carve_out 흡수)

## CI 거동

- `backend-unit` job 은 `make test` 만 실행 → `DEVHUB_TEST_DB_URL` 미설정 → `t.Skip`
- 기존 postgres_integration_test.go / users_units_test.go / postgres_rbac_test.go 와 일관
- 본 sprint 의 신규 test 도 동일 skip 메커니즘
- 후속 carve out: CI 에 integration test 분리 잡 추가 (DB setup + 마이그레이션 적용)

## 추적성 영향

| 단계 | 영향 |
| --- | --- |
| REQ / UC / ARCH | 변경 없음 |
| API | 변경 없음 |
| RM | 변경 없음 |
| IMPL | 검증 layer 추가 (책임 변화 없음) |
| UT | 신규 UT-{application,repo_ops,rollup,integration,project}-store-integration-XX 다수 |
| TC | carve out (e2e 후속) |

## P1/P2 회귀 guard 가 핵심

- **P1 (ComputeApplicationRollup 정규화)**: custom_weights 합 1.0 + 누락 repo 가 있는 시나리오에서 fallback 후 합이 다시 1.0 (정규화) 되는지 검증. 만약 1.0 이 아니면 hotfix #108 의 P1 정정이 회귀.
- **P2 (UpdateIntegration unique)**: 2 integration 의 external_key 충돌 시 ErrConflict 반환 검증.

본 sprint 의 핵심 가치는 P1/P2 의 회귀 방지 + Application 도메인 전체의 SQL 정합 검증.
