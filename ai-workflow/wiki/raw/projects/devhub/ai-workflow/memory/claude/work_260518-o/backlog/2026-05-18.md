# Sprint backlog — `claude/work_260518-o`

- 문서 목적: ADR-0017 §6 atomicity 실 구현 sprint 작업 항목 정리
- 범위: store CTE refactor + concurrent race integration test + ADR/traceability 갱신
- 대상 독자: 본 sprint 작업자
- 상태: in_progress
- 최종 수정일: 2026-05-18

## scope

다음 세션 directive 3순위 ([main flat state.json](../../../state.json) `다음 세션 directive (3)`):

> **ADR-0017 §6 atomicity 실제 구현** (CTE refactor) — `store.UpdateDevRequestIntakeTokenIPs` 의 2 query 패턴을 단일 CTE + `FOR UPDATE` row lock 으로 atomic 보장. ADR §6 의 reference SQL 그대로 적용 + 회귀 race test (concurrent goroutine).

## 결정

- ADR-0017 §6 의 reference SQL (CTE + VALUES anchor + FOR UPDATE) 그대로 구현
- handler 분기 contract 동일 (frontend 영향 없음)
- 회귀 가드는 Postgres integration test (CI backend-integration job)

## 체크리스트

- [ ] `backend-core/internal/store/dev_request_intake_tokens.go` — CTE refactor
- [ ] `backend-core/internal/store/dev_request_intake_tokens_integration_test.go` — race test 신규 (Happy/NotFound/Revoked/ConcurrentUpdateAndRevoke 4 sub-test)
- [ ] `docs/adr/0017-dreq-intake-token-operational-hardening.md` §6 carve out 마지막 항목 → resolved + §7 변경 이력
- [ ] `docs/traceability/report.md` §6 변경 이력
- [ ] go vet PASS + test compile PASS
- [ ] PR + self-review + 머지

## 검증 절차

```
cd backend-core && go vet ./internal/store/
cd backend-core && go test ./internal/store/ -run TestIntegration_UpdateDevRequestIntakeTokenIPs -short  # skip without DB
# DEVHUB_TEST_DB_URL=postgres://... 환경에서:
cd backend-core && go test ./internal/store/ -run TestIntegration_UpdateDevRequestIntakeTokenIPs -v
```
