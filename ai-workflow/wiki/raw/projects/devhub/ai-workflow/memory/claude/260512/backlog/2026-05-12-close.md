# 슬롯 종료 로그 (2026-05-12)

## 본 세션이 PR #67 안에서 한 일

1. **PS dev-up.ps1 / dev-down.ps1 신규** + bash 두 셸 공통 하드닝.
   - ASCII 영어 (메모리 정책: PS 5.1 + 비 BOM UTF-8 의 한글 깨짐 회피).
   - TCP 비차단 readiness probe (TcpClient async / bash /dev/tcp). 무한 hang 방지.
   - migrate-up 자동 호출 — N1 의 root cause 였던 마이그레이션 미적용 환경 제거. DEVHUB_SKIP_MIGRATE 로 우회 가능.
   - PID 파일 경로 `.pids/<name>.pid` 로 통일 + dev-down 의 graceful → kill -9 fallback.

2. **M2 audit consumer UI**.
   - gemini 가 추가한 audit.service.ts / audit.types.ts 가 dead code + 와이어 미스매치였음. backend audit.go 의 auditLogResponse 와 1:1 정렬 (id→audit_id, occurred_at→created_at, actor_id→actor_login, since/until 제거 등).
   - `/admin/settings/audit` 페이지 신규 — 4 필터, 행 확장, 50/page 페이지네이션.
   - settings/layout.tsx 에 \"Audit\" sub-tab.
   - e2e audit.spec 추가 (charlie smoke).
   - signout.spec 의 Next 16 turbopack ERR_ABORTED flake 안정화 (catch).

3. **Kratos identity O(1) fast path**.
   - GET /admin/identities?credentials_identifier=<user_id> 단발 질의.
   - metadata_public.user_id 검증으로 system_id ≠ user_id divergence 는 slow scan fall-through.
   - older Kratos 의 query 거절도 silent miss → scan 진행.
   - httptest 3 신규 (FastPathHit / MetadataMismatchFallsThrough / Non200FallsThrough).

4. **워크플로 메모리** 초기화 (d411542) — 이번 본 close commit 으로 마무리.

## 발견한 외부 사실
- Next 16 turbopack dev 모드는 새 디렉터리/페이지를 hot detect 못 함. 새 라우트 추가 후 dev server 재시작 필요.
- Next 16 의 `react-hooks/set-state-in-effect` 규칙은 useEffect 안에서 동기 setState 를 금지. cancelled flag + handler 에서 setIsLoading(true) 트리거 패턴이 정답.
- Kratos v26.2.0 의 `credentials_identifier` 쿼리는 GET /admin/identities 에서 정상 동작 (1 결과 반환). 향후 keyset pagination (page_token) 으로 전환될 가능성 있음.
- Next 16 turbopack 의 client-side `window.location.assign` 은 원래의 `page.goto()` 를 ERR_ABORTED 로 끊는 경우가 있음. catch 우회 패턴이 안전.

## 다음 세션이 이어받을 컨텍스트
- `gemini/frontend_260510` 의 head 가 PR #67 squash 머지본을 포함. 그 위에서 새 슬롯 (`claude/<날짜>`) 만들면 됨.
- 운영 환경에서 dev-up.ps1 실 검증은 아직 안 함 (사용자 머신에서 직접 한 번 돌려보면 좋음).
- 로컬 PG: `postgres://postgres:postgres@localhost:5432/devhub?sslmode=disable`. 마이그레이션 자동 적용 가능.
