# 2026-05-12 — work_260512-f 슬롯 closure (PR #76)

PR-T3.5 (a18e785, #62) 의 stale rotation 한계 제거 hardening. 단일 sprint, 단일 PR.

## PR 라인업

| # | 주제 | 슬롯 |
| --- | --- | --- |
| #76 | test(e2e): force-reset seeded passwords in globalSetup (PR-T3.5 hardening) | work_260512-f |

## 변경 요약

`frontend/tests/e2e/global-setup.ts`:
- `listExistingIdentityEmails(): Set<string>` → `listExistingIdentities(): Map<email, KratosIdentityFull>`.
- `KratosIdentityFull` 추가 (id/schema_id/state/traits/metadata_{public,admin}).
- 신규 `resetKratosPassword(identity, seed)` — PUT `/admin/identities/{id}`. payload 의 traits/metadata 는 list 응답에서 echo, `state="active"` 강제, `credentials.password.config.password = seed.password` (plaintext, Kratos 가 hash).
- `seedKratos()` 분기: 누락 → POST, 존재 → force-reset.

`docs/setup/e2e-test-guide.md`:
- §2.0 자동 시드 동작 — POST/PUT 분기 명시, "변경하지 않는다" 한계 문구 삭제.
- §6 401/current password incorrect 항목 — operator 수동 절차 → "재실행 1번 자동 복구".
- §8 변경 이력 +1줄.

## Self-review 패스 결과

- **보강 commit `df7b099`**: PUT payload 의 `state` echo → `"active"` 강제. inactive 회귀 안전망.
- 노트 3건 (블로커 아님, PR 코멘트로 박제):
  1. Kratos PUT method preservation 의미는 v26 source 기준 보장. 미래 totp/webauthn 도입 시점 재검증 필요.
  2. `password-change.spec.ts` 의 finally rollback 이 hardening 이후 redundant. 다음 슬롯 정리 가능.
  3. 실 e2e 실행 검증은 사용자 환경 (5 프로세스 native) 에서 별도 수행 필요 (test plan unchecked 2건).

## main 머지 결과

- `gh pr merge 76 --squash --delete-branch` — 단일 squash commit `e2393a3`.
- main HEAD = `e2393a3`.
- `claude/work_260512-f` 브랜치 자동 삭제 (로컬 + 원격).

## 다음 세션 출발점

main 에서 새 branch 로 시작. 자연 후보:
1. PR-T3.5 follow-up #2 — `password-change.spec.ts` finally 블록 제거 (hardening 으로 redundant 화).
2. M2 hygiene — `users.kratos_identity_id` 칼럼.
3. PR-D follow-up — commands audit actor context / log request_id / `DEVHUB_TRUSTED_PROXIES`.
4. M4 진입 — WebSocket UI / 확장 / AI Gardener / Gitea Pull.
