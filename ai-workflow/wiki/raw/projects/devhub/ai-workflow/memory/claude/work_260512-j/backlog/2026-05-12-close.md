# 2026-05-12 — work_260512-j 슬롯 closure (PR #82)

PR #80 (PR-D follow-up) second-pass review 의 단일 fix.

## PR 라인업

| # | 주제 | 슬롯 |
| --- | --- | --- |
| #82 | fix(audit): fall back to nil trust when DEVHUB_TRUSTED_PROXIES invalid | work_260512-j |

## 변경 요약

`internal/httpapi/router.go`:
- `_ = router.SetTrustedProxies(...)` → err 검출 시 `log.Printf("[trusted-proxies] ...invalid entry...")` 경고 + `SetTrustedProxies(nil)` 폴백.

`internal/httpapi/audit_test.go`:
- `TestAuditEnrichment_InvalidTrustedProxiesFallsBackToNil` — 폴백 동작 검증.

## Second-pass review 결과 (full audit)

PR #80 머지 후 main 에 대한 review 패스에서 발견 사항:

| # | 항목 | 결정 |
| --- | --- | --- |
| 1 | router.go SetTrustedProxies err 무시 → partial-trust silent | **본 sprint 보강** |
| 2 | caller-supplied X-Request-ID 콘텐츠 validation (newline/control) | 별도 sprint (net/http 가 CRLF 차단해 보안 회귀 아님, 위생 차원) |
| 3 | ctx 표준 request_id 전파 (client/background log untraced) | 별도 sprint |
| 4 | dev-fallback path 의 log 부재 | 의도된 silent path, fix 불필요 |
| 5 | memoryCommandStore vs production INSERT 검증 한계 | DB integration test 별도 |

## main 머지 결과

- `gh pr merge 82 --squash --delete-branch` — squash commit `6121828`.
- main HEAD = `6121828`.

## 다음 슬롯 출발점

main 에서 새 branch. 자연 후보 (위 review 의 #2~#5):
1. caller-supplied X-Request-ID validation
2. ctx 표준 request_id 전파
3. writeRBACServerError → writeServerError 통합
4. M4 진입
