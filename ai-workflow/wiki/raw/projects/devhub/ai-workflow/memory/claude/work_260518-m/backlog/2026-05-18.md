# Sprint backlog — `claude/work_260518-m`

- 문서 목적: External Integration bindings 관리 UI sprint 작업 항목 정리
- 범위: frontend 진입점 + service/types + E2E + traceability
- 대상 독자: 본 sprint 작업자
- 상태: in_progress
- 최종 수정일: 2026-05-18

## scope

다음 세션 directive 1순위 ([state.json](../../../state.json) `다음 세션 directive (1)`):

> **Bindings 관리 UI** (API-74, 75) — provider ↔ application/project 매핑. 별도 페이지 `/admin/settings/integration-bindings` 또는 `/admin/settings/integrations` 의 sub-tab. backend 는 이미 activated, frontend 진입점만 필요. system_admin only RBAC.

## 결정

- **별도 페이지** `/admin/settings/integration-bindings` (layout subTabs 패턴 일관성).
- 첫 진입은 진입점 활성화 우선 — scope_id 는 자유 텍스트 입력 (Application/Project ID lookup combobox 는 후속 carve out).
- 페이지 내 modal-based create — provider 선택은 modal 의 select dropdown.

## 체크리스트

- [ ] `frontend/lib/services/integration.types.ts` — `IntegrationBinding` + `ListBindingsOptions` + `CreateBindingInput` + `IntegrationScopeType` + `IntegrationPolicy` 추가
- [ ] `frontend/lib/services/integration.service.ts` — `listBindings()` + `createBinding()` 메소드 추가
- [ ] `frontend/components/integration/BindingsTable.tsx` 신규
- [ ] `frontend/components/integration/CreateBindingModal.tsx` 신규
- [ ] `frontend/app/(dashboard)/admin/settings/integration-bindings/page.tsx` 신규
- [ ] `frontend/app/(dashboard)/admin/settings/layout.tsx` — subTabs 에 Bindings 추가 (Link2 icon)
- [ ] `frontend/tests/e2e/admin-integration-bindings.spec.ts` 신규 (또는 admin-integrations.spec.ts 확장)
- [ ] `docs/domain/integration-registry/test_cases.md` — TC-INT-FRONTEND-BIND-{LIST,CREATE,RBAC}-01 발급
- [ ] `docs/traceability/report.md` §3 External Integration row + §5.1 TC 매트릭스 갱신
- [ ] backend 회귀: `cd backend-core && go test ./internal/httpapi/ -run Binding`
- [ ] frontend 회귀: `cd frontend && npm run build` (Next.js 16.x static check) + `npm run test` (Vitest)
- [ ] PR + self-review 4단계 + squash merge

## 신규 TC ID

- `TC-INT-FRONTEND-BIND-LIST-01` — bindings 페이지 로드 + 목록 표시 + scope_type 필터
- `TC-INT-FRONTEND-BIND-CREATE-01` — CreateBindingModal 으로 신규 binding 발급 + table 업데이트
- `TC-INT-FRONTEND-BIND-RBAC-01` — developer role 의 페이지 접근 차단 (defaultLanding redirect)

## 검증 절차

```
cd backend-core && go test ./internal/httpapi/ -run Binding -v
cd frontend && npm run build
cd frontend && npm run test -- --run integration  # 또는 전체 vitest
cd frontend && npx playwright test admin-integration-bindings.spec.ts --reporter=line
```
