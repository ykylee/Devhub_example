# 상세 작업 로그 (2026-05-12)

## 진행한 일 (이전 세션, gemini/frontend_260510 검토 → 4 PR 머지)

### 1. 리뷰
gemini 가 `bf5fa13` (feat: standardize local admin seeding via OIDC APIs) 에서 `kratos_admin_client.go` 를 전면 재작성. 회귀 3건 + 운영/UX 이슈 다수 식별.

### 2. PR 흐름
- **PR #63 — Critical 핫픽스**: 파일을 작업 직전 (`1aa8da4`) 상태로 통째 복원. httptest 단위 테스트 16 케이스 추가 (CreateIdentity payload, FindIdentityByUserID nested decode + 페이지네이션 + 10k 캡, UpdatePassword GET→PUT 라운드트립, SetIdentityState active/inactive, DeleteIdentity status surface + PathEscape).
- **PR #64 — H/M follow-ups**: seedLocalAdmin 에러 surface (H3), postgres.go 빈줄 revert (M5), .pids/ gitignore (H5), dev-up.sh DB_URL 환경화 (H4), auth/callback console.log 제거 (M1), next 버전 pin (M4), MemberManagementModal leader invariant 코멘트 (M3). 추가로 seedLocalAdmin 시그니처를 `httpapi.OrganizationStore` (14 메서드) 에서 로컬 `seedOrgStore` (2 메서드) 로 좁혀 main_test.go 의 3 분기 테스트 작성.
- **PR #65 — N1**: e2e 재현 중 발견. `authenticateActor` 의 `if user, err := ...; err == nil` 이 모든 GetUser 에러를 묵묵히 삼킴. switch 로 ErrNotFound 와 그 외를 분기, 그 외는 `[authenticateActor] GetUser ... failed` 로 surface. 단위 테스트 2개. 최종 리뷰 중 추가 발견: `PostgresStore.GetUser` 가 NotFound 를 `pgx.ErrNoRows` 로 직접 래핑해 sentinel 컨벤션 우회 (organization.go:203 의 404 매핑도 silently broken). 한 줄 수정으로 `store.ErrNotFound` 로 정렬.
- **PR #66 — N2**: Kratos v26.2.0 의 `/admin/identities` 가 0-based 페이지네이션인데 코드 두 군데가 `page=1` 부터 시작 → 항상 빈 첫 페이지 → 사용자 존재해도 NotFound. backend client 와 e2e globalSetup 둘 다 정정. 라이브 BEFORE/AFTER 로그로 검증.

### 3. 검증
- `go test ./...` 11/11 패키지 통과
- e2e `auth.spec` 4/4 통과 (alice → /developer, bob → /manager, charlie → /admin, AuthGuard 차단)
- 백엔드 로그 5xx 0건 — login 500 의 root cause 는 schema/payload 회귀 (C1-C3) 였음을 e2e 로 확정

## 발견한 외부 사실 / 운영 컨텍스트
- Kratos v26.2.0 의 `/admin/identities` 는 **0-based** 페이지네이션 (`page=0` 이 첫 페이지). 이전 PoC 의 1-based 가정과 불일치.
- `seedLocalAdmin` 의 4자 \"test\" 비밀번호는 Kratos 의 `min_password_length: 12` 정책과 충돌하지 않음 (admin API `/admin/identities` 가 self-service 정책 우회).
- `PostgresStore.GetUser` 만 codebase 의 NotFound sentinel 컨벤션에서 단독 이탈해 있었음 → 정렬 후 organization.go 의 404 경로 부산물 회복.
- 로컬 PG: `postgres/postgres@localhost:5432/devhub`. 마이그레이션 000009 적용 안 되어 있으면 GetUser 자체가 SQL error 로 떨어짐 (운영자가 backend.log 에서 SQL error 를 찾기 전까지 모든 사용자가 토큰 default role 로 라우팅 — N1 가 잡음).

## 특이 사항
- gemini 의 `MockKratosAdmin` \"필드 추가\" 작업은 사실 직전 revert 직후 부분 재구현이었음 (이전 파일이 이미 슈퍼셋을 가지고 있었음).
- gemini 의 `dev-up.sh` 가 `DB_URL` 을 macOS 사용자 `yklee` 로 하드코딩 → 환경변수 fallback 으로 보정.
- 사용자 워크플로: 본인 PR 도 \"리뷰어 모드\" 로 다시 한 번 비판적으로 보고 코멘트 후 머지. 본 세션에서 두 라운드 (PR #63/64, #65/66) 모두 적용.
