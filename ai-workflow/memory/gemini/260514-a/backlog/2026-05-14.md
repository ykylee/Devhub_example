# 2026-05-14 작업 백로그 (gemini/260514-a)

- 문서 목적: `Application`, `Project`, `Repository` 3-티어 도메인 구현
- 상태: done
- 최종 수정일: 2026-05-14

## 1. TASK-GOV-FRONTEND-SCAFFOLD: 3-티어 도메인 프론트엔드 UI/UX 설계 및 구현

- 상태: done
- 모드: Feature
- 우선순위: high
- 영향 문서: `frontend/app/(dashboard)/applications`, `frontend/app/(dashboard)/projects`, `frontend/app/(dashboard)/repositories`, `frontend/components/project`, `frontend/lib/services/project.*`

### 1.1 계획 (Plan)
- 작업 목표:
  - Application, Project, Repository 목록 및 상세 페이지 UI/UX 프로토타입 구현.
  - 전역 네비게이션(사이드바) 및 관리자 설정 메뉴에 관련 항목 추가.
  - 백엔드 API 연동을 위한 서비스 및 타입 레이어 정의.

### 1.2 실행 (Act)
- `project.types.ts`: 백엔드 `domain` 모델을 기반으로 TypeScript 타입 정의.
- `project.service.ts`: `axios` 클라이언트를 사용하여 API 호출을 추상화하는 서비스 클래스 구현.
- `Sidebar.tsx`, `admin/settings/layout.tsx`: 신규 메뉴 항목 추가.
- `ApplicationTable`, `RepositoryTable`, `ProjectTable`: 목록 UI 컴포넌트 구현.
- `ApplicationCreationModal`: Application 생성/수정 모달 UI 구현.
- `applications/`, `repositories/`, `projects/` 페이지 및 상세 페이지(`[id]`, `[...key]`) 라우팅 및 UI 구현.

### 1.3 결과 (Result)
- (완료) 3-티어 도메인 관리를 위한 프론트엔드 기본 구조 및 UI/UX 구현 완료.
- (완료) 목록-상세 페이지 간 동적 라우팅 및 네비게이션 적용.

## 2. TASK-GOV-BACKEND-CRUD: 3-티어 도메인 백엔드 CRUD API 구현

- 상태: done
- 모드: Feature
- 우선순위: high
- 영향 문서: `backend-core/internal/store/applications.go`, `backend-core/internal/httpapi/`

### 1.1 계획 (Plan)
- 작업 목표:
  - `Application` 및 `Project` 도메인의 전체 CRUD(Create, Read, Update, Delete) API 구현.
  - `ApplicationRepository` 연결/해제(Create/Delete) API 구현.

### 1.2 실행 (Act)
- `store/applications.go`:
  - `Application` CRUD: `CreateApplication`, `UpdateApplication`, `ArchiveApplication` 구현.
  - `Project` CRUD: `CreateProject`, `GetProject`, `UpdateProject`, `ArchiveProject`, `ListProjects` 구현.
  - `ApplicationRepository` C/D: `CreateApplicationRepository`, `DeleteApplicationRepository` 구현.
- `httpapi/applications.go`: `Application` 및 `ApplicationRepository` 핸들러 구현.
- `httpapi/projects.go`: `Project` 관련 핸들러를 담는 신규 파일 생성 및 구현.
- `httpapi/router.go`: 신규 `Project` API 엔드포인트 등록.

### 1.3 결과 (Result)
- (완료) `Application` 도메인 전체 CRUD API 구현 완료.
- (완료) `Project` 도메인 전체 CRUD API 구현 완료.
- (완료) `ApplicationRepository` 연결(Create) 및 해제(Delete) API 구현 완료.
- (완료) 프론트엔드가 호출할 수 있는 백엔드 API 기반 마련.

## 3. TASK-GOV-INTEGRATION: 3-티어 도메인 프론트엔드-백엔드 연동

- 상태: done
- 모드: Feature
- 우선순위: high
- 영향 문서: `backend-core/internal/store/applications.go`, `backend-core/internal/httpapi/projects.go`, `frontend/lib/services/project.service.ts`, `frontend/app/(dashboard)/applications/[id]/page.tsx`, `frontend/components/project/RepositoryLinkModal.tsx`, `frontend/components/project/ProjectCreationModal.tsx`

### 3.1 계획 (Plan)
- 작업 목표:
  - 애플리케이션 상세 페이지 내 저장소 연결 및 프로젝트 생성 기능 활성화.
  - 백엔드 `ListProjects` API의 애플리케이션 단위 필터링 지원.
  - 전반적인 시스템 안정성을 위한 백엔드 빌드 오류 수정.

### 3.2 실행 (Act)
- 백엔드: `store/applications.go` 및 `httpapi/projects.go` 수정하여 `application_id` 필터링 구현.
- 백엔드: `PostgresStore`의 UUID 처리 및 리턴 타입 오류 등 빌드 방해 요소 해결.
- 프론트엔드: `project.service.ts`에 애플리케이션별 프로젝트 조회 메서드 추가.
- 프론트엔드: `RepositoryLinkModal` 및 `ProjectCreationModal` 신규 컴포넌트 구현.
- 프론트엔드: 애플리케이션 상세 페이지에서 모달 연동 및 실제 데이터 로드 로직 적용.

### 3.3 결과 (Result)
- (완료) 애플리케이션 상세 페이지에서 저장소 연결 및 프로젝트 생성 기능 실작동 확인.
- (완료) 백엔드 `go build` 정상 통과 및 API 기능 확장 완료.
- (완료) 3-티어 거버넌스 도메인의 핵심 Full-stack 연동 완료.
