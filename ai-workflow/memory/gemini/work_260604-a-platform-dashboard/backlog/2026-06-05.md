# Backlog — 2026-06-05

- 문서 목적: 2026-06-05 스프린트 `gemini/work_260604-a-platform-dashboard` 브랜치에서 진행된 작업의 상세 타임라인 및 일별 이력을 기록합니다.
- 범위: Codex 및 User PR 리뷰 피드백 보완 작업 (깨진 빌드 상태 알림 복원, API ID 충돌 해결, 요구사항/유스케이스 정합성 보완, 2D RBAC 표준 동기화)
- 대상 독자: 개발자, AI 에이전트, 운영자
- 상태: active
- 최종 수정일: 2026-06-05
- 관련 문서: [PROJECT_PROFILE.md](../../../PROJECT_PROFILE.md), [session_handoff.md](../session_handoff.md), [work_backlog.md](../work_backlog.md)

## 1. 당일 주요 작업 상세
- **Codex PR 리뷰 피드백 보완 (깨진 빌드 알림 및 상세 로그 딥링크 뷰 복원)**:
  - `frontend/app/(dashboard)/platforms/[id]/page.tsx`에 `buildStatus`에 따른 헬스 표시 로직 복원.
  - 대시보드 플랫폼 명칭 우측 영역에 `buildStatus` 값("healthy" | "broken" | "unknown")에 맞춰 `Healthy 🟢` / `Build Broken 🔴` / `Build Unknown ⚪` 배지(Badge)가 동적으로 렌더링되도록 구현.
  - `buildStatus === "broken"`인 경우, Overview stats 바로 아래에 시인성이 높은 Red Neon 펄스 애니메이션 배경이 적용된 글래스모피즘 Alert banner가 노출되도록 보완.
  - 해당 배너 내부에 실패한 빌드 목록(`repo_slug`, `branch`, `build_number`, `error_snippet`)과 빌드 로그로 직접 이동할 수 있는 `Log URL` [로그 진입 딥링크] 버튼(Play 아이콘 포함)을 구성.
  - 이로써 `docs/domain/platform-lifecycle/requirements.md` §3.1 REQ-FR-APPDASH-001의 플랫폼 빌드 실패 broken/red 상태 표기 및 실패 빌드 런 표시 요구사항을 완벽하게 재충족함.

- **User 2차 리뷰 피드백 보완**:
  - **API ID 중복 충돌 해결**:
    - 프로젝트 상세 대시보드 API ID가 기존 Task Webhook 수신 API의 ID `API-94`와 중복 충돌하는 문제를 확인하고, 프로젝트 상세 대시보드 API ID를 신규 고유 ID인 **`API-98`**로 개칭함.
    - `docs/domain/platform-lifecycle/api.md` 및 `docs/traceability/report.md` 에 기재된 프로젝트 상세 대시보드 API 참조를 모두 `API-98`로 일괄 변경 및 동기화 처리 완료.
  - **대시보드 UI 레이아웃과 요구사항/유스케이스 문서 정합성 보완**:
    - 플랫폼 대시보드 최상단 카드가 메트릭으로 재배치되고 빌드 실패가 그 하단 전용 배너로 이관된 실제 UI 구성에 맞게, 요구사항 `REQ-FR-APPDASH-001` 및 유스케이스 `UC-APPDASH-01` 설명 문구를 갱신하여 문서적 사양 정합성 확보.
  - **PROJDASH 역할 매핑 2D RBAC 표준 동기화**:
    - 기존 `requirements.md`, `architecture.md`, `project_dashboard_concept.md`에서 Keycloak의 legacy role 명칭(`contributor` 등)을 언급하던 부분을 2차원 RBAC 표준 명칭(System Role: `developer/team_manager/system_admin` 및 Resource Role: `project_member/project_leader`)으로 전면 수정함.
    - `project_leader`는 Keycloak token의 global role이 아니라 `project_members.project_role = 'lead'` 데이터 식별 로직을 따름을 명문화하여 구현자와의 계약 정합성을 완벽하게 보장함.

- **자가 검증 수행**:
  - backend `go test ./...` in `backend-core` -> All pass.
  - frontend `npm run test` -> 1015 pass.
  - frontend `npm run build` -> Success.

- **원격 PR 업데이트**:
  - `gemini/work_260604-a-platform-dashboard` 브랜치 변경 사항을 깃 커밋 및 푸시하여 PR [#479](https://github.com/ykylee/Devhub_example/pull/479)에 연계 반영 완료.
