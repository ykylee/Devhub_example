# 2026-05-16 백로그 (gemini/dreq_e2e_260515)

## 작업 목표
- DREQ Hardening (P2) 항목 완수
- 토큰 만료 정책(`expires_at`) 및 IP Mutation 엔드포인트 구현

## 진행 상황
- `[x]` **Backend - Token Expiration**
  - migration `000027` 생성 및 적용
  - `domain.DevRequestIntakeToken` 구조체 업데이트
  - `PostgresStore` (Create, List, Lookup) 업데이트
  - `requireIntakeToken` middleware 만료 체크 강화
  - 유닛 테스트(`TestIntakeAuth_ExpiredTokenDenies`) 통과
- `[x]` **Backend - IP Mutation**
  - `UpdateDevRequestIntakeTokenIPs` Store 메서드 구현
  - `PATCH /api/v1/dev-request-tokens/:token_id` 핸들러 및 라우터 등록
  - RBAC 권한 매핑(`dev_request_intake_tokens:edit`)
- `[x]` **Frontend - UI Updates**
  - `IssueIntakeTokenModal` 만료일 입력 필드 추가
  - `IntakeTokenTable` 만료 상태 표시 배지 및 컬럼 추가

## 다음 단계
- [ ] PR 생성 및 리뷰 대응
- [ ] main 병합 후 운영 환경 배포 확인

## 비고
- `multi_replace_file_content` 도구의 파싱 오류로 인해 일부 파일 수정을 개별 `replace_file_content`로 나누어 진행함.
- 백엔드 유닛 테스트 전체 통과 확인 완료.
