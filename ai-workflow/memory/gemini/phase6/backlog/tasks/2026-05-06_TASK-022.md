---
id: TASK-022
status: done
created_at: 2026-05-06
---
# TASK-022 Command lifecycle 안전화 및 Admin service action 실연동

- 상태: done

## Description

- 백엔드 command/audit 구현에서 idempotency race fallback을 수정한다.
- 프론트 Admin dashboard의 service action mock 호출을 실제 `POST /api/v1/admin/service-actions` 호출로 전환한다.
- 프론트 TypeScript lint를 막는 service layer `any` 타입을 정리한다.

## Implementation Log

- 2026-05-06: 백엔드 로드맵과 프론트 Phase 2/3 구현을 비교해 우선순위를 재확정했다.
- 2026-05-06: Admin service action은 백엔드 API가 이미 있으나 프론트가 mock sleep만 수행하고 있음을 확인했다.
- 2026-05-06: command idempotency race fallback에서 `risk_mitigation`/`service_action` command type을 반대로 조회하던 버그를 수정했다.
- 2026-05-06: `infraService.controlService()`를 실제 `POST /api/v1/admin/service-actions` 호출로 전환했다.
- 2026-05-06: frontend service/realtime event 타입을 정리해 `no-explicit-any` lint 오류를 제거했다.

## Validation

- `cd backend-core && go test ./...` 통과.
- `cd frontend && npm run lint` 통과.
- `PYTHONPATH=ai-workflow <bundled-python> ai-workflow/tests/check_docs.py` 실행 예정.

## Outcome

- 작업 결과: Admin service action command 생성이 프론트에서 실제 백엔드 API로 연결됐다.
- 다음 작업: command status transition worker와 `command.status.updated` WebSocket publish 경계 구현.
