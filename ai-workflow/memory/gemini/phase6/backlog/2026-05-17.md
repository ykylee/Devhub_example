# 2026-05-17 백로그 (E2E Verification, Projects Refinement & DREQ Stabilization)

- 상태: done
- 담당: Antigravity
- 요약: RBAC E2E 검증 성공, Projects 상세 페이지 실시간 API 연동, 추적성 매트릭스 동기화, Frontend Turbopack & Standalone Docker Build 에러 완전 조치, DREQ 도메인 SQL/RBAC 마이그레이션 수동 반영, DREQ 라이프사이클 E2E 테스트 최종 Green Pass, 개발 의뢰 페이지(dev-requests) 필터 바 공통 표준화 완료

## 🎯 목표
1. RBAC permissions E2E 테스트 환경 구축 및 전체 Green Pass 달성
2. Projects 상세 페이지 실시간 API 연동 (팀 멤버/마일스톤 동적 매핑)
3. SDLC 추적성 매트릭스 최신화 (report.md IMPL/TC ID 동기화)
4. Frontend Next.js Turbopack 컴파일 & 타입 에러 전면 해결
5. Next.js Standalone Dockerfile 빌드 정상화 및 컨테이너 기동 보증
6. 로컬 DB(5433 포트) 상에 누적된 DREQ SQL/RBAC 마이그레이션 반영
7. DREQ Intake to Promote to Revoke 라이프사이클 E2E 테스트 100% 통과
8. 개발 의뢰 설정 페이지 (`dev-requests/page.tsx`) 필터 바 공통 표준 컴포넌트(`FilterBar`)로의 완전 교체 및 UI/UX 정합성 확립

## 📋 작업 내역 (E2E Verification & Projects Refinement)
- [x] **RBAC E2E 테스트 통과**
    - [x] `002_seed_e2e_users.sql` 실행 시 `charlie@example.com` 중복으로 발생하는 고유 제약 조건 위반을 식별하고, 호스트 Docker Postgres 내 중복 레코드 삭제로 해결.
    - [x] `DSN` 환경변수(`postgres://postgres:your_password@localhost:5433/devhub?sslmode=disable`)를 주입하여 `admin-permissions.spec.ts` 하의 모든 E2E 권한 매트릭스 테스트(2건) Green Pass 달성.
- [x] **Projects 상세 페이지 실시간 API 연동**
    - [x] `identity.service.ts`를 연동하여 전체 유저 목록을 Fetch, `owner` 및 `contributors`로 분류 매핑.
    - [x] 프로젝트 `start_date` 및 `due_date` 데이터를 활용해 Kickoff와 Target Delivery 마일스톤을 실시간 동적으로 렌더링.
    - [x] Frontend Vitest 테스트(48/48) 및 Next.js 정적 타입 검사 성공 확인.
- [x] **SDLC 추적성 문서 업데이트**
    - [x] `docs/traceability/report.md`에 공통 필터 표준화 및 감사 로그 리팩토링 연관 IMPL/TC ID 갱신.
    - [x] Frontend IMPL 개수 35개 및 E2E TC 개수 32개로 최신화하여 최하단 이력 변경 완료.

## 📋 작업 내역 (DREQ Stabilization & Build Fixes)
- [x] **Next.js Turbopack 컴파일 & 타입 에러 완전 해결**
    - [x] `repositories` 페이지들의 미정의 `Github` 아이콘을 표준인 `Globe` 아이콘으로 전격 치환하여 Lucide icon 임포트 에러 해소.
    - [x] `dev-requests/page.tsx`, `admin/settings/audit/page.tsx` 에 `"use client";` 지시어 추가하여 클라이언트 훅 빌드 에러 봉쇄.
    - [x] `dev-requests/page.tsx` 내 필터링 쿼리의 잘못된 `description` 속성을 API 스펙인 `details`로 수정.
    - [x] `projects/[id]/page.tsx` 와 `projects/page.tsx` 내 `Project` 타입의 임포트 출처를 `project.types`로 정정.
    - [x] `projects/page.tsx` 에서 `DashboardHeader` 의 `children` 사용법을 정의된 `actions` Prop으로 교체.
    - [x] Badge 컴포넌트의 비정상 variant `"outline"`을 표준인 `"glass"`로 교체.
- [x] **Next.js Standalone Dockerfile 빌드 정상화**
    - [x] `frontend/Dockerfile` 빌드 시 `NEXT_OUTPUT=standalone` 환경 변수를 주입하여 standalone 빌드 아티팩트가 문제없이 생성되고 프론트엔드 컨테이너가 원활하게 Recreation 되도록 구현.
- [x] **DREQ 도메인 SQL & RBAC 마이그레이션 반영**
    - [x] 로컬 DB(5433 포트) 환경에 `golang-migrate` 를 통해 DREQ Intake Token 자원 권한 부여를 처리하는 `000026_rbac_dev_request_intake_tokens` 등 누적 마이그레이션 반영 완료.
    - [x] `system_admin` 역할이 `dev_request_intake_tokens` 자원에 대한 전체 CRUD 권한을 가지도록 DB 권한 매트릭스를 확립하고 백엔드 컨테이너 재시작.
- [x] **DREQ 라이프사이클 E2E 테스트 최종 합격**
    - [x] `dev-requests.spec.ts`를 실행하여 Intake Token 발급 ➡️ API 요청 ➡️ Promote ➡️ Revoke 로 연결되는 DREQ 전체 수명 주기가 100% 정상 작동하며 테스트를 완벽하게 통과함을 입증 (`1 passed`).
- [x] **개발 의뢰 설정 페이지 필터 바 표준화 및 UI/UX 일관성 강화**
    - [x] `dev-requests/page.tsx` 내의 비표준 인라인 검색 인풋 및 개별 버튼 형태의 상태 필터를 공통 표준 컴포넌트인 `FilterBar`로 전격 대체.
    - [x] 표준화 완료 후 Next.js Turbopack 빌드 및 타입 검증을 성공적으로 마쳤으며, 관련 `dev-requests.spec.ts` 및 `admin-permissions.spec.ts` E2E 테스트 전체 Green Pass를 보장함.

## 🧪 검증 결과
- `playwright test tests/e2e/dev-requests.spec.ts`: **1 passed**
- `playwright test tests/e2e/admin-permissions.spec.ts`: **2 passed**
- Frontend Vitest: **48 passed**
- Next.js Production Build: **Compiled successfully**

## 🔗 관련 문서
- [세션 인계 문서](../session_handoff.md)
- [Traceability Report](../../../../../docs/traceability/report.md)
