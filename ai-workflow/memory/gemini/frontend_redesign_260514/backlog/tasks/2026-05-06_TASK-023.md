---
id: TASK-023
status: done
created_at: 2026-05-06
---
# TASK-023 Command status transition worker 및 WebSocket publish 경계 구현

- 상태: done

## Description

- `pending` dry-run command를 자동으로 `succeeded` 상태로 전이시키는 backend worker를 구현한다.
- command status 전이 결과를 `command.status.updated` WebSocket event로 publish한다.
- 프론트 Phase 3 WebSocket 클라이언트가 기대하는 `/api/v1/realtime/ws` endpoint를 제공한다.

## Implementation Log

- 2026-05-06: TASK-022 이후 다음 우선순위로 command status transition과 WebSocket publish 경계를 확정했다.
- 2026-05-06: `backend-core/internal/commandworker`를 추가해 승인 불필요 dry-run command를 `pending -> running -> succeeded`로 전이한다.
- 2026-05-06: `PostgresStore.ListRunnableDryRunCommands`, `PostgresStore.UpdateCommandStatus`를 추가했다.
- 2026-05-06: `/api/v1/realtime/ws` endpoint와 `RealtimeHub`를 추가하고 command status 전이 결과를 `command.status.updated` event로 publish하도록 연결했다.
- 2026-05-06: `main.go`에서 DB-backed command worker를 2초 주기로 실행하도록 연결했다.

## Validation

- 통과: `cd backend-core && go test ./...`
- 통과: `cd frontend && npm run lint`
- 통과: `PYTHONPATH=ai-workflow /Users/yklee/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 ai-workflow/tests/check_docs.py`
- 통과: `python3 -m json.tool ai-workflow/memory/state.json`
- 통과: `git diff --check`

## Outcome

- 작업 결과: TASK-023 구현 완료. dry-run command 자동 상태 전이와 command status WebSocket publish 경계가 백엔드에 추가됐다.
- 다음 작업: 프론트 `RealtimeService` 구독을 command toast/status UI에 연결하고, 실제 executor/approval 경계를 설계한다.
