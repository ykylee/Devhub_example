---
id: TASK-021
status: done
created_at: 2026-05-06
---
# TASK-021 Workflow kit v0.5.0-beta 적용 및 existing project onboarding 갱신

- 상태: done

## Description

- 새 workflow kit `v0.5.0-beta` 파일을 현재 저장소 런타임 구조에 맞게 적용한다.
- 기존 프로젝트 온보딩 runner를 실행해 session-start, validation-plan, code-index-update 기준선을 확인한다.
- 운영 문서는 `ai-workflow/memory/` 기준으로 유지한다.

## Implementation Log

- 2026-05-06: `ai-workflow/VERSION` 기준 `v0.5.0-beta` 확인.
- 2026-05-06: 새 runner가 `workflow-source/` 경로를 찾는 문제를 확인하고 설치형 경로인 `ai-workflow/` 기준으로 Python runtime path를 보정.
- 2026-05-06: bundled Python에 `pydantic 2.13.3`이 있음을 확인하고 onboarding runner를 해당 Python으로 실행.
- 2026-05-06: `session_handoff.md`에 새 session-start parser가 요구하는 `현재 기준선`, `현재 주 작업 축` 섹션을 추가.
- 2026-05-06: `AGENTS.md`, `PROJECT_PROFILE.md`, `repository_assessment.md`의 실행 명령과 stack 추정을 실제 저장소 기준으로 정리.

## Validation

- `PYTHONPATH=ai-workflow <bundled-python> ai-workflow/scripts/run_existing_project_onboarding.py ...` 통과.
- `PYTHONPATH=ai-workflow <bundled-python> ai-workflow/tests/check_docs.py` 통과.
- `python3 -m json.tool ai-workflow/memory/state.json` 통과.
- `cd backend-core && go test ./...` 통과.
- `git diff --check` 통과.
- `cd frontend && npm run lint` 실패: main의 frontend Phase 2/3 코드에 남아 있는 `no-explicit-any` 11건과 warning 2건이 원인이다.

## Outcome

- 작업 결과: workflow kit `v0.5.0-beta` 온보딩을 현재 저장소 기준으로 적용했다.
- 남은 리스크: frontend lint 오류는 별도 프론트 정리 작업으로 분리한다.
