# 2026-05-07 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과를 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황 (Phase 6: 권한 관리 UI 고도화)
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress
- 최종 수정일: 2026-05-07
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md)

## 1. TASK-FRONTEND-PHASE6-RBAC 권한 관리 UI 고도화

- 상태: in_progress
- 모드: Implementation
- 우선순위: high
- 영향 문서: `docs/frontend_development_roadmap.md`, `ai-workflow/memory/session_handoff.md`

### 1.1 계획 (Plan)
- 작업 목표: `OrganizationPage` 내 권한 관리(RBAC) UI 고도화.
  - 사용자 및 그룹별 세부 권한을 설정할 수 있는 권한 편집기(Permission Editor) 설계.
  - 시스템 관리자 및 각 역할(Role)에 따른 제어 화면 접근성 강화.
  - 기존 조직 관리 UI 컴포넌트와의 통합 및 일관성 있는 레이아웃 구성.
- 완료 기준: Role Mapping 설정, Permission Editor UI 컴포넌트 동작, 권한 변경 상태 반영 확인 (인메모리 또는 연동 전 Mock 형태).

### 1.2 실행 (Act)
- 진행 현황: 
- `main` 브랜치 최신 상태(`7316ddd`, Phase 13) 확인 및 `gemini/phase6` 병합 완료.
- `docs/backend_api_contract.md`에 RBAC 정책 관리 API 계약(12절) 추가 정의.
- `frontend/lib/services/rbac.service.ts` 스캐폴딩 (정책 조회/업데이트, 역할 할당 API 연동 준비).
- `frontend/app/login/page.tsx` 프리미엄 로그인 페이지 구현 (OIDC/Kratos 연동 대비 Mock 로직 포함).
- `docs/frontend_development_roadmap.md`에 Phase 6.1(RBAC API 통합) 계획 수립 및 반영.
- `frontend/components/layout/AuthGuard.tsx` 생성 및 대시보드 레이아웃에 적용.
- `frontend/app/(dashboard)/account/page.tsx` 내 계정(프로필/비번 변경) 화면 구현.
- `frontend/components/organization/MemberTable.tsx`에 시스템 관리자 전용 계정 제어(발급/회수/재설정) 액션 통합.
- `frontend/lib/services/account.service.ts` 스캐폴딩 (Mock 구현).
- `frontend/lib/services/websocket.service.ts` 생성 (Pub/Sub 구조, 지수 백오프 기반 재연결, Mock 이벤트 타이머 내장).
- `frontend/lib/store.ts` 전역 알림 증가 액션(`incrementNotifications`) 추가.
- `AuthGuard.tsx` 내 웹소켓 생명주기 관리 및 전역 이벤트(`notification.created`, `risk.critical.created`) 구독 연동.

### 1.3 결과 (Result)
- (완료) RBAC API 계약 정의 및 프론트엔드 서비스 레이어 스캐폴딩.
- (완료) IdP 연동을 위한 로그인 UI 진입점 구축.
- (완료) 인증 가드(AuthGuard), 내 계정 화면, 조직 멤버 어드민 제어 UI 구현.
- (완료) Phase 3 기반: 웹소켓 클라이언트 Pub/Sub 로직 및 실시간 알림/에러 UI 피드백 연동.
- (완료) **백엔드 통합**: `test/backend-integration` 브랜치 생성 및 `codex/service-action-command` 병합 완료. API 계약서, 로드맵, RBAC 서비스/UI 충돌 해결 및 통합 검증 완료.
- (진행 중) RBAC 실데이터 연동 및 Kratos 백엔드 API 통합 대기.
