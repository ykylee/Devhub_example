# 2026-05-01 작업 백로그

- 문서 목적: 일일 작업 계획 및 결과 기록
- 범위: TASK-001 온보딩 및 TASK-002 프론트엔드 초기 개발
- 대상 독자: AI Agent, 프로젝트 관리자
- 작성자: Antigravity
- 프로젝트명: DevHub
- 상태: done
- 최종 수정일: 2026-05-01
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [백엔드 개발 로드맵](../../../backend_development_roadmap.md)

## TASK-001 표준 AI 워크플로우 초기 도입
- 상태: done
- 우선순위: high
- 담당: Antigravity
- 결과: 브랜치 기반 메모리 구조(`gemini/frontend_phase1`) 적용 및 문서 정합성 강화 완료.

## TASK-002 프론트엔드 초기 개발 (레이아웃 및 대시보드)
- 상태: done
- 우선순위: high
- 담당: Antigravity
- 영향 문서: docs/requirements.md, docs/architecture.md, frontend/app/

### 1. 계획 (Plan)
- 작업 목표: 역할별(개발자, 관리자, 시스템 관리자) 대시보드 레이아웃 구축 및 프리미엄 UI 디자인 적용.
- 완료 기준: 역할별 뷰 전환 정상 작동, Glassmorphism 스타일 적용, Lint 통과.

### 2. 실행 (Act)
- 진행 현황: 
    - `globals.css` 및 `layout.tsx` 스타일링 완료.
    - `Sidebar`, `Header` 인터랙티브 컴포넌트 구현 완료.
    - 역할별(Developer, Manager, Admin) 대시보드 스캐폴딩 완료.
- 수정 사항: ESLint 경고 및 에러 대응 완료.

### 3. 검증 (Validate)
- 테스트 결과: `npm run lint` 통과.
- 미실행 사유: 없음.

### 4. 결과 (Result)
- 작업 결과: 프론트엔드 초기 시각화 및 인터페이스 구축 완료.
- 다음 세션 시작 포인트: TASK-003 백엔드 gRPC 연동 및 실시간 데이터 스트리밍.
- 남은 리스크 및 후속 작업: 실제 데이터 연동 시 gRPC 클라이언트 설정 필요.
# 2026-05-03 작업 백로그

- 목적: 백엔드 도메인 정규화 1차 기반 작업 기록
- 관련: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [백엔드 개발 로드맵](../../../backend_development_roadmap.md)

## TASK-013 도메인 정규화 1차 기반 구현

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: `docs/backend_api_contract.md`, `backend-core/migrations/`, `backend-core/internal/normalize/`, `ai-workflow/memory/backend_development_roadmap.md`

### 1. 계획 (Plan)

- 작업 목표: raw webhook 수집 이후 repository/issue/PR/CI/risk 도메인으로 확장할 수 있는 최소 DB schema와 fixture 기반 정규화 processor를 추가한다.
- 범위:
  - API 계약에 role wire format, WebSocket envelope, command/audit lifecycle 초안 보강.
  - `gitea_users`, `repositories`, `issues`, `pull_requests`, `ci_runs`, `risks` migration 추가.
  - Gitea `issues`, `pull_request`, `action_run/workflow_run`, `push` 이벤트를 최소 domain change set으로 해석하는 processor와 fixture 테스트 추가.

### 2. 실행 (Act)

- `docs/backend_api_contract.md`에 공통 enum, WebSocket envelope, command lifecycle 예시를 추가했다.
- `backend-core/migrations/000002_create_domain_tables.*.sql`을 추가했다.
- `backend-core/internal/normalize/gitea.go`에 `Normalize`와 `Processor`를 추가했다.
- `backend-core/internal/normalize/testdata/`에 issue, pull request, action run fixture를 추가했다.
- 홈랩 PostgreSQL `devhub` DB에 migration version `2`를 적용했다.

### 3. 검증 (Validate)

- `cd backend-core && go test ./...` 통과.
- `migrate ... up`으로 migration `2/u create_domain_tables` 적용 확인.
- `migrate ... version` 결과 `2`.

### 4. 결과 (Result)

- 작업 결과: Phase 6 도메인 정규화가 `in_progress`로 진입했다. 아직 Postgres upsert sink와 raw webhook 저장 이후 자동 처리 경로는 남아 있다.
- 다음 작업:
  1. `backend-core/internal/store`에 domain upsert sink 구현.
  2. `normalize.Processor`를 raw webhook 처리 경로 또는 worker에 연결.
  3. repository/issue/PR/ci_run/risk 조회 API 초안 구현.

## TASK-014 도메인 정규화 sink 및 webhook 처리 경로 연결

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: `backend-core/internal/domain/`, `backend-core/internal/store/`, `backend-core/internal/httpapi/`, `backend-core/main.go`, `ai-workflow/memory/backend_development_roadmap.md`

### 1. 계획 (Plan)

- 작업 목표: fixture 기반 normalizer를 실제 PostgreSQL 저장소와 webhook 처리 경로에 연결한다.
- 범위:
  - 정규화 domain type과 sink interface를 순환 참조 없이 분리.
  - `PostgresStore`에 repository/user/issue/pull_request/ci_run upsert 구현.
  - `webhook_events` 상태 전이 메서드(`processed`, `failed`, `ignored`) 구현.
  - webhook 저장 성공 이후 optional processor 실행 경로 추가.

### 2. 실행 (Act)

- `backend-core/internal/domain/domain.go`를 추가해 정규화 결과 type과 sink interface를 분리했다.
- `backend-core/internal/store/postgres.go`에 domain upsert와 webhook 상태 전이 메서드를 추가했다.
- `backend-core/internal/httpapi`에 `WebhookEventProcessor` interface를 추가하고, webhook 저장 성공 이후 processor를 호출하도록 연결했다.
- `backend-core/main.go`에서 DB store가 설정된 경우 `normalize.Processor{Sink: pgStore}`를 router에 주입했다.
- `backend-core/internal/store/postgres_integration_test.go`를 추가해 홈랩 PostgreSQL 기반 통합 테스트를 환경 변수 opt-in 방식으로 구성했다.

### 3. 검증 (Validate)

- `cd backend-core && go test ./...` 통과.
- `DEVHUB_TEST_DB_URL=... go test ./internal/store -run TestPostgresStoreProcessesNormalizedWebhookEvent -count=1 -v` 통과.

### 4. 결과 (Result)

- 작업 결과: Phase 6 도메인 정규화 1차의 핵심 경로가 연결됐다. raw webhook은 저장 후 즉시 정규화 processor를 거쳐 도메인 테이블 upsert와 `processed|failed|ignored` 상태 전이를 수행할 수 있다.
- 다음 작업:
  1. repository/issue/PR/ci_run/risk DB-backed 조회 API 구현.
  2. static fallback snapshot provider를 DB-backed provider로 교체할 경계 설계.
  3. command/audit 최소 schema 및 service action/risk mitigation API 구현.

## TASK-015 DB-backed 도메인 조회 API 1차 구현

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: `backend-core/internal/domain/`, `backend-core/internal/store/`, `backend-core/internal/httpapi/`, `backend-core/main.go`, `docs/backend_api_contract.md`, `ai-workflow/memory/backend_development_roadmap.md`

### 1. 계획 (Plan)

- 작업 목표: 정규화된 PostgreSQL 도메인 테이블을 프론트 조회 API로 노출하고, 기존 static CI snapshot을 DB-backed provider로 일부 전환한다.
- 범위:
  - repository/issue/pull_request/ci_run list store 메서드 추가.
  - `GET /api/v1/repositories`, `GET /api/v1/issues`, `GET /api/v1/pull-requests` handler 추가.
  - `GET /api/v1/ci-runs`가 DB 결과를 우선 사용하고 데이터가 없으면 static fallback을 유지하도록 연결.
  - API 계약 문서에 도메인 조회 API 1차를 반영.

### 2. 실행 (Act)

- `domain.ListOptions`와 조회 응답에 필요한 row id/updated_at 필드를 추가했다.
- `PostgresStore`에 `ListRepositories`, `ListIssues`, `ListPullRequests`, `ListCIRuns`를 추가했다.
- `httpapi.DomainStore`를 router config에 추가하고 `main.go`에서 PostgreSQL store를 주입했다.
- `backend-core/internal/httpapi/domain.go`에 repository/issue/pull request handler와 공통 list envelope를 추가했다.
- `ciRuns` handler는 DB 결과가 있으면 `meta.source=db`, 없으면 기존 static fallback과 `meta.source=static`을 반환하도록 정리했다.

### 3. 검증 (Validate)

- `cd backend-core && go test ./...` 통과.
- `DEVHUB_TEST_DB_URL=... go test ./internal/store -run TestPostgresStoreProcessesNormalizedWebhookEvent -count=1 -v` 통과.

### 4. 결과 (Result)

- 작업 결과: 정규화된 repository/issue/PR/CI run 데이터를 프론트가 조회할 수 있는 1차 API가 열렸다.
- 다음 작업:
  1. risk DB-backed 조회와 risk 생성 경로 설계.
  2. metrics/infra snapshot provider를 DB 또는 adapter 기반으로 전환.
  3. command/audit 최소 schema 및 service action/risk mitigation API 구현.

## TASK-016 metrics/infra snapshot provider 경계 분리

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: `backend-core/internal/httpapi/`, `backend-core/main.go`, `docs/backend_api_contract.md`, `ai-workflow/memory/backend_development_roadmap.md`

### 1. 계획 (Plan)

- 작업 목표: metrics/infra/risk snapshot handler가 static 함수에 직접 묶이지 않도록 provider 경계를 만든다.
- 범위:
  - `SnapshotProvider` interface와 `StaticSnapshotProvider` 추가.
  - dashboard metrics, infra nodes/edges/topology, CI fallback, CI logs, critical risks handler를 provider 기반으로 전환.
  - provider source를 응답 meta에 포함.
  - custom provider 주입 테스트 추가.

### 2. 실행 (Act)

- `backend-core/internal/httpapi/snapshot_provider.go`를 추가해 static snapshot data source를 provider로 분리했다.
- `RouterConfig`에 `SnapshotProvider`를 추가하고 `main.go`에서 `StaticSnapshotProvider`를 주입했다.
- 기존 `snapshot.go` handler들은 `h.snapshotProvider()`를 통해 data source를 읽도록 변경했다.
- `snapshot_test.go`에 injected provider 기반 metrics/topology 테스트를 추가했다.

### 3. 검증 (Validate)

- `cd backend-core && go test ./...` 통과.

### 4. 결과 (Result)

- 작업 결과: snapshot API의 data source 교체 경계가 생겼다. 다음 단계에서 metrics/infra/risk별 DB 또는 adapter provider를 별도로 구현할 수 있다.
- 다음 작업:
  1. infra provider를 Gitea/Runner/PostgreSQL health adapter 기반으로 구현할 범위 확정.
  2. risk DB-backed 조회와 risk 생성 경로 설계.
  3. command/audit 최소 schema 및 service action/risk mitigation API 구현.

## TASK-017 Runtime infra snapshot provider 구현

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: `backend-core/internal/config/`, `backend-core/internal/httpapi/`, `backend-core/main.go`, `docker-compose.yml`, `docs/backend_api_contract.md`, `docs/tech_stack.md`

### 1. 계획 (Plan)

- 작업 목표: infra snapshot provider가 실제 런타임 health 정보를 반영하도록 1차 adapter를 구현한다.
- 범위:
  - `RuntimeSnapshotProvider` 추가.
  - PostgreSQL은 `HealthStore.Ping`, Gitea는 `GITEA_URL`, Python AI는 `BACKEND_AI_URL/health`로 상태 확인.
  - infra node/edge status를 runtime check 결과로 보강.
  - Docker Compose와 기술 스택 문서에 `BACKEND_AI_URL` 반영.

### 2. 실행 (Act)

- `backend-core/internal/httpapi/runtime_snapshot_provider.go`를 추가했다.
- `backend-core/internal/config/config.go`에 `BACKEND_AI_URL` 설정을 추가했다.
- `backend-core/main.go`에서 `RuntimeSnapshotProvider`를 주입하도록 변경했다.
- `docker-compose.yml`의 backend-core 환경 변수에 `BACKEND_AI_URL=http://backend-ai:8000`을 추가했다.
- runtime provider 단위 테스트를 추가했다.

### 3. 검증 (Validate)

- `cd backend-core && go test ./...` 통과.

### 4. 결과 (Result)

- 작업 결과: `/api/v1/infra/*` snapshot은 static topology shape를 유지하면서 DB/Gitea/Python AI health 상태를 반영할 수 있게 됐다.
- 다음 작업:
  1. Gitea Runner 세부 상태 adapter 또는 Gitea REST client 연동 범위 확정.
  2. risk DB-backed 조회와 risk 생성 경로 설계.
  3. command/audit 최소 schema 및 service action/risk mitigation API 구현.

## TASK-018 risk DB-backed 조회 및 CI 실패 risk 생성 경로 구현

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: `backend-core/internal/domain/`, `backend-core/internal/normalize/`, `backend-core/internal/store/`, `backend-core/internal/httpapi/`, `docs/backend_api_contract.md`, `ai-workflow/memory/backend_development_roadmap.md`

### 1. 계획 (Plan)

- 작업 목표: risk 도메인을 static snapshot에서 실제 정규화/DB 조회 경로로 연결한다.
- 범위:
  - `domain.Risk`와 sink/list query 경계를 추가한다.
  - CI 실패 이벤트에서 risk를 생성해 PostgreSQL `risks` 테이블에 upsert한다.
  - `GET /api/v1/risks` 목록 API와 `/api/v1/risks/critical` DB 우선 경로를 추가한다.

### 2. 실행 (Act)

- `domain.ChangeSet`과 `domain.Sink`에 risk를 추가했다.
- Gitea `action_run/workflow_run` 실패 정규화 시 `ci_failure:{ci_run_id}` risk를 생성하도록 확장했다.
- `PostgresStore`에 `UpsertRisk`, `ListRisks`를 추가하고 suggested actions JSONB를 처리하도록 했다.
- `GET /api/v1/risks` handler를 추가했고, `status`, `impact`, `limit`, `offset` query를 지원한다.
- `/api/v1/risks/critical`은 action-required high risk가 DB에 있으면 `meta.source=db` 응답을 반환하도록 연결했다.

### 3. 검증 (Validate)

- `cd backend-core && go test ./...` 통과.
- `DEVHUB_TEST_DB_URL=... go test ./internal/store -run TestPostgresStoreProcessesNormalizedWebhookEvent -count=1 -v` 통과.

### 4. 결과 (Result)

- 작업 결과: CI 실패 webhook은 정규화 과정에서 manager dashboard가 조회할 수 있는 risk로 이어질 수 있게 됐다.
- 다음 작업:
  1. command/audit 최소 schema 및 risk mitigation command API 구현.
  2. Gitea Runner 세부 상태 adapter 또는 Gitea REST client 연동 범위 확정.
  3. metrics DB-backed provider 구현 범위 확정.
