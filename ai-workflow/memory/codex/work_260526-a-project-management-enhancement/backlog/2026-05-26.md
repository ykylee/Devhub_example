# Backlog — 2026-05-26

- 목적: 프로젝트 관리 기능 고도화 작업을 위한 브랜치 및 workflow memory를 초기화한다.
- 상태: in_progress

## 오늘 완료

- [x] `main` 최신 동기화 (`origin/main` fast-forward)
- [x] 작업 브랜치 생성 (`codex/work_260526-a-project-management-enhancement`)
- [x] 브랜치 전용 memory 문서 4종 초기화
- [x] 프로젝트 모델 1단계 hybrid 골격 구현
  - `project_repositories` migration + 기존 `projects.repository_id` 백필
  - v2 API 추가: `/applications/:application_id/projects`, `/projects/:project_id/repositories`
  - legacy API 유지: `/repositories/:repository_id/projects`
  - 권한 매핑(routePermissionTable) + 테스트 통과
- [x] deploy/JWKS 안정화
  - `scripts/deploy-from-env.sh`: local-idp 기본 JWKS를 `http://nginx/.../certs`로 고정
  - `scripts/deploy-preflight.sh`: `host.docker.internal` 경고 + `nginx` loopback fallback 검증 추가
- [x] e2e seed + strict 검증 정비
  - `global-setup.ts`: users + repositories fixture seed
  - admin e2e `page.goto("/...")` -> `appPath("/...")` 전수 정비
  - strict 모드(`DEVHUB_E2E_STRICT_ADMIN_UI=1`) admin 핵심 23개 `passed`

## 다음 액션

- [ ] PR 생성(변경 요약 + 검증 로그 + 리스크/가드 설명)
- [ ] traceability report/PR template "추적성 영향" 섹션 동기화
- [ ] 리뷰 피드백 반영(필요 시 strict 모드/기본 모드 분리 설명 보강)

## 메모

- strict 실패 원인의 대부분은 RBAC가 아니라 basePath(`/devhub`) 누락으로 확정.
- 운영/테스트 환경 편차를 위해 `DEVHUB_E2E_STRICT_ADMIN_UI` 토글을 유지:
  - 기본: 환경 편차 시 skip(안정)
  - strict=1: 동일 조건 fail 승격(프로필 고정 검증)
