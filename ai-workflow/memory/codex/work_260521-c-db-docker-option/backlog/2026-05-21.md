# Backlog — 2026-05-21

- 목적: DB docker 옵션을 추가하고 local-db compose profile 정합성을 검증한다.
- 상태: done

## 오늘 완료

- [x] `deploy-from-env.sh` 에 `DB_MODE=docker` 옵션 추가
- [x] env 파일 shell-safe 출력으로 변경
- [x] `deploy-preflight.sh` / `deploy-up.sh` 가 `COMPOSE_PROFILES` 를 읽도록 수정
- [x] `DB_MODE=docker` 기준 이미지 빌드 경로 검증
- [x] `deploy-preflight.sh` 렌더 검증 통과
- [x] `docker build` 의 Dockerfile 경로를 명시적으로 고정
- [x] `IMAGE_REPO_PREFIX` 기본값을 `local/devhub` 로 변경
- [x] Dockerfile 3개를 git 추적 대상으로 전환

## 다음 액션

- [x] 변경분 커밋 완료 (`cf29f9c`)
- [ ] 푸시/PR 생성

## 메모

- local-db profile 렌더를 위해 keycloak 기본값도 env 파일에 같이 저장한다.
