# Backlog — 2026-05-21

- 목적: host build 패키징으로 전환하고 nginx 23000 ingress 정합성을 검증한다.
- 상태: in_progress

## 오늘 완료

- [x] `scripts/build-artifacts.sh` 추가
- [x] `deploy-from-env.sh` 가 host build 를 선행하도록 연결
- [x] backend-core / backend-ai / frontend Dockerfile 을 runtime-only 로 전환
- [x] nginx 80 server block 의 HTTPS redirect 제거
- [x] compose nginx 의 443/TLS 포트와 인증서 마운트 제거
- [x] `absolute_redirect off` 로 `/devhub` 리다이렉트 포트 누락 방지
- [x] `http://127.0.0.1:23000/devhub/api/runtime-config` 검증
- [x] `http://127.0.0.1:23000/devhub/` 검증
- [x] `infra/idp/keycloak-realm.dev.json` 의 `sslRequired` 를 `none` 으로 완화
- [x] `infra/idp/keycloak-realm.prod.json` 의 `sslRequired` 를 `none` 으로 완화
- [x] Keycloak `master` realm 의 `sslRequired` 를 `none` 으로 갱신
- [x] `backend-core/go.mod` 의 Go 버전을 `1.25.9` 로 고정
- [x] `setup-keycloak.sh` 로 `devhub-e2e-seeder` client secret 재발급
- [x] e2e seed 의 Keycloak HTTPS 요구 해소
- [x] `backend-core/Dockerfile` 의 `apk add --no-cache ca-certificates` 제거
- [x] `frontend/Dockerfile` 의 빈 `public` copy 제거
- [x] `deploy-from-env.sh` 의 `ACTION=push` 분기 제거
- [x] 외부 접속 기준 주소/포트를 `PUBLIC_ACCESS_*` 로 분리

## 다음 액션

- [ ] 변경분 커밋/푸시 및 PR 반영
- [ ] host-run e2e 의 DB 접근 경로 분리
- [ ] HTTPS outbound 가 필요할 때만 별도 CA 번들 전략 검토

## 메모

- e2e seed 는 더 이상 HTTPS 때문에 막히지 않지만, host-run `idp-apply-schemas` 는 DB 호스트명 해석이 필요하다.
