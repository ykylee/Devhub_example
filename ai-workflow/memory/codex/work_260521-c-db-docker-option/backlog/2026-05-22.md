# Backlog — 2026-05-22

- 목적: 본 브랜치를 `origin/main` 위로 rebase 하고 메모리 정합을 정정한다.
- 상태: in_progress

## 오늘 완료

- [x] `origin/main` (`1239f3c`) 위로 rebase
- [x] PR #282 squash 흡수분 3 commit (`6ed6d51` / `ef244a0` / `f08360f`) drop
- [x] 잔여 12 commit replay (새 HEAD `61e0937`)
- [x] 충돌 해소 (memory 파일 4건 skip, deploy 스크립트는 replay 시 자동 처리)
- [x] `bash -n` syntax 검증 (deploy-from-env / deploy-up / build-artifacts / deploy-preflight)
- [x] 핵심 변경 보존 검증 (Go 1.25.9 / sslRequired=none / PUBLIC_ACCESS_*)
- [x] `git push --force-with-lease` 로 원격 갱신
- [x] 백업 ref `backup/pre-rebase-main-260522` (구 HEAD `3385bec`) 보존
- [x] `state.json` / `work_backlog.md` / `session_handoff.md` / `backlog/2026-05-21.md` 정합 갱신
- [x] BUILD-ARTIFACT-09/10/12 done 처리 + BUILD-ARTIFACT-13 (rebase) 항목 추가

## 추가 완료 (rebase 이후)

- [x] nginx conf ↔ template auto-sync 도입 (`3ae46cf`) — `scripts/nginx-conf-sync.sh` + preflight `[0/3]` hook + `.gitignore` + stale `devhub.deploy.conf` git rm. 외부 `:13000` → `:80/devhub` redirect 회귀 원인 fix.
- [x] Keycloak image 26.0 → 25.0 pin (`42b18b1`) — compose/CI/dev-up 3 위치 active code only, docs historical immutable 보존.
- [x] ADR-0022 draft 발행 (`af9b301`) — `docs/adr/0022-keycloak-version-pin-25-0.md`. §3.1 retreat 사유 placeholder.
- [x] Python image 3.13 → 3.12 pin (`7626a8c`) — backend-ai/Dockerfile + tech_stack.md + environment-setup.md + build-artifacts.sh ABI 주의 주석.
- [x] state.json / work_backlog.md / session_handoff.md / backlog 정합 갱신 (다회).

## 다음 액션

- [ ] **사용자 사내 환경 redeploy + :13000 smoke test** — preflight 자동 sync + Keycloak 25.0 pull. 절차는 `session_handoff.md` 참조.
- [ ] ADR-0022 §3.1 retreat 사유 finalize → Accepted 승격.
- [ ] BUILD-ARTIFACT-08 (host-run e2e DB 접근 경로) 진입.
- [ ] PR 생성 여부 결정 (현재 push 만 완료).
- [ ] HTTPS outbound 필요 시점에 별도 CA 번들 전략 검토 (선택).

## 메모

- rebase 시 git 이 `f08360f` 는 `patch contents already upstream` 으로 자동 drop, 나머지 2 commit (`6ed6d51`, `ef244a0`) 은 memory 파일 충돌로 `--skip` 처리. 셋 다 PR #282 가 squash 흡수해 main 에 동등 내용 존재.
- 12 commit 은 모두 새 hash 로 재작성됨 (`bcdabe9` → ... → `61e0937`).
- local/remote 동기화 확인 (`git rev-list --left-right --count HEAD...origin/<branch>` = `0 0`).
- `latest` tag 사용 검토 → 4 risk (재현 불가 / silent upgrade / rollback 소실 / Keycloak 호환 risk) 로 거부. 현재 마이너 pin (`25.0`) sweet spot 유지.
- ADR-0022 의 §3.1 사유 placeholder 는 사용자가 finalize 동반. 임시 보관 후보 사유: 사내 표준 25.x 정합 / 26.x known issue / JDK/JDBC 호환 등. 실 결정 확인 후 채움.
- Python 3.12 pin 은 별도 ADR 발행 안 함 — base image 의 routine minor pin 결정 (ADR-worthy 한 strategic shift 아님). 향후 메이저 bump (3.13/3.14) 진입 시 ADR 검토.
- host build ABI 호환 강제: `pip install --target` 패턴이 site-packages 를 컨테이너에 복사하므로 host python = container python (마이너 일치) 필수. 사내 배포 머신 환경 점검 동반.
