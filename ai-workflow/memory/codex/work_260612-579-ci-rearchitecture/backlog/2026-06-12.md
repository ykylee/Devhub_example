# Backlog — 2026-06-12

## 브랜치
`codex/work_260612-579-ci-rearchitecture`

## 작업 요약
**CI 재구성 설계 + flaky 복구 구현 + draft PR 준비**

### 완료
- `docs/planning/2026-06-12-ci-rearchitecture-design.md` 작성
- `.github/workflows/ci.yml` fast required + smoke 구조로 정리
- `.github/workflows/e2e-regression.yml` 신규 추가
- `.github/workflows/e2e-quarantine.yml` 신규 추가
- `frontend/tests/e2e-manifests/{smoke,quarantine}.txt` 추가
- `scripts/select-playwright-specs.sh` 추가
- `frontend/tests/e2e/fixtures.ts` OIDC restart/login loop recovery 강화
- `frontend/tests/e2e/signout.spec.ts` timeout 상향

### 검증
- workflow YAML parse OK
- `scripts/ci-e2e-sync-check.sh` 3 workflow OK
- selector script smoke/quarantine/regression 출력 OK
- focused `tsc --noEmit` OK

### 결과
- PR #580 merged
- old CI failure PR #579도 최신 `main` 반영 후 새 workflow 구조에서 재평가 통과
- full frontend `tsc --noEmit` 는 기존 repo 선행 오류로 여전히 별도 과제

## 다음 작업 제안
- branch protection required check 목록 문서/설정 정합
- quarantine spec 추세 관찰 자동화 검토
