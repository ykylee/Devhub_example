# 2026-06-10 sprint backlog — feat/work_260610-d-72-wiki-phase-1

- 문서 목적: 본일 작업 항목 + 본일 학습 / 발견.
- 범위: 본일 session (2026-06-10, single session) 의 6 단계 작업 — (1) D-72 응답 검토. (2) 기존 docs/wiki/ 와 의 분리 정공법. (3) docs/llm-wiki/ 5 file 작성. (4) scripts/wiki-sync-devhub.sh 작성 (BSD-rsync safe, 82 file source list, --dry-run + vault 부재 no-op). (5) lint 4종 all PASS + script smoke test. (6) sprint memory 5 file. 본일 학습 8건 (두 wiki 분리 / D-72 §3 단순화 정합 / 82 file core subset / ADR file naming 4-digit / list_sources helper / supersede L07 면제 / scratch/ exclude / mirror 실행 out-of-scope).
- sprint branch: feat/work_260610-d-72-wiki-phase-1
- 대상 독자: 후속 에이전트, owner
- 상태: in_progress
- 최종 수정일: 2026-06-10
- 관련 문서: session_handoff.md (../session_handoff.md), work_backlog.md (../work_backlog.md), state.json (../state.json), pr_body.md (../pr_body.md)
## 본일 작업 (1 회 session, in_progress → done)

### 1. D-72 응답 검토 (T-d-72-0, done)
- scratch/devhub_wiki_integration_response/RESPONSE.md (15KB) — my_harness 작업 에이전트 의 6 질문별 권장안 + 8 위험 + 6 next-step.
- yklee 결정: 권장안 전체 승인.

### 2. 기존 docs/wiki/ 와 의 분리 (T-d-72-1, done)
- 본 저장소 (DevHub) 의 기존 `docs/wiki/` = **Public Wiki** (GitHub Wiki 게시 source, 인간 큐레이션, mtime 2026-05-20).
- D-72 응답의 `~/wiki/` = **LLM Wiki** (Obsidian vault, LLM agent RAG source, out-of-repo).
- 두 wiki 의 audience + source-of-truth 가 다름 → 디렉터리 분리 결정 = **`docs/llm-wiki/`** (vs `docs/wiki/` 또는 `docs/wiki-integration/`).

### 3. docs/llm-wiki/ 5 file 작성 (T-d-72-1, done)
- README.md (7.8KB) — 5 file root index + 두 wiki 분리 + 디렉터리 구조 + Phase 1 의 의의/한계 + forward path + Tier 정책 정합.
- scope-and-rationale.md (10.6KB) — D-72 6 질문의 본 저장소 측 적용.
- mirror-list.md (10KB) — Phase 1 source list (core subset ~82 file).
- lint-config.toml (4.4KB) — per-project config (TOML, L07 ADR 면제).
- operation-sop.md (10.7KB) — sync + lint SOP + sync 위험 10건.

### 4. scripts/wiki-sync-devhub.sh 작성 (T-d-72-1, done)
- BSD-rsync safe pattern (my_harness 의 wiki-sync-ai-workflow.sh 와 동일).
- 7 source 패턴 (82 file, ~3.5MB).
- --dry-run / real / --help mode + exit 0/1.
- vault 부재 시 명시적 error + hint.

### 5. lint 4종 all PASS (T-d-72-2, done)
- check-tier-separation.sh: no changes between origin/main and HEAD (이 PR 의 모든 변경 = docs/llm-wiki/ + scripts/ = 공용).
- check-openapi-yaml-lint.sh: passed.
- check-migration-uniqueness.sh: valid and unique.
- check_docs.py: 본 PR 의 5 file 정합 (metadata 6 field + cross-link + 제목 헤더).
- bash scripts/wiki-sync-devhub.sh --dry-run: PASS (82 file, exit 0).
- HOME=/tmp/fake_home bash scripts/wiki-sync-devhub.sh: vault 부재 시 명시적 error + exit 1.

### 6. sprint memory 5 file 작성 (T-d-72-2, done)
- state.json + session_handoff.md + work_backlog.md + backlog/2026-06-10.md + pr_body.md.

## 잔여 / 후속

- **T-d-72-3** (pending): commit + push + gh pr create --body-file pr_body.md + gh pr merge --squash --delete-branch — 사용자 confirm 대기.
- **T-d-72-4** (planned): main flat memory 3 file sync (post-merge).

## 본일 학습 / 발견

- 본 저장소 (DevHub) 의 기존 `docs/wiki/` = Public Wiki (GitHub Wiki 게시 source, 인간 큐레이션). **두 wiki 의 분리 정공법** = 디렉터리 이름 분리 (`docs/wiki/` Public vs `docs/llm-wiki/` LLM).
- D-72 응답 §3 단순화 (lint L11 + sa-internal/ 격리 불요) 의 본 저장소 측 정합 = `lint-config.toml` 의 lint config 가 L01~L10 + L07 ADR 면제 만 포함. lint L11 (사내 패턴 검출) 미사용. yklee 결정 (wiki 는 Gitea private 만) 의 정책 정합.
- D-72 응답 §4 #3 의 "100~200 파일" 의 1/2 (~82 file) = **core subset** 정공법. domain (66) + architecture + infrastructure + validation (~100 file) 은 Phase 3 (mass ingest) 의 별도 PR. 본 PR 의 검증 가능한 정공법 (CI 4/4 + script smoke test) = 작은 core subset.
- ADR file naming = `0001-idp-selection.md` (4-digit prefix + title), NOT `ADR-0001-...`. mirror-list.md §1.1 + script 의 `0[0-9][0-9][0-9]-*.md` 패턴 정합.
- 스크립트 의 `list_sources` helper = dry-run + real 양쪽 source list 의 single source-of-truth. 중복 코드 회피. (초기 script 에서 stale block + syntax error → rewrite 로 clean version 작성)
- lint-config.toml 의 `[rules.L07].skip_if_frontmatter = ["supersedes"]` = frontmatter 의 `supersedes:` 가 있으면 L07 skip (DevHub 의 의도적 supersede 정공법, ADR-0030 → ADR-0031).
- 스크립트 의 source list 에서 `scratch/devhub_wiki_integration_response/RESPONSE.md` (D-72 응답) 미포함 — vault 비대화 + D-72 응답은 my_harness 측 작업. 마찬가지로 `backend-core/` / `frontend/` / `backend-ai/` 의 source code 미포함 — LLM agent 의 코드 정합은 `code-index-update` 의 영역.
- mirror 실행은 본 PR scope 외 (사용자 confirm 후 T-d-72-5). 본 PR 의 lint 검증 = `--dry-run` source list 정합만. **mirror 실행은 `~/wiki/` out-of-repo 변경** — 본 PR 의 in-repo 검증 가능 영역 (CI 4/4) 의 검증 범위 외.
