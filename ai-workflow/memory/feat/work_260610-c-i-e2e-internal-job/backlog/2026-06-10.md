- 문서 목적: 본일 작업 항목 + 본일 학습 / 발견.
- sprint branch: feat/work_260610-c-i-e2e-internal-job
- 대상 독자: 후속 에이전트, owner
- 상태: in_progress
- 최종 수정일: 2026-06-10
- 관련 문서: session_handoff.md (../session_handoff.md), work_backlog.md (../work_backlog.md), state.json (../state.json)

## 본일 작업 (1 회 session, in_progress → done)

### 1. C-i scope research (T-c-i-e2e-internal-0, done)
- explore subagent 로 C-i scope 4 section research 발효.
- 결과: 옵션 A 권장 (e2e shard 1/2/3 + e2e-internal 1개). 5 file scope 확인 (ci.yml + script + e2e shard 1/2/3 변경 0 + backend 0 + frontend 0 + e2e spec 0 + docs 0 + Makefile 0 + ADR 0 + migration 0).
- 변경 file 2개 (ci.yml + script), 변경 line ≈ 50 (research report 의 estimate + 5 comment line).

### 2. ci.yml e2e-internal job 신규 (T-c-i-e2e-internal-1, done)
- 23 step 의 e2e-internal job 추가. PG 15 + Keycloak port 8181 + apply migrations + validate contract + Start Backend (DEVHUB_BUILD_TIER=internal) + Start Frontend + Wait + Run E2E Tests + Upload Report + Upload Logs.
- e2e shard 1/2/3 의 env block, start command, test invocation 모두 변경 0.
- YAML parse: ✅ jobs = 10 (e2e-internal 신규), 23 step, DEVHUB_BUILD_TIER env 1+ 곳.

### 3. ci-e2e-sync-check.sh 의도적 미포함 (T-c-i-e2e-internal-2, done)
- required_e2e_tokens 변경 0, DEVHUB_BUILD_TIER 미포함 rationale comment 4 line 추가.
- 의도: e2e shard 1/2/3 의 saovae_stub default 의 env block 미설정 유지. e2e-internal 의 DEVHUB_BUILD_TIER env 정합은 actionlint + 실제 e2e run 이 검증.

### 4. lint 검증 (T-c-i-e2e-internal-2, done)
- check-tier-separation.sh: ✅ no changes between origin/main and HEAD (이 PR 의 변경 = ci.yml + script = 공용)
- check-openapi-yaml-lint.sh: ✅ passed
- check-migration-uniqueness.sh: ✅ valid and unique
- ci-e2e-sync-check.sh: ✅ E2E-CI sync contract check passed

### 5. sprint memory 4종 + pr_body.md (T-c-i-e2e-internal-2, done)
- ai-workflow/memory/feat/work_260610-c-i-e2e-internal-job/{state.json, session_handoff.md, work_backlog.md, backlog/2026-06-10.md, pr_body.md} 5 file 신규.

## 잔여 / 후속

- **T-c-i-e2e-internal-3** (pending): commit + push + gh pr create --body-file pr_body.md — 사용자 confirm 대기.
- **T-c-i-e2e-internal-4** (planned): main flat memory 4종 sync (post-merge).

## 본일 학습 / 발견

- ADR-0030 §2.3 의 runtime injection 결정 (build tag 미사용 + `DEVHUB_BUILD_TIER` env var) 의 code-level 정합 = CI matrix 1쌍 (default + internal). 본 PR 이 본 결정의 code-level 적용.
- ci-e2e-sync-check.sh 의 contract 의미 = "ci.yml + e2e helper 양쪽 어디든 token 존재". e2e-internal job 의 env block 에 DEVHUB_BUILD_TIER 가 있어도 e2e shard 1/2/3 (saovae_stub default) 의 env block 에는 미설정. script 의 required_e2e_tokens 에 DEVHUB_BUILD_TIER 추가 시 e2e shard 의 env block 도 추가해야 contract pass — 본 PR 의 의도 (saovae_stub default 유지) 와 위배. 정공법 = script token 미포함.
- frontend e2e env 의 DEVHUB_BUILD_TIER 추가 불요 — frontend 의 e2e logic 가 backend 의 build tier 무관 (auth, RBAC, CRUD 모두 backend API 만 호출). e2e-internal shard 의 frontend test 가 backend 가 real adapter 인 채로 정상 동작 (frontend 는 backend 의 API contract 만 의존).
- Keycloak container port 8181 (e2e shard 의 8180 과 분리) — e2e shard 와 e2e-internal 동시 trigger 가능. CI runtime 이 둘 다 동시 trigger 되어도 port 충돌 0.
- Playwright shard 1/1 — logout flow 외 다른 e2e suite (auth, RBAC, application CRUD 등) 는 backend 의 build tier 무관. 1 shard (≈ 4-5min) 만으로 충분.
- research artifact: explore subagent 가 artifact://research-c-i-scope.md 에 write 시도했으나 EPERM 으로 실패. inline emit only. 본인이 caller 의 session 에 직접 emit 한 결과는 architecture 필드 + summary.
