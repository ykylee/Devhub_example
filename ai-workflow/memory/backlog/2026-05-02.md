# 2026-05-02 작업 백로그

- 목적: 백엔드 초기 구현 작업 계획 및 진행 상태 기록
- 관련: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [백엔드 개발 로드맵](../backend_development_roadmap.md)

## TASK-010 프론트엔드 백엔드 요구사항 상세 리뷰 문서화

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: docs/backend/requirements.md, docs/backend/requirements_review.md, docs/backend_api_contract.md, docs/architecture.md, docs/README.md, docs/assessment.md

### 1. 계획 (Plan)

- 작업 목표: 프론트엔드 개발 중 작성된 백엔드 요구사항을 현재 아키텍처와 API 계약 기준으로 상세 검토하고, 백엔드 구현자가 참고할 수 있는 리뷰 문서로 남긴다.
- 완료 기준:
  - gRPC/WebSocket 경계, 관리자 명령 권한/감사 계약, role enum, 데이터 모델 부족분을 문서화.
  - 원 요구사항 문서에서 리뷰 문서로 이동할 수 있는 링크 추가.
  - `ai-workflow/project`에서 `ai-workflow/memory`로 변경된 workflow 경로를 관련 문서 링크에 반영.

### 2. 실행 (Act)

- `docs/backend/requirements_review.md`를 추가해 상세 리뷰, 권장 endpoint, WebSocket 메시지 타입, command lifecycle, 후속 작업 우선순위를 정리했다.
- `docs/backend/requirements.md`에 상세 리뷰 문서 참조를 추가했다.
- `docs/backend_api_contract.md`의 관련 문서 경로를 `ai-workflow/memory/backend_development_roadmap.md` 기준으로 수정했다.
- `docs/architecture.md`, `docs/README.md`, `docs/assessment.md`의 workflow 관련 링크를 `ai-workflow/memory/` 기준으로 정리했다.

### 3. 검증 (Validate)

- `rg`로 `docs/backend_api_contract.md` 내 `ai-workflow/project` 잔여 참조가 없음을 확인한다.
- 문서 변경만 포함되어 코드 테스트는 별도 실행하지 않는다.

### 4. 결과 (Result)

- 작업 결과: 프론트엔드 백엔드 요구사항 리뷰가 별도 문서로 정리되어 후속 API 계약 재작성과 백엔드 구현 우선순위 판단에 사용할 수 있는 상태가 됐다.

## TASK-011 프론트엔드 현재 구현 기반 백엔드 연동 요구사항 및 로드맵 갱신

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: docs/backend/frontend_integration_requirements.md, docs/backend/requirements.md, docs/backend/requirements_review.md, docs/backend_api_contract.md, ai-workflow/memory/backend_development_roadmap.md

### 1. 계획 (Plan)

- 작업 목표: frontend phase1의 현재 화면, mock data, service layer를 기준으로 백엔드가 프론트엔드에 전달해야 할 연동 요구사항과 백엔드 개발 필요 항목을 도출하고 로드맵에 반영한다.
- 완료 기준:
  - 프론트 역할별 dashboard가 요구하는 REST snapshot API, WebSocket event, command lifecycle 항목 정리.
  - 프론트팀에 요청할 role wire format, fetch adapter, gRPC 표현 정리, mock type 정합성 요구사항 문서화.
  - 백엔드 개발 로드맵 phase와 다음 작업 큐를 프론트 연동 우선순위에 맞게 갱신.

### 2. 실행 (Act)

- `frontend/app/(dashboard)/*`, `frontend/lib/services/*`, `frontend/lib/mockData.ts`, `frontend/lib/store.ts`, `frontend/components/layout/*`를 확인했다.
- `docs/backend/frontend_integration_requirements.md`를 추가해 프론트 전달 요구사항과 백엔드 개발 필요 항목을 정리했다.
- `ai-workflow/memory/backend_development_roadmap.md`를 프론트 snapshot API, command/audit, WebSocket, Python AI gRPC 순서가 드러나도록 갱신했다.
- `docs/backend_api_contract.md`의 예정 API 목록에 프론트 연동에 필요한 endpoint 후보를 추가했다.
- `docs/backend/requirements_review.md`의 상세 리뷰 finding을 로드맵 phase 완료 조건과 P1/P2 작업 큐에 명시적으로 반영했다.

### 3. 검증 (Validate)

- `python3 -m json.tool ai-workflow/memory/state.json`으로 workflow state JSON을 검증한다.
- `rg`로 새 연동 요구사항 문서 링크와 workflow 경로 정합성을 확인한다.
- 문서 변경만 포함되어 코드 테스트는 별도 실행하지 않는다.

### 4. 결과 (Result)

- 작업 결과: 백엔드 다음 단계가 raw webhook API 중심에서 프론트 교체 가능한 REST snapshot API, command/audit 계약, WebSocket event 계약 중심으로 재정렬됐고, 프론트엔드 작성 요구사항 리뷰의 P1/P2 finding이 로드맵 고려 사항으로 포함됐다.

## TASK-012 프론트 snapshot API 1차 구현

- 상태: done
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: backend-core/internal/httpapi/, docs/backend_api_contract.md, ai-workflow/memory/backend_development_roadmap.md

### 1. 계획 (Plan)

- 작업 목표: frontend phase1 mock service를 실제 백엔드 호출로 교체할 수 있도록 Go Core에 초기 REST snapshot API를 추가한다.
- 범위:
  - `GET /api/v1/dashboard/metrics?role=...`
  - `GET /api/v1/infra/nodes`
  - `GET /api/v1/infra/edges`
  - `GET /api/v1/infra/topology`
  - `GET /api/v1/ci-runs`
  - `GET /api/v1/ci-runs/{ci_run_id}/logs`
  - `GET /api/v1/risks/critical`
- 구현 방침:
  - Phase 5의 1차 구현으로 DB 정규화 전 static fallback provider를 사용한다.
  - 응답 envelope는 `status`, `data`, `meta`를 따른다.
  - role wire format은 `developer`, `manager`, `system_admin`으로 검증한다.

### 2. 실행 (Act)

- `backend-core/internal/httpapi/snapshot.go`를 추가해 static fallback 기반 snapshot handler를 구현했다.
- `backend-core/internal/httpapi/router.go`에 프론트 snapshot API route를 연결했다.
- `backend-core/internal/httpapi/snapshot_test.go`를 추가해 role wire format, infra topology envelope, CI logs not found, critical risk 필드를 검증했다.
- `docs/backend_api_contract.md`에 프론트 Snapshot API 1차 섹션과 응답 예시를 추가했다.
- `ai-workflow/memory/backend_development_roadmap.md`에서 Phase 5를 `in_progress`로 전환하고 현재 완료 범위와 다음 작업 큐를 갱신했다.

### 3. 검증 (Validate)

- `cd backend-core && go test ./...` 통과.

### 4. 결과 (Result)

- 작업 결과: 프론트 mock service를 실제 백엔드 호출로 교체하기 위한 1차 REST snapshot API가 Go Core에 추가됐다. 현재는 정규화 테이블 전 단계이므로 static fallback data source를 사용하며, 후속 작업에서 PostgreSQL 정규화 테이블 또는 Gitea/Runner adapter로 교체한다.

## TASK-007 Gitea Webhook 수신부 및 데이터 모델링 구현

- 상태: in_progress
- 우선순위: high
- 담당: Codex
- 영향 문서/코드: backend-core/main.go, backend-core/internal/, backend-core/migrations/, backend-core/go.mod, docs/backend_api_contract.md, docs/architecture.md, docs/tech_stack.md

### 1. 계획 (Plan)

- 작업 목표: Go Core에서 Gitea Webhook을 검증, 중복 방지, 원본 저장할 수 있는 초기 백엔드 기반을 구현한다.
- 프론트엔드 전제: 프론트엔드는 별도 브랜치에서 개발 후 병합 예정이므로, 백엔드는 REST/WebSocket 계약이 흔들리지 않도록 API 경로와 응답 형태를 안정적으로 유지한다.
- 초기 완료 기준:
  - Go Core 구조를 설정/HTTP/store 계층으로 분리.
  - `webhook_events` PostgreSQL migration 추가.
  - `POST /api/v1/integrations/gitea/webhooks` endpoint 구현.
  - `GITEA_WEBHOOK_SECRET` 기반 signature 검증 및 중복 dedupe key 처리.
  - 단위 테스트와 `cd backend-core && go test ./...` 통과.

### 2. 실행 (Act)

- 진행 현황:
  - `2026-05-02` 현재 브랜치를 `codex/backend_init`으로 생성하고 작업 시작.
  - `2026-05-02` 요구사항/아키텍처/기술 스택 문서를 확인하고 백엔드 개발 로드맵 수립.
  - `2026-05-02` 백엔드 개발 로드맵을 `ai-workflow/memory/backend_development_roadmap.md`로 문서화하고 phase별 진척 관리 기준 추가.
  - `2026-05-02` Go Core를 `internal/config`, `internal/httpapi`, `internal/gitea`, `internal/store`로 분리.
  - `2026-05-02` PostgreSQL 연결 계층과 `webhook_events` migration 초안 작성.
  - `2026-05-02` Gitea Webhook 수신 endpoint, HMAC-SHA256 signature 검증, delivery id/payload hash 기반 dedupe 처리 구현.
  - `2026-05-02` `GET /api/v1/events` raw event 조회 API와 프론트엔드 초기 연동용 API 계약 문서 작성.
  - `2026-05-02` `make migrate-tools` 실행으로 migrate CLI 설치 완료.
  - `2026-05-02` 홈랩 PostgreSQL(`192.168.0.38:5432`) TCP 연결 확인 성공, 사용자 제공 `postgres` 계정 인증 확인 실패.
  - `2026-05-02` 정정된 `postgres` 인증 정보로 재확인했으나 인증 실패 지속.
  - `2026-05-02` DevHub 전용 `devhub` 계정으로 홈랩 PostgreSQL 인증 성공 및 `webhook_events` migration version 1 적용 완료.
  - `2026-05-02` DB 접속 정보는 repo root `.env.local`에 로컬 저장하고 `.gitignore`로 커밋 제외 처리.
  - `2026-05-02` 원격 `main`의 PR #6(`gemini/frontend_phase1`) 머지 내용을 현재 브랜치에 반영하고 workflow 운영 경로를 `ai-workflow/memory/` 기준으로 정리.

### 3. 검증 (Validate)

- 테스트 결과:
  - `cd backend-core && go test ./...` 통과.
  - `python3 -m json.tool ai-workflow/project/state.json` 통과.
  - `python3 -m json.tool ai-workflow/memory/state.json` 통과.
  - `MIGRATE_DB_URL=... make migrate-up` 성공.
  - `migrate ... version` 결과 `1`.
  - `cd frontend && npm install` 완료.
  - `cd frontend && npm run lint` 통과.
- 미실행/잔여 검증:
  - Docker Compose 기반 통합 검증은 Docker daemon socket 연결 실패로 미실행.
  - 실제 Gitea 서버 Webhook 연동 검증은 후속 작업에서 진행.

### 4. 결과 (Result)

- 작업 결과: TASK-007의 raw Webhook 수집 기반과 `GET /api/v1/events` 조회 API 초안 구현 완료. 홈랩 PostgreSQL에 초기 migration 적용 검증 완료. 도메인 정규화, WebSocket publish, Hourly Pull reconciliation, Python AI gRPC 연동은 후속 세부 작업으로 남음.
- 다음 세션 시작 포인트:
  1. Issue/PR/Commit/Actions 이벤트 정규화 테이블 설계.
  2. repository/issue/PR 조회 API 초안 작성.
  3. WebSocket 이벤트 publish 메시지 타입 초안 작성.
