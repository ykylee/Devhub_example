# Backlog — 2026-05-14 (sprint claude/work_260514-c)

## 진입 컨텍스트
- Base: main @ `66ab5ff` (PR #106 머지 직후)
- 직전 sprint: claude/work_260514-b (PR #106) — API-41..50 activated.
- 본 sprint scope: API-51..58 세트 활성화 (4 도메인 = Repository 운영 지표 + Project CRUD + Application 롤업 + Integration CRUD).

## 추적성 영향 사전 진단 (sync-checklist §1)

| 단계 | 영향 | 본 sprint 처리 |
| --- | --- | --- |
| REQ-FR | 변경 없음 (이미 발급) | N/A |
| REQ-NFR | 변경 없음 | N/A |
| UC | 변경 없음 | N/A |
| ARCH | 신규 ARCH-XX 발급 후보 (rollup compute) — 본 sprint 1차는 carve out, 실측 후 결정 | 보류 |
| API | API-51..58 planned → activated | 활성화 |
| RM | 미발급 — design entry. 후속 RM 발급 검토 | 보류 |
| IMPL | IMPL-application-{repo-ops, rollup, integration}-01 + IMPL-project-handler-01 | 신규 |
| UT | UT-application-{rollup, integration}-XX + UT-project-handler-XX | 신규 |
| TC | carve out 후속 | N/A |

## 작업 순서

1. **API-51..54 Repository 운영 지표** — store 메서드 4개 + handler + route
2. **API-55..56 Project CRUD** — handler 5개 + route (store 이미 준비됨)
3. **API-57 Application 롤업** — store ComputeApplicationRollup + handler + active→closed critical 가드
4. **API-58 Integration CRUD** — store 4개 + handler 4개 + scope polymorphism
5. **UT + 매트릭스 + commit + push + PR**

## RBAC matrix 영향

신규 endpoint 의 (resource, action) 매핑:
- API-51..54 (repository read) — 어디에 매핑? `pipelines` (기존, repository / PR / CI / quality 모두 pipelines) 또는 `application_repositories` (신규). 후자가 일관성 ↑ → `application_repositories:view`.
- API-55..56 (project CRUD) — `projects:*`.
- API-57 (rollup) — `applications:view` (Application 의 집계 메타).
- API-58 (integration) — scope 별 다른가? 1차는 `applications:edit` (admin 전용 일관).

## 1차 결정 사항 (carve in/out)

**carve in**:
- 4 도메인 모든 endpoint 활성화 (read 위주 / 단순 CRUD)
- 롤업 1차 정확성 (성능 최적화 carve out)
- active→closed 가드 흡수
- audit emit 과거형 (직전 sprint 패턴)
- handler 단위테스트 핵심 case

**carve out**:
- Repository 운영 지표의 write endpoint (ingest pipeline 으로만)
- Project member CRUD endpoint (별도 sprint)
- Integration policy validation 강화 (1차는 enum check)
- 롤업 cache / pre-aggregation
- e2e TC
- frontend UI
