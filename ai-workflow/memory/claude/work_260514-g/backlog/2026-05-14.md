# Backlog — 2026-05-14 (sprint claude/work_260514-g — housekeeping)

## 진입 컨텍스트
- Base: main @ `d29f2ac` (PR #110 머지 직후)
- Trigger: 사용자가 memory 관리 상태 확인 → main flat / auto memory 모두 stale 확인 → 정리 진행 결정

## 흡수 PR 7건 (오늘 누적)
- #104~#107: API 도메인 1차 완성 (Application/Repository/Project, ID 공간 58 전체 activated)
- #108: codex P1/P2 hotfix (custom weight normalize / UpdateIntegration unique)
- #109: postgres integration test 23
- #110: CI backend-integration job + 2 SQL fix + step summary

## 새 학습 / feedback 후보

### codex review cycle 패턴 (feedback memory 후보)
- 머지 후 codex 외부 리뷰가 inline P1/P2 로 도착하는 시나리오 발견
- 패턴: 머지 → codex 리뷰 도착 → 발견 항목 분석 → hotfix PR 으로 별도 정정 → 회귀 guard test 추가 → squash merge
- 핵심 원칙: codex review 항목은 self-review 가 놓친 의미적 버그 (수치/SQL 정합) 가 많음 → integration test 가 결정적 회귀 가드

### 학습: GitHub Actions log endpoint 의 위치 의존성
- `results-receiver.actions.githubusercontent.com` 가 한국 위치에서 TCP timeout
- 다른 endpoint (api.github.com) 는 동작
- 우회 패턴: `$GITHUB_STEP_SUMMARY` 로 step output 적재 → PR 페이지 Summary 탭에서 fail 메시지 즉시 확인

### 학습: integration test 의 CI 분리 잡
- 기존 unit test 잡이 `make test` + `DEVHUB_TEST_DB_URL` 미설정으로 integration test 가 자동 skip
- → 회귀 가드 무효 위험
- 해결: 별도 backend-integration job (PG 15 + 마이그레이션 + go test -run 'TestIntegration_')

## 추적성 영향
| 단계 | 영향 |
| --- | --- |
| REQ / UC / ARCH / API / RM / IMPL | 변경 없음 |
| UT / TC | 변경 없음 |
| trace.md §6 변경 이력 | 1 row 추가 (sprint -g housekeeping) |
