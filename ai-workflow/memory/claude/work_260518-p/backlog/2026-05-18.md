# Sprint backlog — `claude/work_260518-p`

- 문서 목적: ADR-0015 §6 carve outs 부분 처리 sprint
- 범위: size limit + streaming decode (file + HTTP puller) + agent token rotation SOP
- 대상 독자: 본 sprint 작업자
- 상태: in_progress
- 최종 수정일: 2026-05-18

## scope

다음 세션 directive 4순위 ([main flat state.json](../../../state.json) `다음 세션 directive (4)`):

> **ADR-0015 §6 carve outs** — (a) size limit + streaming decode, (b) agent token rotation SOP, (c) dedicated worker binary (M4 carve out 유지), (d) push/pull 동시 운영 dedup (별도 ADR 후보).

본 sprint 는 (a) + (b) 만 처리. (c) + (d) 는 carve out 유지.

## 결정

- 신규 env `DEVHUB_HOMELAB_PULL_MAX_BYTES` (default 0 = unlimited, 운영 권장 5 MB).
- file puller — os.Stat 으로 사전 검사 + os.Open + json.NewDecoder streaming.
- HTTP puller — Content-Length 사전 검사 + io.LimitReader(body, limit+1) + streaming decode.
- agent token rotation SOP — `docs/setup/homelab_agent_token_rotation.md` 별도 문서.

## 체크리스트

- [ ] backend-core/internal/integrations/adapters/homelab_file_puller.go — MaxBytes + streaming
- [ ] backend-core/internal/integrations/adapters/homelab_http_puller.go — MaxBytes + Content-Length + LimitReader
- [ ] backend-core/main.go — env wire
- [ ] file/HTTP puller test 의 oversized 회귀 test
- [ ] docs/setup/homelab_agent_token_rotation.md 신규
- [ ] docs/adr/0015-homelab-adapter-pull-strategy.md §4.3 + §6 + §7
- [ ] docs/traceability/report.md §6
- [ ] PR + self-review + 머지

## 검증 절차

```
cd backend-core && go vet ./internal/integrations/adapters/
cd backend-core && go test ./internal/integrations/adapters/ -v
```
