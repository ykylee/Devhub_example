# Backlog 2026-05-11 — work_26_05_11-b sprint (M1 부채 정리)

- 문서 목적: 본 문서는 `claude/work_26_05_11-b` sprint 의 **개발 계획서 + 진척 추적 source-of-truth**. 모든 PR 진입/완료/블록은 본 문서의 상태 라벨과 §5 체크리스트로 관리한다.
- 범위: M1 sprint (`m1-sprint-plan/backlog/2026-05-08.md`) 의 잔여 PR-B (T-M1-02·07·08) + PR-C (T-M1-03·05) 청산.
- 대상 독자: 본 sprint 담당자, 후속 인계자, 프로젝트 리드
- 상태: in_progress (결정 3건 확정 2026-05-11 권장안 채택. PR-B 진단 진행 중)
- 최종 수정일: 2026-05-11
- 관련 문서: [세션 인계](../session_handoff.md), [상태 스냅샷](../state.json), [상위 backlog](../work_backlog.md), [M1 sprint plan (원본)](../../m1-sprint-plan/backlog/2026-05-08.md), [M1 PR review actions](../../../M1-PR-review-actions.md), [통합 로드맵](../../../../../docs/development_roadmap.md), [backend 로드맵](../../../backend_development_roadmap.md), [API 계약](../../../../../docs/backend_api_contract.md), [직전 sprint closure](../../work_26_05_11/session_handoff.md)

## 0. Sprint baseline

- 시작 시점: 2026-05-11, `claude/work_26_05_11-b` (HEAD `818d54a`, work_26_05_11 sprint 직후 main fast-forward)
- 직전 sprint 가 M2 (인증/계정) 와 M3 일부 (사용자/조직 관리) 닫음. 본 sprint 는 그 전 M1 RBAC sprint 가 남긴 PR-B / PR-C / (PR-D) 부채를 청산한다.
- PR-D (T-M1-04 audit actor 보강) 는 마이그레이션 포함이라 별도 sprint 로 분리. 본 sprint 는 PR-B + PR-C 두 개만.

## 1. 현재 코드베이스 진단 (2026-05-11, grep + 코드 점검 결과)

### 1.1 envelope (T-M1-02)

- `docs/backend_api_contract.md §1`: 성공 = `{status, data, meta}`, 실패 = `{status, error}`, 단일 command = `status` + 결과 key.
- 직전 sprint 의 신규 핸들러 (`auth_*`, `accounts_admin`) 는 모두 envelope 사용 ✓
- 전체 230 `c.JSON(http.Status…)` 호출 중 122 개 (53%) 가 `"status":` 키 사용. **잔여 108 개 점검 필요** — 그 중 일부는 의도 (health, error-only, webhook ack), 일부는 누락. 라우트별 1:1 검증 필요.
- 점검 대상 후보: `snapshot.go` (11), `domain.go` (10), `events.go` (4), `hr_lookup.go` (4), `me.go` (1), `gitea_webhook.go` (7), `audit.go` (2), `auth_login.go` (1).

### 1.2 role wire format (T-M1-02 일부) — ✅ 이미 정상

- backend grep `\b(DEVELOPER|MANAGER|ADMIN)\b`: **0 건** (M1-PR-G 머지 시점 이후 정상화)
- frontend grep 동일: **0 건**
- 추가 작업 불필요

### 1.3 WebSocket envelope (T-M1-08) — ✅ 이미 정상

- `internal/httpapi/realtime.go:14-20` `realtimeEvent` 가 5 키 모두 정의: `SchemaVersion("1")`, `Type`, `EventID`, `OccurredAt`, `Data`.
- `RealtimeHub.Publish` (l.93-108) 가 모든 publish 에 `SchemaVersion="1"` + `EventID=prefixedEventID()` + `OccurredAt=time.Now().UTC()` 자동 부착.
- frontend `types.ts:77` `WSEvent<T>` 도 5 키 정의됨.
- 추가 작업 불필요. 단 backlog 의 spec 변경 이력을 `docs/backend_api_contract.md §8 메시지 envelope` 에서 확인할 가치는 있음 (spec ↔ 구현 정합 검증).

### 1.4 frontend types split (T-M1-07)

- `frontend/lib/services/types.ts:1-23` 이 이미 `UserRole` (UI) vs `ApiUserRole` (wire) 와 `ApiResponse<T>` envelope 분리.
- `WSEvent<T>` 도 정의됨 (l.77).
- 다만 wire-snake (`cpu_percent`, `source_id`, `command_id`) 와 UI-camel/표시명 이 단일 파일에 섞임.
- 컴포넌트가 wire 타입을 직접 import 하는 케이스 grep 검증 필요. lint 강제 또는 `wire.ts` 분리.

### 1.5 command lifecycle (T-M1-03)

- `domain/command.go` 의 상태 enum 확인 필요. spec: `pending | running | succeeded | failed | rejected | cancelled`.
- dry-run 경계: `commandworker.Worker` (dry-run) + `commandworker.LiveWorker` (live) 가 main.go 에 분리 주입되어 있음 ✓
- DEC-2=A: dry-run 은 store 미기록 + 응답만 succeeded. 현재 worker 가 dry-run 도 store 기록하는지 확인 후 정책 일치.

### 1.6 auth_test 매트릭스 (T-M1-05)

- `auth_test.go` 와 `authz_test.go` 가 이미 존재. spec 의 16+ 매트릭스 차이 점검.
- DEVHUB_ENV=prod + verifier nil 시 startup 거부 통합 테스트 필요.

## 1.7 진단 결과 요약 — 작업 압축

| 원본 task | spec 시점 상태 | 현재 상태 | 작업 |
| --- | --- | --- | --- |
| T-M1-02 envelope | 미정합 | 53% 적용 | 잔여 108 응답 중 누락분 정정 |
| T-M1-02 role wire | 일부 잔존 | ✅ 0 건 | done |
| T-M1-07 types split | 단일 파일 혼재 | ApiResponse/UserRole 분리됨, snake/camel 혼재 | wire.ts 분리 + 컴포넌트 import lint |
| T-M1-08 WS envelope | 미적용 | ✅ 5 키 모두 적용 | done — spec ↔ 구현 정합 확인만 |
| T-M1-03 cmd lifecycle | 미일관 | dry/live worker 분리됨, 상태 enum 확인 필요 | enum 점검 + dry-run 영속화 정책 일치 |
| T-M1-05 auth_test | 부분 케이스 | 기본 케이스 있음 | 16+ 매트릭스 보강 + prod 가드 |

→ **PR-B 분할**: backend envelope 정합 + types split 만 남음. WS 와 role wire 는 done.
→ **PR-C**: command lifecycle (enum + dry-run 정합) + auth_test 매트릭스 보강.

## 2. 결정 (확정 2026-05-11)

| ID | 결정 | 채택 옵션 | 의미 | 영향 PR |
| --- | --- | --- | --- | --- |
| **DEC-1** | envelope source-of-truth | **A `backend_api_contract.md` 우선** | 외부 client/integrator 의 의존 안정성을 위해 계약 문서가 source-of-truth. 차이가 보이면 코드를 문서에 맞춘다. | PR-B |
| **DEC-2** | dry-run command 영속화 정책 | **A 응답만 succeeded** | dry-run 은 사용자 의도 미리보기가 본질. store 미기록 / audit 미기록 / worker enqueue 없음. live 경로만 store + worker. | PR-C |
| **DEC-3** | WebSocket envelope `schema_version` 형식 | **A `"1"` 고정** | semver 는 backward compat 가치가 의미 있을 때만. 정수 카운터로 시작, 필요 시 후속 phase 에서 semver 도입. | PR-B |

## 3. PR 분할 + Task

### PR-B — API envelope 정합 + frontend types split (planned)

진단 결과 (§1.7) role wire + WS envelope 는 이미 done. 본 PR 는 backend
envelope 정합 + frontend wire/UI 타입 분리 두 영역만 다룬다.

원본 spec 대비 축소: T-M1-02 (envelope 부분만) + T-M1-07.

원본 spec: M1 sprint plan §1 의 T-M1-02 + T-M1-07 + T-M1-08 묶음.

- 위치 (예정)
  - backend 핸들러 일괄: `backend-core/internal/httpapi/{commands,organization,rbac,audit,infra,risks,events,...}.go`
  - backend 도메인: `backend-core/internal/domain/{command,user,role}.go`
  - backend realtime: `backend-core/internal/httpapi/realtime.go`, `realtime_hub.go` (envelope 표준화)
  - backend 테스트 신규: `internal/httpapi/envelope_test.go` (envelope 키 set + role wire 검증 매트릭스)
  - frontend 분리: `frontend/lib/services/wire.ts` (신규, snake_case backend 응답 타입), `frontend/lib/services/types.ts` (UI 전용 PascalCase 타입)
  - frontend service: `frontend/lib/services/*.service.ts` 가 wire → UI 변환 책임 일관 (이미 `identity.service.ts` 패턴 적용 — 다른 service 동기화)
  - frontend 컴포넌트: wire 타입 직접 import 0건 (lint 또는 grep 검증)
  - 계약: `docs/backend_api_contract.md` envelope 섹션 + WebSocket §8 갱신

- DoD
  1. `go test ./internal/httpapi/...` envelope 검증 매트릭스 PASS — 모든 v1 라우트가 응답 키 set `{status, data, error?}` 일관
  2. role wire format `developer | manager | system_admin` 잔존 검증 — `DEVELOPER|MANAGER|ADMIN` grep 0건 (테스트 파일 제외)
  3. WebSocket publish 가 envelope 5 키 (`schema_version, type, event_id, occurred_at, data`) 모두 채움 — backend test + frontend decode 매트릭스
  4. frontend `npm run build` PASS, `tsc --noEmit` clean
  5. UI 컴포넌트가 `wire.*` 직접 import 0건 (grep 검증)
  6. `docs/backend_api_contract.md` diff 가 코드와 정합 — section 변경 이력 추가
  7. main 회귀 — `go test ./...` 전 패키지 PASS

- 위험 / 메모
  - 응답 envelope 변경은 frontend snapshot 계층에 영향 → frontend 동시 변경 필수
  - PR 규모 큼 (~300 LoC backend + ~150 LoC frontend + 테스트). 작업 단위가 너무 크면 PR-B1 (envelope) + PR-B2 (types split) + PR-B3 (WS envelope) 로 분할 검토

- 상태: planned

### PR-C — Command lifecycle 6 상태 + dry-run/live 경계 + auth_test 매트릭스 (planned, blocked by DEC-2)

원본 spec: M1 sprint plan §1 의 T-M1-03 + T-M1-05 묶음.

- 위치 (예정)
  - 도메인: `backend-core/internal/domain/command.go` (상태 enum 일괄 적용)
  - 핸들러: `backend-core/internal/httpapi/commands.go` (`createServiceAction`/`createRiskMitigation`/`approveCommand`/`rejectCommand`/`getCommand`)
  - worker: `backend-core/internal/commandworker/worker.go` + `live_worker.go`
  - store: `backend-core/internal/store/postgres.go` (commands 관련 SELECT/INSERT/UPDATE)
  - 테스트 신규: `internal/domain/command_test.go` (6 상태 transition 매트릭스), `internal/httpapi/commands_test.go` (dry-run/live 경계)
  - auth 테스트: `internal/httpapi/auth_test.go` (prod 가드 통합), `authz_test.go` (role guard 매트릭스 16+ 케이스)

- DoD
  1. 상태 enum `pending | running | succeeded | failed | rejected | cancelled` 6 종 일괄 적용. 잔존 boolean / 다른 표현 0건
  2. dry-run 경로는 DEC-2 결정에 따라 store 영속화 정책 일관 적용. 통합 테스트로 dry-run 과 live 의 audit/store/응답 차이 매트릭스 검증
  3. 6 상태 transition 매트릭스 단위 테스트 (e.g. pending→running, running→succeeded, pending→cancelled, …)
  4. `// COMMAND-LIFECYCLE` 임시 마커 0건
  5. auth 테스트: prod 모드 verifier nil → startup 거부 통합 1건
  6. authz 테스트 매트릭스: actor_role (미설정/developer/manager/system_admin) × 보호 라우트 5종 = 16+ 케이스 PASS
  7. main 회귀 — `go test ./...` 전 패키지 PASS

- 위험 / 메모
  - worker 가 다른 store 호출에 의존 → 변경 후 e2e PASS 까지 확인 필요
  - dry-run 변경이 service action / risk mitigation 양쪽 흐름에 영향
  - 규모 중 (~200 LoC + 테스트 ~120 LoC)

- 상태: planned

## 4. 의존 그래프

```
DEC-1 ─→ PR-B (envelope + types + WS)
DEC-3 ─→ PR-B
DEC-2 ─→ PR-C (cmd lifecycle + auth_test)
```

PR-B 와 PR-C 는 코드 영역이 다르므로 독립 머지 가능. 본 sprint 는 직렬로 진행 권장 (PR-B 먼저 — envelope 정합이 깔린 위에서 PR-C 의 command 응답 형태 검증이 자연스러움).

권장 머지 순서: **PR-B → PR-C**.

## 5. 진척 추적 (체크리스트)

본 sprint 의 완료 판단은 아래 체크리스트로 단일화한다. PR 머지 시 해당 항목을 `[x]` 로 갱신한다.

### 결정
- [x] DEC-1 envelope source-of-truth (2026-05-11, A: contract 문서 우선)
- [x] DEC-2 dry-run 영속화 정책 (2026-05-11, A: 응답만 succeeded, store 미기록)
- [x] DEC-3 WebSocket schema_version 형식 (2026-05-11, A: `"1"` 고정)

### PR-B (envelope 정합 + types split)
- [x] role wire grep 0건 (진단 시점에 이미 done)
- [x] WebSocket envelope 5 키 검증 (진단 시점에 이미 done — realtime.go:14-20, 93-108)
- [ ] backend 108 status-less 응답 1:1 검증 + 누락분 정정
- [ ] frontend `wire.ts` 신설 (또는 types.ts 내 wire 섹션 명시화) + 컴포넌트 import 패턴 정리
- [ ] `docs/backend_api_contract.md` ↔ 코드 정합 sub-agent 검증
- [ ] PR 머지

### PR-C (command lifecycle + dry-run + auth_test)
- [ ] 상태 enum 6종 일괄 + 잔존 표현 0건
- [ ] dry-run/live 경계 매트릭스 PASS
- [ ] 6 상태 transition 단위 테스트
- [ ] prod 가드 통합 테스트
- [ ] role guard 매트릭스 16+ 케이스
- [ ] PR 머지

## 6. 검증 절차 (PR 공통)

각 PR 머지 전 본 절차를 실행한다.

1. Backend: `cd backend-core && go test ./...`
2. Frontend (PR-B): `cd frontend && npm run build` + `npx tsc --noEmit`
3. 계약 정합 (PR-B): `docs/backend_api_contract.md` diff sub-agent 검증
4. grep 검증 (PR-B): role wire (`DEVELOPER|MANAGER|ADMIN`) 0건, wire.ts UI import 0건
5. 회귀: 머지 후 `go test ./...` 풀 매트릭스 PASS 유지
6. 마커 청소: 본 sprint 종료 시 `// M1-TODO|// M1-LIFECYCLE|// COMMAND-LIFECYCLE` 임시 마커 0건

## 7. 위험 / 메모

- **PR-B 규모**: backend + frontend 동시 변경이라 commit 분리 권장 (PR-B1 envelope, PR-B2 types split, PR-B3 WS envelope) — 실제 진입 시 분할 여부 재검토.
- **PR-D (audit actor)**: 본 sprint 범위 밖. 마이그레이션 포함이라 별도 sprint 로 분리 (운영 down-time 정책 결정 필요).
- **계약 source-of-truth**: PR-B 진입 시 `backend_api_contract.md` 와 코드의 차이 누적 — 일괄 검증 sub-agent 호출 권장.
- **frontend npm 의존**: 사용자 환경 npm registry mirror 동작 가정. PR-B 의 frontend 빌드 검증은 사용자 환경에서 PASS 확인.

## 8. 변경 이력

| 일자 | 변경 |
| --- | --- |
| 2026-05-11 | 초판 작성. M1 PR-B + PR-C 부채 청산 sprint 진입 결정 후 작성 |
