# Sprint backlog — `claude/work_260518-n`

- 문서 목적: Infra topology v2 시각화 sprint 작업 항목 정리
- 범위: frontend 진입점 + service wrapper + E2E + traceability
- 대상 독자: 본 sprint 작업자
- 상태: in_progress
- 최종 수정일: 2026-05-18

## scope

다음 세션 directive 2순위 ([main flat state.json](../../../state.json) `다음 세션 directive (2)`):

> **Infra topology v2 시각화** (API-76, 78) — React Flow 그래프. TC-INT-HOMELAB-03 활성화 후보. backend `/api/v1/infra/services` + `/infra/topology/v2` 가 hydrate 된 상태이므로 frontend 패턴은 기존 infra-topology.spec.ts + frontend/app/(dashboard)/infra/ 의 React Flow 패턴 따름.

## 결정

- **별도 페이지** `/admin/topology-v2` (기존 `/admin/page.tsx` v1 와 분리).
- 기존 v1 dashboard 헤더에 v2 nav link.
- v2 응답의 services 는 sidebar list 로 시작 (React Flow sub-node grouping 은 후속 carve out).
- `meta.snapshot_at` + `meta.degraded_providers` 는 페이지 헤더 + banner 로 노출.

## 체크리스트

- [ ] `frontend/lib/services/infra.service.ts` — `getTopologyV2()` + 신규 v2 타입
- [ ] `frontend/app/(dashboard)/admin/topology-v2/page.tsx` — React Flow + 사이드바 + degraded banner
- [ ] `frontend/app/(dashboard)/admin/page.tsx` — v2 nav link
- [ ] `frontend/tests/e2e/admin-topology-v2.spec.ts` — TC-INT-HOMELAB-03 active + RBAC negative
- [ ] `docs/domain/integration-registry/test_cases.md` — TC-INT-HOMELAB-03 active + §3.1 카버리지 매핑
- [ ] `docs/traceability/report.md` — §5.1 + §6
- [ ] frontend 회귀: `cd frontend && npm run build && npm run test`
- [ ] PR + self-review + 머지

## 신규 TC ID 갱신

- `TC-INT-HOMELAB-03` (P1, E2E) — system_admin 이 `/admin/topology-v2` 접근 + nodes/services/edges 일관 노출 + degraded banner 동작 (현재 카탈로그 P1 carve out → spec ts active)
- `TC-INT-FRONTEND-TOPOLOGY-V2-NAV-01` (P2, E2E) — `/admin` 페이지의 v2 nav link 클릭 시 navigation 정합
- `TC-INT-FRONTEND-TOPOLOGY-V2-RBAC-01` (P2, E2E) — developer 의 접근 차단

## 검증 절차

```
cd frontend && npm run build
cd frontend && npm run test
cd frontend && npx playwright test admin-topology-v2 --reporter=line
```
