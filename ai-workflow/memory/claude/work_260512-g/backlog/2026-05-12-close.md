# 2026-05-12 — work_260512-g 슬롯 closure (PR #78)

PR-T3.5 hardening (PR #76) 직후 follow-up #2. password-change.spec finally 의 best-effort 화.

## PR 라인업

| # | 주제 | 슬롯 |
| --- | --- | --- |
| #78 | test(e2e): best-effort cleanup in password-change.spec (PR-T3.5 follow-up) | work_260512-g |

## 변경 요약

`frontend/tests/e2e/password-change.spec.ts`:
- finally 블록을 `try { ... } catch (err) { console.warn(...) }` 로 감싸 best-effort 화.
- 헤더 코멘트 갱신 — "other specs in the same npm run e2e (signout.spec reuses alice)" + globalSetup backstop 명시.

## 회귀 분석 (PR 코멘트 박제)

| 시나리오 | 본 PR 이전 | 본 PR 이후 |
| --- | --- | --- |
| try 정상 + rollback 정상 | PASS | PASS |
| try 정상 + rollback throw | finally 의 throw 가 spec fail | spec PASS (warn + swallow) |
| try throw (passwordRotated=false) | early return, try throw 가 spec fail | 동일 |
| try throw (passwordRotated=true) + rollback throw | finally throw 가 try throw 를 가림 → 진짜 원인 흐림 | try throw 만 spec fail → 진짜 원인 보존 |

같은 run 의 후속 signout.spec 영향: rollback 정상 시만 alice 시드 비밀번호로 복귀. 본 PR 은 추가 회귀를 만들지 않고 디버깅 측면만 개선.

## main 머지 결과

- `gh pr merge 78 --squash --delete-branch` — 단일 squash commit `6d2274c`.
- main HEAD = `6d2274c`.

## 다음 슬롯

- work_260512-h: M2 hygiene — `users.kratos_identity_id` 컬럼.
- work_260512-i: PR-D follow-up.
