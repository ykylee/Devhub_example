# Sprint backlog — `claude/work_260518-t`

- 문서 목적: ADR-0017 §6 잔여 carve outs (a)+(c)+(d) 처리 sprint
- 범위: 자동 cron revoke + 만료/staleness Prometheus metric + Alertmanager 가이드 갱신
- 대상 독자: 본 sprint 작업자
- 상태: in_progress
- 최종 수정일: 2026-05-18

## scope

main flat state.json `다음 세션 directive (2)`: ADR-0017 §6 잔여 carve outs.

본 sprint 처리:
- (a) 자동 cron revoke — `expires_at <= NOW()` 인 token 을 주기적으로 hard-revoke
- (c) 만료 알림 metric — `devhub_intake_token_expiring_soon`
- (d) staleness alert metric — `devhub_intake_token_stale`

carve out 유지:
- (b) PATCH 의 `expires_at` 갱신 — frontend UI 영향 + ADR §4.2 'allowed_ips only' 정책 변경 필요, 별도 sprint

## 결정

- main.go cron goroutine = HomeLab pull loop 패턴 정합 (env gate + interval + ctx cancellation + audit + metric)
- metric package = `internal/devrequest/metrics.go` 신규 (HomeLab adapters/metrics.go 패턴 — sync.Once init + registerCollector)
- threshold env: expiring_soon 기본 24h, stale 기본 30d (0 = disable)

## 체크리스트

- [ ] store 3 메서드 + IntakeTokenStore interface 확장
- [ ] metric package + Prometheus emit
- [ ] Config + env + main.go cron wire
- [ ] store integration test + cron unit test
- [ ] ADR-0017 §5/§6/§7 + ADR-0016 가이드 + traceability
- [ ] PR + self-review + 머지

## 검증 절차

```
cd backend-core && go vet ./internal/...
cd backend-core && go test ./internal/store/ -run "TestIntegration_HardRevoke|TestIntegration_CountExpiring|TestIntegration_CountStale"
cd backend-core && go test ./internal/devrequest/
```
