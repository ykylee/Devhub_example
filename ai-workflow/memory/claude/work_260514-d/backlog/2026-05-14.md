# Backlog — 2026-05-14 (sprint claude/work_260514-d — hotfix)

## 진입 컨텍스트
- Base: main @ `f11bdbb` (PR #107 머지 직후)
- Trigger: PR #107 머지 후 codex 외부 리뷰 (#issuecomment 의 inline 2건) 가 발견한 실제 버그.

## 추적성 영향 (sync-checklist §1)

| 단계 | 영향 |
| --- | --- |
| REQ / UC | 변경 없음 |
| ARCH | 변경 없음 |
| API | 응답 sematic 변경 없음 (P2 만 응답 코드 500→409 정정) |
| RM | 변경 없음 |
| IMPL | IMPL-application-rollup-01 + IMPL-application-integration-01 의 책임 fix (정확성) |
| UT | 신규 UT 회귀 guard 2개 |
| TC | N/A (carve out) |

## 정정안 상세

### P1
```go
// 현재 (잘못된 동작):
for _, l := range contributing {
    if w, ok := opts.CustomWeights[l.FullName]; ok {
        rawWeights[l.FullName] = w
    } else if n := len(contributing); n > 0 {
        fallbackW := 1.0 / float64(n)
        rawWeights[l.FullName] = fallbackW
        // fallback 기록
    }
}

// 정정 후 (sum=1.0 정규화):
// ... 위 루프 그대로 ...
// missingCount > 0 이면 정규화:
total := 0.0
for _, w := range rawWeights { total += w }
if total > 0 && total != 1.0 {
    for k := range rawWeights { rawWeights[k] /= total }
    // fallbacks meta 갱신
}
```

### P2
```go
// store/integrations.go UpdateIntegration:
if errors.Is(err, pgx.ErrNoRows) { return ErrNotFound }
if isUniqueViolation(err) { return ErrConflict }  // 추가
if err != nil { ... }

// handler updateIntegration:
if errors.Is(err, store.ErrConflict) {
    c.JSON(409, gin.H{
        "status": "conflict",
        "error":  "integration with the same (scope target, type, external_key) already exists",
        "code":   "integration_conflict",
    })
    return
}
```
