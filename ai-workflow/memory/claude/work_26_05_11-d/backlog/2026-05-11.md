# Backlog 2026-05-11 — work_26_05_11-d sprint (TDD foundation)

- 문서 목적: 본 sprint 의 **개발 계획서 + 진척 추적 source-of-truth**. 모든 PR 진입/완료/블록은 본 문서의 상태 라벨과 §5 체크리스트로 관리한다.
- 범위: TDD 기반 마련 — Frontend 단위 테스트 인프라 (Vitest) + E2E 인프라 (Playwright) + 첫 시나리오 세트. 본 sprint 는 **회귀 안전망 구축**이 본질. TDD 의 실제 적용 (테스트 먼저 → 구현) 은 후속 sprint 부터.
- 대상 독자: 본 sprint 담당자, 후속 인계자, 프로젝트 리드
- 상태: in_progress (결정 4건 확정 2026-05-11. PR-T1 진입 가능)
- 최종 수정일: 2026-05-11
- 관련 문서: [세션 인계](../session_handoff.md), [상태 스냅샷](../state.json), [상위 backlog](../work_backlog.md), [통합 로드맵](../../../../../docs/development_roadmap.md), [배포 가이드](../../../../../docs/setup/test-server-deployment.md), [직전 sprint closure](../../work_26_05_11-c/session_handoff.md)

## 0. Sprint baseline

- 시작 시점: 2026-05-11, `claude/work_26_05_11-d` (HEAD `fe27845`, work_26_05_11-c CLOSED + 본 sprint baseline cherry-pick)
- 직전 sprint 가 M1 마지막 부채 (PR-D audit actor + request_id) 청산. M1 sprint 100% 완결.
- 본 sprint 는 hygiene/quality phase — 기능 변경 없이 회귀 안전망 마련.

## 1. 현재 코드베이스 진단 (2026-05-11)

| 영역 | 현황 |
| --- | --- |
| Backend Go | 32 test files, 187 Test 함수, `go test ./...` PASS. 단위 테스트 충실 |
| Frontend | **0 test files** (lib/components/app). Vitest/Jest/RTL 미설치. `package.json scripts.test` 없음 |
| backend-ai (Python) | 0 test files. 코드 자체가 minimum (`main.py + requirements.txt`) |
| E2E | 부재. `docs/setup/test-server-deployment.md` §6-7 의 수동 절차만 |
| CI | `.github/workflows/` 없음 |
| Makefile | `setup/build/run/migrate-*`. **`test` target 없음** |

## 2. 결정 (확정 2026-05-11)

| ID | 결정 | 채택 | 의미 |
| --- | --- | --- | --- |
| **DEC-1** | Frontend 단위 테스트 러너 | **Vitest** | Next.js 16 + Turbopack 호환, ESM 친화, 빠른 watch |
| **DEC-2** | E2E 러너 | **Playwright** | 표준, Chromium/Firefox/Webkit 멀티 브라우저, Hydra/Kratos native 환경 통합 가능 |
| **DEC-3** | E2E 실행 환경 | **A 사용자 native** | Hydra/Kratos native binary + DevHub OIDC client 등록 의존. 실 운영 흐름과 동일 |
| **DEC-4** | CI 도입 시점 | **B 별도 sprint** | 본 sprint 는 PR-T1+T2+T3 인프라 + 첫 시나리오. CI 는 인프라 안정화 후 |

## 3. PR 분할 + Task

### PR-T1 — Makefile test targets + sprint baseline (planned, no decision blocker)

- 위치 (예정)
  - `Makefile` — 3 target 추가: `test`, `test-race`, `test-coverage`
  - `ai-workflow/memory/claude/work_26_05_11-d/{state.json,session_handoff.md,work_backlog.md,backlog/2026-05-11.md}` (본 sprint baseline)
  - `ai-workflow/memory/state.json` (main 의 sprint 인덱스 갱신 — work_26_05_11-c CLOSED, work_26_05_11-d in progress)

- DoD
  1. `make test` 가 `cd backend-core && go test ./...` 호출 (frontend 는 PR-T2 머지 후 확장)
  2. `make test-race` 가 `-race -count=1` 옵션
  3. `make test-coverage` 가 `-coverprofile=coverage.out` + `go tool cover -func=coverage.out`
  4. 회귀: `go test ./...` 그대로 PASS

- 상태: planned

### PR-T2 — Frontend Vitest 인프라 + 첫 단위 테스트 (planned)

DEC-1=Vitest 반영. 핵심 utility 부터 단위 테스트.

- 위치 (예정)
  - `frontend/package.json` — devDependencies: `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`. `scripts.test` / `scripts.test:watch` / `scripts.test:coverage` 추가
  - `frontend/vitest.config.ts` — jsdom 환경 + paths alias (`@` → `./`)
  - `frontend/lib/test-setup.ts` — `@testing-library/jest-dom` 매처 + 글로벌 cleanup
  - 첫 단위 테스트 (5개):
    - `frontend/lib/auth/role-routing.test.ts` — `defaultLandingFor` / `isSystemAdmin` / `pathRequiresSystemAdmin` 매트릭스
    - `frontend/lib/auth/pkce.test.ts` — `createPkceState` / `consumeVerifier` CSRF 검증
    - `frontend/lib/auth/token-store.test.ts` — save/load/clear + getRefreshToken + getIdToken
    - `frontend/lib/auth/kratos-logout.test.ts` — fetch mocking 으로 navigation 분기 검증
    - `frontend/lib/services/account.service.test.ts` — `SettingsFlowError` 분기 (REAUTH_REQUIRED / VALIDATION / SUBMIT_FAILED) + redirect 감지

- DoD
  1. `npm test` (CI mode, watch 없이 1회 실행) PASS, 케이스 ≥ 20
  2. `npm run test:watch` 동작
  3. `npm run test:coverage` 가 lib/auth/ 영역 ≥ 80% line coverage
  4. `npm run build` 영향 없음 (test 파일은 build 제외)

- 위험 / 메모
  - `kratos-logout` 의 `window.location.assign` 은 jsdom 에서 mock 필요 (vi.stubGlobal 또는 location 모킹)
  - 첫 도입 PR 이라 npm 의존성 + lock file 변경량 큼 (lock 변경 그대로 commit)
  - Next.js 16 / React 19 / Vitest 호환 — 최신 Vitest (v3 권장)

- 상태: planned

### PR-T3 — Playwright E2E 인프라 + 첫 시나리오 (planned)

DEC-2=Playwright + DEC-3=A 반영. 사용자 native Hydra/Kratos 환경 가정.

- 위치 (예정)
  - `frontend/package.json` — devDependencies: `@playwright/test`. `scripts.e2e` / `scripts.e2e:ui` 추가
  - `frontend/playwright.config.ts` — `webServer` 자동 시작 안 함 (사용자가 backend + frontend + Hydra + Kratos 별도 기동), `baseURL: http://localhost:3000`
  - `frontend/tests/e2e/` (신규 디렉터리)
    - `fixtures.ts` — Kratos identity seed + login helper (page.goto('/login') + fill + submit)
    - `auth.spec.ts` — 4 시나리오: ① developer 로그인 → /developer 진입, ② manager → /manager, ③ system_admin → /admin, ④ system_admin 외 사용자가 `/admin` 직진입 시 default landing 으로 redirect
    - `signout.spec.ts` — 2 시나리오: ① Sign Out 후 `/login` 진입 시 password 재입력 요구 (Hydra session 종료), ② RP-initiated `/auth/logout?logout_challenge=...` 흐름
    - `password-change.spec.ts` — 1 시나리오: 로그인 → `/account` 비밀번호 변경 → Sign Out → 새 비밀번호로 재로그인
  - `docs/setup/e2e-test-guide.md` (신규) — 운영자가 e2e 실행 전 준비할 사전 조건 (Hydra/Kratos 가동, OIDC client 등록, Kratos identity seed 3 role)

- DoD
  1. `npx playwright install --with-deps chromium` (1회) 후 `npm run e2e` 가 시나리오 7건 PASS
  2. Playwright HTML report 가 `frontend/playwright-report/` 에 생성
  3. `tests/e2e/fixtures.ts` 의 시드 헬퍼가 idempotent (이미 존재하는 identity 는 reuse)
  4. `e2e-test-guide.md` 가 처음부터 끝까지 자동화 절차 안내

- 위험 / 메모
  - **사용자 환경 의존**: Hydra `urls.login=http://localhost:3000/auth/login`, Kratos `flows.settings.ui_url=http://localhost:3000/account` 모두 hard-coded. e2e 가동 시 backend + frontend + IdP 5개 프로세스 가동 필요.
  - **시드 identity**: Kratos admin `POST /admin/identities` 로 3 role 사용자 (`alice`/`bob`/`charlie`) + DevHub `users` 행. 시드 후 e2e 가 즉시 password 로 로그인 가능
  - **Hydra session**: 시나리오 간 cross-contamination 방지 위해 각 test 가 새 browser context (Playwright 기본 동작)
  - **시간**: 시나리오 7건 약 1-2분 소요. 빠른 피드백 위해 시나리오 병렬화 권장

- 상태: planned

## 4. 의존 그래프

```
PR-T1 (Makefile) — 독립
PR-T2 (Vitest)   — 독립 (frontend 만)
PR-T3 (Playwright) — 독립 (frontend 만, IdP 환경 의존)
```

세 PR 모두 독립적. 권장 순서: **PR-T1 → PR-T2 → PR-T3** (인프라 학습 곡선 점진).

## 5. 진척 추적 (체크리스트)

### 결정
- [x] DEC-1 Vitest (2026-05-11)
- [x] DEC-2 Playwright (2026-05-11)
- [x] DEC-3 A 사용자 native E2E (2026-05-11)
- [x] DEC-4 B 별도 sprint CI (2026-05-11)

### PR-T1 Makefile + baseline
- [x] Makefile 5 target 추가 (`test`, `test-race`, `test-coverage`, `test-frontend` placeholder, `e2e` placeholder)
- [x] sprint baseline 4 문서 작성
- [x] main state.json 갱신 (work_26_05_11-d 인덱스 추가)
- [ ] PR 머지

### PR-T2 Frontend Vitest
- [ ] devDependencies 설치 + scripts 추가
- [ ] vitest.config.ts + test-setup.ts
- [ ] 5 단위 테스트 파일 (≥ 20 케이스)
- [ ] `npm test` PASS
- [ ] `npm run test:coverage` 가 lib/auth/ ≥ 80%
- [ ] PR 머지

### PR-T3 Playwright E2E
- [ ] @playwright/test devDep + scripts
- [ ] playwright.config.ts
- [ ] fixtures.ts (Kratos seed + login helper)
- [ ] auth.spec.ts (4 시나리오)
- [ ] signout.spec.ts (2 시나리오)
- [ ] password-change.spec.ts (1 시나리오)
- [ ] docs/setup/e2e-test-guide.md 작성
- [ ] 사용자 환경에서 e2e 7건 PASS 1회 검증
- [ ] PR 머지

## 6. 검증 절차

PR-T1: `make test` 가 `go test ./...` PASS

PR-T2: `npm test` (CI mode) PASS + `npm run test:coverage` ≥ 80% (lib/auth)

PR-T3: 사용자 native 환경에서 `npm run e2e` 7건 PASS — `docs/setup/e2e-test-guide.md` 절차 1회 완수

## 7. 위험 / 메모

- **본 sprint 의 본질**: TDD 의 *기반* 마련 — 인프라 + 첫 시나리오. TDD 의 실제 적용(테스트 먼저 → 구현)은 다음 sprint 부터.
- **Frontend 단위 테스트 coverage 80%**: lib/auth/ 영역만 첫 sprint 목표. 전체 frontend coverage 는 후속 sprint 에서 lib/services/ + components/ 확장.
- **E2E 시간**: 시나리오 7건 → 1-2분. 후속 sprint 에서 시나리오 확장 시 parallelization 검토.
- **CI 부재**: 본 sprint 범위 밖 (DEC-4=B). 다음 sprint 의 첫 작업이 CI 도입.

## 8. 변경 이력

| 일자 | 변경 |
| --- | --- |
| 2026-05-11 | 초판 작성. TDD foundation sprint 진입 결정 후 작성. DEC-1/2/3/4 모두 권장안 채택 |
