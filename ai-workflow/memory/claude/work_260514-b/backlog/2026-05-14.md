# Backlog — 2026-05-14 (sprint claude/work_260514-b)

## 진입 컨텍스트
- Base: main @ `642d976` (PR #105 머지 직후)
- 직전 sprint: claude/work_260514-a (PR #105 closed) — Application Design 1차 scaffolded
- 본 sprint scope: A1+A2+A3 묶음 (store body + handler body + UT)

## 추적성 영향 사전 진단 (sync-checklist §1)

| 단계 | 영향 | 본 sprint 처리 |
| --- | --- | --- |
| REQ-FR | 변경 없음 (이미 발급) | N/A |
| REQ-NFR | 변경 없음 | N/A |
| UC | 변경 없음 | N/A |
| ARCH | 신규 발급 후보 — `ARCH-18 Application Design Backend (body 도입)` | 발급 검토 |
| API | API-41..50 scaffolded → **activated** (501 → 정식 응답 + 422/409 분기 + audit emit) | 활성화 |
| RM | 미발급 — design entry 라 RM 코드 아직 없음. 본 sprint 가 body 도입이라 RM 발급 가능성 평가 | 결정 |
| IMPL | IMPL-application-store-01 책임 확장 (stub → body) + 신규 sub-IMPL | 발급 |
| UT | UT-application-store-XX (16+) + UT-application-handler-XX (10+) 신규 발급 | 신규 |
| TC | 미진입 (carve out) | N/A |

## 작업 순서

1. **A1 postgres_applications.go 본체** — 16 메서드 PostgreSQL 구현. pgx 패턴, ErrConflict / ErrNotFound 분기. (~600-800 lines)
2. **A2 handler body** — 10 endpoint 실 응답 + 검증 + audit emit. (~400-600 lines)
3. **A3 UT** — store + handler 단위테스트. (~500-800 lines)
4. **C 매트릭스 동기** — trace.md / backend_api_contract.md / state.json + handoff 갱신.
5. **commit + push + PR**.

## 1차 결정 사항 (carve in/out)

**carve in (본 sprint)**:
- store body 16 메서드
- handler body 10 endpoint
- 상태 전이 가드 1차: planning→active 의 활성 Repository 1개 이상, immutable key, on_hold→active 의 resume_reason, * → archived 의 archived_reason
- audit emit: `application.{create,update,archive}.requested` + `application_repository.{link,unlink}.requested` + `scm_provider.update.requested` (read 는 carve out)
- UT 1차: 각 handler/store 메서드 happy + 1~2 edge

**carve out (다음 sprint)**:
- 상태 전이 가드의 critical 롤업 0건 검증 (롤업 store 의존)
- API-51..58 활성화
- e2e TC
- frontend UI
- 실 SCM 어댑터 구현
- list/get handler 의 audit emit (read 노이즈 회피)

## 위험 사전 평가

- **PR 크기**: 1500+ lines 예상. squash merge 1 PR. 자체 commit 은 logical 단위 (store / handler / UT / docs) 로 분리하여 review 가독성 ↑.
- **audit naming convention**: 기존 audit_logs 의 action 필드 패턴 (`auth.role_denied`, `account.signup.partial_failure`, `command.status.updated`) 와 일관. `application.{verb}.requested` 가 자연스러움. 결정 후 다른 PR 일관성 확인.
- **검증 깊이**: 본 sprint 의 1차는 happy path + 주요 422/409 만. 완전한 RBAC denial 테스트는 기존 enforceRoutePermission middleware 가 처리하므로 handler test 는 stub-style 로 충분.
