# 2026-05-12 — work_260512-i 슬롯 closure (PR #80)

PR-D (audit enrichment) 의 남은 3 항목을 한 sprint 안에서 처리.

## PR 라인업

| # | 주제 | 슬롯 |
| --- | --- | --- |
| #80 | feat(audit): PR-D follow-up — commands enrichment + log request_id + TRUSTED_PROXIES | work_260512-i |

## 4 commit 분해

| commit | sub-task |
| --- | --- |
| `277b53b` | #3 DEVHUB_TRUSTED_PROXIES env |
| `7a0e6ce` | #1 commands-flow audit enrichment |
| `ac22ade` | #2 handler/middleware log request_id |
| `47c9264` | self-review fix — logRequest format-safety |

## Self-review 패스 결과

- **보강 commit `47c9264`**: logRequest 의 prefix 가 문자열 연접 → caller-supplied X-Request-ID 의 percent 가 printf verb 로 해석되는 회귀. rid 를 `%s` verb 로 렌더링하도록 수정 + 단위 테스트 2건.
- 노트 3건 (블로커 아님, PR 코멘트로 박제):
  1. caller-supplied X-Request-ID 콘텐츠 validation (newline / control char) — 별도 sprint.
  2. ctx 표준 request_id 전파 — kratos_login_client / kratos_identity_resolver 도 자동 tagged 화. 별도 sprint.
  3. writeRBACServerError → writeServerError 통합 TODO.

## main 머지 결과

- `gh pr merge 80 --squash --delete-branch` — 단일 squash commit `ffcaec6`.
- main HEAD = `ffcaec6`.
- 19 파일 변경, +475 / -28 lines.

## handoff 후보 표 outdated 관찰

세션 시작 시 `session_handoff.md` 의 next-action 표가 outdated 였다:
- PR-T3.5 (#62) 이미 머지 — Sprint 진입 후 발견
- PR-L4 (#60) 이미 머지 — 같이
- M2 hygiene `kratos_identity_id` (PR-L4 안에 포함) — Sprint B 폐기

handoff 의 출처가 work_26_05_11-d (2026-05-11 EOD) 스냅샷이라 그 후 머지된 5 PR 을 반영 못함. 다음 슬롯 진입 시 후보 표 작성 전에 `git log --oneline` 으로 main 변동 확인하는 단계가 일관 필요.

## 다음 슬롯 출발점

main 에서 새 branch 로 시작. 자연 후보:
1. ctx 표준 request_id 전파 — kratos_login_client / kratos_identity_resolver 도 tagged.
2. caller-supplied X-Request-ID validation.
3. writeRBACServerError → writeServerError 통합.
4. M4 진입.
