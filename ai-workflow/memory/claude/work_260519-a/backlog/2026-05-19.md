- 문서 목적: 2026-05-19 sprint `claude/work_260519-a` 의 ADR-0019 재산정 작업 backlog.
- 범위: ADR-0019 신규 발행 + ADR-0001 supersession + 2 planning 문서 status 갱신 + traceability sync + sprint EOD.
- 대상 독자: 본 sprint 작업자 + 후속 리뷰어.
- 상태: in_progress
- 최종 수정일: 2026-05-19
- 관련 문서: `docs/planning/keycloak_sso_federation.md`, `docs/planning/keycloak_only_refactor_execution_plan.md`, `docs/adr/0001-idp-selection.md`, `docs/traceability/report.md`

## 1. 컨텍스트

직전 sprint (post-EOD #2, sprint -x, PR #165) EOD 이후 외부 PR 3건 main 머지:

- **PR #166** (sha `694e694`) — ADR-0018 단일 포트 reverse proxy 실 구현 (nginx + `/devhub` prefix)
- **PR #167** (merge `dff487d`) — **Keycloak-only refactor** (codex/keycloak-only-refactor-plan): Hydra/Kratos webhook 제거 + `keycloak_verifier.go` 신규 + `keycloak_admin_client.go` 신규 + migration 000021 + JWKS cache + resource_access role fallback
- **PR #168** (merge `09f4ca1`) + post-update (`bff2827`) — workflow memory sync

PR #167 의 **결정 문제**:
- design 문서 PR #163 (`docs/planning/keycloak_sso_federation.md`) 는 **옵션 B (Kratos federation, Keycloak upstream OIDC) 권장**
- PR #167 의 actual implementation 은 **옵션 A (Keycloak 단일화)** 채택 — design 권장을 reversal
- PR #167 의 codex 는 ADR-0001 의 **제목 + §3.5 heading 만** Keycloak 으로 수정 + 본문 (§3.5 body / §4 결정 / §5 데이터 모델 / §6 흐름 / §7 결과 / §8 미해결 / §9 구현) 은 **Hydra+Kratos 기준 그대로** 보존 → **inconsistent ADR governance 상태**

본 sprint 가 ADR-0019 신규 발행으로 위 결정 reversal 사후 명문화 + ADR-0001 supersession 처리.

## 2. 결정

| # | 결정 | 근거 |
| --- | --- | --- |
| D1 | ADR-0019 신규 발행 — "Keycloak 단일화 결정 (Hydra+Kratos 폐기)" | ADR governance 의 immutable history 원칙. 원본 결정 (Hydra+Kratos, ADR-0001) 의 historical context 보존 + reversal 결정 (Keycloak-only, ADR-0019) 의 명문화 분리. |
| D2 | ADR-0001 의 제목 + §3.5 heading 을 원래 (Hydra+Kratos) 로 복원 + status 헤더에 "superseded by ADR-0019" 추가 | PR #167 의 partial heading 수정이 ADR governance immutability 위반. 본문 전체를 historical 상태로 복원하면서 supersession note 로 reader 가 현재 결정 (ADR-0019) 으로 navigate 가능하도록. |
| D3 | PR #163 design 문서 (`keycloak_sso_federation.md`) status: planning → rejected. §15 의 ADR-0019 후보 문장 → "옵션 B 미채택, ADR-0019 가 옵션 A 채택" 으로 갱신. | 옵션 B 의 design 은 채택되지 않았으나 historical context 로 보존 가치. status 표기만 rejected 로 갱신. |
| D4 | PR #167 execution plan (`keycloak_only_refactor_execution_plan.md`) status: planned → done. §7 PR-A..F 표에 done 마킹 + PR #167 commit reference. | 실 구현 완료된 plan 의 status 정정. |
| D5 | traceability §3 RM-M4-09 row 의미 재정의 — "외부 SSO 통합 (Gitea 등)" 의 일부 (Keycloak federation) 가 ADR-0019 의 옵션 A 채택으로 본질 변경됨. RM-M4-09 는 Keycloak 단일화 이후의 외부 IdP federation (Gitea 등) 으로 scope 좁힘. | 본 결정 reversal 이 RM-M4 scope 에도 영향. |

## 3. 작업 단위 (sprint -a 의 단일 PR)

1. **T1**: `docs/adr/0019-keycloak-only-idp.md` 신규 작성
   - §1 상태 (Accepted, 결정 근거 sprint = `claude/work_260519-a`)
   - §2 컨텍스트 (PR #163 의 옵션 분석 + PR #167 의 reversal 사실)
   - §3 결정 사항 (옵션 A 채택 + 결정 근거)
   - §4 결과 (PR #167 의 KC-PR-A..F 머지 사실 + ADR-0001 supersession)
   - §5 운영 영향 + 잔여 carve out
   - §6 변경 이력
2. **T2**: `docs/adr/0001-idp-selection.md` 정정
   - 제목 "(Ory Keycloak)" → "(Ory Hydra + Kratos)"
   - status 헤더에 "superseded by ADR-0019 (2026-05-19)" 추가
   - §3.5 heading "Ory Keycloak" → "Ory Hydra + Kratos"
   - 본문 §4/§5/§6/§7/§8/§9 는 historical context 로 immutable 유지
3. **T3**: `docs/planning/keycloak_sso_federation.md`
   - status: planning → rejected (ADR-0019 가 옵션 A 채택)
   - §15 결정 후보 문장 갱신
   - §16 변경 이력 sprint -a row 추가
4. **T4**: `docs/planning/keycloak_only_refactor_execution_plan.md`
   - status: planned → done
   - §7 PR 분할 표에 done 마킹 + PR #167 commit reference
5. **T5**: `docs/traceability/report.md`
   - §3 RM-M4-09 row 의미 재정의
   - §4 ADR table: ADR-0001 status → superseded, ADR-0019 신규 row
   - §6 변경 이력 sprint -a row
6. **T6** (sprint EOD): main flat memory (state/handoff/work_backlog) + auto-memory + sprint state finalize

## 4. 검증

docs-only sprint 이므로:
- `pytest ai-workflow/tests/check_docs.py` 그린
- workflow-lint (action lint) 그린
- 본인 4단계 self-review (diff 재검토 → gh pr comment → 보강 commit → squash merge)

## 5. 잔여 carve out (sprint -a 종료 후)

- (carve) RM-M4-09 의 Keycloak federation 부분 — Keycloak 단일화 완료로 의미가 변경됐으나 외부 IdP federation (Gitea, AD 등) 의 후속 통합은 여전히 carve out.
- (carve) Keycloak realm/client/role 운영 SOP — `docs/setup/` 에 신규 가이드 추가 후보 (별도 sprint).
- (carve) JWKS rotation 운영 SOP — ADR-0019 §5 운영 영향에서 명시 + 별도 SOP 문서 carve.
- (carve) Keycloak SSO 외부 로그아웃 chain (RP-initiated logout) — design 문서 PR #163 §14 carve 중 일부는 옵션 A 환경에서도 유효, 본 ADR-0019 의 §5 carve 로 이전.
- (carve) ADR-0015 §6 (3)+(4), ADR-0016 §6 (3)+(4)+(5), ADR-0017 §6 (b), M4 RM-M4-XX — work_backlog flat 의 직전 directive 그대로 남음.

## 6. 변경 이력

| 일자 | 변경 |
| --- | --- |
| 2026-05-19 | sprint 진입. ADR-0019 재산정 작업 단위 6 항목 정의. |
