# 2026-06-02

- Verified local toolchain and fresh E2E stack.
- Found and fixed local-only Keycloak setup drift vs CI (`manage-users` missing for `devhub-backend`).
- Confirmed fresh local auth passes after rebuilding frontend with correct `BACKEND_API_URL`.
- Reproduced CI failures locally:
  - `admin-applications.spec.ts:73` created application row not visible after reload.
  - `repositories-publish.spec.ts:46` created repository draft row not visible in table.
