# Traceability Report — 1차 종합

- 문서 목적: DevHub 의 SDLC 자산 (요구사항 → Usecase → 설계 → 로드맵 → 구현 → 단위테스트 → E2E) 사이 추적 관계를 단일 매트릭스로 시각화한다.
- 범위: M0–M7 + v1.0 (M5 DREQ / M6 External Integration / M7 Onboarding 모두 closing). ADR 은 별도 §4 인덱스 (0001..0024).
- 대상 독자: 모든 contributor, 후속 리뷰어, 외부 감사.
- 상태: accepted
- 최종 수정일: 2026-06-07 (대시보드 UI 개선 및 신규 KPI/테스트 대시보드 추가, 추적성 ID 동기화)
- 결정 근거 sprint: `claude/work_260513-c` (초판), `claude/work_260527-codebase-review-roadmap-refresh` (2026-05-27 스냅샷 정합), `claude/work_260529-h` (Phase 4 §3 매트릭스 재구성), `claude/work_260529-o` (Phase 4 후속 §2 단계별 인덱스 정합).
- 관련 문서: [`README.md`](./README.md), [`conventions.md`](./conventions.md), [`sync-checklist.md`](./sync-checklist.md), [`../governance/document-standards.md`](../governance/document-standards.md), [코드베이스 분석 스냅샷](../analysis/2026-05-27-codebase-snapshot/README.md).

## 1. 사용법

- 단계 간 추적 관계를 한 페이지에서 찾을 때 §3 종합 매트릭스 사용.
- 특정 단계의 ID 정의를 찾을 때 §2 단계별 인덱스 사용.
- ADR 은 §4 의 링크.
- 갱신 절차는 [`sync-checklist.md`](./sync-checklist.md) §3.6 참조.

## 2. 단계별 인덱스 (요약)

> **Phase 4 정합 (2026-05-29, sprint `claude/work_260529-o`)** — 본 §2 단계별 인덱스의 row 그룹화는 §3 매트릭스의 **10 core 도메인 + Shared + Infrastructure + Cross-cutting** SoT 와 정렬한다. 각 row 는 §3 의 어느 도메인 행에 매핑되는지 명시 + 도메인 sub-document path 를 source-of-truth 로 link. 기존 cross-cutting 분류 (인증 / 회원가입 / 계정 관리 분리 row 등) 는 §3 통합 도메인 (예: auth-session) 으로 묶어 표기.

### 2.1 Requirements (REQ)

- **Functional**: REQ-FR-01 ~ REQ-FR-111 (총 111 항목, `docs/requirements.md` §2–§5 + `docs/backend/requirements.md` §1–§5 + `docs/backend_requirements_org_hierarchy.md` §1–§3 + `docs/frontend_integration_requirements.md` §2–§3 분포). **§3 매트릭스 도메인 매핑**: FR-15..26, 61..67 = auth-session ([`docs/domain/auth-session/requirements.md`](../domain/auth-session/requirements.md)) / FR-27, 86 = rbac-permissions ([`docs/domain/rbac-permissions/requirements.md`](../domain/rbac-permissions/requirements.md)) / FR-18, 26, 102 = audit-ops ([`docs/domain/audit-ops/requirements.md`](../domain/audit-ops/requirements.md)) / FR-68..80 = organization-management ([`docs/domain/organization-management/requirements.md`](../domain/organization-management/requirements.md)) / FR-1..11, 28..36, 81, 85, 88, 89, 96 = application-lifecycle ([`docs/domain/application-lifecycle/requirements.md`](../domain/application-lifecycle/requirements.md)) / FR-49..55 = repository-integration ([`docs/domain/repository-integration/requirements.md`](../domain/repository-integration/requirements.md)) / FR-56, 57, 60, 82, 83, 104, 105 = realtime ([`docs/domain/realtime/requirements.md`](../domain/realtime/requirements.md)) / FR-58, 59, 84, 95, 100, 101 = realtime cross-cut (명령 lifecycle publish) / FR-12, 13, 97, 98, 99 = Infrastructure (infra-topology) / FR-37..48, 90..94 = Cross-cutting (M4 planned); **REQ-FR-106** = integration-registry / **REQ-FR-107** = auth-session / **REQ-FR-108** = repository-integration / **REQ-FR-109..111** = application-lifecycle.
- **Non-functional**: REQ-NFR-01 ~ REQ-NFR-26 (총 26 항목, 보안/성능/배포 정책 + 운영 hygiene + API 표준). **§3 도메인 매핑**: NFR-3, 4, 5, 7, 17..20 = auth-session / NFR-26 = rbac-permissions / NFR-4 = audit-ops / NFR-21 = organization-management / NFR-11, 22, 23 = realtime / NFR-12, 16 = Infrastructure (infra-topology) / NFR-1 = Infrastructure (deployment-automation, no-docker).
- **Application/Project 요구사항 확장 (§3 application-lifecycle row)**: `REQ-FR-APP-001..012`, `REQ-FR-PROJ-000..010`, `REQ-NFR-PROJ-001..006` (`docs/requirements.md` §5.4 → [`docs/domain/application-lifecycle/requirements.md`](../domain/application-lifecycle/requirements.md)).
- **Dev Request (DREQ) 도메인 신규 (§3 dev-request row)**: `REQ-FR-DREQ-001..013`, `REQ-NFR-DREQ-001..006` (`docs/requirements.md` §5.5 → [`docs/domain/dev-request/requirements.md`](../domain/dev-request/requirements.md), sprint `claude/work_260515-f`, **컨셉/요구사항 단계 — 구현은 carve out**).
- **External Integration 도메인 신규 (§3 integration-registry row)**: `REQ-FR-INT-001..015`, `REQ-NFR-INT-001..009` (`docs/requirements.md` §5.6 → [`docs/domain/integration-registry/requirements.md`](../domain/integration-registry/requirements.md), sprint `codex/memory-next-step-20260515`).
- **Onboarding 도메인 신규 (§3 onboarding row)**: `REQ-FR-ONBOARD-001..012`, `REQ-NFR-ONBOARD-001..008` (`docs/requirements.md` §5.7 → [`docs/domain/onboarding/requirements.md`](../domain/onboarding/requirements.md), sprint `claude/onboarding-requirements-2026-05-21`, **요구사항 단계 — concept `docs/domain/onboarding/concept.md` 의 결정을 acceptance criteria 로 변환**).
- **SCM↔시스템 Repository 연동 + Lifecycle 신규 (§3 repository-integration row)**: `REQ-FR-REPO-001..005`, `REQ-NFR-REPO-001..003` (`docs/requirements.md` §5.8 → [`docs/domain/repository-integration/requirements.md`](../domain/repository-integration/requirements.md), 2026-05-27 코드베이스 스냅샷 정합 — 소유권 분리 / 원격 import / outbound create / draft→publish / capability gate).
- **Application 개발 대시보드 도메인 (§3 application-lifecycle row 흡수)**: `REQ-FR-APPDASH-001..006`, `REQ-NFR-APPDASH-001..003` (`docs/requirements.md` §5.9 → [`docs/domain/application-lifecycle/requirements.md`](../domain/application-lifecycle/requirements.md), sprint `gemini/application-dashboard-concept`, **요구사항 단계**).
- **Task Item Ingestion 도메인 (§3 integration-registry row 흡수)**: `REQ-FR-TASK-001..010`, `REQ-NFR-TASK-001..004` (`docs/requirements.md` §5.10 → [`docs/domain/integration-registry/task_requirements.md`](../domain/integration-registry/task_requirements.md), sprint `deepseek/work_260528-a-task-item-ingestion`, **요구사항 단계**).
- **Phase 3 신규 임시 ID (Phase 4 §3 매트릭스 흡수, 2026-06-02 RBAC 확장 반영)**: `REQ-RBAC-001..016` (§3 rbac-permissions row, [`docs/domain/rbac-permissions/requirements.md`](../domain/rbac-permissions/requirements.md)) + `REQ-AUDIT-001..007` (§3 audit-ops row, [`docs/domain/audit-ops/requirements.md`](../domain/audit-ops/requirements.md)) + `REQ-ORG-001..007` (§3 organization-management row, [`docs/domain/organization-management/requirements.md`](../domain/organization-management/requirements.md) — 기존 REQ-FR-68..80 과 정합).

### 2.1.5 Usecase (UC)

- `UC-AUTH-01..03`, `UC-ACCOUNT-01..03`, `UC-ORG-01..04`, `UC-RBAC-01..07`, `UC-GITEA-01..03`, `UC-CMD-01..03`, `UC-AUD-01..02`, `UC-RT-01..02`, `UC-APP-01..10`, `UC-PROJ-01..10`, `UC-DREQ-01..12`, `UC-INT-01..18`, `UC-ONBOARD-01..11`, `UC-REPO-01..07`, `UC-APPDASH-01..07`, `UC-TASK-01..06`, `UC-KPI-01..02`, `UC-TEST-01..03` (`docs/planning/system_usecases.md`; §2.4 RBAC 고도화로 `UC-RBAC-04..07` 추가(2026-06-02) + §2.12 INT 에 `UC-INT-15..18` auth_mode/연결테스트/Gitea sync/webhook alias + §2.14 `UC-REPO-01..07` SCM 연동·draft/publish + §2.15 `UC-APPDASH-01..07` Application 대시보드 + §2.16 `UC-TASK-01..06` Task Item Ingestion + KPI/테스트 대시보드 추가).
- **§3 매트릭스 도메인 매핑**: `UC-AUTH-01..03` + `UC-ACCOUNT-01..03` = **auth-session** / `UC-RBAC-01..07` = **rbac-permissions** / `UC-AUD-01..02` = **audit-ops** / `UC-ORG-01..04` = **organization-management** / `UC-APP-01..10` + `UC-PROJ-01..10` + `UC-APPDASH-01..07` + `UC-GITEA-03` (Application/Project ↔ SCM cross-cut) + `UC-RT-01` (대시보드 realtime cross-cut) = **application-lifecycle** / `UC-GITEA-01..03` + `UC-REPO-01..07` = **repository-integration** / `UC-DREQ-01..12` = **dev-request** / `UC-INT-01..18` + `UC-TASK-01..06` = **integration-registry** / `UC-RT-01..02` + `UC-CMD-01..03` (명령 lifecycle realtime publish cross-cut) = **realtime** / `UC-ONBOARD-01..11` = **onboarding**.
- 정책: 신규 도메인은 `REQ → UC → ARCH/API` 순서를 기본 체인으로 사용한다. 기존 도메인은 점진 전환. **Phase 4 정합 후 도메인 sub-document SoT** = [`docs/domain/<domain>/{requirements,architecture,api,test_cases}.md`](../domain/) (Phase 3 이관 결과).

### 2.2 Design (ARCH / API)

- **Architecture**: ARCH-01 ~ ARCH-22 + `ARCH-DREQ-01..06` + `ARCH-INT-01..07` + `ARCH-ONBOARD-01..06` + `ARCH-REPO-01..07` + `ARCH-APPDASH-01..06` + `ARCH-TASK-01..06` + **Phase 3 신규 임시 ID** `ARCH-RBAC-01..06` ([`docs/domain/rbac-permissions/architecture.md`](../domain/rbac-permissions/architecture.md)) + `ARCH-AUDIT-01..04` ([`docs/domain/audit-ops/architecture.md`](../domain/audit-ops/architecture.md)) + `ARCH-ORG-01..05` ([`docs/domain/organization-management/architecture.md`](../domain/organization-management/architecture.md)). (`ARCH-INT-07` = Gitea pull sync 워커 + auth_mode/OutboundAuth, §8.7; `ARCH-REPO-01..07` = Repository 소유권·연동·lifecycle; `ARCH-APPDASH-01..06` = Application 개발 대시보드; `ARCH-TASK-01..06` = Task Item Ingestion, sprint `deepseek/work_260528-a-task-item-ingestion`; **ARCH-18/19/20** CI/Logout/Build-runs; **ARCH-21..22** KPI/테스트 대시보드 설계).
- **§3 매트릭스 도메인 매핑**: ARCH-11, 12, 14 = **auth-session** ([`docs/domain/auth-session/architecture.md`](../domain/auth-session/architecture.md)) / ARCH-13 + ARCH-RBAC-01..06 = **rbac-permissions** / ARCH-14 + ARCH-AUDIT-01..04 = **audit-ops** / ARCH-15, 16, 17 + ARCH-ORG-01..05 = **organization-management** / ARCH-10 + ARCH-APPDASH-01..06 = **application-lifecycle** ([`docs/domain/application-lifecycle/architecture.md`](../domain/application-lifecycle/architecture.md)) / ARCH-06, 07, 08 + ARCH-REPO-01..07 = **repository-integration** ([`docs/domain/repository-integration/architecture.md`](../domain/repository-integration/architecture.md)) / ARCH-DREQ-01..06 = **dev-request** ([`docs/domain/dev-request/architecture.md`](../domain/dev-request/architecture.md)) / ARCH-INT-01..07 + ARCH-TASK-01..06 = **integration-registry** ([`docs/domain/integration-registry/architecture.md`](../domain/integration-registry/architecture.md) + [`task_architecture.md`](../domain/integration-registry/task_architecture.md)) / ARCH-05 = **realtime** ([`docs/domain/realtime/architecture.md`](../domain/realtime/architecture.md)) / ARCH-ONBOARD-01..06 = **onboarding** ([`docs/domain/onboarding/architecture.md`](../domain/onboarding/architecture.md)) / ARCH-04, 09 = Infrastructure (infra-topology cross-cut). 분포: 도메인 sub-document SoT 는 [`docs/domain/<domain>/architecture.md`](../domain/) (Phase 3 이관 결과), 보조 `docs/org_chart_ux_spec.md` + `docs/organizational_hierarchy_spec.md` 는 organization-management cross-cut.
- **API contract**: API-01 ~ API-101 (INT draft 포함 + DREQ token PATCH allowed_ips & expires_at + Onboarding §16 + **API-87 test-connection / API-88..90 SCM repositories list·import·create (§15.3) / API-91 `POST /repositories` draft / API-92 `POST /repositories/:id/publish` (§13.9) / API-93 `GET /applications/:id/dashboard` (§13.10, APPDASH) / API-94..96 Task Item Ingestion (§17) / API-98..100 CI/Logout/Build-runs / API-101 KPI/테스트 대시보드 가상 클라이언트 목킹**, 2026-06-07 정합). 기존 composite/결손 정책은 유지. **API-69..78, 80..82** 은 외부 시스템 연동 도메인 (§15) 으로 activated. **API-79** 는 DREQ intake token IP & Expiry mutation (§14.10, ADR-0017). **API-83..86** 은 Onboarding 도메인 (§16, sprint `claude/onboarding-arch-2026-05-21`). **API-32** (`GET /me`) / **API-33** (`POST /users`) 는 §16 에서 응답 shape / 동작 명시 확장. **§3 매트릭스 도메인 매핑**: API-01, 05 (대시보드), 32 (me), 36 = application-lifecycle + auth-session cross-cut / API-02, 04, 08..13 = repository-integration (webhook + pipelines) / API-14, 15..17, 36, 37 = realtime / API-18, 39 = audit-ops / API-19, 23, 25 (폐기), 32, 33, 35 = auth-session / API-26..29, 38..40 (API-30/31 폐기) = rbac-permissions / API-33, 34 = organization-management / API-41..58, **API-101** = application-lifecycle (Application/Repository/Project / KPI/Tests) / API-59..68, 79 = dev-request / API-69..78, 80..82, 87, 94..96 = integration-registry / API-88..92, **API-100** = repository-integration (SCM 양방향 + draft/publish / build-runs) / API-93 = application-lifecycle (APPDASH) / API-83..86 = onboarding. API sub-document SoT = [`docs/domain/<domain>/api.md`](../domain/).

#### DREQ API §14 — endpoint 매핑 (spec staged sprint `claude/work_260515-f`, **activated sprint `claude/work_260515-i`**, §3 dev-request row → [`docs/domain/dev-request/api.md`](../domain/dev-request/api.md))

| API ID | 본문 위치 | 항목 | 상태 |
| --- | --- | --- | --- |
| `API-59` | §14.1 | `POST /api/v1/dev-requests` (외부 수신, intake auth) | **activated (sprint `i`)** |
| `API-60` | §14.2 | `GET /api/v1/dev-requests` (목록) | **activated (sprint `i`)** |
| `API-61` | §14.3 | `GET /api/v1/dev-requests/:id` (상세) | **activated (sprint `i`)** |
| `API-62` | §14.4 | `POST /api/v1/dev-requests/:id/register` (Promote) | **activated (sprint `i`)** — sprint `i` 의 단순 매핑 → **단일 트랜잭션 활성화 (sprint `m` PR #128)**. ADR-0013 + `RegisterDevRequestWithNewApplication` / `RegisterDevRequestWithNewProject`. |
| `API-63` | §14.5 | `POST /api/v1/dev-requests/:id/reject` | **activated (sprint `i`)** |
| `API-64` | §14.6 | `PATCH /api/v1/dev-requests/:id` (Reassign — system_admin only) | **activated (sprint `i`)** |
| `API-65` | §14.7 | `DELETE /api/v1/dev-requests/:id` (Close — system_admin only) | **activated (sprint `i`)** |
| `API-66` | §14.10 | `POST /api/v1/dev-request-tokens` (intake token 발급) | **activated (sprint `o` PR #130 / ADR-0014)** — plain 1회 노출 + SHA-256 hashed 저장. **token expires_at 추가** (PR #137 `gemini/dreq_e2e_260515`, migration 000027). |
| `API-67` | §14.10 | `GET /api/v1/dev-request-tokens` (intake token 목록) | **activated (sprint `o` PR #130)** |
| `API-68` | §14.10 | `DELETE /api/v1/dev-request-tokens/:token_id` (intake token revoke, idempotent) | **activated (sprint `o` PR #130)** — `revoked_at = COALESCE(revoked_at, NOW())`. |
| `API-79` | §14.10 | `PATCH /api/v1/dev-request-tokens/:token_id` (intake token IP & Expiry mutation — allowed_ips & expires_at 갱신) | **activated (PR #137, sprint gemini/work_260521-c-219-patch-token)** — 운영 IP allowlist 및 만료일 동적 변경 endpoint (만료 토큰의 연장/재활성화 허용하는 옵션 B 반영). |

#### Integration API §15 — endpoint 매핑 (sprint `codex/next-step-20260516`, API-69..75 baseline activated, §3 integration-registry row → [`docs/domain/integration-registry/api.md`](../domain/integration-registry/api.md); API-88..90 SCM 양방향은 §3 repository-integration row 와 cross-cut)

| API ID | 본문 위치 | 항목 | 상태 |
| --- | --- | --- | --- |
| `API-69` | §15.2 | `GET /api/v1/integration/providers` | **activated (baseline)** |
| `API-70` | §15.2 | `POST /api/v1/integration/providers` | **activated (baseline)** |
| `API-71` | §15.2 | `PATCH /api/v1/integration/providers/{provider_id}` | **activated (baseline)** |
| `API-72` | §15.2 | `POST /api/v1/integration/providers/{provider_id}/sync` | **activated (baseline)** |
| `API-73` | §15.3 | `POST /api/v1/integration/providers/{provider_key}/webhook` | **activated (baseline)** |
| `API-74` | §15.3 | `GET /api/v1/integration/bindings` | **activated (baseline)** |
| `API-75` | §15.3 | `POST /api/v1/integration/bindings` | **activated (baseline)** |
| `API-80` | §15.2 | `DELETE /api/v1/integration/providers/:provider_id` | **activated (sprint `j`)** |
| `API-81` | §15.3 | `PATCH /api/v1/integration/bindings/:binding_id` | **activated (sprint gemini/work_260520-d)** |
| `API-82` | §15.3 | `DELETE /api/v1/integration/bindings/:binding_id` | **activated (sprint gemini/work_260520-d)** |
| `API-76` | §15.4 | `GET /api/v1/infra/services` | **activated (baseline)** |
| `API-77` | §15.4 | `POST /api/v1/infra/services/snapshot` | **activated (baseline)** |
| `API-78` | §15.4 | `GET /api/v1/infra/topology/v2` | **activated (baseline)** |
| `API-87` | §15.2 | `POST /api/v1/integration/test-connection` | **activated** (등록 UX 고도화 #5, base_url reachability, PR #352) + API-70/71 에 base_url 필드 추가 (migration 000038). auth_mode full 모델 (token/basic/app_password/oauth2/agent) + api_token write-only slot (migration 000040/000041, PR #355/#358). |
| `API-88` | §15.3 | `GET /api/v1/integration/providers/:provider_id/scm-repositories` (원격 SCM repo 목록 + imported 여부) | **activated (PR #363, SCM↔시스템 연동 A+B)** — import=pull capability gate. disabled provider 거부 409 (PR #371). |
| `API-89` | §15.3 | `POST /api/v1/integration/providers/:provider_id/import-repositories` (선택 import — SCM 재조회 값으로 source=scm/provider_id upsert) | **activated (PR #363)** — system-owned 필드 보존(ON CONFLICT mirror만 갱신). |
| `API-90` | §15.3 | `POST /api/v1/integration/providers/:provider_id/create-repository` (시스템→SCM 실제 저장소 생성, gitea CreateRepo) | **activated (PR #366, Phase C)** — push capability + gitea-compat gate → source=system. |

> **Repository draft→publish lifecycle** (PR #368, codex; spec `backend_api_contract.md §13.9`): **API-91** `POST /api/v1/repositories` (createRepositoryDraft) + **API-92** `POST /api/v1/repositories/:id/publish` (requestRepositoryPublish). ARCH-REPO-05 (`architecture.md §10`). migration 000043 `repositories.{repository_status(draft|active),publish_requested_at,published_at}`. provider 참조는 #373 (migration 000045) 로 `provider_id`(FK) 단일화 — handler 가 입력 `provider_key`→provider_id 해석. **handler 무테스트 머지 — UT/TC 보강 carve** (분석 §G4, [N-2/N-3](../analysis/2026-05-27-codebase-snapshot/06_future_direction.md)).

#### Application/Repository API §13 — endpoint 매핑 (sprint `claude/work_260514-a`, §3 application-lifecycle row [API-41..47, 55..58] → [`docs/domain/application-lifecycle/api.md`](../domain/application-lifecycle/api.md) + §3 repository-integration row [API-48..54] → [`docs/domain/repository-integration/api.md`](../domain/repository-integration/api.md))

| API ID | 본문 위치 | 항목 | 상태 |
| --- | --- | --- | --- |
| `API-41` | §13.1.1 | `GET /api/v1/scm/providers` | activated (sprint claude/work_260514-b) |
| `API-42` | §13.1.1 | `PATCH /api/v1/scm/providers/:provider_key` | activated (sprint claude/work_260514-b) |
| `API-43` | §13.2 | `GET /api/v1/applications` | activated (sprint claude/work_260514-b) |
| `API-44` | §13.2 | `POST /api/v1/applications` | activated (sprint claude/work_260514-b) |
| `API-45` | §13.2 | `GET /api/v1/applications/:application_id` | activated (sprint claude/work_260514-b) |
| `API-46` | §13.2 | `PATCH /api/v1/applications/:application_id` | activated (sprint claude/work_260514-b) |
| `API-47` | §13.2 | `DELETE /api/v1/applications/:application_id` (archive) | activated (sprint claude/work_260514-b) |
| `API-48` | §13.3 | `GET /api/v1/applications/:application_id/repositories` | activated (sprint claude/work_260514-b) |
| `API-49` | §13.3 | `POST /api/v1/applications/:application_id/repositories` | activated (sprint claude/work_260514-b) |
| `API-50` | §13.3 | `DELETE /api/v1/applications/:application_id/repositories/*repo_key` | activated (sprint claude/work_260514-b) |
| `API-51` | §13.4 | `GET /api/v1/repositories/:repository_id/activity` | activated (sprint claude/work_260514-c) |
| `API-52` | §13.4 | `GET /api/v1/repositories/:repository_id/pull-requests` | activated |
| `API-53` | §13.4 | `GET /api/v1/repositories/:repository_id/build-runs` | activated |
| `API-54` | §13.4 | `GET /api/v1/repositories/:repository_id/quality-snapshots` | activated |
| `API-55` | §13.5 | `GET /api/v1/repositories/:repository_id/projects` + `POST` | activated |
| `API-56` | §13.5 | `GET /api/v1/projects/:project_id` + `PATCH` + `DELETE` | activated |
| `API-57` | §13.6 | `GET /api/v1/applications/:application_id/rollup` (concept §13.4 normalize) | activated |
| `API-58` | §13.7 | `GET /api/v1/integrations` + CRUD (scope polymorphism) | activated |

> **scaffolded 단계 정의**: gin 라우트 등록 + RBAC matrix 통합 + 핸들러 stub (501 응답). store body / 요청 검증 / 응답 body 는 후속 sprint 에서 채움. RBAC 거부 (developer/manager 의 4 신규 resource 는 모든 axis false — migration 000018 + ADR-0011 §4.1) 가 stub 호출 전에 enforce 되므로 system_admin 만 stub 까지 도달.

#### RBAC API §12 — endpoint 매핑 (sprint `claude/work_260513-f`, 본문 ID 노출 1차 도메인, §3 rbac-permissions row → [`docs/domain/rbac-permissions/api.md`](../domain/rbac-permissions/api.md))

| API ID | 본문 위치 | 항목 |
| --- | --- | --- |
| `API-26` | §12.2 | `GET /api/v1/rbac/policies` |
| `API-27` | §12.3 | `PUT /api/v1/rbac/policies` |
| `API-28` | §12.4 | `POST /api/v1/rbac/policies` (사용자 정의 role 생성) |
| `API-29` | §12.5 | `DELETE /api/v1/rbac/policies/:role_id` (사용자 정의 role 삭제) |
| ~~`API-30`~~ | ~~§12.6~~ | ~~`GET /api/v1/rbac/subjects/:subject_id/roles`~~ — **폐기 (sprint -d, ADR-0020, PR TBD)**: backend-only dead-end (frontend UI 미구현). `users.role` 직접 read 였고 Keycloak group composite 가 실 권한 source. |
| ~~`API-31`~~ | ~~§12.7~~ | ~~`PUT /api/v1/rbac/subjects/:subject_id/roles`~~ — **폐기 (sprint -d, ADR-0020, PR TBD)**: `users.role` 직접 write. 결정 C (event listener 자동 sync) 와 충돌, `PATCH /api/v1/users/:id` 와 중복. |
| `API-38` | §12.8 | 라우트 → (resource, action) 매핑 표 (정책 정의) |
| `API-39` | §12.9 | 매핑 누락 정책 (deny-by-default) |
| `API-40` | §12.10 | Cache 와 무효화 정책 |

> 본 매핑 표는 `document-standards.md` §8 우선순위 3 (본문 ID 노출) 의 RBAC 도메인 1차 적용. 다른 도메인 (`account` §11.4, `commands/audit` §9 등) 은 후속 sprint 에서 동일 패턴으로 점진 확장.
>
> **표 가독성 정책**: 정밀 매핑 (endpoint 별 본문 위치, IMPL 별 책임) 은 본 §2 서브 표가 source-of-truth. §3 종합 매트릭스의 도메인 행은 ID 범위 (`API-26–31`) + 서브 표 참조 노트만 두어 표 시인성을 유지한다.

#### Auth API §11 — endpoint 매핑 (sprint `claude/work_260513-g`, 본문 ID 노출 2차 도메인, §3 auth-session row → [`docs/domain/auth-session/api.md`](../domain/auth-session/api.md))

| API ID | 본문 위치 | 항목 |
| --- | --- | --- |
| `API-19` | §11.3 | Go Core Bearer token 경계 (`Authorization: Bearer ...` verifier interface + actor context) |
| `API-32` | §10 | `GET /api/v1/me` (OIDC subject 기준 profile/role/org context 조회) |
| `API-35` | §11.5.1 | `POST /api/v1/account/password` (self-service password change — 인증/계정 도메인 cross-cut) |

> `/api/v1/auth/*` 는 Keycloak 전환 이후 제거된 legacy endpoint다. §11.2 OIDC 표준 endpoint(외부, DevHub 재정의 0) 는 본 매핑 표에서 제외 — `conventions.md` §5.2 정책.

#### Infra / Dashboard API §6 — endpoint 매핑 (sprint `claude/work_260513-i`, §3 application-lifecycle row [API-05 대시보드 metrics] + Infrastructure infra-topology row [API-06, 07] + repository-integration row [API-02, 04])

| API ID | 본문 위치 | 항목 |
| --- | --- | --- |
| `API-01` | §3 | `GET /health` |
| `API-02` | §4 | `POST /api/v1/integrations/gitea/webhooks` |
| `API-04` | §5 | `GET /api/v1/events` |
| `API-05` | §6.200 | `GET /api/v1/dashboard/metrics` |
| `API-06` | §6.234 | `GET /api/v1/infra/nodes` |
| `API-07` | §6.240, §6.245 | `GET /api/v1/infra/edges` + `GET /api/v1/infra/topology` (composite) |

#### Pipelines API §7 — endpoint 매핑 (sprint `claude/work_260513-i`, §3 repository-integration row → [`docs/domain/repository-integration/api.md`](../domain/repository-integration/api.md))

| API ID | 본문 위치 | 항목 |
| --- | --- | --- |
| `API-08` | §7 | `GET /api/v1/repositories` |
| `API-09` | §7 | `GET /api/v1/issues` |
| `API-10` | §7 | `GET /api/v1/pull-requests` |
| `API-11` | §7 | `GET /api/v1/ci-runs` |
| `API-12` | §7 | `GET /api/v1/ci-runs/:ci_run_id/logs` |
| `API-13` | §7 | `GET /api/v1/risks` + `GET /api/v1/risks/critical` (composite) |

#### Realtime / Command / Audit API §8, §9 — endpoint 매핑 (sprint `claude/work_260513-i`, §3 realtime row [API-14..17, 36, 37] → [`docs/domain/realtime/api.md`](../domain/realtime/api.md) + §3 audit-ops row [API-18] → [`docs/domain/audit-ops/api.md`](../domain/audit-ops/api.md))

| API ID | 본문 위치 | 항목 |
| --- | --- | --- |
| `API-14` | §8 | `GET /api/v1/realtime/ws` (WebSocket endpoint) |
| `API-15` | §9.1 | `POST /api/v1/admin/service-actions` |
| `API-16` | §9.2 | `POST /api/v1/risks/:risk_id/mitigations` |
| `API-17` | §9.3 | `GET /api/v1/commands/:command_id` |
| `API-18` | §9.4 | `GET /api/v1/audit-logs` |
| `API-36` | §8 envelope | `command.status.updated` WebSocket event envelope |
| `API-37` | §11.6 | command lifecycle audit 매핑 (`auth.role_denied`, command-target audit action) |

#### Account / Organization / Me API §10.1 — endpoint 매핑 (sprint `claude/work_260513-i`, §3 auth-session row [API-25 폐기, API-32 me] + §3 organization-management row [API-33, 34] → [`docs/domain/organization-management/api.md`](../domain/organization-management/api.md))

| API ID | 본문 위치 | 항목 |
| --- | --- | --- |
| ~~`API-25`~~ | ~~§10.1 (spec carve out)~~ | ~~`/api/v1/accounts/*` admin endpoint set~~ — **폐기 (sprint -i, ADR-0020 sub-carve B, 2026-05-20)**. lazy auto-create (`authenticateActor`, ADR-0020 §5.2) 가 첫 진입 시 `users` row 자동 생성. IdP 팀 (Keycloak admin console) + HRDB ETL push 책임. |
| `API-32` | §10 | `GET /api/v1/me` (현재 §10 의 1차 노출, 별도 spec 절은 후속 sprint) |
| `API-33` | §10.1 (spec carve out) | `/api/v1/users` CRUD set |
| `API-34` | §10.1 (spec carve out) | `/api/v1/organization/*` set (hierarchy + units + members) |

> §10.1 에 본문 spec 부재 endpoint 의 위치 표가 명시되어 있음. 본문 spec 작성은 후속 sprint 후보 (`work_backlog.md` 의 미진입 항목).

### 2.3 Roadmap (RM)

- **M0**: RM-M0-01 (1 항목, `X-Devhub-Actor` deprecation — §3 auth-session).
- **M1**: RM-M1-01 ~ RM-M1-04 (4 항목, RBAC track + API 계약 §11 재작성 — §3 rbac-permissions + auth-session).
- **M2**: RM-M2-01 ~ RM-M2-16 (16 항목, 인증/계정/조직/UX/audit + CI). 1차 완성 sprint (PR #85) 가 이전에 M3 으로 분류됐던 사용자/조직 관리 대부분을 흡수. **§3 매핑**: M2-01..06, 09 = auth-session / M2-07, 08 = organization-management / M2-11 = rbac-permissions / M2-15 = audit-ops / M2-16 = Infrastructure (deployment-automation, CI).
- **M3**: 사용자 및 조직 관리 — 대부분 M2 1차 완성에서 흡수, 잔여 정의는 §2.3.1 표 참조 (§3 auth-session / organization-management cross-cut + Infrastructure hrdb).
- **M4**: 실시간 + 과제 추적 + 시스템 관리. AI Gardener는 v2 범위. 정의는 §2.3.2 표 참조 (§3 realtime / repository-integration / rbac-permissions / Infrastructure keycloak-idp / gitea-scm cross-cut).
- **M5..M7**: M5 = dev-request (DREQ 도메인 1차 완성, ADR-0012/0013/0014/0017) / M6 = integration-registry (External Integration 1차 완성, ADR-0015/0016) / M7 = onboarding (RM-ONBOARD-01..04 풀스택 closing, ADR-0021).

> **drift 정합 (2026-05-13, sprint `claude/work_260513-k`)**: 본 §2.3 + `docs/development_roadmap.md` §3 가 M3/M4 정의의 single source-of-truth. 매트릭스 §3 의 도메인 행 인용 + state.json + backend_roadmap §5 모두 이 정의 기준으로 정합화. 본 sprint 이전에는 매트릭스가 RM-M3 = "Sign Up + WebSocket + AI" 로 정의해 development_roadmap.md M4 항목을 cross-cut 한 drift 상태였다.

#### 2.3.1 RM-M3 정의 (sprint `claude/work_260513-k`)

| RM ID | 항목 | 출처 |
| --- | --- | --- |
| `RM-M3-01` | Sign Up (셀프 가입) — `POST /api/v1/auth/signup` 의 hrdb lookup arm | development_roadmap §3 M3 |
| `RM-M3-02` | 인사 DB 스키마 (`name`, `system_id`, `employee_id`, `department_name`) + `internal/hrdb/` 모듈 활용 | development_roadmap §3 M3 |
| `RM-M3-03` | 조직 polish — `backend_api_contract.md` §10.4 자세한 schema + `parent_id` 검증 + primary_dept 자동 판정 (§5 백로그) | development_roadmap §3 M3 + §5 백로그 |

#### 2.3.2 RM-M4 정의 (sprint `claude/work_260513-k`, 본 sprint 가 부여)

| RM ID | 항목 | 출처 |
| --- | --- | --- |
| `RM-M4-01` | WebSocket 확장 — `infra.node.updated` / `ci.run.updated` / `risk.updated` event publish | development_roadmap §3 M4 + backend_roadmap §2 Phase 8 |
| `RM-M4-02` | WebSocket replay (last event) + 리소스 필터링 | backend_roadmap §2 Phase 8 잔여 |
| `RM-M4-03` | frontend command status WebSocket UI (Phase 4 마무리) | development_roadmap §4.2 Dashboard |
| `RM-V2-01` | AI Gardener gRPC — Python `AnalysisService` server + Go Core client | backend_roadmap §2 Phase 9 |
| `RM-V2-02` | AI Suggestion Feed 실데이터 바인딩 (frontend) | development_roadmap §4.6 AI(v2) |
| `RM-M4-06` | Gitea Hourly Pull worker (과제 추적 reconciliation). **1차 구현 PR #341** — `internal/gitea/` SCM pull sync worker (IMPL-gitea-03..05). 잔여 = hourly 스케줄 정밀화 + reconciliation 정책 ([issue #231](https://github.com/ykylee/Devhub_example/issues/231)). | backend_roadmap §2 Phase 10 |
| `RM-M4-07` | System Admin 대시보드 (Gitea Runner 상태 + 시스템 설정 관리) | development_roadmap §3 M4 |
| `RM-M4-08` | RBAC PermissionCache 다중 인스턴스 일관성 구현 ([ADR-0007](../adr/0007-rbac-cache-multi-instance.md) PG `LISTEN/NOTIFY`) | ADR-0007 §4.2 |
| `RM-M4-09` | 외부 SSO 통합 (Keycloak federation → Gitea / AD / LDAP 등 사내 시스템). **2026-05-19 의미 재정의**: 본 항목의 Keycloak 단일화 측면은 [ADR-0019](../adr/0019-keycloak-only-idp.md) 가 완료 (PR #167, 2026-05-18). 잔여 scope = Keycloak 의 identity broker / user federation 기능을 통한 외부 시스템 (Gitea, AD 등) 통합 — DevHub 코드 변경 없이 Keycloak 운영 layer 에서 처리. | development_roadmap §3 M4; [ADR-0019](../adr/0019-keycloak-only-idp.md) |
| `RM-ONBOARD-01` | IMPL-onboarding-backend — `users` migration (`onboarding_completed_at` + `review_status` + CHECK 제약) + `onboardingGate` middleware + 5 handler (API-83/84/85/86 + API-32/33 확장) + `lazy_auto_create.go` 폐기 + audit event const 3종 (`account.onboarding_completed` / `review_confirmed` / `unit_changed`). Feature flag `DEVHUB_ONBOARDING_GATE_ENABLED` default OFF. | [`docs/domain/onboarding/impl_plan.md`](../domain/onboarding/impl_plan.md) §2.1 + [ADR-0021 §6.1](../adr/0021-onboarding-self-service-unit-selection.md) |
| `RM-ONBOARD-02` | IMPL-onboarding-frontend — `/onboarding` page + `OnboardingForm` + `OrganizationPicker` (typeahead + tree hybrid) + skip flag sessionStorage + `OnboardingBanner` + `(dashboard)/layout.tsx` 3-branch gating + `/account` self-service unit edit. | [`docs/domain/onboarding/impl_plan.md`](../domain/onboarding/impl_plan.md) §2.2 |
| `RM-ONBOARD-03` | IMPL-onboarding-admin — `/admin/settings/users` 의 "Confirm Review" 액션 + `ConfirmReviewModal` + pending_review filter + `users.service.ts` 확장 (`confirmUserReview`). | [`docs/domain/onboarding/impl_plan.md`](../domain/onboarding/impl_plan.md) §2.3 |
| `RM-ONBOARD-04` | IMPL-onboarding-tests — backend UT (handler + middleware + token-only actor 회귀 + mega lifecycle) + frontend E2E (TC-ONBOARD-* 11건 mega spec + 6 test seed 정합) + `docs/domain/onboarding/test_cases.md` 카탈로그. | [`docs/domain/onboarding/impl_plan.md`](../domain/onboarding/impl_plan.md) §2.4 |

### 2.4 Implementation (IMPL)

- **Backend (`backend-core`)**: IMPL-auth-01..03, rbac-01..04, audit-01..02, account-01..05 (account-01..04 strikethrough — ADR-0020 sub-carve B 폐기, account-05 신규 = lazy auto-create sprint -i, 이후 Carve D 에서 삭제), org-01..04, command-01..05, serviceaction-01, domain-01..03, dashboard-01, infra-01, store-01..03, gitea-01..05, config-01, hrdb-01..02, realtime-01..02, me-01, health-01, idp-schema-01 (축약 반영; 상세는 각 서브 표 기준).
- **§3 매트릭스 도메인 매핑**: IMPL-auth-01..03 + IMPL-account-01..05 + IMPL-me-01 = **auth-session** ([`docs/domain/auth-session/`](../domain/auth-session/)) / IMPL-rbac-01..04 = **rbac-permissions** ([`docs/domain/rbac-permissions/`](../domain/rbac-permissions/)) / IMPL-audit-01..02 = **audit-ops** ([`docs/domain/audit-ops/`](../domain/audit-ops/)) / IMPL-org-01..04 = **organization-management** ([`docs/domain/organization-management/`](../domain/organization-management/)) / IMPL-application-XX + IMPL-project-XX + IMPL-dashboard-01 + IMPL-frontend-dashboard-01 + IMPL-onboarding-XX (Carve A) = **application-lifecycle** ([`docs/domain/application-lifecycle/`](../domain/application-lifecycle/)) — Onboarding row 는 [`docs/domain/onboarding/`](../domain/onboarding/) sub-document 참조 / IMPL-gitea-01..02 (webhook ingestion foundation) + IMPL-repository-XX (SCM 양방향 + draft/publish) = **repository-integration** ([`docs/domain/repository-integration/`](../domain/repository-integration/)) / IMPL-dreq-XX = **dev-request** ([`docs/domain/dev-request/`](../domain/dev-request/)) / IMPL-int-XX (domain/store/handler/webhook/router-rbac/migration/adapter) = **integration-registry** ([`docs/domain/integration-registry/`](../domain/integration-registry/)) / IMPL-realtime-01..02 + IMPL-command-01..05 + IMPL-serviceaction-01 = **realtime** ([`docs/domain/realtime/`](../domain/realtime/)) / IMPL-gitea-03..05 (SCM pull sync worker) = **Infrastructure (gitea-scm)** ([`docs/infrastructure/`](../infrastructure/) — domain layer 는 repository-integration cross-cut) / IMPL-hrdb-01..02 = **Infrastructure (hrdb)** / IMPL-infra-01 + IMPL-int-adapter-01 (HomeLab) = **Infrastructure (infra-topology)** / IMPL-keycloak-* (verifier / admin-client / event-puller / SPI) = **Infrastructure (keycloak-idp)** ([`docs/infrastructure/keycloak-idp/`](../infrastructure/keycloak-idp/)).

#### IMPL-rbac-XX 정의 (sprint `claude/work_260513-f`, §3 rbac-permissions row)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-rbac-01` | `backend-core/internal/httpapi/rbac.go` | API-26..29 의 4 endpoint handler + §6 legacy gone (`listRBACPolicies`, `createRBACPolicy`, `updateRBACPolicies`, `deleteRBACPolicy`, `getRBACPolicyLegacyGone`). API-30/31 `getSubjectRoles`/`setSubjectRoles` 는 sprint -d (ADR-0020) 로 폐기. |
| `IMPL-rbac-02` | `backend-core/internal/store/postgres_rbac.go` | RBAC role persistence (`ListRBACRoles`, `GetRBACRole`, `CreateRBACRole`, `UpdateRBACRolePermissions`, `UpdateRBACRoleMetadata`, `DeleteRBACRole`). subject-role assignment (`GetSubjectRoles`, `SetSubjectRole`) 은 sprint -d (ADR-0020) 로 폐기 — `users.role` 직접 read/write 였음, event listener 자동 sync 로 대체 예정. |
| `IMPL-rbac-03` | `backend-core/internal/httpapi/permissions.go` (`routePermissionTable` + `enforceRoutePermission`) | API-38 라우트 매핑 source-of-truth + 미들웨어 enforcement + API-39 deny-by-default |
| `IMPL-rbac-04` | `backend-core/internal/httpapi/permissions.go` (`PermissionCache`) | API-40 in-memory matrix cache + Invalidate |

#### IMPL-auth-XX 정의 (sprint `claude/work_260513-g`, §3 auth-session row)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-auth-01` | `backend-core/internal/auth/keycloak_verifier.go` + `internal/httpapi/auth.go` (`BearerTokenVerifier` interface) | §11.3 Bearer token verifier interface + Keycloak JWKS/discovery verifier 구현 |
| `IMPL-auth-02` | `internal/httpapi/auth.go` (`authenticateActor` middleware + `AuthenticatedActor` context) | §11.3 검증 결과를 request context 의 actor 로 전파 |
| `IMPL-auth-03` | `internal/httpapi/router.go` + `internal/httpapi/permissions.go` | 인증 경계 라우팅/권한 enforcement + legacy `/api/v1/auth/*` 제거 반영 |

> API-35 `POST /api/v1/account/password` 의 IMPL 은 account 도메인 (`internal/httpapi/account_password.go`). 매트릭스 §3 의 인증 행 + 계정 관리 행 양쪽에 ID 가 노출되는 cross-cut.

#### IMPL-account-XX 정의 (sprint `claude/work_260513-i`, §3 auth-session row)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| ~~`IMPL-account-01`~~ | ~~`internal/httpapi/accounts_admin.go` (`createAccount`)~~ | ~~API-25 POST~~ — **폐기 (sprint -i, ADR-0020 sub-carve B)**. 대체 = `lazy_auto_create.go::lazyAutoCreateUser` (authenticateActor 가 ErrNotFound 시 호출). |
| ~~`IMPL-account-02`~~ | ~~`internal/httpapi/accounts_admin.go` (`resetAccountPassword`)~~ | ~~API-25 PUT password~~ — **폐기**. 대체 = Keycloak admin console 또는 Account Console (§8.5b). |
| ~~`IMPL-account-03`~~ | ~~`internal/httpapi/accounts_admin.go` (`updateAccountStatus`, `deleteAccount`)~~ | ~~API-25 PATCH/DELETE~~ — **폐기**. 대체 = Keycloak admin (Enabled toggle / Delete) + event listener (sub-carve C, sprint -g 후속) sync. |
| ~~`IMPL-account-04`~~ | ~~`internal/httpapi/account_password.go`~~ | ~~API-35 self-service~~ — sprint -ad 에서 이미 폐기 (Keycloak Account Console redirect). |
| `IMPL-account-05` (신규) | `internal/httpapi/lazy_auto_create.go` (`lazyAutoCreateUser`) + `internal/httpapi/auth.go::authenticateActor` 의 lazy 분기 | ADR-0020 sub-carve B — 신규 user 첫 진입 시 `users` row 자동 생성 + audit `account.lazy_provisioned` + role default fallback (`user.role_default_assigned`). sprint -i (PR TBD). |

#### IMPL-org-XX 정의 (sprint `claude/work_260513-i`, §3 organization-management row)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-org-01` | `internal/httpapi/organization.go` (users handlers: `listUsers`, `getUser`, `createUser`, `updateUser`, `deleteUser`) | API-33 `/api/v1/users` CRUD |
| `IMPL-org-02` | `internal/httpapi/organization.go` (hierarchy + units handlers: `getOrganizationHierarchy`, `getUnit`, `createUnit`, `updateUnit`, `deleteUnit`) | API-34 hierarchy + units endpoint |
| `IMPL-org-03` | `internal/httpapi/organization.go` (members handlers: `listUnitMembers`, `replaceUnitMembers`) | API-34 unit members endpoint |
| `IMPL-org-04` | `internal/store/users_units.go` + organization store impl | persistence layer (users / org_units / unit_appointments). 2026-05-14 PR #112: `CreateOrgUnit` / `UpdateOrgUnit` 가 `unit_appointments` 의 leader appointment 를 자동 동기화 (demote-then-promote in tx, `SELECT ... FOR UPDATE` 직렬화, partial unique index `unit_single_leader_idx` 로 single-leader invariant SQL 강제). |

#### IMPL-command-XX 정의 (sprint `claude/work_260513-i`, §3 realtime row + Infrastructure commandworker)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-command-01` | `internal/httpapi/commands.go` (`createServiceAction`) | API-15 `POST /api/v1/admin/service-actions` handler |
| `IMPL-command-02` | `internal/httpapi/commands.go` (`createRiskMitigation`) | API-16 `POST /api/v1/risks/:id/mitigations` handler |
| `IMPL-command-03` | `internal/httpapi/commands.go` (`getCommand`) | API-17 `GET /api/v1/commands/:command_id` handler |
| `IMPL-command-04` | `internal/commandworker/*` | command worker (dry-run / live executor, `command.status.updated` publisher) |
| `IMPL-command-05` | `internal/serviceaction/*` | service-action 도메인 로직 (executor adapter 후보) |

#### IMPL-audit-XX 정의 (sprint `claude/work_260513-i`, §3 audit-ops row)

> auth 의 IMPL-auth-01..07 과 별도. audit 는 독립 모듈.

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-audit-01` | `internal/httpapi/audit.go` (`listAuditLogs`, `recordAudit`, `recordAuditBestEffort`) | API-18 `GET /api/v1/audit-logs` handler + 다른 도메인의 audit emit helper |
| `IMPL-audit-02` | `internal/store/audit_logs.go` | audit_logs persistence + source_type/request_id/source_ip 컬럼 (M1 PR-D 의 actor enrichment) |

#### IMPL-infra-XX / dashboard / realtime / me 정의 (sprint `claude/work_260513-i`, §3 application-lifecycle (dashboard/me) + realtime + Infrastructure infra-topology)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-infra-01` | `internal/httpapi/snapshot.go` (`getInfraNodes`, `getInfraEdges`, `getInfraTopology`) | API-06 + API-07 infra topology endpoint |
| `IMPL-dashboard-01` | `internal/httpapi/snapshot.go` (`getDashboardMetrics`) | API-05 dashboard metrics endpoint |
| `IMPL-realtime-01` | `internal/httpapi/realtime.go` + `internal/httpapi/snapshot.go` 의 critical risks / ci-runs / ci-run-logs handlers | API-14 WebSocket + API-36 `command.status.updated` publisher |
| `IMPL-realtime-02` | `internal/httpapi/realtime_ticket.go` (`realtimeTicketStore` interface + in-memory `RealtimeTicketStore` + PG `DBRealtimeTicketStore` + `NewRealtimeTicketStoreFor` selector) + `internal/store/realtime_tickets.go` + migration `000035_create_realtime_tickets` | ADR-0024 §3.2 ticket pattern (`POST /api/v1/realtime/ticket` 발급 + `auth.go` `?ticket=` consume). single-use 60s TTL. multi-instance 는 PG 백킹 (§6 carve 6, `DELETE ... RETURNING` 원자 single-use), DB 미연결은 in-memory fallback. (PR #336 in-memory 1차 + 본 sprint PG 백킹) |
| `IMPL-me-01` | `internal/httpapi/me.go` (`getMe`) | API-32 `GET /api/v1/me` handler |

#### IMPL-hrdb-XX 정의 (sprint `claude/work_260513-l`, §3 Infrastructure hrdb row)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-hrdb-01` | `internal/hrdb/mock.go` (`MockClient`) | RM-M3-01 PoC HR DB Client — 3 시드 인원 (`yklee`, `akim`, `sjones`). dev / 테스트 용도. |
| `IMPL-hrdb-02` | `internal/hrdb/postgres.go` (`PostgresClient`) + `migrations/000010_create_hrdb_persons.up.sql` | [ADR-0008](../adr/0008-hrdb-production-adapter.md) 채택 — PostgreSQL `hrdb.persons` schema 구현 (sprint `claude/work_260513-m`). ETL / cron sync 는 carve out. |

> `internal/httpapi/hr_lookup.go::hrLookup` (`GET /api/v1/hr/lookup`) 은 signup endpoint 외에 직접 HR DB 조회를 제공하는 helper endpoint (IMPL-auth 또는 IMPL-hrdb cross-cut). 매트릭스 §2.2 의 본문 ID 노출은 후속 sprint 후보.

#### IMPL-application-XX 정의 (sprint `claude/work_260514-a`, §3 application-lifecycle row)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-application-domain-01` | `internal/domain/application.go` | Application / ApplicationRepository / Project / ProjectMember / ProjectIntegration / SCMProvider 도메인 타입 + status / visibility / sync_status / sync_error_code / role enum 상수 + `IsRetryableSyncError`. |
| `IMPL-application-store-01` | `internal/store/applications.go` | Application + ApplicationRepository + SCMProvider + Project store body — 16 메서드 PostgreSQL 구현 (pgx, ErrConflict / ErrNotFound 분기, archived consistency 자동, CountActiveApplicationRepositories 추가). sprint `claude/work_260514-b` 가 stub → body. |
| `IMPL-application-handler-01` | `internal/httpapi/applications.go` | API-41..50 handler body — 요청 binding + validation (key 정규식, immutable, visibility/status enum) + 상태 전이 가드 (concept §13.2.1: planning→active 활성 repo ≥1, hold/resume/archived_reason 필수, transitions table) + audit emit (`application.{create,update,archive}`, `application_repository.{link,unlink}`, `scm_provider.update`). sprint `claude/work_260514-b` 가 stub → body. |
| `IMPL-application-router-01` | `internal/httpapi/router.go` (라인 추가) | API-41..50 의 10 endpoint gin v1 group 등록. |
| `IMPL-application-rbac-01` | `internal/httpapi/permissions.go` (`routePermissionTable` 14 신규 row) + `internal/domain/rbac.go` (4 신규 Resource + `AllResources()` + `DefaultPermissionMatrix()`) | ADR-0011 §4.1 1차 — system_admin 일임. developer/manager 는 4 신규 resource 모든 axis false. |
| `IMPL-application-migration-01..07` | `backend-core/migrations/000012..000018_*.sql` | scm_providers / applications / application_repositories / projects / project_members + project_integrations / pr_activities + build_runs + quality_snapshots / RBAC seed 확장. up + down + 인덱스 + CHECK 제약. |

> store body 는 미작성 (`ErrNotImplemented`). handler 는 stub (501). 응답 body schema / 요청 validation / status transition guard 는 후속 sprint 의 carve out (state.json `carve_out` 참조).

#### IMPL-int-XX 정의 (sprint `codex/next-step-20260516`, API-69..75 baseline, §3 integration-registry row)

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-int-domain-01` | `backend-core/internal/domain/application.go` | Integration Provider/Binding 도메인 타입 + enum(`provider_type`, `auth_mode`, `scope_type`) 정의. |
| `IMPL-int-store-01` | `backend-core/internal/store/integration_registry.go` | provider/binding 목록/생성/수정 + sync job 생성 persistence 구현 (API-69..75). |
| `IMPL-int-handler-01` | `backend-core/internal/httpapi/integration_registry.go` | API-69..75 handler 구현 (Provider Catalog/등록/수정/sync/binding 조회·생성 + webhook ingest baseline). |
| `IMPL-int-handler-02` | `backend-core/internal/httpapi/infra_integrations.go` | API-76..78 handler 구현 (서비스 조회/snapshot ingest/topology v2 + infra agent token 인증 + persisted snapshot hydrate). |
| `IMPL-int-webhook-01` | `backend-core/internal/httpapi/integration_registry.go` | API-73 provider webhook ingest baseline (provider 존재/상태 검증 + dedupe + accepted envelope). |
| `IMPL-int-router-rbac-01` | `backend-core/internal/httpapi/router.go`, `permissions.go`, `auth.go` | Integration endpoint route 등록 + RBAC/public path 매핑 반영. |
| `IMPL-int-migration-01` | `backend-core/migrations/000028_integration_registry_*.sql` | `integration_providers`, `integration_bindings`, `integration_sync_jobs` 스키마 및 인덱스/제약 생성. |
| `IMPL-int-store-02` | `backend-core/internal/store/infra_snapshots.go` | HomeLab snapshot 영속화 (`SaveInfraSnapshot`, `LoadLatestInfraSnapshot`) persistence 구현. |
| `IMPL-int-migration-02` | `backend-core/migrations/000029_infra_service_snapshots_*.sql` | `infra_service_snapshots` 스키마 + 최신 snapshot 조회 인덱스 생성. |
| `IMPL-int-adapter-01` | `backend-core/internal/integrations/adapters/{contract.go,homelab.go,homelab_file_puller.go}` | HomeLab adapter 고도화 1차 (`InfraSnapshotStore`, `HomeLabPuller`, `HomeLabAdapter.{NormalizeSnapshot,IngestSnapshot,PullAndIngest,LoadLatestSnapshot}`, health policy override, File puller). |

#### IMPL-gitea-XX 정의 (sprint `claude/work_260527-gitea-traceability`, §3 repository-integration (gitea-01..02 webhook) + Infrastructure gitea-scm (gitea-03..05 pull worker))

> `gitea-01..02` 은 기존 webhook ingestion foundation (2026-05-02 `b1b6849` "feat: add Gitea webhook ingestion foundation") — `internal/gitea/signature.go` 의 HMAC `VerifySignature` + `PayloadHash` + webhook handler/normalize (`UT-gitea-01` = `signature_test.go::TestVerifySignature`). 본 표는 PR #341 (2026-05-26) 의 신규 SCM **pull** sync worker (`gitea-03..05`) 를 정의한다 — webhook **push** 와 별개 concern (RM-M4-06 1차 구현).

| IMPL ID | 코드 위치 | 책임 |
| --- | --- | --- |
| `IMPL-gitea-03` | `backend-core/internal/gitea/client.go` (`Client`) | Gitea REST API client — `ListUserRepos` / `ListIssues` / `ListPullRequests` (page+limit 50 pagination, `Authorization: token` 헤더, 10s timeout, 2xx 외 에러). PR #341. |
| `IMPL-gitea-04` | `backend-core/internal/gitea/syncer.go` (`Syncer`) | repo 별 open+closed issues/PRs pull → `domain.{Issue,PullRequest,User}` 정규화 + upsert (author/assignee user 동반 upsert, `normalizePullRequestState` merged/closed/open). PR #341. |
| `IMPL-gitea-05` | `backend-core/internal/gitea/worker.go` (`SyncWorker`) + `main.go` wire (`GITEA_URL`/`GITEA_TOKEN` gate + 30s interval goroutine) | 주기적 sync 루프 + `integration_sync_jobs` 큐 소비 — `AcquireNextQueuedSyncJob` 우선 → `syncAll` → `UpdateIntegrationSyncJobStatus` (succeeded/failed), queued job 없으면 periodic full sync. PR #341. |

> 의존 store/migration 은 신규 ID 미발급 (cross-cut): `AcquireNextQueuedSyncJob` / `UpdateIntegrationSyncJobStatus` + `UpsertRepository/User/Issue/PullRequest` 는 `internal/store/{integration_registry.go,postgres.go}` (IMPL-int-store-01), `integration_sync_jobs` 테이블은 migration `000028` (IMPL-int-migration-01).

- **Frontend**: IMPL-frontend-auth-01..06, login-01..03, logout-01, dashboard-01, account-01, admin-01, admin-users-01, admin-org-01, admin-rbac-01, admin-audit-01, org-01, org-comp-01..03, role-01..03, service-auth-01, service-account-01, service-rbac-01, service-audit-01, service-org-01, service-project-01, service-realtime-01, service-api-01, layout-01..02, store-01, filterbar-01, audit-redesign-01 (35 항목).
- **Backend AI**: 미사용 (Phase 4 분석에서 placeholder 만 발견).

### 2.5 Unit tests (UT)

- **Backend Go**: UT-httpapi-01..26 (`UT-httpapi-25`: integration registry handler, `UT-httpapi-26`: infra integration handler), rbac-01..03, domain-01..02, auth-01, gitea-01..04 (`gitea-01`: webhook HMAC `signature_test.go`; `gitea-02..04`: SCM sync worker `client_test`/`syncer_test`/`worker_test`, PR #341), normalize-01, store-01..03, commandworker-01..02, serviceaction-01, config-01, main-01, onboarding 12건 (`internal/httpapi/onboarding_test.go`), keycloak 4건 + sub-carve D 4건 (`UT-auth-StaleWhileError_KeycloakUnreachable` 등), realtime_ticket (in-memory + DB fake).
- **Frontend Vitest**: UT-frontend-utils-01 (lib/utils — lifecycle-status 6, last-build 13, integration-provider-presets 14 vitest), frontend-auth-01..04 (Header/Sidebar/AuthGuard regression), frontend-services-01..02 (login/session) + recovery sprint 신규 (audit/onboarding/dev_request/auth/realtime service 신규 + 9 file 보강, PR #407), AuthGuard whitelist→blocklist 4 회귀 가드 (PR #291), `IntakeTokenTable` / `IssueIntakeTokenModal-01..02` (DREQ admin).
- **§3 매트릭스 도메인 매핑**: `UT-httpapi-01..07` + `UT-auth-01` + frontend-auth-01..04 + onboarding 12 = **auth-session** + **onboarding** / `UT-rbac-01..03` + `UT-domain-01` = **rbac-permissions** / `UT-httpapi-19, 24` = **audit-ops** / `UT-httpapi-07` + `UT-store-01` = **organization-management** / `UT-httpapi-08, 11, 22` + application/project/rollup/integration handler 46 + frontend lib/utils vitest = **application-lifecycle** / `UT-httpapi-10, 14` + `UT-gitea-01` (HMAC) + `UT-normalize-01` + `UT-store-03` + repository handler 3 + gitea-compat gate 1 = **repository-integration** / `UT-httpapi-DREQ-*` (15 + 5 promote + 8 intake + 4 intake_auth + 1 clientIP + 1 token-expiry + 1 allowed-ips-mutation) + DREQ frontend vitest = **dev-request** / `UT-httpapi-25, 26` + `UT-int-adapter-01` + gitea `auth_test` (7) + handler `BasicAuthCredentials` 등 + frontend `integration-provider-presets` vitest 14 = **integration-registry** / `UT-httpapi-13` + realtime_ticket + `UT-commandworker-01..02` + `UT-serviceaction-01` + `UT-domain-02` + `UT-httpapi-09` = **realtime** / `UT-gitea-02..04` (SCM sync worker) + `TestSyncWorker_ProcessOnce_NoEnvTokenLeakForExplicitProvider` = **Infrastructure (gitea-scm)** / `UT-auth-{StaleWhileError, StaleExpired, FreshCache, StaleFallback}-01` + `UT-keycloak-*` + audit metric 회귀 3종 = **Infrastructure (keycloak-idp)** / `UT-httpapi-12` + `UT-int-adapter-01` (HomeLab) = **Infrastructure (infra-topology)**.

### 2.6 E2E TC

- **§3 auth-session row (32 TC)**: TC-USR-01..06, TC-USR-CRUD-01..03, TC-ACC-01..03, TC-ACC-PROFILE-01, TC-NAV-01..03 + TC-NAV-SIM-01, TC-AUTH-NEG-01, TC-AUTH-NOAUTH-01, TC-AUTH-SIGNOUT-REDIR-01, TC-USER-SWITCH-01, TC-SIGNUP-01..04, TC-PERMISSIONS-SMOKE-01, TC-FILTERBAR-USERS-01 ([`docs/domain/auth-session/test_cases.md`](../domain/auth-session/test_cases.md)).
- **§3 audit-ops row**: TC-AUD-01..02, TC-AUDIT-PREMIUM-01 (도메인 `test_cases.md` 미작성 — Phase 2 잔여 carve out).
- **§3 rbac-permissions row**: TC-RBAC-POLICY-01, TC-RBAC-ROUTE-01, TC-RBAC-DENY-01, TC-RBAC-ROW-WRITE-01, TC-RBAC-ROLE-SYNC-01..02, TC-RBAC-ROLE-DRIFT-01, TC-RBAC-LEGACY-01, TC-RBAC-LOGOUT-01..02, TC-RBAC-ROW-READ-01..02, TC-RBAC-CODE-01, TC-RBAC-TRACE-01 + 기존 TC-RBAC-SUB-01, TC-RBAC-MGR-01, TC-PERMISSIONS-SMOKE-01 ([`docs/domain/rbac-permissions/test_cases.md`](../domain/rbac-permissions/test_cases.md), 2026-06-02 active).
- **§3 organization-management row (9 TC)**: TC-ORG-LIST-01..02, TC-ORG-UNIT-01..03, TC-ORG-MEM-01..02, TC-ORG-CHART-01..02 ([`docs/domain/organization-management/test_cases.md`](../domain/organization-management/test_cases.md)).
- **§3 application-lifecycle row**: TC-FILTERBAR-APPS-01 (Application/Project E2E 본격 carve out — APPDASH 미진입 TC-APPDASH-XX 후보). 도메인 `test_cases.md` 미작성 — Phase 2 잔여 carve out.
- **§3 repository-integration row**: `repositories-ui.spec.ts` + `repositories-ui-negative` + `repositories-detail-negative` + `admin-catalog.spec.ts` (negative). import/create/publish happy-path E2E carve [N-3]. 도메인 `test_cases.md` 미작성 — Phase 2 잔여 carve out.
- **§3 dev-request row**: TC-DREQ-{ADMIN-TOKEN-01, INTAKE-AUTH-01, WIDGET-FLOW-01, PROMOTE-TX-01, ADMIN-TOKEN-REVOKE-01, INTAKE-AUTH-NEG-01..03} + TC-DREQ-ADMIN-TOKEN-PATCH-01 + TC-DREQ-PROMOTE-PROJ-01 + TC-DREQ-NOTI-01..03 + TC-FILTERBAR-DREQ-01 + E2E `dev-requests.spec.ts` ([`docs/domain/dev-request/test_cases.md`](../domain/dev-request/test_cases.md)).
- **§3 integration-registry row (M4 baseline 10 TC + mega lifecycle)**: TC-INT-PROVIDER-01..02, TC-INT-INGEST-01..02, TC-INT-BINDING-01..02, TC-INT-HOMELAB-01..03, TC-INT-RESILIENCE-01 (IT 중심 PASS + E2E 시나리오 후속) + TC-INT-FRONTEND-{LIST,CREATE,EDIT,SYNC,RBAC,DELETE}-01 mega lifecycle + TC-INT-FRONTEND-DELETE-NEG-01 + TC-INT-FRONTEND-BIND-LIST/CREATE/RBAC + TC-INT-FRONTEND-TOPOLOGY-V2-NAV/RBAC-01 ([`docs/domain/integration-registry/test_cases.md`](../domain/integration-registry/test_cases.md)). TC-TASK-XX 미진입 (Task Item Ingestion 후속).
- **§3 onboarding row**: TC-ONBOARD-* mega lifecycle (`frontend/tests/e2e/onboarding-first-login.spec.ts`, Carve D PR #289) ([`docs/domain/onboarding/test_cases.md`](../domain/onboarding/test_cases.md)).
- **§3 realtime row**: (E2E 미커버 — gap §5.1, TC-CMD-* / TC-WS-* 카탈로그 후속).
- **§3 Infrastructure (infra-topology)**: `admin-topology-v2.spec.ts` (TC-INT-HOMELAB-03 `/admin/topology-v2`) — legacy `/admin` 토폴로지 archived.
- **추가** (spec 파일 안에만 정의되어 TC 카탈로그 외): `auth.spec.ts` 의 TC-AUTH-01..06 등 — 향후 TC 카탈로그로 흡수 권장 (closed 2026-05-13 sprint `claude/work_260513-d`, `test_cases.md` 흡수 확인).

## 3. 종합 매트릭스 (도메인 그룹 단위)

> **Phase 4 재구성 (2026-05-29)** — code-taxonomy SoT ([`docs/governance/code-taxonomy.md`](../governance/code-taxonomy.md)) 의 **10 core 도메인 + Shared + Infrastructure + Cross-cutting** 그룹화. 기존 21 row (cross-cutting 그룹 위주) → 19 row 로 재구성 (10 core + 1 Shared + 7 Infrastructure + 1 Cross-cutting). 모든 기존 REQ/UC/ARCH/API/RM/IMPL/UT/TC ID 보존 + Phase 3 신규 임시 ID (REQ-RBAC-001..016 / REQ-AUDIT-001..007 / REQ-ORG-001..007 / ARCH-RBAC-01..06 / ARCH-AUDIT-01..04 / ARCH-ORG-01..05) 흡수. 도메인 sub-document path 는 `docs/domain/<domain>/{requirements,architecture,api,test_cases}.md` 단계별 SoT 로 link 갱신.

### 3.1 Core 도메인 (10 도메인)

| 도메인 | REQ | USECASE | ARCH / API | ROADMAP | IMPL | UT | TC |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **auth-session** ([`docs/domain/auth-session/`](../domain/auth-session/)) — 인증 (auth/OIDC) + 회원가입 (signup) + 계정 관리 (account admin + self-service) 통합 | FR-15–26, 61–67; NFR-3, 4, 5, 7, 17, 18, 19, 20 (정밀 매핑: [`requirements.md`](../domain/auth-session/requirements.md)); **REQ-FR-107** (sprint -h, Sign-out — 2026-06-06, [`requirements.md`](../domain/auth-session/requirements.md) §5.11) | UC-AUTH-01..03, UC-ACCOUNT-01..03 | ARCH-11, 12, 14; **ARCH-19** + **API-99** `POST /api/v1/auth/logout` (sprint -h, N-8/P1-6 — BUG-03, 2026-06-01 통합 테스트); API-19, 23 (legacy 제거), 25 (폐기 strikethrough), 32, 33, 35 (정밀 매핑: §2.2 Auth API + Account/Org/Me 서브 표 + [`architecture.md`](../domain/auth-session/architecture.md) + [`api.md`](../domain/auth-session/api.md)) | M1-04, M2-01..06, 09; RM-M3-01 (Sign Up — IdP admin 연계 재설계 in_progress); RM-M3-02 (ADR-0008 결정 — `hrdb` schema, ETL carve); **N-8 / P1-6** ([#488](https://github.com/ykylee/Devhub_example/issues/488)) | auth-01..03 (§2.4 IMPL-auth-XX 서브 표); account-01..04 (strikethrough — ADR-0020 sub-carve B 폐기) + account-05 신규 (lazy auto-create, sprint -i — 이후 Carve D 에서 삭제); **IMPL-auth-logout-01** (`internal/httpapi/auth_logout.go`, sprint -h); frontend-auth-01..06; login-01..03; logout-01; frontend-account-01; frontend-admin-users-01; frontend-service-{auth,account}-01 | httpapi-01..07; auth-01; **UT-auth-logout-01** (sprint -h); frontend-auth-01..04 | TC-AUTH-NEG-01, TC-AUTH-NOAUTH-01, TC-AUTH-SIGNOUT-REDIR-01; **TC-AUTH-LOGOUT-01** (sprint -h, BUG-03); TC-USER-SWITCH-01; TC-SIGNUP-01..04; TC-USR-01..06; TC-USR-CRUD-01..03; TC-ACC-01..03; TC-ACC-PROFILE-01 ([`test_cases.md`](../domain/auth-session/test_cases.md)) |
| **audit-ops** ([`docs/domain/audit-ops/`](../domain/audit-ops/)) — 감사 로그 + 운영 hygiene | FR-18, 26, 102; NFR-4; **REQ-AUDIT-001..007** (Phase 3 신규 — [`requirements.md`](../domain/audit-ops/requirements.md)) | UC-AUD-01..02 | ARCH-14; **ARCH-AUDIT-01..04** (Phase 3 신규 — [`architecture.md`](../domain/audit-ops/architecture.md)); API-18, 39 (39 = §12.9 cross-cut from RBAC, 정밀 매핑: §2.2 Realtime/Command/Audit 서브 표 + [`api.md`](../domain/audit-ops/api.md)) | M2-15 (in_progress, PR-M2-AUDIT) | audit-01, 02 (§2.4 IMPL-audit-XX 서브 표); frontend-audit-redesign-01 | httpapi-19, 24; frontend (Vitest 없음 — §5.2 open) | TC-AUD-01, 02; TC-AUDIT-PREMIUM-01 |
| **rbac-permissions** ([`docs/domain/rbac-permissions/`](../domain/rbac-permissions/)) — RBAC matrix + route enforcement + cache | FR-27, 86; NFR-26; **REQ-RBAC-001..016** (Phase 3 신규 + 2026-06-02 고도화 반영 — [`requirements.md`](../domain/rbac-permissions/requirements.md)) | UC-RBAC-01..07 | ARCH-13; **ARCH-RBAC-01..06** (Phase 3 신규 — [`architecture.md`](../domain/rbac-permissions/architecture.md)); API-26..29, 38..40 (API-30/31 폐기 strikethrough — ADR-0020 sub-carve A); 정밀 매핑: §2.2 RBAC API 서브 표 + [`api.md`](../domain/rbac-permissions/api.md) | M1-01..03; M2-11; RM-M4-08 (PermissionCache 다중 인스턴스 — ADR-0007, PG LISTEN/NOTIFY, M3 carve) | rbac-01..04 (§2.4 IMPL-rbac-XX 서브 표); frontend-admin-rbac-01; frontend-service-rbac-01; org-comp-02 | rbac-01..03; domain-01 | TC-RBAC-POLICY-01, TC-RBAC-ROUTE-01, TC-RBAC-DENY-01, TC-RBAC-ROW-WRITE-01, TC-RBAC-ROLE-SYNC-01..02, TC-RBAC-ROLE-DRIFT-01, TC-RBAC-LEGACY-01, TC-RBAC-LOGOUT-01..02, TC-RBAC-ROW-READ-01..02, TC-RBAC-CODE-01, TC-RBAC-TRACE-01, TC-RBAC-SUB-01, TC-RBAC-MGR-01, TC-PERMISSIONS-SMOKE-01 ([`docs/domain/rbac-permissions/test_cases.md`](../domain/rbac-permissions/test_cases.md)) |
| **organization-management** ([`docs/domain/organization-management/`](../domain/organization-management/)) — 조직 계층 + units + members | FR-68–80; NFR-21; **REQ-ORG-001..007** (Phase 3 신규 — [`requirements.md`](../domain/organization-management/requirements.md), 기존 REQ-FR-68..80 와 정합) | UC-ORG-01..04 | ARCH-15, 16, 17; **ARCH-ORG-01..05** (Phase 3 신규 — [`architecture.md`](../domain/organization-management/architecture.md)); API-33, 34 (정밀 매핑: §2.2 Account/Org/Me 서브 표 + §10.4.1..§10.4.4 mutation endpoint 본문 spec — sprint `claude/work_260513-l` + [`api.md`](../domain/organization-management/api.md)) | M2-07, 08; RM-M3-03 (조직 polish 1차 §10.4 schema 보강, primary_dept 자동 판정 코드는 carve) | org-01..04 (§2.4 IMPL-org-XX 서브 표); frontend-org-01; frontend-admin-org-01; frontend-org-comp-01..03; frontend-service-org-01 | httpapi-07; store-01 | TC-ORG-LIST-01..02; TC-ORG-UNIT-01..03; TC-ORG-MEM-01..02; TC-ORG-CHART-01..02 ([`test_cases.md`](../domain/organization-management/test_cases.md)) |
| **onboarding** ([`docs/domain/onboarding/`](../domain/onboarding/)) — first-login self-service unit selection + admin review | REQ-FR-ONBOARD-001..012; REQ-NFR-ONBOARD-001..008 ([`requirements.md`](../domain/onboarding/requirements.md)) | UC-ONBOARD-01..11 (`docs/planning/system_usecases.md §2.13`) | ARCH-ONBOARD-01..06 ([`architecture.md`](../domain/onboarding/architecture.md)); API-83..86 신규 + API-32/API-33 확장 ([`api.md`](../domain/onboarding/api.md)) — **Carve A backend activated** (PR #278). **[ADR-0021](../adr/0021-onboarding-self-service-unit-selection.md)** accepted (ADR-0020 partial supersession, 5 위치 inline banner). | M7 + M-v1.1 — Concept (PR #260+#265) + Requirements (PR #266) + Design (PR #267) + ADR (PR #269) + IMPL plan (PR #271) + **RM-ONBOARD-01..04** (Carve A backend PR #278 + Carve B/C frontend+admin PR #288 + Carve D tests PR #289 + flag default ON flip & `lazy_auto_create.go` 삭제 PR #290 + codex hotfix #3 PR #291). **M7 풀스택 closing (2026-05-21)**. | IMPL-onboarding-{migration, store, gate, submit-handler, search-handler, patch-me, review-confirm, me-response}-01 (Carve A); Carve B/C frontend (`app/onboarding/page.tsx` + `components/onboarding/{OnboardingForm,OnboardingBanner,OrganizationPicker}` + `(dashboard)/layout.tsx` 3-branch gating + `account/page.tsx` self-service unit edit + admin `ConfirmReviewModal`/pending_review filter). `lazy_auto_create.go` + `onboarding_feature_flag.go` 삭제 (ADR-0021 §3.3 정공법). | UT-onboarding-{gate, submit, search, review-confirm, patch-me}-01 (12 test, `internal/httpapi/onboarding_test.go`) + AuthGuard whitelist→blocklist 회귀 가드 (PR #291). | TC-ONBOARD-* mega lifecycle active — `frontend/tests/e2e/onboarding-first-login.spec.ts` (Carve D PR #289) + [`test_cases.md`](../domain/onboarding/test_cases.md). |
| **application-lifecycle** ([`docs/domain/application-lifecycle/`](../domain/application-lifecycle/)) — Application / Project / 개발 대시보드 + me + 메트릭 통합 | FR-1–11, 28–36, 81, 85, 88, 89, 96 (대시보드/메트릭); REQ-FR-APP-001..012, REQ-FR-PROJ-000..010, REQ-NFR-PROJ-001..006 (Application/Project); REQ-FR-APPDASH-001..006, REQ-NFR-APPDASH-001..003 (개발 대시보드 §5.9 — [`requirements.md`](../domain/application-lifecycle/requirements.md)); **REQ-FR-109** (플랫폼 대시보드 UI 개선), **REQ-FR-110** (KPI 대시보드 및 Python 연산엔진), **REQ-FR-111** (테스트 대시보드 및 Recharts 도넛 차트) | UC-APP-01..10, UC-PROJ-01..10, UC-APPDASH-01..07, UC-GITEA-03, UC-RT-01, **UC-KPI-01..02**, **UC-TEST-01..03** | ARCH-10; API-05 (대시보드), 32 (me), 36, 38; API-41..58 (Application/Repository 전 도메인 activated — §2.2 Application/Repository 서브 표); API-93 (개발 대시보드 §13.10 신규) + ARCH-APPDASH-01..06 ([`architecture.md`](../domain/application-lifecycle/architecture.md) + [`api.md`](../domain/application-lifecycle/api.md)); **ARCH-21** (KPI 동적 스크립트), **ARCH-22** (테스트 대시보드/Recharts) + **API-101** (KPI/테스트 가상 클라이언트 목킹). ADR-0011 (RBAC row-scoping, accepted). | M3 backlog (Application/Project design entry); **RM-KPI-01** (KPI 대시보드), **RM-TEST-01** (테스트 관리 대시보드) | IMPL-application-{store,handler,router,rbac,repo_ops,rollup,integration}-01 + IMPL-project-handler-01 + IMPL-frontend-service-project-01 + frontend-filterbar-01 + migration 000012..000018; dashboard-01; me-01; frontend-dashboard-01; frontend-role-01..03; frontend-store-01; frontend-layout-01..02; frontend-service-api-01; frontend-repository-dashboard-view-01; **IMPL-dashboard-ui-01** (플랫폼 개선), **IMPL-kpis-dashboard-01** (KPI/Python), **IMPL-tests-dashboard-01** (테스트/타임라인/케이스/사이클) | UT-application-handler-XX (25, sprint -b) + UT-project-handler-XX (8) + UT-integration-handler-XX (6) + UT-application-rollup-XX (4) + UT-frontend-services-02 (3) = 46 test (sprint -b + -c + gemini/phase6); httpapi-08, 11, 22; UT-frontend-repository-dashboard-01 (7); **UT-frontend-dashboards-01** (Vitest 1025개 PASS 회귀 검증) | TC-NAV-01..03; TC-NAV-SIM-01; TC-FILTERBAR-APPS-01; (Application/Project E2E — carve out); TC-REPO-DASH-DEV; TC-REPO-DASH-MGR; **TC-PLAT-DASH-01** (플랫폼 개선), **TC-KPI-DASH-01** (KPI/Python), **TC-TEST-DASH-01** (테스트 관리) |
| **repository-integration** ([`docs/domain/repository-integration/`](../domain/repository-integration/)) — SCM↔시스템 Repository 양방향 연동 + Webhook + 도메인 데이터 (push) | FR-49–55 (repo/issue/PR 도메인 데이터 — push + pull 동일 REQ); REQ-FR-REPO-001..005 + REQ-NFR-REPO-001..003 (§5.8 — 소유권 분리 / 원격 import / outbound create / draft→publish / capability gate; [`requirements.md`](../domain/repository-integration/requirements.md)); **REQ-FR-108** (sprint -h, Repository build-runs — 2026-06-06, [`requirements.md`](../domain/repository-integration/requirements.md) §5.12) | UC-GITEA-01..03; UC-REPO-01..07 (§2.14) | ARCH-06, 07, 08; ARCH-REPO-01..07 (§10 — [`architecture.md`](../domain/repository-integration/architecture.md)); **ARCH-20** + **API-100** `GET /api/v1/repos/:id/build-runs` (sprint -h, N-9/P1-7 — ISSUE-04, 2026-06-01 통합 테스트); API-02, 04, 08–13 (push webhook + pipelines); **API-88/89/90** (§15.3 scm-repositories list/import/create) + **API-91/92** repository draft→publish (§13.9, #368); 정밀 매핑: §2.2 Infra/Dashboard + Pipelines 서브 표 + [`api.md`](../domain/repository-integration/api.md). capability gate (import=pull / create=push + gitea-compat). | (M0 이전 push phase 완료); v1.1 carve (Phase D project flow ↔ SCM create, [X-4](../analysis/2026-05-27-codebase-snapshot/06_future_direction.md)); **N-9 / P1-7** ([#487](https://github.com/ykylee/Devhub_example/issues/487)) | gitea-01, 02 (webhook ingestion foundation `signature.go` + handler/normalize); domain-01..03; normalize; IMPL-repository-{handler-create-draft, handler-publish, scm-list, scm-import, scm-create}-01 (`internal/httpapi/integration_scm_repositories.go` API-88/89/90 + `repository_ops.go` draft/publish + store `UpsertRepository` ON CONFLICT SCM mirror만 갱신 + `internal/gitea/client.go::CreateRepo` + migration 000042/000043/000045). PR #363/#366/#368/#373. **IMPL-repository-build-runs-01** (`internal/httpapi/repository_build_runs.go`, sprint -h). | httpapi-10, 14; gitea-01 (signature HMAC `signature_test.go`); normalize-01; store-03; gitea CreateRepo 3 + handler create-repository 3 + ListSCM/ImportSCM 5 + gitea-compat gate 1. **draft→publish handler 무테스트 carve [N-2]**; **UT-repository-build-runs-01** (sprint -h) | `repositories-ui.spec.ts` + `repositories-ui-negative` + `repositories-detail-negative` + `admin-catalog.spec.ts` (negative). **import/create/publish happy-path E2E carve [N-3]**; **TC-BUILD-RUNS-01** (sprint -h, ISSUE-04) |
| **dev-request** ([`docs/domain/dev-request/`](../domain/dev-request/)) — Dev Request 외부 수신 + intake token + promote-to-application/project | REQ-FR-DREQ-001..013; REQ-NFR-DREQ-001..006 (§5.5 — [`requirements.md`](../domain/dev-request/requirements.md)) | UC-DREQ-01..12 | ARCH-DREQ-01..06 ([`architecture.md`](../domain/dev-request/architecture.md)); API-59..65 (§14, **activated** sprint `claude/work_260515-i`); API-66..68 intake token admin (§14.10, **activated** sprint `claude/work_260515-o` / ADR-0014); **API-79 PATCH allowed_ips & expires_at** (§14.10, **activated** PR #137, 옵션 B). 정밀 매핑: §2.2 DREQ API 서브 표 + [`api.md`](../domain/dev-request/api.md). ADR-0012 + ADR-0013 + ADR-0014 + ADR-0017 accepted. 기획/설계: `project_creation_dreq_notification_concept.md` (active). | M5 — Concept staged (sprint `f`) + AuthADR (sprint `g`) + Backend 1차 (sprint `i`) + RBAC-ADR / Promote-Tx (sprint `m`) + Admin backend (sprint `o`) + token hardening (PR #137) + ADR-0017 운영 hardening + DREQ-Notification-Promote (sprint `codex/work_260526-b`). RM-DREQ-XX 본격 발급은 후속. | IMPL-dreq-{domain,store,handler,router,rbac,intake_auth,promote_tx,intake_admin,token_expiry,allowed_ips_mutation}-01 + IMPL-dreq-frontend-{page,widget,table,modal,service,intake_admin,destructive_confirm,show_hide_toggle,notification_bell,prefill_promotion}-01 (sprint `j` + `p` + PR #140 + `codex/work_260526-b`) + frontend-filterbar-01 + migration 000022 dev_requests + 000023 dev_request_intake_tokens + 000024 RBAC seed + 000026 RBAC dev_request_intake_tokens seed + 000027 token expires_at | UT-dreq-handler-XX (15 + 5 promote + 8 intake admin, sprint `i` + `m` + `o`) + UT-dreq-intake_auth-XX (4) + UT-clientIP-01 + UT-dreq-token-expiry-01 + UT-dreq-allowed-ips-mutation-01 + UT-frontend-IntakeTokenTable-01 + UT-frontend-IssueIntakeTokenModal-01..02 | [`test_cases.md`](../domain/dev-request/test_cases.md) — TC-DREQ-{ADMIN-TOKEN-01, INTAKE-AUTH-01, WIDGET-FLOW-01, PROMOTE-TX-01, ADMIN-TOKEN-REVOKE-01, INTAKE-AUTH-NEG-03} + TC-DREQ-INTAKE-AUTH-NEG-01 + TC-DREQ-ADMIN-TOKEN-PATCH-01 + **TC-DREQ-PROMOTE-PROJ-01** + **TC-DREQ-NOTI-01..03** (Header Bell 배지 실시간 갱신, 알림 상세, Promote-to-Project 연계 및 프리필 검증) + E2E `tests/e2e/admin-projects.spec.ts` + `dev-requests.spec.ts` (active). |
| **integration-registry** ([`docs/domain/integration-registry/`](../domain/integration-registry/)) — External Integration provider/binding + Task Item Ingestion + HomeLab adapter | REQ-FR-INT-001..015; REQ-NFR-INT-001..009 (§5.6 — auth_mode full / base_url / webhook 헤더 alias / write-only secret 경계 포함); REQ-FR-TASK-001..010; REQ-NFR-TASK-001..004 (§5.10 — Task Item Ingestion, sprint `deepseek/work_260528-a-task-item-ingestion`); [`requirements.md`](../domain/integration-registry/requirements.md) + [`task_requirements.md`](../domain/integration-registry/task_requirements.md); **REQ-FR-106** (sprint -h, CI Run 생성 — 2026-06-06, [`requirements.md`](../domain/integration-registry/requirements.md) §5.11) | UC-INT-01..18 (§2.12 — auth_mode/연결테스트/Gitea sync/webhook alias 포함); UC-TASK-01..06 (§2.16 — Task Item Ingestion) | ARCH-INT-01..07 (§8 + §8.7 Gitea pull + auth_mode/OutboundAuth — [`architecture.md`](../domain/integration-registry/architecture.md)); ARCH-TASK-01..06 (§12 — [`task_architecture.md`](../domain/integration-registry/task_architecture.md)); **ARCH-18** + **API-98** `POST /api/v1/ci-runs` (sprint -h, N-7/P0-4 — ISSUE-05, 2026-06-01 통합 테스트); API-69..78 (baseline activated); API-80..82 (delete/binding mutation activated); API-87 test-connection (PR #352, base_url reachability + migration 000038); API-94..96 Task Item Ingestion (§17 — [`task_api.md`](../domain/integration-registry/task_api.md)); [`api.md`](../domain/integration-registry/api.md). [ADR-0015](../adr/0015-homelab-adapter-pull-strategy.md) + [ADR-0016](../adr/0016-prometheus-alerts-policy.md) accepted. | M4/M5 설계 진입 + API-69..78 baseline 구현 완료 (`codex/next-step-20260516`); RM-INT-XX / RM-TASK-XX 발급은 후속; **N-7 / P0-4** ([#486](https://github.com/ykylee/Devhub_example/issues/486)) | IMPL-int-{domain-01, store-01..02, handler-01..02, webhook-01, router-rbac-01, migration-01..02, adapter-01} (HomeLab adapter 고도화 + file/http puller + scheduler/metrics) + IMPL-int-frontend-{service, types, page, table, modal}-01 (sprint `claude/work_260518-g`: `/admin/settings/integrations` + ProviderTable + ProviderModal + integration.{service,types}.ts) + auth_mode full 모델 (migration 000040/000041, api_token write-only slot, PR #355/#358) + webhook 헤더 alias (`X-Integration-*`→`X-Gitea-*`→`X-Gogs-*` fallback); **IMPL-ci-runs-01** (`internal/httpapi/ci_runs.go`, sprint -h) | UT-httpapi-25 (integration registry baseline), UT-httpapi-26 (infra integration baseline), UT-int-adapter-01 (HomeLab adapter normalize/pull/ingest + puller), gitea `auth_test`(7) + worker `BasicAuthOutbound` + handler `BasicAuthCredentials`/`InvalidAuthTokenURL` + `GiteaNativeHeaders` (webhook); frontend `integration-provider-presets` vitest 14; **UT-ci-runs-01** (sprint -h) | [`test_cases.md`](../domain/integration-registry/test_cases.md) — TC-INT-PROVIDER-01..02; TC-INT-INGEST-01..02; TC-INT-BINDING-01..02; TC-INT-HOMELAB-01..03; TC-INT-RESILIENCE-01 (IT PASS, E2E 후속); TC-INT-FRONTEND-{LIST,CREATE,EDIT,SYNC,RBAC,DELETE}-01 (mega lifecycle 5 step + RBAC negative, `frontend/tests/e2e/admin-integrations.spec.ts`); TC-INT-FRONTEND-DELETE-NEG-01 (binding 존재 시 409, UT only); (TC-TASK-XX 미진입 — Task Item Ingestion 후속); **TC-CI-RUN-01** (sprint -h, ISSUE-05) |
| **realtime** ([`docs/domain/realtime/`](../domain/realtime/)) — WebSocket + ticket pattern + 명령 lifecycle publish | FR-56, 57, 60, 82, 83, 104, 105; FR-58, 59, 84, 95, 100, 101 (명령 lifecycle realtime publish cross-cut); NFR-11, 22, 23 ([`requirements.md`](../domain/realtime/requirements.md)) | UC-RT-01..02; UC-CMD-01..03 (realtime publish cross-cut) | ARCH-05; API-14 (WebSocket endpoint), 15..17, 36, 37; ADR-0024 (WS 인증 ticket pattern, `?access_token=` query → ticket-only cutover) ([`architecture.md`](../domain/realtime/architecture.md) + [`api.md`](../domain/realtime/api.md)) | M1-01..03 (envelope 정합); RM-M4-01 (event publish 확장, planned); RM-M4-02 (replay + 리소스 필터, planned); RM-M4-03 (frontend WS UI, planned) | realtime-01, 02 (§2.4 IMPL-realtime-XX 서브 표 — realtime-02 = ADR-0024 ticket store, migration 000035 PG `DELETE...RETURNING` single-use + in-memory fallback); command-01..05 (§2.4 IMPL-command-XX 서브 표); serviceaction-01 | httpapi-13; realtime_ticket (in-memory 단위 + DB fake) + store integration (single-use/race); commandworker-01, 02; serviceaction-01; domain-02; httpapi-09 | (E2E 미커버 — gap §5.1, sprint `claude/work_260513-i` 에서 TC-CMD-* / TC-WS-* 카탈로그 추가 후보) |

### 3.2 Shared 그룹

| 도메인 | REQ | USECASE | ARCH / API | ROADMAP | IMPL | UT | TC |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **Shared** ([`docs/shared/`](../shared/)) — 공통 ui-foundation / utils / config / integrationcaps (PR #406+ code-taxonomy code-level 적용; PR #409 integrationcaps 분리) | N/A (cross-cut — 도메인별 REQ 가 source) | N/A | N/A (cross-cut) | (code-taxonomy 적용 — PR #406+) | IMPL-shared-ui-foundation (frontend `components/ui/*` 공통 컴포넌트); IMPL-shared-utils (`lib/utils/*` 순수 함수 — `lifecycle-status.ts`, `last-build.ts`, `integration-provider-presets.ts`); IMPL-shared-config (env 해석/feature flag); IMPL-shared-integrationcaps (PR #409 capability matrix shared 분리) | frontend lib/utils vitest (lifecycle-status 6, last-build 13, integration-provider-presets 14) — 순수 함수 단위테스트 | (cross-cut — 도메인별 TC 가 source) |

### 3.3 Infrastructure 그룹

| 도메인 | REQ | USECASE | ARCH / API | ROADMAP | IMPL | UT | TC |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **keycloak-idp** ([`docs/infrastructure/keycloak-idp/`](../infrastructure/keycloak-idp/)) — Keycloak 단일화 + admin event listener + JWKS rotation + SSO logout chain + failover + off-boarding | NFR-3 (인증), NFR-18 (세션) — auth-session 도메인 REQ cross-cut | N/A (운영 SOP) | ARCH-12 (cross-cut from auth-session); [ADR-0001](../adr/0001-idp-selection.md) (superseded by ADR-0019); [ADR-0019](../adr/0019-keycloak-only-idp.md) accepted (PR #167, §5.3 8 carve out + §5.4 RM-M4-09 의미 재정의); [ADR-0022](../adr/0022-keycloak-version-pin-25-0.md) (superseded by ADR-0023); [ADR-0023](../adr/0023-keycloak-version-pin-26-0.md) (26.0 pin). SOP: [`failover.md`](../infrastructure/keycloak-idp/failover.md), [`offboarding_immediacy.md`](../infrastructure/keycloak-idp/offboarding_immediacy.md), [`refactor_execution_plan.md`](../infrastructure/keycloak-idp/refactor_execution_plan.md), [`service_account_min_role.md`](../infrastructure/keycloak-idp/service_account_min_role.md), [`sso_federation.md`](../infrastructure/keycloak-idp/sso_federation.md), [`e2e_migration.md`](../infrastructure/keycloak-idp/e2e_migration.md). | RM-M4-09 (외부 SSO 통합 — Keycloak federation, ADR-0019 §5.4 의미 재정의: Keycloak 단일화 완료, federation 잔여 = identity broker/user federation 사내 운영 layer). ADR-0019 §5.3 carve: (1)/(2)/(3)/(4)/(5)/(7)/(8) Phase 1 design 완결; (6) HA Phase 2 carve. ADR-0020 sub-carve B (lazy auto-create deprecated, ADR-0021 §3.3 정공법) + sub-carve C (event listener users sync) + sub-carve D (JWKS stale-while-error) + sub-carve E (service account min role) closed. | IMPL-keycloak-{verifier (`internal/auth/keycloak_verifier.go` — JWKS stale fallback + metric), admin-client (`backend-core/internal/keycloak/admin_client.go` read-only `view-users`), event-puller (`internal/audit/keycloak_event_puller.go` + `keycloak_events_webhook.go` + `audit/user_sync.go` 207 lines — Group Membership CREATE/DELETE sync + Prometheus 3종 metric + partial UNIQUE INDEX dedup), SPI (Java event listener push) | UT-auth-{StaleWhileError_KeycloakUnreachable, StaleExpired_Fails401, FreshCache_NoStaleFallback, StaleFallback_DefaultMaxStale}-01 (sub-carve D 4건); UT-keycloak-{FindAndAuthFlow, EventClassify, UserSync_Profile/Membership/Deactivated}-XX (sub-carve C 4건 + sub-carve E read-only flow); audit metric 회귀 3종 | (E2E — Keycloak 운영 환경 의존, 사내 staging 1주 monitoring 인계) |
| **gitea-scm** ([`docs/infrastructure/`](../infrastructure/) — 인프라 layer; 도메인 layer 는 repository-integration) — Gitea SCM 동기화 워커 (pull) + queue SKIP LOCKED | FR-49..55 cross-cut from repository-integration (동기화 대상 = repo/issue/PR 도메인 데이터) | UC-GITEA-01..03 (cross-cut) | ARCH-06, 07, 08; ARCH-INT-07 (Gitea pull sync 워커 + auth_mode/OutboundAuth, §8.7); Integration API §15 — `integration_sync_jobs` 큐 (API-72 sync trigger cross-cut) | **RM-M4-06 (Gitea Hourly Pull worker) — 1차 구현** (PR #341; hourly 스케줄 정밀화 / reconciliation 정책은 issue #231 잔여) | gitea-03..05 (§2.4 IMPL-gitea-XX 서브 표 — `client.go` REST pull client / `syncer.go` 정규화 upsert / `worker.go` SyncWorker + queue 소비 + main.go wire); IMPL-int-store-01 (sync job 큐 + upsert, cross-cut); IMPL-int-migration-01 (000028 `integration_sync_jobs`) | gitea-02..04 (`client_test` / `syncer_test` / `worker_test`); `TestSyncWorker_ProcessOnce_NoEnvTokenLeakForExplicitProvider` ([[feedback-no-env-credential-fallback-for-explicit-provider]]); `TestSCMRepositories_RejectsDisabledProvider` | (E2E 미커버 — 외부 Gitea 의존, gap §5.1; 단위테스트 + 모의 server 로 검증) |
| **hrdb** ([`docs/infrastructure/`](../infrastructure/) — 인프라 layer) — HR DB ETL + Keycloak ↔ HRDB sync | FR-25, 61–63 cross-cut from auth-session (signup) | UC-AUTH-01, UC-ACCOUNT-01 (cross-cut) | (cross-cut — auth-session ARCH-12); [ADR-0008](../adr/0008-hrdb-production-adapter.md) (PostgreSQL `hrdb` schema, accepted, sprint `claude/work_260513-l`); [ADR-0010](../adr/0010-primary-dept-resolution.md) (`users.primary_unit_id` 자동 판정 알고리즘, accepted). | RM-M3-01 (Sign Up — IdP admin 연계 재설계 in_progress); RM-M3-02 (PostgresClient + migration 000010 1차 완료, ETL cron carve); off-boarding Phase 2 (ADR-0008 §6 carve) | IMPL-hrdb-01 (`internal/hrdb/mock.go` `MockClient` — 3 시드 인원 dev/테스트); IMPL-hrdb-02 (`internal/hrdb/postgres.go` `PostgresClient` + migration `000010_create_hrdb_persons.up.sql` — ADR-0008 1차 구현, ETL/cron sync carve) | (후속 갱신 — UT-hrdb-XX 후보) | (E2E — HRDB ETL pipeline 의존, 사내 운영 layer) |
| **commandworker** ([`docs/infrastructure/commandworker/`](../infrastructure/commandworker/)) — 명령 worker dry-run/live executor | FR-58, 59 cross-cut from realtime (명령 lifecycle) | UC-CMD-01..03 (cross-cut) | (cross-cut — realtime ARCH-05 + API-15..17, 36, 37) | RM-M4-02 (replay, planned); RM-M4-03 (frontend WS UI, planned) | command-04 (`internal/commandworker/*` — dry-run/live executor, `command.status.updated` publisher); command-05 (`internal/serviceaction/*` — service-action 도메인 로직, executor adapter 후보) | commandworker-01, 02; serviceaction-01 | ([`test_cases.md`](../infrastructure/commandworker/test_cases.md) — sprint `claude/work_260513-i` 카탈로그 / E2E spec ts 작성은 후속) |
| **infra-topology** (Infrastructure cross-cut) — 인프라 토폴로지 v2 + HomeLab snapshot | FR-12, 13, 97, 98, 99; NFR-12, 16 | UC-GITEA-03 | ARCH-04, 09; API-06, 07, 76..78 (infra services / snapshot / topology v2 — integration-registry cross-cut) (정밀 매핑: §2.2 Infra/Dashboard 서브 표) | RM-M4-01 (infra event publish, planned) | infra-01 (§2.4 IMPL-infra-XX 서브 표 — `internal/httpapi/snapshot.go`); IMPL-int-adapter-01 (HomeLab adapter, integration-registry cross-cut); frontend-role-03 (gardener) + `admin-topology-v2.spec.ts` 화면 | httpapi-12; UT-int-adapter-01 (HomeLab adapter) | `admin-topology-v2.spec.ts` (TC-INT-HOMELAB-03 `/admin/topology-v2`) — legacy `/admin` 토폴로지는 archived (`infra-topology.spec.ts` 삭제) |
| **database-migration** (Infrastructure cross-cut) — PostgreSQL baseline seed/schema | (cross-cut — 도메인별 REQ 가 source) | N/A | (cross-cut — 도메인별 ARCH 가 source) | (cross-cut — 도메인별 RM 이 source) | **active chain (2026-06-04 baseline reset)**: `000001_initial_schema` (최종 schema object 통합), `000002_seed_system_rbac` (system role/permission seed), `000003_seed_bootstrap_catalog` (`org_units`/`users`/`unit_appointments`/`scm_providers` bootstrap seed). Historical chain `000001_create_webhook_events` ~ `000047_normalize_team_manager_display_name` 는 `backend-core/migrations-legacy-20260604/` 로 보관되며 runtime source-of-truth 에서는 제외. baseline reset 이전 세부 변천은 historical reference 로 유지. | (cross-cut — migration 자체는 schema 변경, UT 는 도메인별 store test 에서 cover) | (cross-cut) |
| **deployment-automation** ([`docs/infrastructure/deployment-automation/`](../infrastructure/deployment-automation/)) — CI / 거버넌스 / nginx single-port reverse proxy | NFR-1 (no-docker) | UC-GITEA-01 (CI gate) | ADR-0001 §5; [ADR-0003](../adr/0003-no-docker-policy-ci-scope.md) (No-Docker 정책 CI 범위); [ADR-0005](../adr/0005-workflow-lint-actionlint.md) (workflow lint actionlint); [ADR-0018](../adr/0018-single-port-reverse-proxy-policy.md) (단일 외부 80/443 포트 + `/devhub` sub-path — [`single_port_reverse_proxy.md`](../infrastructure/deployment-automation/single_port_reverse_proxy.md)) | M2-16 (CI 1차, PR #86); FU-CI-1..4 (PR #87); ADR-0003 (PR #88); 거버넌스 + 매트릭스 1차 (PR #89); 갭 분석 + 메타 헤더 표준화 (sprint `claude/work_260513-d`); ADR-0005 workflow-lint (sprint `claude/work_260513-i`) | build infra: `.github/workflows/ci.yml`, `scripts/ci-setup.sh`, `infra/idp/*.ci.yaml`; `docs/governance/*`, `docs/traceability/*` | (lint 미도입 본격, FU-CI 향후) | (CI run 자체가 검증) |

### 3.4 Cross-cutting 그룹

| 도메인 | REQ | USECASE | ARCH / API | ROADMAP | IMPL | UT | TC |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **M4 (planned, 정의: §2.3.2) + 거버넌스/추적성 SoT (Phase 1~5)** | FR-37–48, 56–57, 60, 90–94 (Realtime / Task / Admin 미진입 부분) | UC-RT-01..02 (확장 예정), UC-APP/UC-PROJ (확장 예정) | API-14 (WebSocket 확장); 거버넌스 진입점 [`docs/governance/README.md`](../governance/README.md) + 추적성 진입점 [`docs/traceability/README.md`](./README.md); [`code-taxonomy.md`](../governance/code-taxonomy.md) (SDLC 문서 재정비 Phase 1~5 의 SoT) | RM-M4-01..03, 06..09 (정의: §2.3.2 RM-M4 표); SDLC 문서 재정비 Phase 1 (디렉터리 골격 PR #410) + Phase 2 (이관 PR #411) + Phase 3 (REQ/ARCH/API split PR #413) + Phase 4 (§3 매트릭스 SoT 재구성 — 본 PR) + Phase 5 (후속 carve) | (미진입 — sprint plan 진입 시 IMPL-task-XX 발급) | (미진입) | (미진입) |

> Note — 매트릭스 셀의 ID 는 §2 의 단계별 인덱스를 줄여서 표기 (예: `auth-01..07` = `IMPL-auth-01..IMPL-auth-07`). 한 도메인이 여러 단계에 걸쳐 영향을 주므로 정확한 1:1 매핑은 §2 인덱스 (REQ/UC/ARCH/API) + 도메인 sub-document 본문의 ID 노출 (`document-standards.md` §5) 로 확인. 도메인 sub-document SoT 는 [`docs/domain/<domain>/{requirements,architecture,api,test_cases}.md`](../domain/) (Phase 3 이관 결과).

## 4. ADR 인덱스

| ADR | 제목 | 상태 | 영향 도메인 |
| --- | --- | --- | --- |
| [ADR-0001](../adr/0001-idp-selection.md) | IdP 선정 (Ory Hydra + Kratos) | **superseded by [ADR-0019](../adr/0019-keycloak-only-idp.md) (2026-05-19)**. 원본 결정 (2026-05-07): Hydra+Kratos. PR #167 (2026-05-18) 가 Keycloak 단일화 실 구현 + ADR-0019 가 결정 reversal 사후 명문화. 본 ADR 본문은 historical context 로 immutable 보존. | 인증, 회원가입, 계정 관리 |
| [ADR-0002](../adr/0002-rbac-policy-edit-api.md) | RBAC policy edit API (DB-backed matrix) | accepted (2026-05-08) | RBAC |
| [ADR-0003](../adr/0003-no-docker-policy-ci-scope.md) | No-Docker 정책 CI 범위 명문화 | accepted (2026-05-13, PR #88) | CI / 거버넌스 |
| [ADR-0004](../adr/0004-x-devhub-actor-removal.md) | `X-Devhub-Actor` 헤더 폐기 완료 선언 | accepted (2026-05-13, sprint `claude/work_260513-h`) | 인증 (auth) |
| [ADR-0005](../adr/0005-workflow-lint-actionlint.md) | GitHub Actions workflow lint (actionlint) CI 잡 도입 | accepted (2026-05-13, sprint `claude/work_260513-i`) | CI / 거버넌스 |
| [ADR-0006](../adr/0006-x-devhub-actor-reject-inbound.md) | inbound `X-Devhub-Actor` 헤더 명시 거부 (400) | accepted (2026-05-13, sprint `claude/work_260513-j`) | 인증 (auth) |
| [ADR-0007](../adr/0007-rbac-cache-multi-instance.md) | RBAC PermissionCache 다중 인스턴스 일관성 (PG LISTEN/NOTIFY, 구현 carve) | accepted (2026-05-13, sprint `claude/work_260513-j`) | RBAC / 운영 |
| [ADR-0008](../adr/0008-hrdb-production-adapter.md) | HR DB production 어댑터 (PostgreSQL `hrdb` schema) | accepted (2026-05-13, sprint `claude/work_260513-l`). 구현은 sprint `claude/work_260513-m` 에서 PostgresClient + migration 1차 완료. ETL cron carve. | M3 Sign Up / 인사 DB |
| [ADR-0009](../adr/0009-org-secondary-memberships-and-total-count-mv.md) | 파견/겸임 모델 + `total_count` Materialized View | accepted (2026-05-13, sprint `claude/work_260513-m`). 파견/겸임 = `unit_appointments` cover 명문화. MV migration 000011 sprint `claude/work_260513-n` 1차 완료. cron 갱신 + getHierarchy MV join 은 carve. | M3 organization polish |
| [ADR-0010](../adr/0010-primary-dept-resolution.md) | `users.primary_unit_id` 자동 판정 알고리즘 (leader 우선 + total_count tie-break + lexicographic) | accepted (2026-05-13, sprint `claude/work_260513-n`). 1차 알고리즘 + 5 단위테스트 PASS. backfill worker carve. | M3 organization polish |
| [ADR-0011](../adr/0011-rbac-row-scoping.md) | Application/Project Owner 위양과 RBAC row-level scoping | accepted (2026-05-14, sprint `claude/work_260514-a`). 옵션 C (handler/service 코드 검증) 1차 채택, 옵션 B (row_predicate) 단계적 확장 옵션 보존. 옵션 A (Casbin), D (PG RLS) 거부. 1차 매트릭스 seed (4 resource × system_admin) 본 sprint 처리 (migration 000018). | Application/Project RBAC |
| [ADR-0012](../adr/0012-dreq-external-intake-auth.md) | Dev Request 외부 수신 endpoint 인증 정책 | accepted (2026-05-15, sprint `claude/work_260515-g`). 옵션 A (API 토큰 + IP allowlist) 1차 채택, B (HMAC) / C (OAuth client_credentials) 는 후속 마이그레이션 경로. `dev_request_intake_tokens` 테이블 + `requireIntakeToken` middleware + audit 2 action 정책 정의. | DREQ 외부 수신 |
| [ADR-0013](../adr/0013-dreq-rbac-row-scoping.md) | Dev Request 도메인의 RBAC row-scoping 정책 | accepted (2026-05-15, sprint `claude/work_260515-m`). ADR-0011 §4.2 helper `enforceRowOwnership(c, dr.AssigneeUserID, "team_manager")` 의 dev_requests resource 적용 사례 사후 명문화. handler wire-up 은 PR #124 (sprint `claude/work_260515-i`) 에서 이미 도입. reassign/close 의 system_admin only carve 도 본 ADR 가 명시. | DREQ RBAC |
| [ADR-0014](../adr/0014-dreq-intake-token-admin.md) | Dev Request intake token admin (발급/조회/revoke) 정책 | accepted (2026-05-15, sprint `claude/work_260515-o`). 신규 RBAC resource `dev_request_intake_tokens` 도입 (system_admin 일임). plain token 발급 1회 노출 + hashed 절대 미노출 + idempotent revoke (`revoked_at = COALESCE`). accounts_admin temp_password 패턴과 정합. §6 carve out 의 expires_at + allowed_ips mutation 은 ADR-0017 가 활성화 명문화. | DREQ admin |
| [ADR-0015](../adr/0015-homelab-adapter-pull-strategy.md) | HomeLab adapter pull source 전략 | accepted (2026-05-18 사후 명문화, 실 결정 2026-05-16 PR #139, sprint `codex/next-step-20260516` → 본 sprint `claude/work_260518-c`). source mode = file + HTTP mutually exclusive per instance, 실행 컨테이너 = lightweight goroutine + feature flag, retry = exponential backoff env-controlled. 메트릭 5종 명시 (알림 규칙은 ADR-0016 분기). | External Integration / HomeLab |
| [ADR-0016](../adr/0016-prometheus-alerts-policy.md) | Prometheus 알림 규칙 정책 (HomeLab observability) | accepted (2026-05-18 사후 명문화, 실 초안 2026-05-17 PR #139). Alertmanager 규칙 3종 (PullFailing / PullNoRecentSuccess / DegradedProvidersDetected) + stage/prod 임계값 분리 (stage 1주 관찰 후 prod 임계 확정). Grafana panel 5종 권장. multi-instance aggregation = `max by(provider)`. latency p95 alert 는 carve out. | External Integration / 운영 |
| [ADR-0017](../adr/0017-dreq-intake-token-operational-hardening.md) | DREQ intake token 운영 hardening (expires_at + PATCH allowed_ips) | accepted (2026-05-18 사후 명문화, 실 활성화 2026-05-16 PR #137, sprint `gemini/dreq_e2e_260515` → 본 sprint `claude/work_260518-c`). ADR-0014 §6 의 두 carve out 활성화: migration 000027 `expires_at` + `auth_intake_token_expired` middleware 만료 체크 (lazy, cron revoke 는 carve 유지), `PATCH /api/v1/dev-request-tokens/:token_id` (API-79) 의 `allowed_ips` only mutation (audit anchor 보존). | DREQ admin / 운영 |
| [ADR-0018](../adr/0018-single-port-reverse-proxy-policy.md) | 단일 외부 포트 역프록시(Nginx) 구성 정책 | accepted (2026-05-18). 단일 외부 80/443 포트 연동, `/devhub` sub-path prefix 아키텍처 공식화, Same-Origin CORS 무력화 이점, Hydra/Kratos 쿠키 strict isolation (`Path=/devhub/auth/...`), local dev vs prod 비대칭 분기 가드, 컷오버/롤백 SOP 가이드라인 수립. | 인프라 / 인증 / 운영 |
| [ADR-0019](../adr/0019-keycloak-only-idp.md) | Keycloak 단일화 (Hydra+Kratos 폐기) — ADR-0001 supersession | accepted (2026-05-19, sprint `claude/work_260519-a`). 2026-05-18 PR #167 (외부 codex 의 `codex/keycloak-only-refactor-plan`) 가 옵션 A (Keycloak 단일화) 실 구현 머지. [design 문서 PR #163](../infrastructure/keycloak-idp/sso_federation.md) 의 옵션 B (Kratos federation) 권장은 미채택 (rejected). 본 ADR 가 결정 reversal 사후 명문화: §3 결정 근거 6 항목 (운영 단순성 / SSO 사용자 공존 부담 회피 / MFA-WebAuthn-recovery 표준 흡수 / `users.idp_subject` 일반화 / ADR-0018 정합 / off-boarding 즉시성) + §4 KC-PR-A..F 머지 사실 + §5.3 carve out 8 항목 (Keycloak realm SOP / JWKS rotation / Keycloak ↔ HRDB sync / SSO logout chain / MFA / failover / off-boarding / `groups` claim → RBAC role) + §5.4 RM-M4-09 의미 재정의. | 인증, 회원가입, 계정 관리, 운영 |
| [ADR-0020](../adr/0020-account-user-management-boundary.md) | 외부 Keycloak 가정 하의 계정/사용자 관리 책임 경계 — Phase 2 명시 결정 6건 종합 | accepted (2026-05-20, sprint `claude/work_260520-d`). **partial superseded by [ADR-0021](../adr/0021-onboarding-self-service-unit-selection.md) (2026-05-21)** — §3.2 신규 user unit 초기 배치 row + §4.1 sub-carve B 의 lazy auto-create 실 구현 + §4.2 의 lazy auto-create 보안 영향 + §6.1 후속 ADR 후보 row + §6.2 carve out 의 동일 항목 (5 위치) 은 ADR-0021 가 supersede. 핵심 결정 (옵션 A 책임 경계 / `rbac_subject_roles` 제거 / service account 권한 축소) 은 변경 없이 유지. [Phase 1 PR #199](../domain/auth-session/account_redesign.md) 의 4 경로 책임 분산 매트릭스 + 핵심 발견 5건 + [Phase 2 PR #200](../domain/auth-session/account_redesign.md#5-phase-2-책임-분리-design-sprint--a-2026-05-20-확정) 명시 결정 6건 (A 전면 폐기 / B `/login` minimal entry / C event listener 확장 + lazy hot path / D `rbac_subject_roles` 완전 제거 / E read-only mode self-reverse / F JWKS stale-while-error expiry case 확장) 종합. §4.1 sub-carve 6건 분담 (A 본 sprint, B~F 후속). 본 sprint `-d` 는 sub-carve A (`rbac_subject_roles` 완전 제거 — backend rbac.go/router.go/permissions.go/postgres_rbac.go + test 정리) 흡수. | 인증, 회원가입, 계정 관리, 운영 |
| [ADR-0021](../adr/0021-onboarding-self-service-unit-selection.md) | Onboarding self-service unit selection + lazy auto-create supersession — ADR-0020 partial supersession | accepted (2026-05-21, sprint `claude/onboarding-adr-2026-05-21`). 컨셉 (PR #260 + #265) + 요구사항 (PR #266, REQ §5.7) + 설계 (PR #267, UC §2.13 + ARCH §9 + API §16) 의 누적 결정 명문화. §3.1 책임 경계 확장 (사용자 self-service onboarding + admin 검토) + §3.2 미완료 사용자 3 tier 접근 상태머신 (limited / pending_review / reviewed) + §3.3 lazy auto-create 폐기 (ADR-0020 §3.2/§4.1/§4.2/§6.1/§6.2 5 위치 inline banner partial supersession) + §3.4 skip-and-resume + §3.5 backend gating + frontend 3분기. ADR-0020 의 핵심 결정 (옵션 A 책임 경계) 은 reverse 하지 않고 자연 확장. IMPL/UT/TC carve 는 후속. | 인증, 계정 관리, 운영 |
| [ADR-0022](../adr/0022-keycloak-version-pin-25-0.md) | Keycloak 컨테이너 버전 25.0 pin | accepted (2026-05-22). 무버전 latest tag 의 admin bootstrap env 비호환 회귀 회피. `KEYCLOAK_ADMIN`/`KEYCLOAK_ADMIN_PASSWORD` (25.x legacy) 기준. **superseded by [ADR-0023](../adr/0023-keycloak-version-pin-26-0.md) (26.0 승격)**. | 인프라 / 인증 |
| [ADR-0023](../adr/0023-keycloak-version-pin-26-0.md) | Keycloak 컨테이너 버전 26.0 pin (ADR-0022 승격) | accepted (2026-05-26). 26+ 표준 `KC_BOOTSTRAP_ADMIN_USERNAME`/`PASSWORD` 기준 + 25.x legacy 양쪽 동시 주입(silent fail 회피, [[feedback_keycloak_25_26_admin_env]]). §5 사내 redeploy smoke 후속 carve. | 인프라 / 인증 / 운영 |
| [ADR-0024](../adr/0024-websocket-auth-query-token.md) | WebSocket 인증 패턴 — `?access_token=` query string (browser Authorization header 제약 우회) → ticket-only cutover | accepted (2026-05-26, sprint `claude/work_260526-organization-followups`). 사용자 보고 회귀 root cause (frontend `realtime.service.ts:buildURL` 의 token 미첨부) + PR #335 frontend hotfix 명문화. backend `auth.go:79-83` 의 `/api/v1/realtime/ws` query parsing 패턴 active code 단기 채택 + 중기 ticket pattern carve. nginx access_log redact + WS 401 refresh-then-reconnect 후속 carve. | 인증, 운영, realtime |

## 5. Gap 요약

### 5.1 E2E 미커버 도메인

후속 sprint 에서 보완 후보. TC 후보 ID 는 등재만 (실제 spec 작성은 별도).

| 도메인 | 현황 | 가능한 TC 후보 | 우선순위 |
| --- | --- | --- | --- |
| 명령 lifecycle / mitigation | 단위테스트 (`UT-httpapi-09`, `UT-commandworker-01..02`, `UT-domain-02`) 만, e2e UI 흐름 미커버 | TC-CMD-CREATE-01, TC-CMD-STATUS-01 — sprint `claude/work_260513-i` 가 카탈로그 추가, 실제 spec ts 작성은 후속 sprint | P2 (카탈로그 closed, spec ts open) |
| 실시간 (WebSocket) | M3 planned. 현재는 `command.status.updated` 만 publish | M3 진입 시: TC-WS-CONNECT-01, TC-WS-CMD-STATUS-01, TC-WS-RESILIENCE-01 (re-connect) | P3 (M3 의존) |
| 인프라 토폴로지 React Flow | 레거시 `/admin` 토폴로지 화면 아카이브 → topology v2 로 대체 | TC-INFRA-RENDER-01, TC-INFRA-NODE-CLICK-01, TC-INFRA-GROUP-TOGGLE-01 (legacy `/admin`) — `.skip` 상태 `infra-topology.spec.ts` 는 sprint `deepseek/e2e_ci_cleanup` 에서 삭제. 실 검증은 `admin-topology-v2.spec.ts` (TC-INT-HOMELAB-03) 가 `/admin/topology-v2` 에서 담당 | obsolete (legacy surface archived, topology v2 로 대체) |
| Webhook 처리 (gitea HMAC) | 단위테스트 (`UT-httpapi-14`, `UT-gitea-01`) 로 검증, 외부 영향 e2e 어려움 | E2E 후보 없음 — 통합 테스트 (Go test + 모의 webhook server) 가 자연 | P3 (E2E 외 검증으로 충분) |
| Gitea SCM 동기화 워커 (pull, PR #341) | 단위테스트 (`UT-gitea-02..04` client/syncer/worker) 로 검증. 외부 Gitea 인스턴스 + `integration_sync_jobs` 큐 의존으로 e2e 어려움 | E2E 후보 없음 — 통합 테스트 (Go test + 모의 Gitea API server) + 큐 상태 전이 검증이 자연 | P3 (E2E 외 검증으로 충분) |
| External Integration (provider/binding/homelab) | **1차 종합 활성화** — provider lifecycle (`admin-integrations.spec.ts`) + bindings lifecycle (`admin-integration-bindings.spec.ts`) + **topology v2** (`admin-topology-v2.spec.ts`, sprint `claude/work_260518-n`) 모두 active. degraded providers 시각화도 v2 페이지 헤더 banner 로 도입. 잔여: ingest webhook 외부 영향 (carve out — UT/IT 로 충분). | `docs/domain/integration-registry/test_cases.md` §3 의 TC-INT-FRONTEND-LIST/CREATE/EDIT/SYNC/DELETE/RBAC + TC-INT-FRONTEND-BIND-LIST/CREATE/RBAC + TC-INT-HOMELAB-03 + TC-INT-FRONTEND-TOPOLOGY-V2-NAV/RBAC-01 active. 잔여: React Flow group sub-node (services as node children) 시각화 강화, WebSocket 갱신 (real-time topology). | P2 (M4 진입 게이트 1차 통과, 후속 강화 carve out) |
| Dev Request (DREQ) intake auth path | **부분 활성화** — `frontend/tests/e2e/dev-requests.spec.ts` (PR #136 `codex/e2e-fix-20260515`) 가 위젯→list→detail flow + Promote 조건부 분기 + signout flow 안정화 커버. 잔여: admin intake token 발급 → bearer header → POST 외부 의뢰 → assignee dashboard widget → promote (단일 트랜잭션 신규 application/project) → revoke 전체 흐름 + TC-DREQ-* 정식 발급. | TC-DREQ-INTAKE-AUTH-01 (token bearer ok / expired / revoked / IP outside allowlist), TC-DREQ-WIDGET-FLOW-01 (PR #136 가 partial cover), TC-DREQ-PROMOTE-TX-01 (단일 트랜잭션 성공 / 회귀 rollback), TC-DREQ-ADMIN-TOKEN-01 (issue → reveal → revoke + DestructiveConfirmModal) | P1 (DREQ carve out 3/4 잔여) |

### 5.2 ID 부재 / 매핑 누락

| 항목 | 상태 | 처리 |
| --- | --- | --- |
| Backend AI (`backend-ai/`) placeholder | open | v2 진입 시 AnalysisService gRPC client 대상 IMPL-ai-XX 발급. |
| Frontend 컴포넌트 Vitest (Header, Sidebar, AuthGuard 등) | open | 후속 sprint 후보 (P2). |
| `auth.spec.ts` 의 TC ID 카탈로그 흡수 | **closed (2026-05-13, sprint `claude/work_260513-d`)** | 재검증 결과 spec 안의 TC-AUTH-NEG-01 + TC-AUTH-NOAUTH-01 모두 `test_cases_m2_auth.md` 의 TC 카탈로그에 이미 흡수되어 있음. 1차 분석에서 "01..06 미흡수" 라고 적은 것은 사실과 다름. |
| API §12 RBAC 정책 편집 API 의 IMPL 정밀 매핑 | **closed (2026-05-13, sprint `claude/work_260513-f`)** | §12.2~§12.10 의 9 endpoint/정책에 `(API-26..31, 38..40)` 본문 ID 노출 + §2.2 의 RBAC API 매핑 표 + §2.4 의 IMPL-rbac-01..04 책임 정의 (handler / store / enforcement / cache) + §3 RBAC 행 IMPL 컬럼 endpoint-IMPL 1:1 매핑. |
| External Integration RM/IMPL/UT ID 확장 점검 | **closed (2026-05-16, sprint `codex/next-step-20260516`)** | `IMPL-int-store-02`, `IMPL-int-migration-02`, `IMPL-int-adapter-01` 및 `UT-int-adapter-01` 수준 반영 완료. `RM-INT-XX`는 M5 planning gate 시점에 일괄 발급으로 deferred 유지. |
| Backend AI (`backend-ai/`) placeholder | open | (위와 동일 — 본 sprint 변경 없음) — M3-04 진입 시 IMPL-ai-XX 발급. |
| Frontend 컴포넌트 Vitest (Header, Sidebar, AuthGuard 등) | **closed (2026-05-13, sprint `claude/work_260513-i`)** | C1 작업 — Header / Sidebar / AuthGuard 단위테스트 추가. `IMPL-frontend-layout-01..02` + `IMPL-frontend-auth-XX` 의 회귀 안전망. |
| 본문 spec 부재 endpoint (`/api/v1/accounts/*`, `/api/v1/users` CRUD, `/api/v1/organization/*`) | **closed (2026-05-13, sprint `claude/work_260513-j`)** | sprint `claude/work_260513-i` 의 §10.1 carve out 표 + sprint `claude/work_260513-j` 가 `backend_api_contract.md` §10.2 (API-25 accounts admin) + §10.3 (API-33 users CRUD) + §10.4 (API-34 organization) 본문 spec 절 신설. endpoint 표 + 권한 + envelope + 1차 에러 매트릭스. 자세한 schema (모든 endpoint 의 request/response/error 매트릭스) 는 후속 spec hygiene 후보. |
| 카탈로그된 TC 가 spec 으로 구현됐는지 역검증 | open | TC-AUD-02 등 일부 TC 가 카탈로그에만 존재할 가능성 — spec 파일 grep 으로 자동 검증할 hygiene 후보. |

### 5.3 문서 ↔ 코드 불일치

| 항목 | 상태 | 처리 |
| --- | --- | --- |
| ADR-0001 vs `frontend_integration_requirements.md` §3.8 | **closed (2026-05-13, sprint `claude/work_260513-d`)** | §3.8 의 "재설계 예정" 노트를 "deprecated" 노트로 명확화 + Phase 13 머지 후 실제 endpoint (API §11.5 / §12.8.2) 로 redirect 링크. |
| X-Devhub-Actor 완전 제거 trigger | **closed (2026-05-13, sprint `claude/work_260513-h` + `-j`)** | [ADR-0004](../adr/0004-x-devhub-actor-removal.md) 가 폐기 완료를 선언 (sprint `-h`). 후속 [ADR-0006](../adr/0006-x-devhub-actor-reject-inbound.md) (sprint `-j`) 이 silent ignore → 400 명시 거부로 전환. `architecture.md` / `ADR-0001` §8 #4 / `me.go` 주석 + `backend_api_contract.md` §8/§9.1/§9.2/§11.3 + `frontend_integration_requirements.md` §3.5 + `environment-setup.md` §2.4 의 spec 잔재 7 위치 정리. 회귀 방지 4 negative 테스트는 ADR-0006 시점에 "ignore → reject" 의도로 갱신. |
| RBAC cache 다중 인스턴스 일관성 | **closed (decision, 2026-05-13, sprint `claude/work_260513-j`)** | [ADR-0007](../adr/0007-rbac-cache-multi-instance.md) 가 PostgreSQL `LISTEN`/`NOTIFY` 채택을 결정. 검토 옵션 A (선택: PG LISTEN/NOTIFY) / B (Redis pub/sub, ADR-0003 충돌로 거부) / C (폴링, latency tradeoff) / D (carve out, 의도 충돌로 거부). **구현은 M3 진입 시 carve out** — M3 sprint plan 의 명시적 항목. M1-DEFER-E 의 decision 부분 closing. |

## 5.4 GAP 보완 계획

- 로그인 세션 + 사용자/조직 추적성 미흡 항목의 실행 계획은 [`traceability_remediation_plan_auth_org.md`](./traceability_remediation_plan_auth_org.md) 를 참조한다.

## 6. 변경 이력

| 일자 | 변경 |
| --- | --- |
| 2026-06-07 | **대시보드 UI 개선 및 신규 KPI/테스트 대시보드 추가** — 플랫폼 대시보드 UI 개선, 신규 KPI 대시보드(Python 가상 스크립트 실행엔진 executePythonMetric 탑재), 신규 테스트 관리 대시보드(캘린더/간트 타임라인, 테스트케이스 카탈로그, 결과 조율 큐, Recharts 도넛 차트) 구현 완료 및 Vitest 1025개 100% PASS 검증 완료. **신규 ID**: REQ-FR-109..111 + UC-KPI-01..02 + UC-TEST-01..03 + ARCH-21..22 + API-101 + RM-KPI-01 + RM-TEST-01 + IMPL-dashboard-ui-01 + IMPL-kpis-dashboard-01 + IMPL-tests-dashboard-01 + UT-frontend-dashboards-01 + TC-PLAT-DASH-01 + TC-KPI-DASH-01 + TC-TEST-DASH-01. 최종 수정일 갱신. | 본 대시보드 개선 sprint (`gemini/work_260607-a-dashboard-improvements`) |
| 2026-06-04 | OpenCode (Sisyphus) sprint a~j 머지 완료 (#464~#474). **Docs-only/governance**: #464 (opencode 부트스트랩), #466 (워커 §1.4 정의), #467 (N-10 RBAC 검증 보고서). **Frontend fix**: #468 (/projects listAggregation + catalog drill-down), #470 (SCM 표시 결함), #472 (progress bar). **Frontend feat**: #471 (draft repo 관리). **Frontend test**: #473 (N-4 unit test 보강), #474 (admin/catalog page unit test). 모든 PR 이 기존 REQ/UC/ARCH/API/RM/IMPL/UT/TC 에 영향 없음 (frontend fix/test + docs only). §5 gap 분석 변동 없음. §3 매트릭스 변동 없음. 최종 수정일 갱신. | 본 housekeeping sprint (`opencode/work_260604-k-v1-harden`) |
| 2026-06-06 | **sprint -h 신규 carve 의 ID 발급 + 매트릭스 cross-ref** — §3.1 의 auth-session / integration-registry / repository-integration row 3 row cross-ref 갱신. **신규 ID**: REQ-FR-106 (integration-registry, CI Run 생성) + REQ-FR-107 (auth-session, Sign-out) + REQ-FR-108 (repository-integration, Repository build-runs) + ARCH-18/19/20 + API-98/99/100 + IMPL-ci-runs-01 + IMPL-auth-logout-01 + IMPL-repository-build-runs-01 + UT-ci-runs-01 + UT-auth-logout-01 + UT-repository-build-runs-01 + TC-CI-RUN-01 + TC-AUTH-LOGOUT-01 + TC-BUILD-RUNS-01. **ID 슬롯 정합**: 1차 housekeeping 의 API-94/95/96 (Task Item Ingestion 점유) + 2차 housekeeping 의 API-97 (realtime 도메인 점유) 와 충돌 회피 → 최종 API-98/99/100 슬롯 할당. **§2.1/§2.2 인덱스 line**: 도메인 REQ/ARCH 갱신. 출처: Mavis housekeeping `mvs/work_260606-b-traceability-ci-auth-repo` PR. 추적성 영향 — 신규 ID 17 (REQ 3 + ARCH 3 + API 3 + IMPL 3 + UT 3 + TC 3, API-94/95/96/97 cross-ref 정정). | 본 housekeeping sprint (`mvs/work_260606-b-traceability-ci-auth-repo`) |
| 2026-05-31 | sprint `gemini/gitea-simulation-integration` — **외부 Gitea SCM 연동 시뮬레이션 및 2차 UI/Metric 완결**. (1) SCM 중복 정리 및 코드베이스 push 바인딩 완료. (2) SCM pulls/issues 404 API 핫픽스 및 webhook 실시간 동기화 시뮬레이션 성공. (3) 2차 UI 개선으로 `Linked Repositories (N:M)` ➡️ `Linked Repositories` 텍스트 정비 및 백엔드 API 부재에 따른 `opsError` 경고창(일부 프로젝트 데이터를 불러오지 못했습니다) 완벽 제거. (4) Tasks(done) + PR(merged|closed) + Issues(closed)의 실제 종결 비율을 과학적으로 산출하는 **가중 평균 메트릭 진척도 알고리즘**을 프로젝트 상세 진척도(Overall Progress)에 적용. (5) Playwright E2E E2E 검증 2개 시나리오 단 7.3초 만에 100% 그린 PASS 성공 및 스크린샷 갱신 완료. | sprint `gemini/gitea-simulation-integration` |
| 2026-05-29 | sprint `claude/work_260529-o` (SDLC 문서 재정비 Phase 4 후속 — §2 단계별 인덱스 10 도메인 SoT 정합, [issue #423](https://github.com/ykylee/Devhub_example/issues/423) closes) — §3 매트릭스 19 row 그룹화 (10 core 도메인 + Shared + Infrastructure + Cross-cutting) 와 §2.1 (REQ) / §2.1.5 (UC) / §2.2 (ARCH/API) / §2.3 (RM) / §2.4 (IMPL) / §2.5 (UT) / §2.6 (TC) 의 row 그룹화를 1:1 또는 N:1 정합. **변경 영역**: §2 도입 메타 노트 추가 (Phase 4 정합 정책) + §2.1 REQ row 들에 §3 도메인 매핑 line 추가 + Phase 3 신규 임시 ID (REQ-RBAC-001..009 / REQ-AUDIT-001..007 / REQ-ORG-001..007) 흡수 row 추가 + §2.1.5 UC 의 도메인 매핑 line + §2.2 Architecture 의 §3 매핑 line + Phase 3 신규 ARCH-RBAC/AUDIT/ORG 흡수 + §2.2 API contract 의 §3 매핑 line + §2.2 sub-table 7개 헤더에 §3 매핑 + sub-document path 명시 (DREQ §14 / Integration §15 / Application/Repository §13 / RBAC §12 / Auth §11 / Infra/Dashboard §6 / Pipelines §7 / Realtime/Command/Audit §8,§9 / Account/Org/Me §10.1) + §2.3 RM 의 §3 매핑 line + M5..M7 도메인 매핑 추가 + §2.4 IMPL 도입 라인의 §3 매핑 + 8개 sub-table 헤더에 §3 매핑 (rbac / auth / account / org / command / audit / infra / hrdb / application / int / gitea) + §2.5 UT 의 도메인 분류 + §2.6 TC 의 도메인별 그룹 재구성. **보존**: 기존 모든 REQ/UC/ARCH/API/RM/IMPL/UT/TC ID 보존 + sprint reference + PR # + commit 매핑 + status note 모두 보존. 정보 손실 0 — 정합화 + 그룹 재배치 + 도메인 sub-document path 명시만. 추적성 영향 — 신규 ID 없음 (재구성 + sub-document link 갱신, docs-only). | sprint `claude/work_260529-o`, PR pending |
| 2026-05-29 EOD | sprint `claude/work_260529-{b..j}` 묶음 — cleanup-recovery (PR #407) 후속 carve out + SDLC 문서 도메인-모듈 재정비 sprint 8 PR (#408~#415) 머지 종합. **PR #408** (`68c2d15` housekeeping) + **PR #409** (`6eefda9` `refactor(backend/shared-integrationcaps)` providerHasCapability 3 카피 → `internal/shared/integrationcaps` 단일 helper, OR semantics 보존, 11 unit test) + **PR #410** (`33594ed` SDLC Phase 1 — 13 신규 README 골격, `docs/domain/<도메인>/` × 10 + `docs/shared/README.md` + `docs/infrastructure/README.md` + `docs/domain/README.md` index) + **PR #411** (`7d3d20d` SDLC Phase 2 — 25 file rename + cross-reference 일괄 91 file 갱신) + **PR #412** (`0b5907a` view carve — 25 frontend view 단위테스트 +210 신규 test, rbac/repo/dreq/intreg/org 90%+ / app-lifecycle 큰 modal 51~57% 미달 carve) + **PR #413** (`7d390f7` SDLC Phase 3 XL — 34 신규 sub-document (REQ/ARCH/API × 10 도메인 + Task 도메인 3 + `docs/api/conventions.md`) + master 3 file index 전환 (`requirements.md` 674→120 / `architecture.md` 1071→210 / `backend_api_contract.md` 2881→370) + Phase 3 신규 임시 ID REQ-RBAC/AUDIT/ORG + ARCH-RBAC/AUDIT/ORG) + **PR #414** (`c00b104` Phase 4 — 본 §6 직후 row) + **PR #415** (`273d9d4` Phase 5 — `docs/governance/document-standards.md` §4 위치 가이드 갱신). 후속 carve out (다음 sprint): CI e2e+integration 복원 / view 큰 modal 70% / ApplicationRepository decouple / ApplicationStore slim / §2 인덱스 정합. 추적성 영향 — Phase 3 신규 임시 ID 가 Phase 4 매트릭스에 흡수됨. | sprint 8 PR (#408~#415) 머지 완료 |
| 2026-05-29 | sprint `claude/work_260529-h` (SDLC 문서 재정비 Phase 4 — §3 매트릭스 10 도메인 SoT 재구성) — code-taxonomy SoT ([`docs/governance/code-taxonomy.md`](../governance/code-taxonomy.md)) 의 **10 core 도메인 + Shared + Infrastructure + Cross-cutting** 그룹화로 §3 매트릭스 전면 재구성. 기존 21 row (cross-cutting 그룹 위주 — 인증/회원가입/계정 관리/RBAC/조직 계층/감사/명령 lifecycle/실시간/인프라 토폴로지/Webhook+gitea push/Gitea pull 워커/대시보드+me/CI 거버넌스/Application+Project/DREQ/External Integration/SCM Repository 연동/Onboarding/APPDASH/M4 planned) → 19 row (10 core + 1 Shared + 7 Infrastructure + 1 Cross-cutting) 통합. **주요 통합 매핑**: (a) auth-session = 인증 + 회원가입 + 계정 관리 3 row 통합. (b) application-lifecycle = Application/Project + APPDASH + 대시보드/메트릭/me 3 row 통합. (c) repository-integration = Webhook+gitea push + SCM Repository 양방향 연동 2 row 통합 (Gitea SCM 동기화 워커 pull 은 Infrastructure gitea-scm 으로 분리). (d) integration-registry = External Integration + Task Item Ingestion 통합. (e) realtime = 실시간 WebSocket + 명령 lifecycle realtime publish 통합. (f) Infrastructure 신규 7 row (keycloak-idp / gitea-scm / hrdb / commandworker / infra-topology / database-migration / deployment-automation) — 기존 CI/거버넌스 + 인프라 토폴로지 + Gitea pull worker + hrdb 매핑 재배치. (g) Shared 신규 row (PR #406+ code-taxonomy code-level 적용 + PR #409 integrationcaps 분리). **Phase 3 신규 임시 ID 흡수**: REQ-RBAC-001..009 (rbac-permissions row), REQ-AUDIT-001..007 (audit-ops row), REQ-ORG-001..007 (organization-management row, 기존 REQ-FR-68..80 와 정합), ARCH-RBAC-01..06 (rbac-permissions row), ARCH-AUDIT-01..04 (audit-ops row), ARCH-ORG-01..05 (organization-management row). **file path link 갱신**: `docs/architecture.md §X` → `docs/domain/<domain>/architecture.md`, 도메인 sub-document SoT 는 `docs/domain/<domain>/{requirements,architecture,api,test_cases}.md` 단계별로 명시. 메타 주석 §3 도입부에 Phase 4 재구성 일자 + 그룹화 정책 + ID 흡수 명시. 정보 손실 0 — 기존 row 의 모든 REQ/UC/ARCH/API/RM/IMPL/UT/TC ID + sprint reference + PR # + commit 매핑 + status note 모두 새 row 에 보존. 추적성 영향 — 신규 ID 없음 (재구성 + 신규 임시 ID 흡수 + link 갱신, docs-only). | sprint `claude/work_260529-h`, PR pending |
| 2026-05-29 | sprint `gemini/work_260528-code-cleanup` (recovery 통합, PR #407) — **PR #406 의 code-taxonomy SoT 를 backend-core + frontend 에 실 적용한 Gemini cleanup sprint 의 후속 recovery 정리**. 5 commit 통합: (1) `chore(infra/deployment-automation)` e2e + backend-integration job 임시 비활성화 (`if: ... && false`) (2) `refactor(backend/test-recovery)` view cross-package test → httpapi/ 이관, EventCursor import, Repository wrapper, in-memory NewRealtimeTicketStore 복원, router.go shim, `providerHasCapability` AND→OR 회귀 정정 (3) `fix(frontend/cleanup-recovery)` P0 ghost import 10 (project.service + integration-provider-presets) + framer-motion mock children type drift + components/{ui,layout} 14 dead file 정리 (4) `test(frontend/cleanup-recovery)` service test +369 (audit/onboarding/dev_request/auth/realtime 신규 + 9 file 보강 + rbac type drift 6건 정정) (5) `docs(memory)` branch memory 갱신. 검증: backend 21 패키지 / frontend 29 file 431 test 모두 PASS, coverage 21.21% → 28.03%. 추적성 영향 — 신규 ID 없음 (코드 구조 이관 + 단위테스트 보강 + CI 임시 조정). | sprint `gemini/work_260528-code-cleanup`, PR #407 머지 `4a1942e` |
| 2026-05-28 | sprint `gemini/work_260528-architecture-restructure` (governance) — **3대 레이어 아키텍처(Domain-Shared-Infrastructure) 및 도메인 내부 4대 계층(View-Service-Repository-Schema) 고도화 개편**. 사용자의 지침을 수용하여 `docs/governance/code-taxonomy.md`를 3대 레이어 기준으로 전면 리팩토링하고, `docs/architecture.md` §2 시스템 컴포넌트 Mermaid 다이어그램을 신규 3계층 종속성 구조로 전면 수정 및 **아키텍처 호출 규칙(Calling Constraints)** 신설 명문화. PR #406의 Codex 인라인 리뷰 피드백을 반영하여 `ProviderModal` 컴포넌트 오너십의 오분류 정정 (`auth-session` -> `integration-registry` 이관). 추적성 영향 — 신규 ID 없음 (governance 설계 개편). | sprint `gemini/work_260528-architecture-restructure`, PR pending |
| 2026-05-28 | sprint `claude/work_260528-code-taxonomy` (governance) — **`code-taxonomy.md` 신규 + PR template 카테고리/모듈 prefix 컨벤션 도입**. main HEAD `101f4109` 기준 Explore agent 4개 (backend/frontend/infra/docs) 병렬 결과 종합 → `docs/governance/code-taxonomy.md` 신규 (12 도메인 카테고리 + 4 횡단 카테고리 + 모듈 매핑 + 리팩토링 후보 P0~P3). `governance/README` §"두 축" 표에 "코드 분류" 행 추가. `.github/pull_request_template.md` 에 "카테고리·모듈" 섹션 신규 — 모든 작업 PR title/commit/traceability 에 `<카테고리>` prefix 명시. 추적성 영향 — 신규 ID 없음 (governance + 문서 추가). | sprint `claude/work_260528-code-taxonomy`, PR pending |
| 2026-05-28 | sprint `claude/work_260528-router-idle-fix` — **router.replace() 600s idle silent failure root cause 분석 + P0 fix**. PR #404 검토 결과 nginx 변경 (ADR-0018 위반, PR #398 close 사유 반복) 부분 분리 후 분석 문서 + frontend P0 fix 묶음. (1) 분석 문서 `docs/analysis/2026-05-28-router-idle-timeout-analysis.md` (310 LoC) — Next.js 16.2.6 router 내부 5개 파일 추적 + timing chain T=0~600s + root cause: `refresh-scheduler.ts:90-92` 의 `transient_failed` 분기에서 `reschedule()` 호출 누락 → token 만료 후 재시도 기회 자체 사라짐 → `fetchServerResponse` 401 → `navigateToUnknownRoute` `.catch(() => state)` silent drop. (2) P0 fix — `refresh-scheduler.ts` `transient_failed` 분기에 `reschedule()` 호출 추가 (현재 token 의 남은 만료 시간 기준 재계산해 다음 타이머에서 재시도). (3) 회귀 가드 vitest "transient_failed → reschedule() 호출 → 다음 만료 직전 재시도" — 8/8 PASS. 추적성 영향 — 신규 ID 없음 (실 코드 1줄 fix + 분석 문서). 검증: vitest 8/8 + tsc 0 + lint pre-existing 3 warning. | sprint `claude/work_260528-router-idle-fix`, PR pending |
| 2026-05-28 | sprint `claude/work_260528-signup-deprecation` — **self-signup 페이지 deprecation** (사용자 요청 "signup 기능이 이제 사용되지 않는데"). ADR-0019 KC-PR 시점에 backend `auth_signup.go` + Kratos self-signup 은 이미 제거됐고 frontend 는 "Sign Up Unavailable" placeholder 만 유지하던 잔재. frontend surface 3 파일 삭제: `frontend/app/auth/signup/page.tsx` (canonical placeholder) + `frontend/app/signup/page.tsx` (legacy alias redirect) + `frontend/tests/e2e/signup.spec.ts` (placeholder 검증). UI 다른 진입점 (Sidebar / login page) 의 signup link 없음 — 안전. **보존 항목**: backend audit event mapping (`auth.signup.success/failed` in `keycloak_event_puller.go` + `keycloak_events_webhook.go`) — Keycloak admin console 통해 가입한 historical user 의 audit event 가 들어올 수 있어 mapping 유지. ADR-0008/0010/0019/0020 의 signup 언급은 immutable historical record 라 보존. 추적성 영향 — RM-M3-01 (API-23 `POST /api/v1/auth/signup`) 은 ADR-0019 supersession 시점에 이미 deprecated. 검증: `.next` clean rebuild + tsc 0 + npm run build ✓. | sprint `claude/work_260528-signup-deprecation`, PR pending |
| 2026-05-28 | sprint `claude/work_260528-repository-link-classification` (PR #401) — **Repository linked/unlinked 분류** (외부 SCM 에서 import 된 repo 가 시스템 application/project 와 연결됐는지 분류 표기). backend: `domain.Repository.LinkedApplicationsCount`/`LinkedProjectsCount` 신규 + `store.ListRepositories` + `GetRepositoryByID` SQL 에 subquery 추가 (codex P2 정합 — full_name only 매칭, scm_providers ↔ integration_providers catalog 불일치 회피). handler `repositoryResponse` 에 `linked_applications_count`/`linked_projects_count` 노출. frontend: `Repository` type 확장 + `/repositories` list 의 stat 카드 재구성 + linkFilter 버튼 그룹 (all/linked/unlinked) + 각 row 에 Linked(N)/Unlinked 배지. docs: `backend_api_contract.md` API-08 응답 필드에 linked_* 명시. 추적성 영향 — 신규 ID 없음 (분류 메타 추가). 검증: go build+vet+test ./internal/httpapi+store ✓ + tsc+lint+build ✓. | merged 2026-05-28 |
| 2026-05-28 | sprint `claude/work_260528-archived-hard-delete` — **archived application hard-delete 활성화 + status 전이 정책 자유화 + ProjectCreationModal status select 5종**. (1) backend `DeleteApplication` 신규 (store + ApplicationStore interface + memory fake) + `archiveApplication` handler 가 `?hard=true` 분기 (archived 가드 + audit `application.deleted` + 400 `application_not_archived`). project 측 hard-delete 는 이미 활성화됨. (2) `allowedStatusTransitions` 매트릭스 자유화 — 5종 (planning/active/on_hold/closed/archived) 끼리 모든 전이 허용 (`archived → *` 포함). updateApplication/updateProject 의 `hold_reason`/`resume_reason`/`archived_reason` 필수 가드 + `planning→active` active repo ≥1 가드 + `active→closed` critical 0건 가드 모두 제거. reason 필드는 audit details optional 메타로만 유지. 4 test 자유화 정책 expectation 으로 갱신 (`TestUpdateApplication_ActivationWithoutLinkedRepos` / `AnyStatusTransitionAllowed` / `HoldWithoutReason` / `TestUpdateProject_AnyStatusTransitionAllowed`). (3) `ProjectCreationModal` status select 옵션 5종 (Closed/Archived 추가) — initialData.status='archived' 인 project 가 편집 modal 에서 first option (planning) 으로 fallback 보였던 불일치 정정. (4) frontend `archiveApplication(id, hard?)` 시그너처 확장 + admin/catalog + admin/settings/applications 의 archive 진입점이 archived 상태 application 일 때 hard=true 호출 + confirm 메시지 "Permanently delete" 로 분기. project 동일 패턴. codex P2 정합 (post-push) — `projectsInsertQuery` 에 `archived_at` 자동 (Archived create 시 CHECK 위반 회피). docs: `backend_api_contract.md` API-46 (전이 정책 자유화) + API-47 (hard delete 활성화). 추적성 영향 — 신규 ID 없음 (운영 정책 변경). 검증: go build+vet+test ./internal/httpapi+store ✓ + tsc ✓. | sprint `claude/work_260528-archived-hard-delete`, PR pending |
| 2026-05-28 | sprint `claude/work_260528-application-project-carve` (PR #397) — **#395/#396 후속 carve 3건 일괄 진행**. (1) backend `updateProjectRequest.ApplicationID *string` 신규 — nil-skip / 빈 string → NULL (해제) / non-empty → 존재 검증 후 이전 (422 `application_id_invalid` 가드). `store.UpdateProject` SQL 에 `application_id = NULLIF($9, '')::uuid` 추가. handler `project.updated` audit 의 details 에 `application_id_from` / `application_id_to` 기록. (2) `domain.ApplicationRepository.LinkSource` 신규 필드 + `store.ListApplicationRepositories` UNION 의 direct/via_project SELECT 에 `link_source` 컬럼 추가 + `scanApplicationRepository` 갱신 + handler `applicationRepositoryResponse` 에 `link_source` (legacy "" fallback "direct"). memoryApplicationStore.ListApplicationRepositories 가 via_project link 의 LinkSource 명시. (3) frontend `ApplicationCreationModal` 의 "Connected Projects" 섹션을 read-only listing → 편집 가능 체크박스로 복원 (backend `application_id` PATCH 지원 활성화). standalone + 본 application 에 이미 연결된 project 합쳐 표시. handleSubmit 의 toAddProjects/toRemoveProjects PATCH 로직 복원. docs: `backend_api_contract.md` API-48 (link_source) + API-56 (application_id PATCH body + audit). 추적성 영향 — 신규 ID 없음 (#395/#396 의 알려진 gap 해결). 검증: go build+vet+test ./internal/httpapi+store ✓ + tsc+lint+build ✓. | merged 2026-05-28 |
| 2026-05-28 | sprint `claude/work_260528-build-success-rate-to-last-build` — **카드/리스트의 "빌드 성공률 %" 표시를 "마지막 빌드 상태"로 정합 (REQ-FR-APPDASH-001 명문화된 결정 실 구현)**. 시계열 차트(REQ-FR-APPDASH-005)는 그대로 유지. backend: `domain.ApplicationRollup.TargetBranchBuildStatus` (`healthy`/`broken`/`unknown`) + `domain.RepositoryActivity.LastBuildStatus`/`LastBuildAt` 신규 → store `ComputeApplicationRollup` 가 각 repo 의 build_runs 최신 1건 status 종합 derive (broken 우선), store `ListRepositoryActivity` 가 window 무관 latest build run resolution. handler `applicationRollup` (API-13.6) + `repositoryActivity` (API-51) 응답에 신규 필드 추가. frontend: `lib/utils/last-build.ts` (Application 단 derive view + Repository 단 raw enum view, vitest 13건) helper 추출, `applications/[id]` overview "Build Success: %" → "Last Build: Healthy/Broken/Unknown" 카드, `applications` list `Avg. Build Success %` stat → "Broken Builds"/"Healthy Builds" 카운트 2 카드 + 각 row "Build: X%" → "Build: Healthy/Broken" + "Build Success: %" 컬럼 → "Last Build" status, `repositories/[id]` "Build Success: %" → "Last Build" status. docs: `application_dashboard_concept.md` §1.1 build_success_rate 의미 명료화 + target_branch_build_status 신규 정의, `backend_api_contract.md` API-13.6 rollup spec + 응답 예시 + API-51 활동 응답 shape (last_build_status/last_build_at) 동기화. 추적성 영향 — 신규 ID 없음 (REQ-FR-APPDASH-001 결정의 실 구현). 검증: go build+vet+test ./internal/httpapi+store ✓ + tsc+lint+vitest 13+build ✓. | sprint `claude/work_260528-build-success-rate-to-last-build`, PR pending |
| 2026-05-28 | sprint `claude/work_260528-application-status-badge-mapping` (PR #394) — Application/Project lifecycle status badge **5종 명시 매핑 helper** 추출. `lib/utils/lifecycle-status.ts` 신규 + 3 페이지 (Application detail/list, Project detail) 적용. `active=success/planning=primary/on_hold=warning/closed=secondary/archived=glass` + fallback=secondary. vitest 6/6. docs-only refactor. | current session |
| 2026-05-28 | sprint `deepseek/work_260528-a-task-item-ingestion` — **Task Item Ingestion 도메인 요구사항 + 유스케이스 + 아키텍처 + API contract 정의 완료**. `docs/requirements.md` §5.10 신규 (REQ-FR-TASK-001..010 / REQ-NFR-TASK-001..004). `docs/planning/system_usecases.md` §2.16 신규 (UC-TASK-01..06). `docs/architecture.md` §12 신규 (ARCH-TASK-01..06). `docs/backend_api_contract.md` §17 신규 (API-94..96). 컨셉 문서 `docs/domain/integration-registry/task_ingestion_concept.md` §9 결정사항 반영. | current session | `docs/requirements.md` §5.10 신규 (REQ-FR-TASK-001..010 / REQ-NFR-TASK-001..004). `docs/planning/system_usecases.md` §2.16 신규 (UC-TASK-01..06). 컨셉 문서 `docs/domain/integration-registry/task_ingestion_concept.md` §9 결정사항 반영 (webhook_secret 분리 / raw+normalized status 병행 / adapter 정규화 / SEQ gap 탐지 / 범용 REST adapter 우선). | current session |
| 2026-05-28 | sprint `gemini/application-dashboard-dev` — **Application 개발 대시보드 API (API-93) 및 HSL 글래스모피즘(Glassmorphism) 기반 대시보드 UI 연동 완료**. `internal/httpapi/applications.go`에 `applicationDashboard` GET 핸들러를 구현하여 Application, Rollup Metrics, Repositories, Projects, DREQs 및 broken build runs와 history trend 데이터를 병렬/단일 일괄 집계하여 반환하도록 함. `routePermissionTable`에 OIDC/RBAC 권한 매핑 추가. 프론트엔드 `lib/services/application.service.ts`에 `getApplicationDashboard`를 추가하고, `applications/[id]/page.tsx`를 전면 리팩토링하여 프리미엄 HSL 테마 디자인, 빌드 실패 런 및 로그 딥링크, 리스크 감지 로드맵 카드, DREQ 리스트 노출 및 프로젝트 승격 모달 생성/API 연동 완료. | current session |
| 2026-05-28 | sprint `gemini/application-dashboard-concept` — **Application 개발 대시보드 요구사항, 유스케이스 및 상세 설계(ARCH/API) 정의 완료**. `docs/requirements.md` §5.9 요구사항 및 `docs/planning/system_usecases.md` §2.15 유스케이스(`UC-APPDASH-01..07`) 수립 완료. [architecture.md](../architecture.md) §11 상세 아키텍처 설계(`ARCH-APPDASH-01..06`) 및 [backend_api_contract.md](../backend_api_contract.md) §13.10 대시보드 API 명세(`API-93`) 공식 등록 완료. [report.md](./report.md) 내의 종합 추적성 매트릭스 동기화 완료. **(2026-05-28 claude 리뷰 반영)** main 머지 후 §11/§5.9/§2.15/§13.10/API-93 으로 재배치 — 기존 §10/§5.8/§2.14/§13.9/API-87 은 각각 Repository 도메인(#375 머지분) 및 integration test-connection 과 충돌하여 정정. 로컬 절대경로 → 상대경로 정정. ARCH-APPDASH-04 promote 시퀀스의 SCM 호출을 tx 밖으로 분리 + audit action 을 `dev_request.registered` 재사용으로 정정 (codex P1/P2). | current session |
| 2026-05-27 | sprint `claude/work_260527-codebase-review-roadmap-refresh` (PR #375) — **코드베이스 스냅샷 정합**. 헤더 범위 M0–M3 → M0–M7 + v1.0 + 최종 수정일 2026-05-16 → 2026-05-27. §2.2 Integration API 매핑에 **API-88/89/90** (scm-repositories list/import/create) + repository **draft→publish** (#368) note 추가. §3 매트릭스에 신규 도메인 row "SCM↔시스템 Repository 연동 + draft/publish" 추가 + Onboarding row 를 Carve B/C/D 완료 (#288/#289/#290/#291) 로 갱신. §4 ADR 인덱스에 누락된 **ADR-0022 (Keycloak 25.0 pin) + ADR-0023 (Keycloak 26.0 pin)** 2 row 추가 + ADR-0024 제목에 ticket-only cutover 반영. 분석 근거 = [코드베이스 스냅샷](../analysis/2026-05-27-codebase-snapshot/README.md) (01~06 + code/ + docs/ 항목별 상세). 추적성 영향 — 신규 ID 없음 (기존 API-88/89/90 + ADR-0022/0023 등재 누락분 catch-up). docs-only. | current session |
| 2026-05-27 | sprint `claude/work_260527-repositories-provider-id-consolidation` — **repositories SCM provider 참조 중복 정리 (#368 scm_provider ↔ #363 provider_id → provider_id 단일화)**. #368(draft publish)의 `repositories.scm_provider`(provider_key TEXT)와 #363(소유권 분리)의 `provider_id`(FK UUID)가 같은 "연동 SCM provider" 를 두 방식으로 표현(published repo 는 둘 다 set). **migration 000045**: scm_provider(key)→provider_id(FK) backfill 후 `scm_provider` 컬럼 DROP. domain.Repository `SCMProvider` 제거 + `ProviderKey`(derived) 추가. store: `CreateRepositoryDraft`(provider_id 저장, source='system'), `MarkRepositoryDraftPublishRequested`/`GetRepositoryByID`/`ListRepositories`/`UpsertRepository` 의 scm_provider 제거 + Get/List 는 `LEFT JOIN integration_providers` 로 provider_key derive. handler: `createRepositoryDraft` 가 `provider_key`(입력)→provider_id 해석(등록 SCM provider 검증, 미등록 404, 비-SCM 422), `requestRepositoryPublish` 가 `repo.ProviderID`→`GetIntegrationProviderByID`(구 GetByKey 대체). frontend: repository.service Repository `scm_provider`→`provider_id`+`provider_key`, createRepositoryDraft input `provider_key`, admin/catalog filter/prompt/table column. **provider_id(FK) 단일 출처 — import/create/draft/publish 일관**. 참고: project-companion `RepositoryCreatePayload.SCMProvider`(createRepositoryTx placeholder)는 별개 경로라 유지. 검증: go build+vet+test ./...(13 pkg) + tsc+eslint+vitest 55+build green. migration prefix 중복 0. | sprint `claude/work_260527-repositories-provider-id-consolidation`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-fix-migration-042-collision` — **오늘자 PR 전수 점검 hotfix**. (1) **migration 000042 prefix 충돌 정정 (codex #368 P1)**: #368(`draft repository publish flow`)이 `000042_projects_key_unique_active_only` 추가했으나 #363 의 `000042_repositories_source_provider` 와 prefix 충돌 (둘 다 머지됨) → #368 것을 **000044** 로 재번호 (000043=repositories_draft_status 점유, projects 전용이라 순서 무관). migration-prefix-lint 회복. (2) **disabled provider 거부 (codex #366 P2)**: scm-repositories gate(`scmProviderForCapability`)에 `!provider.Enabled` → 409 `integration_provider_disabled` 추가 (list/import/create API-88/89/90 공통, sync/webhook 정책 정합). 회귀 가드 `TestSCMRepositories_RejectsDisabledProvider`. **점검 결과**: #368 의 router(`POST /repositories`, `/publish`)/permissions/store 변경은 내 Phase A~C 와 충돌 없음 (route 비중복, ListRepositories SELECT 17/scan 17 정합 — #363 source/provider_id/description + #368 repository_status/scm_provider/publish_* 병합 clean). backend go build+vet+test ./... (13 pkg) + frontend tsc+eslint+vitest 55+build 전부 green. **soft note**: #368 `scm_provider`(repositories) 가 #363 `provider_id` 와 의미 중복 — 후속 정리 후보. 추적성 영향 — IMPL scm-repositories gate(disabled) + migration 000044 재번호; UT disabled guard 1. | sprint `claude/work_260527-fix-migration-042-collision`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-scm-repo-phase-c` — **Phase C (시스템→SCM 저장소 생성) + #363 codex P2×2 hotfix + #361 build-break hotfix**. **#363 P2 hotfix**: (P2#2) `createRepositoryTx`(project 동반 repo)가 `source='system'` 세팅 — 빈 Source=scm 기본이 소유권 분리 깨던 것 정정. (P2#1) `isGiteaCompatibleProvider` — credentials_ref `provider_sdk:<vendor>` 가 비-gitea(github/gitlab/bitbucket)면 import/create/list 거부(422 `integration_provider_not_gitea_compatible`), gitea/forgejo/gogs/미명시(hmac) 허용. **Phase C**: gitea `Client.CreateRepo(ctx, owner, opts)` (owner 빈값=POST /user/repos, 있으면 /orgs/{owner}/repos) + newRequest body 지원 + **API-90** `POST .../create-repository` (push capability + gitea-compat gate → CreateRepo → `UpsertRepository(source=system, provider_id)` → 201) + routePermissionTable edit + frontend `CreateScmRepositoryModal` + ProviderTable 'New Repo' 액션(scm+push) + integration.service `createScmRepository`. **#361 build-break hotfix**: #361(merge-commit, CI bypass)이 frontend tsc 2 에러 유입 (admin/catalog AdminApplication→Partial<Application> status 불일치 / ProjectCreationModal numericRepositories repository_id number|undefined) → main build 깨짐 → cast + `?? 0` 정정. 추적성 영향 — API: API-90 신규; IMPL: gitea CreateRepo + integration_scm_repositories(create + gitea-compat gate) + createRepositoryTx source; UT: gitea CreateRepo 3 + handler create-repository 3 + gitea-compat gate 1. 검증: go build+vet+test ./... + tsc+eslint+vitest 55 + npm run build ✓. Phase C 로 SCM↔시스템 repository 양방향(import+create) 완료. | sprint `claude/work_260527-scm-repo-phase-c`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-scm-repo-sync` — **SCM ↔ 시스템 repository 연동 (Phase A+B) + capability 기능 gate 전환**. **Phase A (소유권 분리)**: migration 000042 `repositories.{source,provider_id,description}` + domain `Repository.{Source,ProviderID,Description}` + `RepositorySourceSCM/System` + store `UpsertRepository` ON CONFLICT 가 SCM mirror 필드만 갱신·system-owned(description)·source/provider_id 보존(COALESCE) + `ListRepositories` scan 확장 + `ListRepositoriesByProvider` 신규. gitea SyncWorker mirror 시 source=scm + provider_id 태깅(`syncAllWith` providerID 인자). **Phase B (inbound import)**: **API-88** `GET .../scm-repositories`(원격 목록+imported 여부) + **API-89** `POST .../import-repositories`(선택 import, SCM 재조회 값으로 upsert, source=scm/provider_id) + routePermissionTable(view/edit) + frontend `ImportRepositoriesModal` + ProviderTable Import 액션(scm+pull) + integration.service `listScmRepositories`/`importScmRepositories`. **capability gate 전환**: `providerHasCapability` 헬퍼 — import=pull / sync(API-72)=pull|sync 요구(미충족 422 `integration_capability_not_enabled`). 추적성 영향 — API: API-88/89 신규 + API-72 capability gate; IMPL: IMPL-int-handler-01(scm-repositories) + store repositories 소유권 분리 + gitea worker + migration 000042; UT: gitea/httpapi `ListSCMRepositories`/`ImportSCMRepositories`(5) + capability gate + memoryStore parity. 검증: go build+vet+test ./... + tsc+eslint+vitest 55 + npm run build ✓. Phase C(outbound 생성) deferred. | sprint `claude/work_260527-scm-repo-sync`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-gitea-auth-env-leak-hotfix` — **codex #358 P1 hotfix (env token 유출 방지)**. `resolveSyncConfig` 가 명시 provider 해석 후에도 자격증명 미설정(agent / 미완성 basic·oauth2)이면 worker 전역 `GITEA_TOKEN` fallback 을 유지한 채 provider 의 `base_url` 로 sync → provider 고유 host 에 전역 토큰 송신 (잘못된 계정 인증/토큰 유출). 정정: 명시 provider 해석 성공 시 그 provider 의 `base_url` + `ResolveOutboundAuth()` 만 사용, env 혼용 제거 (env fallback 은 providerID 빈 값/lookup 실패 시에만). dead `outboundAuthConfigured` 제거. 회귀 가드 `TestSyncWorker_ProcessOnce_NoEnvTokenLeakForExplicitProvider`. 검증: go build+vet+gitea/httpapi ✓. | sprint `claude/work_260527-gitea-auth-env-leak-hotfix`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-integration-auth-mode` — **외부 연동 webhook 헤더 alias + auth_mode 별 outbound 자격증명 모델 (full)**. (A) **webhook 헤더 정정**: 범용 ingest(`API-73`)가 DevHub-native `X-Integration-*` 만 읽어 Gitea 가 보내는 `X-Gitea-Signature`/`X-Gitea-Event`/`X-Gitea-Delivery` 를 못 받던 버그 정정 — signature/event/delivery 각각 `X-Integration-*`→`X-Gitea-*`→`X-Gogs-*` fallback(`firstHeader`). (B) **auth_mode UI 불일치 해소 (full)**: `auth_mode` 가 프론트/백엔드 어디서도 분기하지 않던 inert 필드였음. **migration 000041** `integration_providers.{auth_username,auth_client_id,auth_token_url,auth_secret}` + domain `OutboundAuth`+`ResolveOutboundAuth()` + store(scan/SELECT×3/INSERT/UPDATE) + `API-70/71` 에 4 필드 (auth_secret **write-only** `auth_secret_set`, 나머지 노출) + `invalid_auth_token_url` 검증. gitea client `AuthHeader` + `AuthorizationHeader`(token/basic/app_password/oauth2 client-credentials/agent) + `NewClientForAuth` + worker `resolveSyncConfig`→`OutboundAuth` + `outboundAuthConfigured`. frontend ProviderModal 이 auth_mode 별 자격증명 입력 필드 렌더 (`SecretField` 공통) + `outboundAuthPayload`. 추적성 영향 — API: API-70/71 auth_* 필드 확장 + API-73 헤더 alias; IMPL: IMPL-int-handler-01 + IMPL-int-domain-01(OutboundAuth) + gitea client/auth/worker 확장 + migration 000041; UT: gitea `auth_test`(7) + worker `BasicAuthOutbound` + handler `BasicAuthCredentials`/`InvalidAuthTokenURL` + `GiteaNativeHeaders`(webhook). 검증: go build+gitea/httpapi/domain/store + tsc+eslint+vitest 17 + npm run build ✓. | sprint `claude/work_260527-integration-auth-mode`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-gitea-provider-token` — **Gitea 연동 full (provider api_token 슬롯 + 다이얼로그 + sync per-provider)**. 검토 결과 Gitea 는 outbound sync(API token)와 inbound webhook(secret) 두 시크릿이 필요한데 모델 슬롯이 1개였음. **Phase 1** (backend): **migration 000040** `integration_providers.api_token` + domain.APIToken + store(scan/SELECT×3/INSERT/UPDATE) + `API-70/71` request `api_token` + **write-only 응답** (`api_token_set`(bool)만, raw 미노출). **Phase 2** (frontend): ProviderModal 에 API Token 필드 (pull/sync capability provider 만 노출, password+show/hide, edit blank=keep) + `integration.types` Create/Update Input + `api_token_set`. **Phase 3** (backend): gitea SyncWorker 가 queued job 의 provider `base_url`+`api_token` 사용 (`resolveSyncConfig`, env fallback) — UI 등록 Gitea 가 실제 sync 동작. main.go 가 pgStore 있으면 worker 항상 기동 (env 미설정 무관). 추적성 영향 — API: API-70/71 `api_token` 확장 (write-only); IMPL: IMPL-int-handler-01 + gitea SyncWorker(resolveSyncConfig) 확장 + migration 000040; UT: handler `APITokenWriteOnly` + gitea `PerProviderConfig`. 검증: go build+vet+gitea/httpapi/store + tsc+eslint+vitest 55 + npm run build ✓. | sprint `claude/work_260527-gitea-provider-token`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-external-integration-enhance` — **외부 연동 등록 UX 고도화** (#1~#5). 등록 UI(ProviderModal) 검토 후 5 포인트 해소. **#1+#3+#4** (frontend): vendor 템플릿 7종 (`integration-provider-presets.ts`, gitea/github/gitlab/bitbucket/jira/jenkins/bamboo + custom) → provider_type/auth_mode/signature/capabilities 자동 + credentials_ref 를 strategy(hmac_sha256/provider_sdk/shared_token)+secret 분리 입력으로 조합 (`composeCredentialsRef`/`parseCredentialsRef`, 백엔드 verifyIntegrationWebhookSignature 정합) + capabilities 체크박스. **#2** (full-stack): `API-70/71` 에 `base_url` 필드 + **migration 000038** `integration_providers.base_url` + domain/store(scan/SELECT×3/INSERT/UPDATE)/validation(`invalid_base_url`). **#5** (full-stack): **API-87** `POST /api/v1/integration/test-connection` (base_url reachability GET, system_admin, SSRF 사내 internal 전제 수용) + ProviderModal Test 버튼. 추적성 영향 — API: API-87 신규 + API-70/71 base_url 확장; IMPL: IMPL-int-handler-01 확장 (test-connection + base_url) + migration 000038; UT: handler test 6 신규 (base_url round-trip/invalid + test-connection reachable/invalid) + frontend presets vitest 14. 검증: go build+vet+httpapi/store + tsc+eslint+vitest 9파일/55 + npm run build ✓. | sprint `claude/work_260527-external-integration-enhance`, PR pending |
| 2026-05-27 | sprint `claude/work_260527-adr0024-ticket-store` (본 PR) — **ADR-0024 §6 carve 6 (multi-instance ticket store) resolved 추적성 정합**. `IMPL-realtime-02` 발급 (`realtime_ticket.go` `realtimeTicketStore` interface + in-memory + PG `DBRealtimeTicketStore` + selector / `store/realtime_tickets.go` / migration `000035_create_realtime_tickets`) — PR #336 in-memory 1차 + 본 sprint PG 백킹 (Redis 미사용 스택, `DELETE ... RETURNING` 인스턴스 간 single-use 원자). §2.4 IMPL-realtime-XX 서브 표 row + §3 실시간 row IMPL/UT/ARCH cell 갱신 (ADR-0024 ticket pattern). 추적성 영향 — IMPL: realtime-02 신규; UT: realtime_ticket 단위 + store integration; ADR-0024 §6 carve 6 ✅ resolved. | current session |
| 2026-05-27 | sprint `claude/work_260527-gitea-traceability` (본 PR) — **Gitea SCM 동기화 워커 (PR #341) 추적성 정합**. 신규 IMPL `gitea-03` (`client.go` REST pull client) + `gitea-04` (`syncer.go` issue/PR 정규화 upsert) + `gitea-05` (`worker.go` `SyncWorker` + `integration_sync_jobs` 큐 소비 + `main.go` wire) 발급 + §2.4 `IMPL-gitea-XX` 서브 표 신규 (기존 `gitea-01..02` webhook foundation 과 push/pull concern 분리 명시 — `signature.go` 는 2026-05-02 `b1b6849`, sync worker 3 파일은 #341 `473c4ec` git 확인). 신규 UT `gitea-02..04` (`client_test`/`syncer_test`/`worker_test`). §3 매트릭스 신규 row "Gitea SCM 동기화 워커 (pull)" — RM-M4-06 1차 구현 매핑 (hourly/reconciliation 잔여 = issue #231) + 기존 webhook row 제목 "(push)" 명시. §2.3.2 RM-M4-06 정의 row 1차 구현 표기 + §2.4/§2.5 인덱스 요약 (`gitea-01..05` / `gitea-01..04`) + §5.1 gap row 추가. 의존 store/migration 은 기존 IMPL-int-store-01 / IMPL-int-migration-01 cross-cut (신규 ID 불필요). 추적성 영향 — IMPL: gitea-03..05 신규; UT: gitea-02..04 신규; ROADMAP: RM-M4-06 1차 구현. docs-only. | current session |
| 2026-05-21 | sprint `claude/issue-272-onboarding-backend` (PR #278) — **RM-ONBOARD-01 Carve A backend activated**. migration 000033 (`users.onboarding_completed_at` + `review_status` + bi-implication CHECK + partial index) + domain model 확장 (AppUser + UpdateUserInput + OnboardingSubmitInput + ReviewStatus enum) + store layer (4 SELECT 갱신 + SubmitOnboarding/ConfirmUserReview/SearchOrgUnits 신규 + UpdateUser ReviewStatus 처리) + authenticateActor 의 feature flag conditional (default OFF = ADR-0020 sub-carve B lazy 유지, ON = token-only actor) + onboardingGate middleware (allowlist 4 endpoint) + 5 handler (API-83 POST /me/onboarding 201 / API-84 GET /organizations/search / API-85 PATCH /me / API-86 POST /admin/users/:id/review / API-32 GET /me 응답 확장) + permissions.go (4 entry) + main.go env wire (`DEVHUB_ONBOARDING_GATE_ENABLED` default false) + UT 12건. Self-review Stage 3 보강 — handler-level flag guard (P1 #1) + `isValidLazyRole` → `onboarding_roles.go` 분리 (P1 #2) + `meResponse.ReviewStatus` `*string` (P2 #3) + `routePermissionTable` Resource = `Organization` 정합 (P2 #4) + ADR-0021 §7 + 본 §3 매트릭스 IMPL/UT cell 채움 (P2 #5). | current session |
| 2026-05-21 | sprint `claude/onboarding-impl-carve-plan-2026-05-21` — **Onboarding 도메인 IMPL carve plan + RM-ONBOARD-01..04 발급**. `docs/domain/onboarding/impl_plan.md` 신규 (4 carve scope + dependency graph + milestone 매핑 + DoD + risk + 후속 carve) + `docs/development_roadmap.md` §3 M7 신규 + `docs/planning/release_v1_roadmap.md` §2.3 Onboarding 도메인 + §3.3 P2-8..11 IMPL carve 4건 + §4.2 M-v1.1 매트릭스 갱신. **ADR-0021 reservation 정정** — HA Phase 2 (ADR-0019 §5.3 (6)) / off-boarding Phase 2 (ADR-0008 §6) / groups → role Phase 2 (ADR-0019 §5.3 (8)) / keycloak_failover.md §9 §11 에서 ADR-0021 reservation 표현 (총 9 위치) 을 "별도 ADR 후보 (ADR-0021 은 Onboarding 으로 2026-05-21 발급됨)" 로 정합 갱신. §3 Onboarding 매트릭스 row 의 ROADMAP cell + IMPL/UT/TC `미진입` 유지 (Carve D 발급) + §6 changelog. concept §10 changelog + §9 next-phase #4 진입 + §6.1 cross-link. | current session |
| 2026-05-21 | sprint `claude/onboarding-codex-hotfix-2026-05-21` (PR #270) — **codex review hotfix**. P1 (PR #267): `backend_api_contract.md` §16.3 응답 description "INSERT users" → "row INSERT (DB 미등록) 또는 UPDATE (관리자 사전 등록된 미완료)" (REQ-FR-ONBOARD-008 + UC-ONBOARD-08 정합). P2 (PR #269): `adr/0020-account-user-management-boundary.md` 메타 헤더 3 line (상태 / partial superseded by / supersession 안내 box) supersession scope "4 위치" → "5 위치" (§6.1 explicit). PR #265 / #266 codex review 는 self-review Stage 3 보강으로 이미 main 정합 — 응답 comment 만 게시. | current session |
| 2026-05-21 | sprint `claude/onboarding-adr-2026-05-21` — **ADR-0021 발급 (Onboarding self-service unit selection + lazy auto-create supersession)**. `docs/adr/0021-onboarding-self-service-unit-selection.md` 신규 — 컨셉 (PR #260 + #265) + 요구사항 (PR #266) + 설계 (PR #267) 의 누적 결정 명문화. §3.1 책임 경계 확장 + §3.2 3 tier 상태머신 + §3.3 lazy auto-create 폐기 (ADR-0020 §3.2/§4.1 sub-carve B/§4.2/§6.1/§6.2 5 위치 partial supersession) + §3.4 skip-and-resume + §3.5 backend gating + frontend 3분기. ADR-0020 메타 헤더 + §3.2/§4.1/§4.2/§6.1/§6.2 5 위치에 inline supersession banner 추가 (메모리 `feedback_adr_supersession_pattern` 패턴). 본 §4 ADR 인덱스에 ADR-0021 row 추가 + ADR-0020 partial supersession 명시. §3 Onboarding 매트릭스 row 의 ADR cell 갱신. concept §10 + concept §9 next-phase #3 done 마크. ADR-0020 의 핵심 결정 (옵션 A 책임 경계 / `rbac_subject_roles` 제거 / service account 권한 축소) 은 변경 없이 유지. | current session |
| 2026-05-21 | sprint `claude/onboarding-arch-2026-05-21` — **Onboarding 도메인 ARCH phase 진입**. `docs/planning/system_usecases.md` §2.13 신규 (UC-ONBOARD-01..11) + `docs/architecture.md` §9 신규 (ARCH-ONBOARD-01..06: 컴포넌트 / 상태머신 3-tier / Gating 정책 / RBAC route permission / 데이터 모델 — users.onboarding_completed_at + review_status + CHECK 제약 / Audit action 카탈로그 3종) + `docs/backend_api_contract.md` §16 신규 (API-83 POST /me/onboarding + API-84 GET /organizations/search + API-85 PATCH /me + API-86 POST /admin/users/:id/review, **spec staged**) + API-32 (GET /me) / API-33 (POST /users) 응답 shape · 동작 확장 명시. §2.1.5 UC 인덱스 + §2.2 ARCH/API 인덱스 + §3 Onboarding row UC/ARCH/API cell 채우기. concept §10 + requirements §5.7 cross-link 갱신. ADR-0020 §3.2 확장 vs 신규 ADR 결정은 본 sprint scope 외 — IMPL carve 전에 별도 결정. IMPL/UT/TC 발급은 후속 carve. | current session |
| 2026-05-21 | sprint `claude/onboarding-requirements-2026-05-21` — **Onboarding 도메인 requirements phase 진입**. `docs/requirements.md` §5.7 신규 (REQ-FR-ONBOARD-001..012, 12개 — 1차 진입 + 입력 항목 + 제출 트랜잭션 + 소속 검색 UX + 검토 상태머신 + 3단계 접근 + self-service 소속 변경 + 관리자 사전등록 + Backend gating 403 + Frontend 3분기 + skip-and-resume + 후속 carve; REQ-NFR-ONBOARD-001..008, 8개 — UI 언어 + a11y + 제출/검증 UX + 모바일 제외 + 단일 포트 정합 + migration + audit 정책 + 테스트 시드 세트). 본 §2.1 인덱스에 Onboarding row + §3 매트릭스에 Onboarding 도메인 row 추가 (UC/ARCH/API/IMPL/UT/TC 는 후속 carve 마킹). concept 결정 (PR #260 + PR #265) 의 acceptance criteria 변환. ADR-0020 §3.2 확장 후보 (concept §7.2) 는 requirements phase 후속 결정. | current session |
| 2026-05-20 | sprint `claude/work_260520-l-213-jwks-stale-expiry` (Phase 3 sub-carve D) — **ADR-0020 sub-carve D 실 구현**. 5 commit. (1) `keycloak_verifier.go` cache struct 확장 + `readStaleCachedKeys` helper. (2) `fetchJWKS` 흐름 변경 — network fetch fail 시 stale fallback 분기 + log + metric observe. `fetchAndCacheJWKS` 별도 함수로 분리. (3) `internal/auth/metrics.go` 신규 (sync.Once + 2 metric `devhub_jwks_stale_while_error_total{result}` CounterVec + `devhub_jwks_stale_age_seconds` Histogram). (4) Config 신규 field `OIDCJWKSMaxStaleDuration` (env `DEVHUB_OIDC_JWKS_MAX_STALE_DURATION`) + main.go wire. (5) 회귀 test 4건 (StaleWhileError_KeycloakUnreachable / StaleExpired_Fails401 / FreshCache_NoStaleFallback / StaleFallback_DefaultMaxStale). backward compatible — MaxStaleDuration 0 시 defaultJWKSMaxStale (24h) 적용. errKidMismatch 흐름 (sprint -r) 그대로 유지. | current session |
| 2026-05-20 | sprint `claude/work_260520-k-212-event-listener-users-sync` (Phase 3 sub-carve C) — **ADR-0020 sub-carve C 실 구현**. 5 commit. (1) `KeycloakAdminClient.GetUserDetails` + `GetUserGroups` 신규 (admin event 시 최신 user state fetch). (2) `audit/user_sync.go` 신규 (207 lines) — `SyncUserProfile`/`SyncUserMembership`/`MarkUserDeactivated` + helper + narrow interface. (3) `keycloak_event_puller.go` 확장 — `KeycloakEventPullerOptions.UserSync UserSyncCallback` + `classifyAdminEventForSync` + `mapAdminEventToAudit` GROUP_MEMBERSHIP CREATE/DELETE 2 row 추가 + admin event loop sync 분기. (4) `audit/metrics.go` 신규 metric 3종 (devhub_keycloak_user_sync_total/_errors_total/_lag_seconds Histogram) + 회귀 test 4건. (5) `main.go` wire (UserSync dispatcher + metric observe + error log + backward compat). group composite role priority filter (system_admin > team_manager > manager > developer) 정합. soft delete (users.status=deactivated, ADR-0020 §5.3.1 P1-2). | current session |
| 2026-05-20 | sprint `claude/work_260520-n-214-service-account-min-role` (Phase 3 sub-carve E) — **ADR-0020 sub-carve E 실 구현 (옵션 A 정공법, 전면 호출처 제거)**. 5 commit. (1) `organization.go` `POST /api/v1/users` `req.Password` 분기 제거 + `createUserRequest.Password` field 폐기 + `audit_logs.details` dead key `kratos_id` 제거 (3 + 20 line). (2) `main.go` `seedLocalAdmin` 함수 + `seedOrgStore` interface + 호출 (cfg.AuthDevFallback 분기) 완전 제거. `main_test.go` 전체 삭제 (3 test + idpAdminFake + orgStoreFake, 209 line). (3) `KeycloakAdminClient.CreateIdentity` + `UpdateIdentityPassword` + `SetIdentityState` + `DeleteIdentity` 4 method 제거 (51 line). `keycloakIDFromLocation` dead helper 제거. `IdentityAdmin` interface 의 write method 4건 제거 (`FindIdentityByUserID` 만 유지, view-users role 만 요구). `MockIdentityAdmin` write fields/methods 제거. `keycloak_admin_client_test.go` `TestKeycloakAdminClient_CRUDFlow` → `TestKeycloakAdminClient_FindAndAuthFlow` (read-only 흐름). (4) `docs/infrastructure/keycloak-idp/service_account_min_role.md` 신규 (현황 매트릭스 + 옵션 A/B/C 비교 + 옵션 A 채택 + 운영 SOP) + ADR-0020 §4.1 sub-carve E done + 본 §6 row + 변경 이력. (5) memory state + sprint -n state. **service account 가 manage-users 없이 동작 가능** — `view-users` (FindIdentityByUserID + user_sync.go GetUserDetails/Groups) + `view-events` (event puller) 만 요구. dev 부트스트랩 = Keycloak admin console 1회 수동 시드 (실 운영 = HRDB ETL push + group composite role). | current session |
| 2026-05-20 | sprint `claude/work_260520-i-209-accounts-deprecation` (Phase 3 sub-carve B) — **ADR-0020 sub-carve B 실 구현**. (Commit 1) worker_division.md §2.5 branch 명명 규칙. (Commit 2) `/api/v1/accounts/*` 4 endpoint 폐기 — `accounts_admin.go` 삭제 + router 4 route 제거 + permissions.go 4 entry 제거 + accounts_admin_test.go 삭제 + identity_resolver_test.go 의 `TestCreateAccount_EagerBackfillsIdPSubject` 제거 + testhelpers_test.go 신규 (doJSON 보존). (Commit 3) lazy auto-create 실 구현 — `lazy_auto_create.go` 신규 + `AuthenticatedActor` Email/DisplayName 필드 추가 + `keycloak_verifier.go::extractDisplayName` 신규 + `authenticateActor::ErrNotFound` 분기에서 `lazyAutoCreateUser` 호출. 신규 audit action 2종 (`account.lazy_provisioned` / `user.role_default_assigned`). 회귀 test 5건 + 기존 test 3건 admin pre-seed fix. backend `go test ./...` PASS. (Commit 4 본) docs (backend_api_contract §10.2 폐기 마킹 + IMPL-account-* strikethrough + IMPL-account-05 신규 + ADR-0020 §4.1 sub-carve B done 마킹) + memory. | current session |
| 2026-05-20 | sprint `claude/work_260520-d` Stage 3 보강 #2 — **PR #205 codex review P1 응답** (organization.go `validAppRoles` 에 `team_manager` 추가, 회귀 test 2건). sprint -d 의 `/rbac/subjects/:id/roles` 폐기 후 `team_manager` API 할당 불가능 회귀 해소. ADR-0020 §5.5 신규 hotfix 섹션 + 변경 이력 row. custom role 임의 할당은 결정 C 의 event listener (sprint -f) 자연 흡수. | current session |
| 2026-05-20 | sprint `claude/work_260520-d` (Phase 3 진입, sub-carve A) — **ADR-0020 발급 + `rbac_subject_roles` 완전 제거**. `docs/adr/0020-account-user-management-boundary.md` 신규 (외부 Keycloak 가정 하의 계정/사용자 책임 경계 — Phase 2 명시 결정 6건 종합, 옵션 A 전면 폐기 채택, sub-carve B~F 분리 plan). `docs/domain/auth-session/account_redesign.md` §6 Phase 3 실행 계획 신규 (sub-carve 분담 표 + sub-carve A 변경 매트릭스 + Strangler Fig 패턴 — sub-carve B 진입 시 적용). backend `rbac.go` (handler 2개 + interface method 2개 + audit action const + wire struct 제거) + `router.go` (2 route 제거) + `permissions.go` (2 routePermissionTable entry 제거 + ADR-0020 reference 주석) + `postgres_rbac.go` (impl 2개 제거) + `postgres_rbac_test.go` (Get/Set 테스트 3건 제거, Delete-in-use 테스트는 CreateUser 의 `Role: domain.AppRole(roleID)` 로 재구성) + `rbac_test.go` (fake mock 2개 + handler test 3건 제거). 본 §2.2 RBAC API 매핑 표 API-30/31 strikethrough + §2.4 IMPL-rbac-01/02 책임 갱신 + §4 ADR-0020 row 추가. 추적성 영향: REQ/ARCH/RM 없음 / API-30/31 폐기 / IMPL-rbac-01 (handler 4 endpoint) + IMPL-rbac-02 (store 6 method) 갱신 / UT `TestRBAC_GetSubjectRoles_*` + `TestRBAC_SetSubjectRole_*` (3건) 제거 / TC 없음 (e2e 미구현). go build + go test (httpapi + store) PASS. | current session |
| 2026-05-13 | 1차 작성 (sprint `claude/work_260513-c`). Phase 1–6 분석 결과 통합 + 도메인 그룹 13행 매트릭스 + Gap 요약 §5. |
| 2026-05-13 | 리뷰어 모드 2-pass: §3 의 CI/거버넌스 행을 PR 단위 산출 (PR #86 / #87 / #88 / 본 PR) 로 명세화. §4 ADR-0003 행에 ※ 노트 추가 — 본 PR 매트릭스가 PR #88 미머지 상태와 정합하지 않을 가능성 자체 인지. |
| 2026-05-13 | 후속 sprint `claude/work_260513-d`: ADR-0003 가 main 에 머지된 후 §4 ADR-0003 행을 정상 link 로 활성화 + §3 의 ※ 마킹 제거. §5.1 / §5.2 / §5.3 을 표 형식으로 통일 + 상태(open/closed) 컬럼 도입. §5.2 의 auth.spec.ts TC 미흡수 항목과 §5.3 의 frontend_integration_requirements §3.8 deprecation 항목을 closed 처리. 본 sprint 의 메타 헤더 표준화 commit 도 §3 CI/거버넌스 행에 추가. |
| 2026-05-13 | sprint `claude/work_260513-e` (A 묶음, M1 PR-D 정합 마무리): backend-core 의 `writeRBACServerError` → `writeServerError` 통합 (`internal/httpapi/rbac.go` helper 제거 + 11 호출 일괄 치환). `requireRequestID` 미들웨어에 caller-supplied X-Request-ID validation 추가 — 정규식 `^[A-Za-z0-9_-]{1,128}$`, 실패 시 server-generated fallback (work_260512-j 발견 항목 closed). request_id 를 표준 ctx key (`requestIDCtxKey{}`) 에도 stash + `requestIDFromContext` / `logRequestCtx` ctx-aware helper 추가, `kratos_login_client.go` 의 untraced `log.Printf` 2건 + `kratos_identity_resolver.go` 1건을 ctx-aware 로 치환 (logRequest 의 untraced fallback 해소 항목 closed). 단위테스트 11건 추가 (validation 양/음 경로 + ctx 전파 + logRequestCtx percent-safety + 미들웨어 e2e). PR #91 (Pass 1 review 에서 `writeAuthLoginServerError` 도 같은 wrapper 발견 + 보강 commit 으로 흡수). |
| 2026-05-13 | sprint `claude/work_260513-f` (B 묶음, RBAC 1차): `backend_api_contract.md` §12.2~§12.10 의 9 헤더에 `(API-26..31, 38..40)` 본문 ID 노출 (`document-standards.md` §8 우선순위 3 RBAC 도메인 1차 적용). 본 §2.2 에 RBAC API 매핑 표 + 본 §2.4 에 IMPL-rbac-01..04 책임 정의 (handler / store / enforcement / cache) 서브 표 추가. §3 RBAC 행 IMPL 컬럼 endpoint-IMPL 1:1 매핑. §5.2 의 "RBAC API §12 IMPL 정밀 매핑" 항목 closed. Pass 1 review 보강으로 §3 RBAC 행을 ID 범위 + §2 서브 표 참조 패턴으로 정리 (PR #92). |
| 2026-05-13 | sprint `claude/work_260513-g` (B1 auth 도메인 2차): `backend_api_contract.md` §11.3 `(API-19)` + §11.5 표에 API ID 컬럼 (`API-20..24, 35`) + §11.5.1 `(API-35)` 본문 ID 노출. 본 §2.2 에 Auth API 매핑 표 + 본 §2.4 에 IMPL-auth-01..07 책임 정의 (verifier / actor / 5 endpoint handler) 서브 표 추가. §3 인증 / 회원가입 / 계정 관리 행에 §2 서브 표 참조 노트 — 회원가입과 계정 관리 행은 cross-cut (API-23 / API-35) 명시. §11.2 외부 의존성 + §11.4 planned 는 매핑 제외 (`conventions.md` §5.2). |
| 2026-05-13 | sprint `claude/work_260513-h` (B4: X-Devhub-Actor 폐기 ADR): [ADR-0004](../adr/0004-x-devhub-actor-removal.md) 발급. M0 SEC-4 + M1 PR-D Bearer token verifier 도입으로 ADR-0001 §8 #4 trigger 가 이미 충족됐음을 ex-post-facto 명문화. §4 ADR 인덱스에 ADR-0004 행 추가. §5.3 "X-Devhub-Actor 완전 제거 trigger" closed. `docs/architecture.md` line 174 + `docs/adr/0001-idp-selection.md` §8 #4 인라인 갱신 + `backend-core/internal/httpapi/me.go` line 16 주석 잔재 정리. 회귀 방지 negative 테스트 4 파일 (audit_test / commands_test / auth_test / me_test) 그대로 유지. Pass 1 review 보강으로 `backend_api_contract.md` §8/§9.1/§9.2/§11.3 + `frontend_integration_requirements.md` §3.5 + `environment-setup.md` §2.4 의 spec 잔재 6 위치도 함께 정리 + ADR-0004 §5 정정. |
| 2026-05-13 | sprint `claude/work_260513-i` (대형 묶음 B1~D5): B1 추가 5 도메인 (account / org / command / audit / infra) 본문 ID 노출 + §10.1 "본문 spec 부재 endpoint" carve out 표 + §2.2 4 신규 서브 표 (Infra/Dashboard, Pipelines, Realtime/Command/Audit, Account/Org/Me) + §2.4 6 신규 IMPL 서브 표 (account / org / command / audit / infra+dashboard+realtime+me). §3 6 행 (감사 / 명령 / 실시간 / 인프라 / Webhook+gitea / 대시보드+me + 조직) 갱신 (ID 범위 + §2 서브 표 참조). §5.1 의 명령/인프라 행은 "카탈로그 closed, spec ts open" 으로 갱신. §5.2 frontend Vitest 부재 closed (C1 1차 도입) + 본문 spec 부재 endpoint open (account / users / org). B2 archive/AGENTS.md + archive/split_checklist.md deprecated 마킹. C1 ThemeToggle Vitest 3 tests. C2 test_cases_m3_command_infra.md 신규 (TC-CMD-* / TC-INFRA-* 5 TC 카탈로그, spec ts 작성은 carve out). D5 [ADR-0005](../adr/0005-workflow-lint-actionlint.md) 발급 + `.github/workflows/ci.yml` 에 `workflow-lint` 잡 추가 (raven-actions/actionlint@v2). |
| 2026-05-13 | sprint `claude/work_260513-j` (M3 진입 전 잔여 후속 일괄): D6 inbound X-Devhub-Actor 거부 (auth.go) + [ADR-0006](../adr/0006-x-devhub-actor-reject-inbound.md) — silent ignore → 400 + `code=x_devhub_actor_removed`. 회귀 4 테스트 의도 갱신 (`me_test`, `audit_test`, `auth_test`, `commands_test`). [ADR-0007](../adr/0007-rbac-cache-multi-instance.md) RBAC cache 다중 인스턴스 일관성 결정 (PG LISTEN/NOTIFY 채택, 구현은 M3 carve out). B2-2 deprecated 추가 마킹 (backend/requirements_review.md / DOCUMENT_INDEX.md / assessment.md). 매트릭스 §2.2 nit 정정 (API-XX ID 공간 + composite + 결손 명시). `backend_api_contract.md` §10.2 (API-25 accounts admin) + §10.3 (API-33 users CRUD) + §10.4 (API-34 organization) 본문 spec 절 신설 — §5.2 "본문 spec 부재 endpoint" closed. AuthGuard smoke Vitest 1 test (loading 상태, 나머지 mock-heavy 본격은 M3+). TC-INFRA-RENDER-01 spec ts (정적 렌더 검증, 인터랙션 TC 는 carve). §4 ADR 인덱스에 ADR-0006 + ADR-0007 행 추가. §5.3 의 X-Devhub-Actor + RBAC cache 항목 closed 갱신. |
| 2026-05-13 | sprint `claude/work_260513-k` (M3/M4 drift 정합): M3/M4 정의의 3 source 사이 drift 해소. `docs/development_roadmap.md` §3 를 single source-of-truth 로 명시 (이전 매트릭스 RM-M3 정의가 development_roadmap M4 항목을 cross-cut 한 drift 상태). development_roadmap §3 의 M3 헤더 중복 + 본문 정리 — M3 잔여 = Sign Up + 인사 DB + 조직 polish (사용자/조직 관리 대부분은 M2 1차 완성 sprint PR #85 가 흡수). 매트릭스 §2.3 갱신 + §2.3.1 RM-M3 정의 표 (3 항목: Sign Up, 인사 DB, 조직 polish) + §2.3.2 RM-M4 정의 표 (9 항목 신규 발급: WebSocket 확장/replay, command status WS UI, AI Gardener, Suggestion Feed, Gitea Pull worker, System Admin 대시보드, RBAC cache LISTEN/NOTIFY, 외부 SSO). 매트릭스 §3 의 회원가입 / 명령 lifecycle / 실시간 / 인프라 행 RM-M3 → RM-M4 정합. M4 row 정의 갱신 (스코프 밖 → planned, 정의: §2.3.2). |
| 2026-05-13 | sprint `claude/work_260513-l` (M3 진입 1차 — RM-M3-01..03): **RM-M3-01 Sign Up 정합화** — auth_signup.go 에 audit emit (`account.signup.requested` + 부분 실패 시 `account.signup.partial_failure`) + `fmt.Printf` → `logRequest` 정리 + `auth_signup_test.go` 신규 4 case (정상 / HRDB miss / Kratos fail / DevHub user 충돌). `backend_api_contract.md` §11.5.2 `POST /api/v1/auth/signup` 본격 spec (요청 body + 응답 + 에러 매트릭스 + audit 매핑). **RM-M3-02 + [ADR-0008](../adr/0008-hrdb-production-adapter.md)** — production HR DB 어댑터 PostgreSQL `hrdb` schema 채택 (검토 옵션 4종: PG schema 선택 / REST API 거부 / LDAP 거부 / MockClient 영구화 거부). 실 구현은 carve out (migration + PostgresClient). **RM-M3-03 조직 polish 1차** — backend_api_contract.md §10.4.1~§10.4.4 mutation endpoint (POST/PATCH/DELETE units + PUT members) 의 request body + 응답 예시 + 에러 매트릭스 신설. parent_id cycle 검증 / primary_dept 자동 판정 코드는 carve. 매트릭스 §2.4 IMPL-hrdb-01 (PoC MockClient) + IMPL-hrdb-02 (planned ADR-0008) 정의 추가. §3 회원가입 / 조직 행 갱신. §4 ADR-0008. |
| 2026-05-13 | sprint `claude/work_260513-m` (M3 후속 1-4 일괄): **(1) ADR-0008 PostgresClient + migration** — `internal/hrdb/postgres.go` (pgxpool 기반) + `migrations/000010_create_hrdb_persons.up.sql` (schema + table + indexes) + `postgres_test.go` (nil-pool guard + integration skip pattern). IMPL-hrdb-02 planned → 구현 1차 완료. **(2) parent_id cycle 검증 1차** — `organization.go::updateOrgUnit` 에 self-reference + ancestor chain cycle 검출 (BFS via GetHierarchy edges) → 422 `cycle_detected`. 단위테스트 2 case (self + indirect). primary_dept 자동 판정은 carve. **(3) [ADR-0009](../adr/0009-org-secondary-memberships-and-total-count-mv.md) 발급** — 파견/겸임 모델 = `unit_appointments` cover 명문화 (별도 테이블 불요). `total_count` Materialized View 채택 (옵션 A, cron 갱신). 구현 (migration + cron) carve. **(4) frontend Sign Up form** — `app/signup/page.tsx` 신규 + `lib/services/auth.service.ts::signup()` helper + `auth.service.signup.test.ts` 2 case. 매트릭스 §2.4 IMPL-hrdb-02 구현 갱신, §4 ADR-0009 행 추가. |
| 2026-05-13 | Usecase 단계(UC) 추가 — `REQ → UC → ARCH/API` 체인 도입. 후속 권한 정책 반영으로 `UC-APP-01..10`, `UC-PROJ-01..10` 및 REQ 범위(`REQ-FR-APP-001..012`, `REQ-FR-PROJ-000..010`, `REQ-NFR-PROJ-001..006`) 정합화. | current session |
| 2026-05-13 | Usecase/ERD 분리 운영 적용 — 합본 usecase+erd 문서를 모듈별 `system_usecases.md` / `system_erd.md` 로 분리하고, §2.1.5 및 §3 USECASE 열을 전 도메인 범위로 확장. | current session |
| 2026-05-13 | 로그인 세션 + 사용자/조직 추적성 미흡 항목 보완 계획 문서 링크 추가 (§5.4). | current session |
| 2026-05-13 | sprint `claude/work_260513-n` (M4 전 잔여 일괄): **(A) ETL seed SQL** — `scripts/hrdb_etl_seed.sql` 1차 (`INSERT ... ON CONFLICT`, mock 3 시드와 동일) + ADR-0008 §6 ETL 정책 inline 갱신 (부분 결정). **(B) MV migration 000011** — `migrations/000011_create_org_units_total_count_mv.{up,down}.sql` 신규 (CREATE MATERIALIZED VIEW + UNIQUE INDEX + REFRESH 정책 명문화). **(C) [ADR-0010](../adr/0010-primary-dept-resolution.md) primary_dept** — 옵션 4종 검토 + 1차 알고리즘 (`domain.ResolvePrimaryUnit`: leader 우선 / total_count tie-break / lexicographic) + 5 단위테스트 PASS. backfill worker carve. **(E) frontend Sign Up alias** — 기존 `/auth/signup` (이미 e2e 4 TC cover) 와 본 sprint 직전이 신설한 `/signup` 중복 발견. `/signup` 을 `/auth/signup` 로 redirect 만 두고 단일화. 매트릭스 §4 ADR-0010 행 추가. `getHierarchy` MV join 코드 변경 (D 항목) 은 backend store 인터페이스 + 통합 환경 의존으로 carve (M4 후속 sprint). |
| 2026-05-14 | PR #104 본인 리뷰 보강 — 13개 항목 (B1~B3 / I1~I4 / Nit 6 / S1~S4) 일괄 정정. §3 Application/Project 행에 신규 ID 동기화 + §2.2 API-41~58 (planned) forward note + §4 [ADR-0011](../adr/0011-rbac-row-scoping.md) placeholder 발급. concept §7 데이터 모델 PK 정합 + project_integrations 명칭 통일 + ERD §2.5 composite PK 표기. requirements §5.4 의 REQ-FR-APP ID 순서 재배치 + key immutable 명문화. `backend_api_contract.md` §13.0 placeholder ID 인덱스 표 + 각 endpoint header 에 (planned) 접미사 + visibility 별 공개 범위 + weight normalize 룰 (±0.001) + sync_error_code link-scope 운영 룰. system_usecases §2.9 UC-APP-04 "상위 마일스톤" 표현 의미 오류 정정. | PR #104 보강 commit |
| 2026-05-14 | sprint `claude/work_260514-a` (Application Design 1차 + ADR-0011 평가/결정 — mixed sprint): **(A) Application 도메인 backend 1차** — 마이그레이션 000012..000018 (scm_providers + seed / applications / application_repositories composite PK / projects / project_members + project_integrations / pr_activities + build_runs + quality_snapshots / RBAC seed 확장). `internal/domain/application.go` 신규 (도메인 타입 + enum 상수 + `IsRetryableSyncError`). `internal/store/applications.go` 신규 (16 메서드 stub, 모두 `ErrNotImplemented`). `internal/httpapi/applications.go` 신규 (API-41..50 handler stub 10개, 501 + envelope). `permissions.go` 의 `routePermissionTable` 에 14 신규 row + `router.go` 에 10 신규 route 등록. `internal/domain/rbac.go` 의 `AllResources()` + `DefaultPermissionMatrix()` 에 4 신규 resource (applications / application_repositories / projects / scm_providers) 추가. **(B) ADR-0011 결정** — proposed → accepted. 옵션 C (handler/service 코드 검증) 1차 채택, 옵션 B (row_predicate) 단계적 확장 옵션 보존. 옵션 A (Casbin), D (PG RLS) 거부. §2.2 Application/Repository API §13 서브 표 (API-41..50 scaffolded + API-51..58 planned). §2.4 IMPL-application-XX (6 row 정의). §3 Application/Project 도메인 row ARCH/API/IMPL 컬럼 채움. §4 ADR-0011 accepted 갱신. backend `go test ./...` 12 패키지 PASS. | current session |
| 2026-05-14 | sprint `claude/work_260514-b` (Application Design 2차 — A1+A2+A3 묶음): **(A1) `internal/store/applications.go` body** — 16 메서드 PostgreSQL 구현 (pgx, ErrConflict / ErrNotFound 분기, archived consistency 자동 갱신, `CountActiveApplicationRepositories` 추가, scan helpers). **(A2) `internal/httpapi/applications.go` body** — API-41..50 handler 10개 정식 응답. 요청 binding + visibility/status enum 검증 + key 정규식 + immutable key 422 + 상태 전이 머신 (allowedStatusTransitions + 가드: planning→active 활성 repo ≥1 / hold/resume/archived_reason 필수). audit emit: `application.{create,update,archive}` + `application_repository.{link,unlink}` + `scm_provider.update`. delete 의 `:repo_key` 가 `/` 포함하므로 gin catch-all (`*repo_key`) 로 전환, leading `/` strip. **(A3) `internal/httpapi/applications_test.go`** — `memoryApplicationStore` (interface compile-time check 포함) + 19 handler test (happy + invalid key + duplicate / immutable / activation precondition / invalid transition / hold_reason / archive / unsupported provider / disabled provider / duplicate link / delete colon / not_found / ServiceUnavailable / SCM list+immutable adapter_version). **(infra)** `router.go` 에 `ApplicationStore` interface 추가 + `RouterConfig.ApplicationStore` + `main.go` 에 `applicationStore = pgStore` 와이어링. **(matrix)** §2.2 Application/Repository API §13 서브 표 scaffolded → activated. §2.4 IMPL-application-{store,handler}-01 책임 갱신. §3 Application/Project row 의 IMPL/UT 컬럼 채움. backend `go test -count=1 ./...` 12 패키지 PASS. | current session |
| 2026-05-14 | sprint `claude/work_260514-g` (세션 종료 housekeeping sync): main flat `ai-workflow/memory/state.json` / `session_handoff.md` / `work_backlog.md` 의 PR #103 시점 (244f6b1) 정보를 PR #110 머지 후 (d29f2ac) 까지 흡수. 7 PR 일괄 반영 (#104~#110), Application 도메인 backend 1차 완성 마일스톤 추가 (work_backlog.md §1), `application_domain_done_2026_05_14` 인벤토리 + `application_domain_carve_out_2026_05_14` next_actions (state.json). Auto memory (개인) — `project_2026_05_14_session_application_domain_done.md` + `feedback_codex_review_cycle.md` 신규, MEMORY.md 인덱스 갱신. | current session |
| 2026-05-14 | sprint `claude/work_260514-f` (hotfix — PR #109 codex review B1 + I1 + I3 묶음): **(B1)** `applicationsFixture` 의 cleanup SQL multi-statement + bind args 가 pgx prepared execution 거부 → 23 test 가 fixture 단계 panic. cleanup 을 `cleanupStatic` (TRUNCATE) + `cleanupRepos` (DELETE with bind) 두 statement 분리 호출로 변경. TestIntegration_FixtureCleanupSanity 회귀 guard 추가. **(I1)** `scripts/setup-test-db.sh` 신규 — 로컬 DEVHUB_TEST_DB_URL 환경 검증 + golang-migrate 자동 설치 + 마이그레이션 적용. **(I3)** `.github/workflows/ci.yml` 에 `backend-integration` job 신설 (4 → 5 job): native PG 15 setup (e2e job 패턴 재사용) + migrate up + `go test -count=1 -run 'TestIntegration_' ./internal/store/...` 실행. DEVHUB_TEST_DB_URL=postgres://runner@localhost:5432/devhub. PR #109 의 23 test 가 본 sprint 부터 CI 에서 실 실행 → codex P1/P2 회귀 guard 가 실제 동작. | current session |
| 2026-05-14 | sprint `claude/work_260514-e` (postgres integration test 도입): work_260514-c carve_out 흡수. 신규 3개 integration test 파일 — `applications_integration_test.go` (Application CRUD + Link composite PK + sync error reset + CountActiveApplicationRepositories + ListApplications filter + SCM adapter_version 보호 = 9 test), `repository_ops_integration_test.go` (ListRepositoryActivity/PR/BuildRuns/Quality + ComputeApplicationRollup equal/repo_role/custom + CountApplicationCriticalWarnings = 8 test, **TestIntegration_ComputeApplicationRollup_CustomNormalization_P1 이 PR #108 P1 회귀 guard**), `integrations_integration_test.go` (Project UNIQUE + Integration CRUD + scope filter, **TestIntegration_UpdateIntegration_UniqueConflict_P2 이 PR #108 P2 회귀 guard** = 6 test). 기존 postgres_integration_test.go 패턴 (DEVHUB_TEST_DB_URL t.Skip) 동일 — CI backend-unit 에서는 자동 skip. 로컬/후속 CI 잡에서 실 실행. backend `go test -count=1 ./...` 12 패키지 PASS. | current session |
| 2026-05-14 | sprint `claude/work_260514-d` (hotfix — PR #107 codex review): **(P1)** `ComputeApplicationRollup` 의 `WeightPolicyCustom` 분기에서 누락 repo 의 equal fallback 후 sum 재정규화 부재 → 합 > 1.0 → weighted metrics 수치 오염. fallback 후 `total` 분모로 전체 가중치 정규화 + fallbacks meta 의 AppliedWeight 정규화 후 값으로 갱신. **(P2)** `UpdateIntegration` 의 partial UNIQUE 위반이 ErrConflict 매핑 누락 → handler 500 응답. store 에 `isUniqueViolation(err) → ErrConflict` 추가 + handler updateIntegration 의 409 `integration_conflict` 분기 + `memoryApplicationStore.UpdateIntegration` 의 unique 검증. **(UT)** TestUpdateIntegration_UniqueConflict + TestApplicationRollup_CustomWeightsExact 회귀 guard. backend `go test -count=1 ./...` 12 패키지 PASS. | current session |
| 2026-05-14 | sprint `claude/work_260514-c` (API-51..58 세트 활성화): **(API-51..54)** Repository 운영 지표 read endpoint 4종 — `internal/store/repository_ops.go` 신규 (pr_activities/build_runs/quality_snapshots 조회 + RepositoryActivity aggregate). `internal/httpapi/repository_ops.go` 신규 (4 handler). **(API-55..56)** Project CRUD 5 endpoint — `internal/httpapi/projects.go` 신규 (5 handler — list/create/get/update/archive, 상태 전이 머신 재사용, `project_key_immutable` / `project_key_conflict` / `invalid_status_transition_payload` 분기). audit `project.{created,updated,archived}`. **(API-57)** Application 롤업 — `internal/store/repository_ops.go::ComputeApplicationRollup` (concept §13.4 weight_policy normalize 실 구현 — equal / repo_role 다중 정규화 / custom ±0.001 허용오차) + `internal/httpapi/application_rollup.go` (handler + meta period/filters/weight_policy/applied_weights/fallbacks/data_gaps). `internal/store/repository_ops.go::CountApplicationCriticalWarnings` 도입 — active→closed 가드 흡수 (직전 sprint carve_out close): handler updateApplication 의 active→closed 분기에서 critical count > 0 면 `422 application_close_precondition_failed`. **(API-58)** Integration CRUD 4 endpoint — `internal/store/integrations.go` 신규 (List/Get/Create/Update/Delete) + `internal/httpapi/integrations.go` 신규. scope polymorphism (application/project) — `scope_target_mismatch` 422 분기. audit `integration.{created,updated,deleted}`. **(domain)** `internal/domain/application.go` 에 PRActivity / BuildRun / QualitySnapshot / RepositoryActivity / WeightPolicy / ApplicationRollupOptions / ApplicationRollupMeta / RollupPeriod / RollupFallback / RollupDataGap / ApplicationRollup + CustomWeightTolerance 상수 추가. **(infra)** ApplicationStore interface 에 11 신규 메서드. router.go 에 15 신규 route. permissions.go 에 14 신규 route policy. **(UT)** projects_test.go 8 test + integrations_test.go 6 test + application_rollup_test.go 4 test (총 18 신규). `memoryApplicationStore` 에 11 신규 메서드 stub (Repository ops zero-value, rollup custom 검증 흉내, Integration in-memory CRUD). **(matrix)** §2.2 Application/Repository API §13 서브 표 API-51..58 activated 완료, ID 공간 58 전 도메인 activated. backend `go test -count=1 ./...` 12 패키지 PASS. | current session |
| 2026-05-14 | PR #112 (codex/frontend_color_review) 자체 + codex 합본 리뷰 보강 commit: **IMPL-org-04** 갱신 — `users_units.go` 의 `CreateOrgUnit` / `UpdateOrgUnit` 에 unit_appointments leader 자동 sync (demote-then-promote in tx). 회귀 가드: `SELECT ... FOR UPDATE` 직렬화 + migration `000019_unit_single_leader.up.sql` 의 partial unique index `unit_single_leader_idx WHERE appointment_role='leader'` 로 single-leader invariant 를 SQL 레벨에서 강제. `normalizedLeaderUserID` helper 로 `org_units.leader_user_id` 와 `unit_appointments.user_id` normalize 단일화 (TrimSpace 1회). 신규 integration test 3건 (`TestPostgresStore_CreateOrgUnit_LeaderSync`, `TestPostgresStore_UpdateOrgUnit_LeaderPromoteDemote`, `TestPostgresStore_UpdateOrgUnit_LeaderClear`). **frontend 보안 hotfix** — `/admin/settings/organization` 의 read-path 자동 leader PATCH 제거 (audit log poisoning + 권한 상승 경로 차단). **PKCE 회귀 가드** — `pkce.test.ts` 에 RFC 7636 Appendix B vector + sha256Fallback ≡ subtle 50개 fuzz parity case 추가. **a11y** — `ActionMenu` keyboard activation (`onClick` fallback + `role="menu"` / `aria-haspopup` / `aria-expanded`) + `touchedRef` 자동 350ms reset (silent click swallow 차단). **policy** — 환경 특화 docker 자산 (`infra/idp/*.docker.yaml`) + 개인 환경 디버그 스크립트 (`frontend/pw-login-check.cjs`) 를 git 추적 외로 이동, `.gitignore` 패턴 추가. | PR #112 보강 commit |
| 2026-05-16 | sprint `gemini/phase6` (Frontend ProjectService): `frontend/lib/services/project.service.ts` 구현 보완 (getProject, listAllProjects) 및 `project.service.test.ts` 정합성 확보 (기존 getApplications 등 유지). Vitest 3 case PASS (`UT-frontend-services-02`). | current session |
| 2026-05-17 | sprint `gemini/phase6` (E2E & Refinements): **(1) RBAC E2E 테스트 검증** — DB connection DSN 환경변수 연동을 통한 Playwright E2E 테스트(`admin-permissions.spec.ts`) 시딩 & 테스트 2건 전면 성공. **(2) Projects 상세 페이지 실시간 API 연동** — `identityService.getUsers()`와 project 속성(`start_date`, `due_date`, `owner_user_id`)을 매핑하여 하드코딩 데이터를 동적 연동 리팩토링. **(3) 추적성 문서 업데이트** — 공통 필터 표준화 및 감사 로그 프리미엄 리팩토링에 따른 IMPL/TC ID 갱신. **(4) 개발 의뢰 필터 바 표준화** — `dev-requests/page.tsx`에 공통 표준 컴포넌트인 `FilterBar`를 바인딩하고 비표준 커스텀 버튼/필터 방식을 제거하여 대시보드 UI의 일관성을 확보함. | current session |
| 2026-05-15 | sprint `codex/docker-packaging-guide` (PR #133, sha `4892a78`) — Docker deploy 패키지 안정화 + E2E 43/43 정합화. `docker-compose.deploy.yml` + `local-db` profile + Keycloak/OIDC deploy yaml + nginx + `/api/runtime-config` + `docker-image-publish.yml` workflow + admin-permissions E2E 2건 안정화. 추적성 변경 없음 (인프라/배포 자산 도입, 신규 ID 발급 없음). | 본 sprint (`claude/work_260518-b`) 가 사후 등록 |
| 2026-05-15 | sprint `codex/memory-next-step-20260515` (PR #135, sha `62a2088`) — **External Integration 컨셉~설계 staged**: `docs/domain/integration-registry/external_system_concept.md` + `external_integration_capability_matrix.md` + `REQ-FR-INT-001..012` + `REQ-NFR-INT-001..008` + `UC-INT-01..14` + `ARCH-INT-01..06` (`architecture.md §8`) + `API-69..78` spec staged (`backend_api_contract.md §15`) + `IMPL-int-XX` planned 분해 + `TC-INT-*` 카탈로그. 본 §3 매트릭스에 External Integration row 신규. §2.2 §15 sub 표 도입은 후속 sprint (`codex/next-step-20260516`) 의 baseline activation 과 함께. | 본 sprint 가 사후 등록 |
| 2026-05-16 | sprint `codex/e2e-fix-20260515` (PR #136, sha `c61274b`) — dev-request + signout E2E flow 안정화. `frontend/tests/e2e/dev-requests.spec.ts` 신규 (위젯→list→detail flow + 타임스탬프 unique title + strict locator + Promote 조건부 분기) + `signout.spec.ts` login form visibility 기반 검증. 본 §5.1 의 DREQ row 가 "부분 활성화" 로 등록되는 근거. | 본 sprint 가 사후 등록 |
| 2026-05-16 | sprint `gemini/dreq_e2e_260515` (PR #137, sha `72bf265`) — **DREQ token expires_at + IP mutation PATCH**. `migration 000027` `expires_at` 추가 + `requireIntakeToken` 만료 체크 + `auth_intake_token_expired` 에러 코드 + `PATCH /api/v1/dev-request-tokens/:token_id` 신규 endpoint (allowed_ips 동적 변경, **API-79** ID 발급). 본 §2.2 DREQ API §14 sub 표에 API-66..68 + API-79 row 추가. §3 매트릭스 DREQ row IMPL/UT 컬럼에 token_expiry / allowed_ips_mutation 추가. ADR-0014 §6 carve out 의 일부 활성화. | 본 sprint 가 사후 등록 |
| 2026-05-17 | sprint `codex/next-step-20260516` (PR #139, sha `e2a76fb`) — **External Integration backend 1차** activated. `internal/integrations/adapters/{contract,homelab,homelab_file_puller,homelab_http_puller,homelab_pull_loop,metrics}.go` + `store/integration_registry.go` + `infra_snapshots.go` + `httpapi/integration_registry.go` + `infra_integrations.go` + API-73..78 baseline activated + Prometheus `/metrics` + `migration 000028` integration_registry + `000029` infra_service_snapshots + `docs/domain/integration-registry/homelab_pull_strategy.md` + `prometheus_homelab_alerts.md` + `docs/domain/integration-registry/test_cases.md` + IT 자동화. §2.2 §15 sub 표 활성화 + §2.4 IMPL-int-XX 정의 + §3 External Integration row + §5.1 External Integration 행 + §5.2 closed entry. ADR 발급은 carve out (다음 sprint 의 HomeLab pull strategy + Prometheus alerts 정책 ADR). | 외부 에이전트가 traceability 갱신 1차 처리 |
| 2026-05-17 | sprint `gemini/ui_standardization_260517` (PR #140, sha `caa80c7`) — FilterBar 표준화 + DREQ E2E 안정화 + **`DestructiveConfirmModal` 신규** (`frontend/components/ui/DestructiveConfirmModal.tsx`) + dev-request-tokens 페이지 wire (P2 carve out #3 해소) + 감사 로그 트렌드 인디케이터 일관화 + dev-requests page FilterBar 적용 (TC-FILTERBAR-DREQ-01 활성화). § 3 매트릭스 DREQ row 의 frontend IMPL 에 destructive_confirm + show_hide_toggle 추가. | 본 sprint 가 사후 등록 |
| 2026-05-18 | sprint `claude/work_260518-a` (PR #141, sha `98fa4a7`) — **메모리 갭 동기화**: 직전 claude EOD (PR #132) 이후 외부 에이전트 8 PR (#133~#140) 의 main flat memory (`ai-workflow/memory/state.json` / `session_handoff.md` / `work_backlog.md`) 흡수. 코드/문서 변경 없음, 추적성 ID 발급 없음. 본 sprint (`claude/work_260518-b`) 가 후속 traceability 동기화로 분리 처리. | 본 sprint 가 사후 등록 |
| 2026-05-18 | sprint `claude/work_260518-b` (PR #142) — **추적성 동기화 (외부 8 PR 사후 흡수)**: (A) §2.2 DREQ API §14 sub 표에 API-66/67/68 + 신규 API-79 (PATCH allowed_ips, PR #137) row 추가, (B) §3 매트릭스 DREQ row 에 API-79 + migration 000027 + IMPL token_expiry/allowed_ips_mutation/destructive_confirm/show_hide_toggle + UT-frontend-IntakeTokenTable-01 + UT-frontend-IssueIntakeTokenModal-01..02 + frontend-filterbar-01 dev-requests page 적용 + TC-FILTERBAR-DREQ-01 추가, (C) §5.1 E2E 미커버 표에 Dev Request intake auth path row 추가 (PR #136 partial cover + intake auth 잔여), (D) §6 변경 이력에 PR #133/#135/#136/#137/#139/#140/#141 row 추가. | PR #142 머지 |
| 2026-05-18 | sprint `claude/work_260518-c` (PR #143) — **3 신규 ADR 발급**: [ADR-0015](../adr/0015-homelab-adapter-pull-strategy.md) HomeLab adapter pull source 전략 (PR #139 1차 결정 사후 명문화 — file+HTTP mutually exclusive, lightweight goroutine + feature flag, exponential backoff, 메트릭 5종) + [ADR-0016](../adr/0016-prometheus-alerts-policy.md) Prometheus 알림 규칙 정책 (3 alert × stage/prod 임계 분리, Grafana panel 5종, multi-instance `max by(provider)` aggregation) + [ADR-0017](../adr/0017-dreq-intake-token-operational-hardening.md) DREQ intake token 운영 hardening (PR #137 의 `expires_at` + middleware 만료 체크 + PATCH allowed_ips 활성화 사후 명문화). ADR-0014 §6 carve out 갱신 — 2 항목 모두 ADR-0017 로 분기. §4 ADR 인덱스에 3 row 추가. | PR #143 머지 |
| 2026-05-18 | sprint `claude/work_260518-d` (PR #144) — **DREQ-E2E TC 정식 발급 + spec ts 정합화**: `docs/domain/dev-request/test_cases.md` 신규 카탈로그 (TC-DREQ-* 13건 발급, E2E + UT 영역 매핑 + 카버리지 매핑 표). `frontend/tests/e2e/dev-requests.spec.ts` 정합화 — 기존 mega test 를 `test.step()` 6 단계로 분리 + 각 step 명에 TC ID 명시. 신규 2 test 추가: (1) TC-DREQ-INTAKE-AUTH-NEG-01 invalid bearer → 401, (2) TC-DREQ-ADMIN-TOKEN-PATCH-01 PATCH allowed_ips happy path (API-79). §3 매트릭스 DREQ row TC 컬럼 갱신 (부분 활성화 → 정식 발급 TC-DREQ-* 카탈로그 + 13 TC). | PR #144 머지 |
| 2026-05-18 | sprint `claude/work_260518-x` (본 PR) — **post-EOD #2 종합 housekeeping + 로드맵 갱신**. 직전 housekeeping (PR #158 sprint -q) 이후 6 PR (#159~#164) 흡수. main flat memory (state.json HEAD `4dd02ad` + merged_prs entry 7건 추가 + carve_out_for_next_session 20 row 갱신 / session_handoff.md post-EOD #2 + 6 PR 표 + ADR carve out 진행 현황 표 + 본 세션 학습 5건 + 다음 directive 8 항목 / work_backlog.md milestone + §5 변경 이력 row 8건). auto-memory `project_2026_05_18_post_eod_session_2.md` 신규 + `MEMORY.md` 인덱스. **로드맵 갱신** (`docs/development_roadmap.md`): §3 M5 DREQ — atomicity + cron + metric 항목 done 표기 (PR #156/#161) + (b) PATCH expires_at carve out 유지 + 신규 **M6 External Integration** 도메인 추가 (concept/backend/ADR/frontend/bindings/topology v2/size limit/Alertmanager 모두 done + carve out 5건 + API/TC 인벤토리), 신규 **Design 검토** 절 추가 (single port + Keycloak SSO 2건 staged + ADR-0018/0019 후보 명시), §7 변경 이력 row 5건 추가 (2026-05-14 Application + 2026-05-15 DREQ + 2026-05-18 M5 closing + M6 closing + Design 검토). 코드 / 운영 자산 변경 없음 — docs/memory only. | current session |
| 2026-05-18 | sprint `claude/work_260518-w` (PR #164) — **codex review hotfix #8** (사용자 요청 "미흡수 codex review 확인" → P1 3건 + P2 6건 일괄 흡수). **P1 #1** (PR #157 `homelab_http_puller.go:107`): `io.ErrUnexpectedEOF` 가 transient transport 실패도 포함 → `*io.LimitedReader.N == 0` 일 때만 oversized 명시 감지 + 그 외 retryable=true 유지. 회귀 unit test `TestHomeLabHTTPPullerTreatsTransientEOFAsRetryable` (hijack + partial JSON 1차 + 정상 2차). **P1 #2** (PR #160 `prometheus_alertmanager_setup.md:73`): rule expr 에 `{environment="prod\|stage"}` matcher 명시 — cross-env contamination 회피. **P1 #3** (PR #162 `single_port_reverse_proxy.md:327`): `DEVHUB_HYDRA_ADMIN_URL` 안내가 public path 가리키던 회귀 → 옵션 A (internal-only `127.0.0.1:4445`, 권장) + 옵션 B (별도 path + IP allowlist) 명시. **P2 #1** (PR #154): `handleCreated` 의 scopeFilter 위반 prepend → 다른 scope_type 신규 binding 은 info toast + table prepend 안 함. **P2 #2** (PR #155): `.react-flow` shell 의존 false positive → `page.waitForResponse` 로 API-78 호출 + body meta/data 검증 + node count 기반 empty state 분기. **P2 #3** (PR #157 `homelab_file_puller.go:53`): Decode 후 `dec.Token()` 으로 `io.EOF` 검증 — 후행 JSON object silent 무시 회귀 가드. 회귀 unit test `TestHomeLabFilePullerRejectsTrailingJSON`. **P2 #4/#5** (PR #158 `jira_status_2026_05_18.md`): ADR-0006 link `inbound-rejected.md` → `reject-inbound.md`, ADR-0012 link `intake-auth.md` → `external-intake-auth.md`. **P2 #6** (PR #160 `prometheus_alertmanager_setup.md:80`): `NoRecentSuccess` + `DegradedProvidersDetected` 의 `max by(provider)` aggregation 적용 (multi-instance 정합). ADR-0015 §7 + ADR-0016 §7 + setup 가이드 §8 + planning 문서 §16 + Jira 보고서 §C 변경 이력 row 추가. backend 회귀 unit test 2건 (http puller + file puller). frontend 변경 (page + spec) docs-only 회귀 risk 낮음. | current session |
| 2026-05-18 | sprint `claude/work_260518-v` (PR #163) — **외부 Keycloak SSO federation design 검토 (planning)** (사용자 추가 요청 정합, RM-M4-09 의 구체화). `docs/infrastructure/keycloak-idp/sso_federation.md` 신규 — 16 section: (§1) 현재 Hydra+Kratos stack + Keycloak 도입 동기 (enterprise SSO + MFA 상속 + off-boarding), (§2) 통합 옵션 3종 비교 (A 전체 대체 / **B Kratos federation 권장** / C Hydra brokering), (§3) Kratos config + jsonnet claim mapper + frontend SSO 버튼 + flow Mermaid sequence diagram, (§4) **HRDB user mapping 4 옵션** (M1 email / **M2 employee_id strict link 권장** / M3 system_id / M4 fallback) + 자체 password 와 SSO 사용자 공존 정책, (§5) backend / Hydra 영향 (변경 없음 — Bearer verifier 그대로), (§6) frontend 영향 (SSO 버튼 + password form transitional), (§7) claim 매핑 명세 + Keycloak mapper SOP, (§8) 보안 점검 (claim spoofing / HRDB lookup bypass / session hijacking / MFA 우회) + audit sso.* 4 action 신규, (§9) Phase 2/3/4 cutover 절차, (§10) local dev 영향 (Keycloak 미실행 — env gate), (§11) CI / E2E 영향, (§12) **단일 포트 design (sprint -u) 정합**, (§13) 단계별 진입, (§14) 잔여 carve out + open question, (§15) **ADR-0019 후보** (Phase 2 진입 시 승격), (§16) 변경 이력. `docs/planning/README.md` §5.1 row + §6 변경 이력 갱신. 코드 / 운영 자산 / ADR 변경 없음 — design 검토만. | current session |
| 2026-05-18 | sprint `claude/work_260518-u` (PR #162) — **단일 외부 포트 reverse proxy design 검토 (planning)** (사용자 추가 요청 정합). `docs/infrastructure/deployment-automation/single_port_reverse_proxy.md` 신규 — 16 section 의 design 검토 문서: (§1) 컨텍스트 + 현재 multi-port 의 문제 + 본 design 목표, (§2) 후보 reverse proxy 5종 비교 표 + nginx 권장, (§3) `/devhub` prefix 경로 매핑 + nginx config skeleton + Mermaid 토폴로지, (§4) OIDC redirect URI / Hydra public_url / issuer drift 회귀 시나리오, (§5) cookie scope `Path=/devhub/auth/{hydra,kratos}` 분리, (§6) CORS 단순화 (same origin), (§7) Next.js `basePath` + 정적 자산, (§8) local dev vs production 비대칭 env 분기 표, (§9) CI / E2E 영향 (옵션 A current 그대로 권장), (§10) Phase 2/3 cutover 절차 + Hydra/Kratos config 변경 + roll-back 시나리오, (§11) 관측 (nginx exporter + access log), (§12) 보안 점검 (TLS + HSTS + CSRF + forwarded-header 신뢰), (§13) 단계별 진입 (Phase 1 본 sprint = 문서만 / Phase 2 staging / Phase 3 prod cutover / Phase 4 local dev option), (§14) 잔여 carve out + open question, (§15) **ADR-0018 후보** (결정 후 승격), (§16) 변경 이력. `docs/planning/README.md` §5.1 index row + §6 변경 이력 갱신. 코드 / 운영 자산 / ADR 변경 없음 — design 검토만. | current session |
| 2026-05-18 | sprint `claude/work_260518-t` (PR #161) — **ADR-0017 §6 잔여 (a)+(c)+(d) resolved** (다음 세션 directive 2순위). (a) 자동 cron revoke: `store.HardRevokeExpiredIntakeTokens(before)` 단일 batch UPDATE + `internal/devrequest/RunIntakeTokenCron` goroutine (env `DEVHUB_DREQ_TOKEN_CRON_*` gate, HomeLab pull loop 패턴) + audit emit `dev_request_intake_token.auto_revoked` (SourceType=system) + counter `devhub_intake_token_auto_revoked_total`. (c) 만료 metric: `store.CountExpiringSoonIntakeTokens` + gauge `devhub_intake_token_expiring_soon` (env threshold 기본 24h). (d) staleness metric: `store.CountStaleIntakeTokens` + gauge `devhub_intake_token_stale` (env threshold 기본 720h=30d, 0=disable). `internal/devrequest/{metrics,intake_token_cron}.go` 신규 (HomeLab adapters/metrics.go 패턴 정합). IntakeTokenStore interface 3 메서드 확장. fakeIntakeTokenStore 도 mirror. store integration test 3 sub-test 신규 (HardRevokeExpired / CountExpiringSoon / CountStale). cron unit test 5건 (FirstTick / StaleDisabled / ExpiringDisabled / StoreErrors / NoRevokesNoAudit). ADR-0017 §6 (a)/(c)/(d) → ✅ resolved. (b) PATCH expires_at 는 frontend UI 영향 + 정책 변경 필요로 carve out 유지. ADR-0016 Alertmanager 가이드 §8 신규 — DREQ token alert rule reference + cron 운영 정책. backend 단독 변경, frontend 영향 0. | current session |
| 2026-05-18 | sprint `claude/work_260518-s` (PR #160) — **ADR-0016 §6 carve outs (1)+(2) resolved** (다음 세션 directive 1순위). (1) Alertmanager 운영 자산 setup 가이드 `docs/setup/prometheus_alertmanager_setup.md` 신규 — 외부 git 이관 layout (ops-monitoring 별도 저장소) + Prometheus scrape config + stage/prod raw YAML reference + Alertmanager 라우팅 예시 (Slack/PagerDuty receiver, severity/environment matchers) + 운영 SOP (rule 추가/변경/검증/배포, 신규 알림 진단, 임계 튜닝 체크리스트, multi-instance aggregation). (2) Grafana dashboard JSON 모델 `docs/setup/grafana/homelab_dashboard.json` 신규 — Grafana schemaVersion 38 호환 dashboard + 5 panel (Pull Success/Error / Pull Latency p95 / Last Success Age / Degraded Providers / Observed Services) + Prometheus datasource + environment template variable + ADR-0015/0016/setup link. PowerShell ConvertFrom-Json syntax validation PASS. ADR-0016 §5 결과 + §6 carve out (1)+(2) → ✅ resolved + §7 변경 이력 row 추가. carve out (3) pull latency p95 alert / (4) push 경로 webhook 알림 / (5) stage→prod 임계 확정 은 baseline 1주 관찰 / push metric 도입 / 운영 데이터 등 사전 조건 부재로 carve out 유지. backend / frontend 영향 0 — docs/운영 자산 only. | current session |
| 2026-05-18 | sprint `claude/work_260518-p` (PR #157) — **ADR-0015 §6 carve outs 부분 처리** (다음 세션 directive 4순위): (1) **size limit + streaming decode** — `HomeLabFilePuller.MaxBytes` + `HomeLabHTTPPuller.MaxBytes` 필드 도입. file mode 는 `os.Stat` 사전 검사 + `os.Open` + `json.NewDecoder` streaming (in-memory full ReadFile 회피), HTTP mode 는 Content-Length 사전 검사 + `io.LimitReader(body, limit+1)` + streaming decode. `Config.HomeLabPullMaxBytes` (env `DEVHUB_HOMELAB_PULL_MAX_BYTES`, default 0 unlimited / 운영 권장 5 MB) + `main.go` puller wire. 회귀 unit test 5건 (file oversized + unlimited + HTTP Content-Length 사전 reject + body over limit + unlimited). (2) **agent token rotation SOP** — `docs/setup/homelab_agent_token_rotation.md` 신규 (발급/주기 rotation/zero-downtime/비상 revoke/자동화 carve out). (3) dedicated worker binary 와 (4) push/pull dedup 정책은 carve out 유지 (M4 진입 시 재평가). ADR-0015 §4.3 env 표에 MAX_BYTES row 추가 + §6 carve out (1)+(2) → resolved + §7 변경 이력 row 추가. backend 단독 변경 — frontend 영향 없음. | current session |
| 2026-05-18 | sprint `claude/work_260518-o` (PR #156) — **ADR-0017 §6 atomicity 실 구현** (다음 세션 directive 3순위): `store/dev_request_intake_tokens.go::UpdateDevRequestIntakeTokenIPs` 의 이전 2 query 패턴 (UPDATE + 별도 SELECT) → 단일 CTE + `FOR UPDATE` row lock + `(VALUES (1)) AS root(_)` anchor 패턴 (ADR-0017 §6 reference SQL 그대로 적용). 회귀 가드 `store/dev_request_intake_tokens_integration_test.go::TestIntegration_UpdateDevRequestIntakeTokenIPs_Atomicity` 신규 (4 sub-test — Happy / NotFound / Revoked / ConcurrentUpdateAndRevoke). handler 분기 contract 동일 유지 (ErrNotFound → 404 / ErrConflict → 409 / nil → 200). between-query race window 종결. ADR-0017 §6 의 atomicity carve out resolved. backend 단독 변경 — frontend 영향 없음. 본 변경은 CI backend-integration job (DEVHUB_TEST_DB_URL) 에서 race test 회귀 검증. | current session |
| 2026-05-18 | sprint `claude/work_260518-n` (PR #155) — **Infra topology v2 시각화 1차** (다음 세션 directive 2순위): `/admin/topology-v2` 페이지 신규 — React Flow + nodes/edges/services 시각화 + degraded providers banner + snapshot_at 메타 + node click 상세 modal. `infra.service.ts::getTopologyV2()` + 신규 v2 타입 (ApiInfraNodeV2 / ApiInfraServiceV2 / ApiServiceEdgeV2 / InfraTopologyV2Meta). `/admin/page.tsx` 헤더에 "Topology v2 →" Link (ArrowRight icon). `test_cases_m4_integration.md` TC-INT-HOMELAB-03 active 갱신 + TC-INT-FRONTEND-TOPOLOGY-V2-{NAV,RBAC}-01 발급. `admin-topology-v2.spec.ts` 신규 (3 test — v2 render + nav + RBAC). React Flow `.or()` chain locator 패턴 (sprint -m 의 hotfix 학습 정합). §5.1 External Integration row "1차 종합 활성화" 로 갱신. 다음 carve out: React Flow group sub-node, WebSocket 실시간 갱신. | current session |
| 2026-05-18 | sprint `claude/work_260518-m` (PR #154) — **External Integration bindings 관리 UI 1차** (다음 세션 directive 1순위): `/admin/settings/integration-bindings` 페이지 신규 (system_admin only via AuthGuard + layout.tsx 의 `isSystemAdmin` 가드) + `BindingsTable` (scope/provider/external_key/policy/created badge) + `CreateBindingModal` (scope_type + scope_id + provider dropdown + external_key + policy) + `integration.{service,types}.ts` 확장 (API-74 GET + API-75 POST + IntegrationBinding/CreateBindingInput/ListBindingsOptions 타입). `admin/settings/layout.tsx` subTabs 에 Bindings 추가 (Link2 icon, Integrations 다음). `test_cases_m4_integration.md` 에 TC-INT-FRONTEND-BIND-LIST/CREATE/RBAC-01 발급 + §3.1 카버리지 매핑. `admin-integration-bindings.spec.ts` 신규 (2 test — bindings lifecycle 3 step + RBAC negative). §3 매트릭스 External Integration row TC 컬럼 갱신, §5.1 외부 통합 row "부분 활성화 — bindings lifecycle 까지 active" 로 갱신. scope_id 의 application/project lookup combobox + Edit/Delete binding 액션은 다음 carve out. | current session |
| 2026-05-18 | sprint `claude/work_260518-k` (PR #152) — **EOD 종합 housekeeping**: 본 단일 일자 11 PR (sprint a..k claude 10 + 외부 #145 codex 1) state.json + session_handoff.md + work_backlog.md 종합 sync + auto-memory project_2026_05_18_session_summary.md 신규. | PR #152 머지 |
| 2026-05-18 | sprint `claude/work_260518-j` (PR #151) — **API-80 DELETE integration provider endpoint** (backend + frontend). `store.DeleteIntegrationProvider` 신규 + FK guard (binding count > 0 시 ErrConflict). handler 4 신규 unit test (Happy/NotFound/HasBindings/ForbiddenForDeveloperRole). RBAC `ResourceInfrastructure` + `ActionDelete` (system_admin only). backend_api_contract §15.1 + §15.2 의 API-80 spec + error 카탈로그 (409 `integration_provider_has_bindings` / 404 `integration_provider_not_found`). frontend: `integration.service.ts::deleteProvider` + `ProviderTable` 의 Delete 버튼 (Trash2 icon, red color) + `DestructiveConfirmModal` 사용 (dev-request-tokens 패턴) + `page.confirmDelete` 의 ApiError 409 분기 → 명시 binding 정리 안내 toast. `test_cases_m4_integration` 에 TC-INT-FRONTEND-DELETE-01 + DELETE-NEG-01 발급. `admin-integrations.spec.ts` 의 cleanup step → 명시 DELETE step 5 로 전환 (`page.waitForResponse` 로 API-80 호출 검증 + table row count → 0 검증). §3 매트릭스 External Integration row TC 컬럼 갱신. | current session |
| 2026-05-18 | sprint `claude/work_260518-i` (PR #150) — **codex hotfix #6 (PR #147 + #148 + #149 review 흡수, P1 ×2 + P2 ×2)**: (P1 #1, PR #148 page.tsx:39) `useToast()` 가 매 render 마다 새 callback → `[toast]` dep 이 effect 무한 재실행 → request spam. dep 을 `[]` 로 + ESLint disable. (P1 #2, PR #148 integration.service.ts:41) API-72 응답이 `{status, job_id}` 인데 service 가 provider envelope 로 cast → `updated.provider_id` throw. service 반환 타입 + handler 의 optimistic update (sync_status="requested" 로 즉시 local 갱신, job_id toast). (P2, PR #147 ADR-0017 §6) reference CTE 의 `FROM locked LEFT JOIN upd` 가 locked empty 시 zero rows → 404 분기 불가. `(VALUES (1)) AS root(_) LEFT JOIN locked LEFT JOIN upd` anchor 패턴으로 정정. (P2, PR #149 admin-integrations.spec.ts:93) SYNC step 의 cell visibility 만 검증하던 약한 assertion → `page.waitForResponse` 로 API-72 호출 자체 + sync_status badge "Pending" 전이 검증. 회귀 detection 강화. | current session |
| 2026-05-18 | sprint `claude/work_260518-h` (PR #149) — **TC-INT-FRONTEND-* E2E spec ts 활성화**: `frontend/tests/e2e/admin-integrations.spec.ts` 신규 (2 test — mega lifecycle 4 step: LIST→CREATE→EDIT→SYNC + 별도 RBAC negative test). admin OIDC session 은 `page.request` 사용 (dev-requests.spec.ts 패턴 정합). EDIT 의 immutable field 가드 검증 (provider_key 입력 부재 + provider_type/auth_mode disabled). `test_cases_m4_integration.md` §3.1 카버리지 매핑 표 신설 — TC ID ↔ spec ts step 1:1. §3 매트릭스 TC 컬럼 "spec ts 작성은 후속 carve out" → "spec ts active". 검증: Playwright `--list` 2 test 인식, `tsc --noEmit` 신규 에러 0건. | current session |
| 2026-05-18 | sprint `claude/work_260518-g` (PR #148) — **External Integration frontend 진입점 1차**: `/admin/settings/integrations` 페이지 신규 (system_admin only via AuthGuard + layout.tsx 의 `isSystemAdmin` 가드) + `ProviderTable` (provider_type/auth_mode/sync_status badge + Sync/Edit actions) + `ProviderModal` (create/edit unified, provider_key/type/auth_mode 는 edit 시 immutable) + `integration.{service,types}.ts` (API-69~72 thin wrapper). `admin/settings/layout.tsx` subTabs 에 Integrations 추가 (Plug icon). `test_cases_m4_integration.md` 에 TC-INT-FRONTEND-{LIST,CREATE,EDIT,SYNC,RBAC}-01 발급 (E2E spec ts 는 후속 carve out). bindings + infra topology v2 시각화는 별도 sprint carve out. 검증: `npx tsc --noEmit` 신규 에러 0건. | current session |
| 2026-05-18 | sprint `claude/work_260518-f` (PR #147) — **PR #146 self-review 보강**: (1) ADR-0017 §6 atomicity carve out 추가 — `UpdateDevRequestIntakeTokenIPs` 의 2 query 패턴 race window 명시 + CTE 단일 query + `FOR UPDATE` reference SQL 본문. (2) `TestUpdateDevRequestIntakeTokenIPs_Revoked` 헤더에 회귀 시나리오 주석 보강 (6개월 후 grep 가독성). 코드 변경 없음 (문서 + test 주석만). | current session |
| 2026-05-18 | sprint `claude/work_260518-e` (PR #146) — **codex hotfix #5 (PR #142 + #143 review 흡수, 3 P2)**: (P2 #1, PR #142) `§2.2 의 "API-01 ~ API-78"` summary 가 API-79 추가 후 미갱신 → `API-01 ~ API-79` 정합. (P2 #2, PR #143) ADR-0015 §4.3 환경 변수 표를 "코드 default" / "권장 운영값" 두 컬럼 분리 — 실제 `main.go` default (interval 30s / retry 0 / backoff 1s) 가 ADR 의 60s/3/2s 와 drift 했던 문제. 운영자가 알림 임계 ([ADR-0016](../adr/0016-prometheus-alerts-policy.md)) 설계 시 오탐 가능성. 권장 운영값은 ADR-0016 임계가 가정하는 값으로 명시. (P2 #3, PR #143) PATCH `/api/v1/dev-request-tokens/:token_id` (API-79) 가 revoked row 도 그대로 UPDATE 하던 backend 동작 → store `UpdateDevRequestIntakeTokenIPs` 에 `WHERE token_id AND revoked_at IS NULL` 가드 + revoked 분기 ErrConflict 반환 + handler 의 409 `intake_token_revoked` + 404 `intake_token_not_found` code 필드 추가. 회귀 가드 unit test 3건 (Happy / NotFound / Revoked). ADR-0017 §4.2/§4.3 정합. | current session |
| 2026-05-18 | sprint `gemini/reverse-proxy` (본 PR) — **단일 외부 포트 역프록시(Nginx) 연동 및 /devhub prefix 활성화**: ADR-0018 아키텍처 결정(Accepted), Nginx skeleton 설정 파일 `devhub.conf` 추가, Next.js dynamic `basePath` 및 multi-environment resolution `endpoints.ts` 구현, `test-server-deployment.md` 역프록시 연동 가이드 섹션 보강. 정적 타입 체킹 및 unit/integration 테스트 전체 패스 검증 완료. | current session |
| 2026-05-18 | sprint `codex/keycloak-only-refactor-plan` (PR #167, merge `dff487d`) — **Keycloak 단일화 refactor** (1차 누락 — 2026-05-19 sprint `claude/work_260519-a` 사후 등록). KC-PR-A..F 6단계 단일 묶음 머지: config/provider 스켈레톤 + Keycloak JWT/JWKS verifier (`internal/auth/keycloak_verifier.go` + JWKS cache + resource_access fallback) + Keycloak Admin Client (`internal/httpapi/keycloak_admin_client.go`) + frontend OIDC discovery 전환 + migration 000021 (`kratos_identity_id` → `idp_subject`) + 테스트/문서 정합. Hydra/Kratos 관련 코드 (auth_login/logout/signup/consent/token + hydra_introspection + hydra_admin_client + kratos_webhook + kratos-logout 등) 대거 삭제. ADR governance 측면 정정 (ADR-0001 partial heading 수정의 inconsistency) 은 sprint `claude/work_260519-a` 가 [ADR-0019](../adr/0019-keycloak-only-idp.md) 신규 발행 + ADR-0001 supersession 으로 마무리. | `codex/keycloak-only-refactor-plan` |
| 2026-05-19 | sprint `claude/work_260519-a` (본 PR) — **ADR-0019 재산정**: PR #167 (옵션 A Keycloak 단일화 실 구현) 의 결정 reversal 사후 명문화. [ADR-0019](../adr/0019-keycloak-only-idp.md) 신규 발행 (§3 결정 근거 6 항목 + §4 KC-PR-A..F 머지 사실 + §5.3 carve out 8 항목 + §5.4 RM-M4-09 의미 재정의). [ADR-0001](../adr/0001-idp-selection.md) supersession 처리: 제목 + §3.5 heading 원래 (Hydra+Kratos) 로 복원 + status 헤더에 "superseded by ADR-0019" 추가 + §0/§4/§5/§6/§7/§8/§9/§10 inline supersession banner 8개 + 본문 §3.5~§9 historical immutable. design 문서 [`keycloak_sso_federation.md`](../infrastructure/keycloak-idp/sso_federation.md) status: planning → rejected (옵션 B 미채택). execution plan [`keycloak_only_refactor_execution_plan.md`](../infrastructure/keycloak-idp/refactor_execution_plan.md) status: planned → done (PR #167 KC-PR-A..F 머지). §2.3 RM-M4-09 의미 재정의 (Keycloak federation 측면 closed by ADR-0019, 잔여 scope = identity broker / Gitea/AD 등). §4 ADR 인덱스: ADR-0001 superseded + ADR-0019 신규 row. Stage 1 self-review 에서 발견된 carve out 항목 수 정합화 (7→8) 도 본 row 에 반영. | current session |
| 2026-05-19 | sprint `claude/work_260519-b` (PR #170, sha `d442e17`) — **main flat memory housekeeping**: sprint -a 직후 main flat state/handoff/work_backlog 갱신 + 외부 누락 PR #165/#166/#167/#168 사후 등록 + sprint -a state finalize (status: done) + auto-memory (project + feedback + index). 추적성 영향 N/A (docs-only memory housekeeping). | current session |
| 2026-05-19 | sprint `claude/work_260519-c` (PR #171, sha `23602f5`) — **ADR-0019 §5.3 carve out (1)+(2)+(3) resolved — Keycloak operations SOP 단일 통합 문서 신규**: [`docs/setup/keycloak_operations.md`](../setup/keycloak_operations.md) 신규 — §1 책임 분리 (devhub git 의도 vs 외부 운영 자산) + §2 realm `devhub` 구조 + §3 client 2종 (`devhub-frontend` public PKCE + `devhub-backend` confidential service account) + §4 realm role 4종 (developer / manager / team_manager / system_admin) + §5 user attribute mapper (preferred_username / email / realm_access.roles / employee_id custom claim SOP) + §6 JWKS rotation (90일 active / 14일 passive + backend cache TTL 10분 + D-Day 절차 + 비상 rotation §6.5) + §7 local embedded vs external 모드 분기 + §8 운영 SOP (생성/회수/client_secret rotation/장애 대응 4 시나리오) + §9 보안 점검 + §10 잔여 carve out 8 항목. [ADR-0019](../adr/0019-keycloak-only-idp.md) §5.3 carve out (1)+(2)+(3) resolved 마킹 + §6 변경 이력 row. 잔여 §5.3 carve = 5 항목 (logout chain / MFA / failover / off-boarding / groups → RBAC) + event listener carve 1 항목 추가. Self-review Stage 3 보강 commit `387a804` (carve 5→6 정합화). | current session |
| 2026-05-19 | sprint `claude/work_260519-d` (PR #172, sha `d11917b`) — **ADR-0019 §5.3 carve out (4) SSO logout chain (RP-initiated logout) resolved**: [`docs/setup/keycloak_operations.md`](../setup/keycloak_operations.md) §8.5 신규 sub-section (§8 운영 SOP 의 5번째 sub) — §8.5.1 frontend `auth.service.ts:100-126` 의 RP-initiated 구현 인용 + 핵심 패턴 5 step 표 + §8.5.2 Keycloak admin console 설정 SOP (Valid post-logout redirect URIs whitelist, wildcard 금지) + §8.5.3 chain order mermaid sequence + §8.5.4 admin force logout (front-channel) + §8.5.5 보안 4 위협 mitigation (open redirect / CSRF / token replay / LogoutOverlay UX) + §8.5.6 sub-carve 3 항목 (front-channel URL / backchannel logout / multi-tab sync). 코드 변경 없음 — frontend 의 RP-initiated 구현은 PR #167 KC-PR-D 시점에 이미 정착 (auth.service.ts logout 함수). [ADR-0019](../adr/0019-keycloak-only-idp.md) §5.3 logout chain carve resolved 마킹 + §6 변경 이력 row. §5.3 잔여 carve = 5 항목 (MFA / failover / off-boarding / groups → RBAC / audit event listener). | current session |
| 2026-05-19 | sprint `claude/work_260519-e` (PR #173, sha `6245bda`) — **ADR-0019 §5.3 carve out (9) audit event listener / SPI → DevHub `audit_logs` 통합 design resolved**: [`docs/planning/keycloak_event_audit_integration.md`](../planning/keycloak_event_audit_integration.md) 신규 (10 section) — §1 컨텍스트 + 동기 (PR-M2-AUDIT 제거 + 인증 audit 누락 + regulatory 요구) + §2 옵션 3종 비교 (A Event Listener SPI / B admin event polling / C webhook hybrid) + §3 권장 옵션 B 상세 (KeycloakEventPuller cron + event_cursors 영구화 + audit_logs INSERT 매핑 + 권한 SOP) + §4 audit_logs `action` 매핑 25 row (login 15 + admin 7 + skip 3) + §5 구현 단계 PR-A..E (design accept → cron + cursor → main wire + integration test → audit_logs schema 정합 → 운영 SOP) + §6 보안 점검 5 위협 + §7 cutover Phase 1..4 + §8 carve 7 항목 + §9 ADR-0020 후보. [ADR-0019](../adr/0019-keycloak-only-idp.md) §5.3 audit event listener carve **design resolved** 마킹 (실 backend 구현 carve 유지) + §4.5 audit_logs 영향 절에 design link + §6 변경 이력 row. §5.3 잔여 carve = 4 항목 (MFA / failover / off-boarding / groups → RBAC) + design+carve 1 항목 (event listener 실 구현). Stage 3 보강 commit 으로 row 수 30→25 정합화. | current session |
| 2026-05-19 | sprint `claude/work_260519-f` (PR #174, sha `947bd2f`) — **ADR-0019 §5.3 carve out (8) Keycloak `groups` → DevHub RBAC role 자동 매핑 design resolved**: [`docs/domain/rbac-permissions/keycloak_groups_mapping.md`](../domain/rbac-permissions/keycloak_groups_mapping.md) 신규 (9 section) — §1 컨텍스트 (옵션 A 현행 부담: 다중 user 일괄 처리 / HR sync / 확장성) + §2 옵션 4종 비교 (A realm role 직접 / B group composite role / C groups claim mapper + backend mapping / D composite role only) + §3 권장 옵션 B 상세 — group ↔ realm role 1:1 매핑 (devhub-developers/managers/pmo-managers/system-admins ↔ developer/manager/team_manager/system_admin) + Keycloak admin 설정 SOP 3 step + backend 동작 (변경 없음 — keycloak_verifier.go realm_access.roles 그대로) + user 운영 SOP 갱신 (Groups 탭 1개 가입) + HR sync 자연 통합 + §4 옵션 C future 가능성 + §5 ADR governance 결정 (1차 별도 ADR 발행 없음, Phase 2 옵션 C 시 ADR-0021 후보) + §6 cutover Phase 1..4 + §7 보안 4 위협 + §8 carve 6 항목 + §9 변경 이력. [ADR-0019](../adr/0019-keycloak-only-idp.md) §5.3 carve (8) design resolved 마킹 + §6 변경 이력 row. [keycloak_operations.md](../setup/keycloak_operations.md) §4.3 group section (carve → group SOP + 1:1 매핑 표 + admin 설정 SOP) + §8.1 step 3 (Role Mapping → Groups 탭 1개 가입) 갱신. backend 변경 없음. §5.3 잔여 carve = 3 항목 (MFA / failover / off-boarding) + design+carve 1 (event listener 실 구현) + 실 SOP carve 1 (group staging/prod 적용). | current session |
| 2026-05-19 | sprint `claude/work_260519-g` (PR #175, sha `aa0c029`) — **ADR-0019 §5.3 carve out (7) off-boarding 즉시성 design resolved**: [`docs/infrastructure/keycloak-idp/offboarding_immediacy.md`](../infrastructure/keycloak-idp/offboarding_immediacy.md) 신규 (10 section) — §1 컨텍스트 (현재 worst case 24-48h propagation chain + 정보 유출 위험 + 즉시성 목표) + §2 옵션 6종 비교 (A daily ETL 현행 / B Keycloak admin force / C HR ETL → Keycloak Admin API push / D HR webhook / E LDAP/AD federation / F SCIM bridge) + §3 Phase 1 권장 옵션 C 상세 (hourly ETL script + Keycloak Admin REST 호출 + access_token TTL 5분 + audit 통합 + 운영 SOP) + §4 Phase 2 권장 옵션 E LDAP/AD federation (5-20분 latency + trade-off + cutover) + §5 Phase 3 carve (SCIM bridge / 실시간 webhook) + §6 보안 5 위협 + §7 cutover Phase 1..3 + §8 ADR governance (1차 별도 ADR 없음, Phase 2 ADR-0021 후보) + ADR-0008 §6 와 정합 + §9 carve 5 + open 2. [ADR-0019](../adr/0019-keycloak-only-idp.md) §5.3 carve (7) design resolved 마킹 + §6 변경 이력 row. [ADR-0008](../adr/0008-hrdb-production-adapter.md) §6 '실시간 sync' 항목에 design link + §7 변경 이력 row 추가. [keycloak_operations.md](../setup/keycloak_operations.md) §8.2 user 회수 SOP 보강 (수동 즉시 회수 4 step + 자동 회수 cron 4 step + Phase 2 carve link). backend 변경 없음. §5.3 잔여 carve = 2 항목 (MFA — 사내 정책 제외 / failover) + design+carve 3 항목 (event listener 실 구현 / group staging/prod 적용 / off-boarding Phase 1 운영 cron 적용). | current session |
| 2026-05-19 | sprint `claude/work_260519-h` (PR #176, sha `455556b`) — **ADR-0019 §5.3 carve out (6) Keycloak failover design resolved — §5.3 모든 carve out design 완결 (MFA 제외)**: [`docs/infrastructure/keycloak-idp/failover.md`](../infrastructure/keycloak-idp/failover.md) 신규 (11 section) — §1 컨텍스트 (현재 SPOF + DevHub graceful degradation 자연 동작 분석 — JWKS cache 5분 + access_token TTL 5분 = 5-10분 graceful window + 가용성 목표) + §2 옵션 6종 비교 (A 단일 SPOF / B HA active-active / C HA active-passive / D DevHub graceful degradation / **E backup IdP fallback ❌ ADR-0019 결정 충돌로 명시 제외** / F DR site) + §3 Phase 1 권장 옵션 D 상세 (현재 자연 동작 + 5/10분 break point 운영 정책 + 권장 모니터링 metric) + §4 Phase 2 권장 옵션 B Keycloak HA (Infinispan + shared PG + LB + 단일 issuer URL) + §5 Phase 3 carve 옵션 F DR site + §6 옵션 E 명시 제외 사유 (ADR-0019 §3.3 결정 근거 회귀 회피) + §7 보안 6 위협 + §8 cutover Phase 1..3 + §9 ADR governance (Phase 2 ADR-0021 후보) + §10 carve 4 + open 2. [ADR-0019](../adr/0019-keycloak-only-idp.md) §5.3 carve (6) design resolved 마킹 + §6 변경 이력 row. [keycloak_operations.md](../setup/keycloak_operations.md) §8.4 장애 대응 표 확장 (graceful degradation 5분/10분 시나리오 + Phase 2 HA carve link + 권장 모니터링 metric). backend 변경 없음. **§5.3 design 완결률 = 7/7 (MFA 제외, 100%)** — 잔여 = MFA 사내 정책 제외 + 4 design+carve (event listener 실 구현 / group staging-prod 적용 / off-boarding Phase 1 운영 cron / HA Phase 2 사내 인프라). | current session |
| 2026-05-19 | sprint `claude/work_260519-i` (PR #177, sha `aac7cf5`) — **main flat memory housekeeping + ADR-0019 §5.3 design 완결 milestone 명문화**: sprint -c~-h 6 PR (#171-#176) 누적 흡수. state.json (head_commit 455556b + merged_prs_2026_05_19 6 PR + adr_0019_section_53_design_completion_milestone 신규 객체 + next_actions 갱신) + session_handoff (header + sprint -a~-i 누적 9 PR 표 + §5.3 design 완결 milestone 표 + 다음 directive 13 항목) + work_backlog (milestone + 변경 이력 7 row). sprint -c~-h state.json finalize 6개. auto-memory project_2026_05_19_phase2_adr_0019_section_53_design_completion.md + MEMORY.md entry. 추적성 영향 N/A (docs-only). | current session |
| 2026-05-19 | sprint `claude/work_260519-j` (PR #178, sha `22a1c15`) — **codex review hotfix #9 — sprint -b~-h 7 PR 의 inline P2 9건 일괄 흡수**: (1) state.json `components.backend.test_check + phase` 의 'Hydra/Kratos test 파일은 제거됨' 부정확 정정 — Kratos 잔재 8 files + `RouterConfig.KratosLogin` + `IDPSessionCache` 명시 (residual migration carve). (2) keycloak_operations.md §8.1 step 5 의 '(자동) 첫 로그인 HRDB sync' 부정확 — backend `authenticateActor` 는 `SetIdPSubject` 호출 안 함 (`accounts_admin.go:123` 만), carve 명시. (3) §6.3 JWKS cache TTL '10분' → '5분' (defaultJWKSTTL) 정합 + 'kid mismatch 자동 refetch' → cache TTL 만료/재시작 필요 + stale-while-error fallback carve. (4) §3.1 client + §8.5.2 logout `Valid post-logout redirect URIs` whitelist 정합 — frontend `${window.location.origin}/` (basePath 미포함) = `https://devhub.example.com/` 와 정합 (이전 `/devhub/` 표기 부정확). (5) keycloak_event_audit_integration.md §3.1 admin event API path 정정 — `/admin/realms/{realm}/events?dateFrom=` (이전 `from`) + `/admin/realms/{realm}/admin-events` (이전 `/events/admin`). (6) keycloak_groups_rbac_mapping.md §3.2 step 3 + §8 Default Group 권장 **철회** — multi-role order-dependency 위험 (backend `extractKeycloakRole` 가 `realm_access.roles[0]` 만 사용). keycloak_operations §4.3 group SOP + §8.1 step 3 도 정합 갱신. (7) keycloak_offboarding_immediacy.md §3.1 ETL script admin REST path `$KC_ADMIN/admin/realms/{realm}/...` 통일 (이전 `$KC_ADMIN/realms/...` 부정확). (8) §3.1 ETL script `hrdb.employees` + `active = false` → `users.status = 'deactivated'` 직접 UPDATE 정합 (실 schema = `hrdb.persons` no `active` column, migration 000010). (9) keycloak_failover.md §1.2 + §3 graceful window '5-10분 always' → '**best case 5-10분 / worst case 0**' 명시 + stale-while-error fallback carve. keycloak_operations §8.4 5분 미만 down 시나리오도 동일 정합 갱신. backend 변경 없음 (모두 docs 정정). | current session |
| 2026-05-19 | sprint `claude/work_260519-k` (본 PR) — **infra/runtime 자산 Keycloak 정합** — PR #167 KC-PR-D 가 backend/frontend code 만 전환했으나 docker-compose.deploy.yml + infra/nginx/devhub.conf + devhub.deploy.conf 의 Hydra+Kratos stack 잔재. 본 sprint 가 ADR-0019 §4.2 환경설정 계약 정합. **infra/nginx/devhub.conf**: upstream devhub_hydra + devhub_kratos 2개 → upstream devhub_keycloak 1개 (port 8180, backend 8080 충돌 회피). /devhub/auth/hydra/ + /devhub/auth/kratos/ 2 location → /devhub/auth/keycloak/ 1 location (ADR-0018 reverse proxy 정합). **infra/nginx/devhub.deploy.conf**: upstream devhub_keycloak (keycloak:8080) + /devhub/auth/keycloak/ location 신규 (basePath /devhub 정합). **docker-compose.deploy.yml**: hydra-migrate / hydra / kratos-migrate / kratos 4 service 제거 + keycloak service 신규 (quay.io/keycloak/keycloak:26.0 LTS + import-realm + KC_DB postgres + KC_HTTP_RELATIVE_PATH=/devhub/auth/keycloak + KC_BOOTSTRAP_ADMIN_*). backend env: DEVHUB_HYDRA_*/DEVHUB_KRATOS_* 제거 + DEVHUB_IDP_PROVIDER=keycloak + DEVHUB_OIDC_ISSUER_URL + DEVHUB_OIDC_CLIENT_ID + DEVHUB_OIDC_CLIENT_SECRET + DEVHUB_OIDC_AUDIENCE + DEVHUB_OIDC_JWKS_URL + DEVHUB_KEYCLOAK_ADMIN_URL + DEVHUB_KEYCLOAK_ADMIN_REALM + DEVHUB_KEYCLOAK_ADMIN_CLIENT_ID + DEVHUB_KEYCLOAK_ADMIN_CLIENT_SECRET (ADR-0019 §4.2 정합). backend depends_on hydra+kratos → keycloak. db-init schema: hydra+kratos → keycloak. frontend OIDC env discovery 기반. 잔여 carve = infra/idp/ historical 자산 (sprint -a deprecation banner 그대로 보존) + frontend/.env.example KRATOS_ADMIN_URL e2e 잔재 + frontend/tests/e2e/{fixtures,global-setup}.ts Kratos reference 잔재. backend/frontend code 변경 없음 — 실 docker-compose up 검증은 사내 운영팀 동반 carve. | current session |
| 2026-05-19 | sprint `claude/work_260519-l` (본 PR) — **critical hotfix — migration 000021 prefix 중복 충돌 정정**: PR #167 KC-PR-E 가 추가한 `000021_rename_kratos_identity_to_idp_subject.{up,down}.sql` + 기존 `000021_rbac_team_manager.{up,down}.sql` (PR #119) 의 동일 prefix `000021` 충돌 — golang-migrate 의 동일 version multiple file 처리 시 ambiguous error 또는 silent drift 가능. **git mv** 로 `000021_rename_kratos_identity_to_idp_subject.{up,down}.sql` → `000030_rename_kratos_identity_to_idp_subject.{up,down}.sql` rename (sequence 재정렬, 000028/000029 다음). [`docs/setup/migration_000021_conflict_resolution.md`](../setup/migration_000021_conflict_resolution.md) 신규 — 충돌 발견 + golang-migrate 동작 분석 + clean DB SOP + 이미 적용된 운영 DB 정정 SOP (case A 두 migration 모두 적용 / case B silent drift / case C 기타) + 검증 절차 + CI lint carve. [ADR-0019](../adr/0019-keycloak-only-idp.md) §4.1 KC-PR-E row 갱신 + 본 sprint 정정 link 명시. file 내용 변경 없음 (git rename detection) — schema 자체는 동일. backend code 변경 없음. 실 운영 DB 정정은 사내 운영팀 동반 carve. | current session |
| 2026-05-19 | sprint `claude/work_260519-m` (본 PR) — **frontend e2e Kratos → Keycloak admin API migration design**: [`docs/infrastructure/keycloak-idp/e2e_migration.md`](../infrastructure/keycloak-idp/e2e_migration.md) 신규 (8 section) — §1 컨텍스트 (sprint -k discovery 의 e2e Kratos 잔재 — global-setup.ts 170 line + fixtures.ts 130 line + .env.example L41-42 KRATOS_ADMIN_URL) + §2 옵션 3종 비교 (A 현행 잔재 / B Keycloak admin 전환 / C e2e 폐기) + §3 옵션 B 상세 (admin token 획득 + user seed payload 변경 + 그룹 가입 + DevHub users sync 3 옵션) + §4 cutover Phase 1..3 + §5 보안 4 위협 + §6 carve 4 항목 + §7 ADR governance + §8 변경 이력. admin API mapping 표 (인증/path/payload/state/password/검색 6 항목). **본 sprint = design + deprecation 명시만** — frontend/.env.example L42 + global-setup.ts + fixtures.ts 상단에 deprecation 주석 추가. 실 코드 전환은 사내 Keycloak e2e 환경 staging 진입 동반 별도 sprint carve. [ADR-0019](../adr/0019-keycloak-only-idp.md) §5.3 잔여 carve 추가 (e2e migration 항목). sprint -k 의 잔여 carve 5건 중 1건 처리 (frontend e2e 영역). | current session |
| 2026-05-19 | sprint `claude/work_260519-n` (본 PR) — **main flat memory housekeeping + ADR-0019 §5.3 10/10 milestone 명문화**: sprint -j~-m 4 PR (#178-#181) 누적 흡수. state.json (head_commit 12cdf81 + merged_prs_2026_05_19 5 PR 추가 + adr_0019_section_53_design_completion_milestone 갱신 7/7 → 10/10 + critical_fixes_phase3 객체 신규 + next_actions 갱신) + session_handoff (header + sprint -a~-n 누적 14 PR + §5.3 design 10/10 표 + Phase 3 critical fixes 표 + 다음 directive 12 항목 재산정) + work_backlog (milestone + 변경 이력 5 row). sprint -j/-k/-l/-m state.json finalize 4개 (status: in_progress → done + ended_at + merged_pr). auto-memory project_2026_05_19_phase3_critical_fixes.md 신규 + MEMORY.md entry 추가. 추적성 영향 N/A (docs-only memory housekeeping). | current session |
| 2026-05-19 | sprint `claude/work_260519-o` (본 PR) — **CI lint migration prefix 회피 검사 신규** (sprint -l §6 carve 구현). `.github/workflows/ci.yml` 에 `migration-prefix-lint` job 신규 — `ls backend-core/migrations/*.up.sql | xargs -n1 basename | awk -F'_' '{print $1}' | sort | uniq -d` 패턴으로 동일 version prefix 발견 시 fail. needs/if = backend 또는 workflow 변경 시만 실행. fail 시 `::error::` + 각 충돌 file list + `docs/setup/migration_000021_conflict_resolution.md` SOP link 안내. golang-migrate 동일 version multiple file 처리 ambiguous error / silent drift 회귀 차단. PR #167 KC-PR-E 가 기존 000021 과 충돌 추가했던 사례 자동 검사. 로컬 dry-run PASS (현재 모든 prefix 유니크 — 000021_rbac_team_manager + 000030_rename_kratos_identity 정합). | current session |
| 2026-05-19 | sprint `claude/work_260519-p` (본 PR) — **off-boarding Phase 1 운영 cron script 신규**: `scripts/hrdb_etl_sync.sh` (sprint -g design §3.1 옵션 C 구현 + sprint -j codex review #9 정정 정합). 7 env (HR_EXPORT_CMD / DEVHUB_DB_URL / KC_ISSUER_URL / KC_ADMIN_URL / KC_ADMIN_REALM / KC_ADMIN_CLIENT_ID / KC_ADMIN_CLIENT_SECRET) + bash 4단계 (client_credentials grant token + system_id list 순회 — psql `UPDATE users SET status = 'deactivated'` + Keycloak user lookup + user disable PUT + force logout POST) + error handling (failed count + exit 1). admin REST base path `/admin/realms/{realm}/` 정합 (codex review #7) + hrdb.persons no active 정합 (codex review #8 — users.status 직접 UPDATE). bash -n syntax check PASS. ADR-0019 §5.3 (7) off-boarding carve Phase 1 실 구현 resolved + keycloak_offboarding_immediacy.md §3.1 script reference 추가. Phase 2 LDAP federation carve 유지. | current session |
| 2026-05-19 | sprint `claude/work_260519-q` (본 PR) — **backend 확장 #1: extractKeycloakRole multi-role priority filter** (sprint -j codex review #9 #6 backend 확장 carve 4건 중 1번째). `internal/auth/keycloak_verifier.go` 에 `devhubRolePriority` map (system_admin 4 > team_manager 3 > manager 2 > developer 1) + `selectHighestPriorityRole(roles)` helper 신규. `extractKeycloakRole` 의 3 fallback path (`claims["roles"]` / `realm_access.roles` / `resource_access.{client}.roles`) 모두 `roles[0]` 직접 반환 → `selectHighestPriorityRole(roles)` 호출로 정합. Multi-role token (group composite 가입 시 자연 발생) 의 order-dependency 해소. Unknown role 은 priority 0 fallback (legacy 호환). Unit test 9건 신규 — multi-role 7 case (highest priority / order-independent / known wins over unknown / 4 roles 등) + selectHighestPriorityRole empty 2 case. ADR-0019 §5.3 (8) row + keycloak_groups_rbac_mapping.md §3.2 step 3 + §3.3 갱신 (Default Group 미설정 권장 유지 + priority filter fallback 정합). 사내 GoProxy 제약으로 local go build/test 차단 — 실 검증은 CI backend-unit job. | current session |
| 2026-05-19 | sprint `claude/work_260519-r` (PR #186, sha `8470302`) — **backend 확장 #2: JWKS kid mismatch stale-while-error fallback** (sprint -j codex review #9 #3 backend 확장 carve 4건 중 2번째). `internal/auth/keycloak_verifier.go` 에 `errKidMismatch` sentinel error 신규 + `parseWithJWKS` helper (parse 단계 분리, retry path 의 single entry) + `invalidateCache` method 신규 (`cachedKeys = nil` + `cachedUntil = time.Time{}`). `VerifyBearerToken` 메인 흐름 = 1차 parseWithJWKS → `errors.Is(err, errKidMismatch)` 시 invalidateCache + 2차 parseWithJWKS retry. 다른 error (signature / expired / issuer / audience) 는 retry 안 함 (security 위협 — attacker token 의 endpoint 폭격 회피). Unit test 2건 — RetriesJWKSOnKidMismatch (2 RSA key rotation 시뮬레이션, fetchCount=2 검증) + DoesNotRetryOnSignatureError (attacker token, fetchCount=1 검증). ADR-0019 §5.3 row 갱신 + keycloak_operations.md §6.3 갱신 (rotation 시 backend 동작 — graceful window 0 + signature error retry 안 함). | current session |
| 2026-05-19 | sprint `claude/work_260519-s` (본 PR) — **backend 확장 #3: basePath 포함 logout URI** (sprint -j codex review #9 #4 backend 확장 carve 4건 중 3번째). `frontend/lib/config/endpoints.ts` 의 `BASE_PATH` const → exported. `frontend/lib/services/auth.service.ts` 의 `OIDC_REDIRECT_URI` 가 `${origin}/auth/callback` → `${origin}${BASE_PATH}/auth/callback` 정합 + L116 `post_logout_redirect_uri` 가 `${origin}/` → `${origin}${BASE_PATH}/` 정합. ADR-0018 basePath /devhub 환경에서 logout 후 사용자가 DevHub 진입점 (`/devhub/`) 으로 redirect — Keycloak whitelist 정합. `keycloak_operations.md` §3.1 + §8.5.2 의 Valid post-logout redirect URIs 표기 — sprint -j 의 basePath 미포함 (`/`) → basePath 포함 (`/devhub/`) 재정정. ADR-0019 §5.3 (8) row 갱신. local 검증은 사용자 환경에서 `npx tsc --noEmit`. | current session |
| 2026-05-19 | sprint `claude/work_260519-t` (본 PR) — **backend 확장 #4: 자동 idp_subject sync (authenticateActor)** — sprint -j codex review #9 #2 backend 확장 carve 4건 중 **마지막**. `backend-core/internal/httpapi/auth.go` 의 `authenticateActor` GetUser 성공 분기에서 `user.IdPSubject == ""` 이고 `actor.Subject != ""` 일 때 `SetIdPSubject` lazy backfill 호출. best-effort (실패 시 log only, 다음 요청 idempotent). 이미 IdPSubject 있는 경우 overwrite 안 함. `keycloak_operations.md §8.1 step 5` 의 "(자동) 첫 로그인 sub ↔ idp_subject 매핑" SOP 정합. unit test 2건 (BackfillsIdPSubjectOnFirstLogin + DoesNotResetIdPSubjectIfAlreadySet) PASS. HRDB lookup + users INSERT 의 full 자동 sync (옵션 B) 는 별도 carve (hrdb.Lookup 의 strict 3-key signature 측면). ADR-0019 §5.3 (8) row — backend 확장 carve 4건 모두 resolved 표기. | current session |
| 2026-05-19 | sprint `claude/work_260519-u` (본 PR) — **ADR-0019 §5.3 (9) audit event listener Phase 2 PR-B (skeleton)** — sprint -e design 의 실 구현 5단계 (PR-A..E) 중 첫 번째. `backend-core/migrations/000031_create_event_cursors.{up,down}.sql` 신규 (PK cursor_key + last_event_at + last_event_hash + updated_at, golang-migrate sequence — sprint -l 의 000021 충돌 정정 + CI lint sprint -o 가드 후 첫 신규 prefix). `backend-core/internal/store/event_cursors.go` 신규 — `EventCursor` struct + `EventCursorStore` interface + `PostgresStore.GetEventCursor` (pgx.ErrNoRows → ErrNotFound wrap) + `UpsertEventCursor` (ON CONFLICT UPDATE). `backend-core/internal/httpapi/keycloak_admin_client.go` 확장 — `KeycloakUserEvent` + `KeycloakAdminEvent` struct + `ListUserEvents` + `ListAdminEvents` (codex review #9 정정 정합 — `?dateFrom=<ISO8601>` + path `/events` user + `/admin-events` admin, NOT `/events/admin`). `backend-core/internal/audit/keycloak_event_puller.go` 신규 — `RunKeycloakEventPuller` cron worker (HomeLab pull + DREQ intake token cron 패턴 정합, 즉시 1차 tick + ticker loop + ctx cancellation) + `pullOnce` + `pullUserEvents` + `pullAdminEvents` + `loadCursor` (row 없으면 now() 초기화 — design §3.3 과거 event 폭격 회피) + `hashUserEvent` / `hashAdminEvent` (SHA256 dedup) + 매핑 표 `mapUserEventToAudit` 15 row (LOGIN..UPDATE_TOTP, design §4.1) + `mapAdminEventToAudit` 7 row (USER:CREATE..REALM:UPDATE, design §4.2) + fallback (`keycloak.event.unknown:<TYPE>` / `keycloak.admin.<resource>:<op>`) + `defaultSkipUserEventTypes` 3 type (REFRESH_TOKEN / CODE_TO_TOKEN / INTROSPECT_TOKEN — design §4.3). circular import 회피로 internal/audit 안에 KeycloakUserEvent/KeycloakAdminEvent mirror 정의 + `KeycloakEventLister` interface (main.go wire 시 adapter 변환). unit test 14건 PASS (audit puller 11건 — map 표 / unknown fallback / skip type / cursor advance / no-events keep / already-processed filter / admin advance / loadCursor first-run init / loadCursor propagate non-NotFound + httpapi events 3건 — query+decode / `/admin-events` path / default max + dateFrom omit when zero). **잔여 carve = PR-C main.go wire + Prometheus metric (`devhub_keycloak_events_processed_total` + `devhub_keycloak_event_cursor_lag_seconds`) + integration test + PR-D `audit_logs.source_type='keycloak_event'` schema 정합 + PR-E [keycloak_operations.md](../setup/keycloak_operations.md) §8.6 운영 SOP**. ADR-0019 §5.3 (9) row + 변경 이력 row 추가. | current session |
| 2026-05-19 | sprint `claude/work_260519-ab` (본 PR) — **codex review hotfix #12 — PR #194 sprint -n~-t branch state finalize 누락 정정**. sprint -z housekeeping 시 work_backlog row 의 "sprint -n~-y 12개 finalize" 가 실제로는 -u~-y 5개만 finalize 했고, sprint -n~-t 7 branch state.json (work_260519-{n,o,p,q,r,s,t}) 의 status: in_progress 유지. 본 PR 이 7 file 모두 `status: done` + `ended_at` + `merged_pr` + `merge_commit` (#182~#188 매핑) 추가. sprint -z + -aa 본인도 finalize (각각 PR #194 / #195 매핑). work_backlog sprint -z row 의 "12개 → 5개 (-n~-t 7개 sprint -ab 후속 정정)" wording 정정. AGENTS.md 의 세션 복원 시 브랜치별 memory 우선 읽기 규칙에 따라 다음 에이전트가 PR #182~#188 작업을 진행 중으로 잘못 복원할 위험 회피. ADR-0019 §6 변경 이력 row 추가. 추적성 영향 N/A (memory housekeeping 정정). | current session |
| 2026-05-19 | sprint `claude/work_260519-aa` (PR #195) — **codex review hotfix #11 (PR #193 P2 단일)**. `pullUserEvents` 의 `latestHash` 가 same-ms boundary 에서 skip event (REFRESH_TOKEN 등) 의 hash 로 set 되어, 다음 tick 의 dateFrom inclusive boundary 에서 emit-able event (LOGIN 등) 가 hash mismatch → boundary dedup 안 됨 → emit() + ObserveEventProcessed counter 매 tick inc 되던 side effect 정정. **실제 영향**: audit_logs row 는 partial UNIQUE INDEX (sprint -w migration 000032) 가 차단 → 정확성 영향 없음. metric counter dashboard 왜곡 + DB INSERT 시도 매 tick (실패하지만 호출 부담). **정정**: `pullUserEvents` 의 cursor advance 분기에 `latestEmittable bool` flag 도입. same-ms 에 skip event + emit-able event 함께 들어오면 emit-able 의 hash 를 우선 — `latestHash` 가 emit-able event 의 hash 로 set 되어 다음 tick 의 boundary dedup 이 정확히 차단. `latestTime` advance 가 처음 emit-able 등장 시 + same-ms 의 후속 emit-able 등장 시 둘 다 처리. admin 측은 skip type 없으므로 영향 없음 (그대로). 신규 test 2건 — `keycloak_event_puller_test.go` 의 SameMsSkipAndEmit_PrefersEmittableHash (cursor.LastEventHash = emit-able hash 검증) + SameMsSkipAndEmit_NextTickDedup (다음 tick 회귀 검증, emit-able boundary dedup 으로 emitCount = 0). go build + go test PASS (audit 1.066s). ADR-0019 §6 변경 이력 row 추가. 추적성 영향 N/A. | current session |
| 2026-05-19 | sprint `claude/work_260519-y` (PR #193) — **codex review hotfix #10 일괄 흡수 (PR #189~#192 의 P1×3 + P2×2)**. P1-A `internal/audit/keycloak_event_puller.go` `pullUserEvents` 의 skip-only page (REFRESH_TOKEN 만 들어온 페이지) 에서도 cursor advance — skip type 분기 전에 `latestTime`/`latestHash` 갱신 (이전: skip type 만 들어오면 cursor 멈춰 다음 tick 무한 같은 페이지 재pull 위험). P1-B `loadCursor` 가 row 미존재 시 in-memory init 후 즉시 `UpsertEventCursor` 호출하여 영구 seed (이전: 첫 poll 빈 결과 시 다음 tick now() 재초기화 → tick 사이 발생 event 영구 누락). P1-C `keycloak_operations.md §8.6.1` 사전 조건 표에 'event_cursors row seed = backend 자동' 항목 추가 (P1-B fix 와 정합). P2-D `hashUserEvent` (time/type/userID/ipAddr/clientID/realmID/sessionId) + `hashAdminEvent` (time/resourceType:operationType/resourcePath/authUserID/authClientID/authIPAddr/realmID) 7-tuple 확장 — burst 동시 ms event 의 client/realm 차이 hash 반영해 store-level UNIQUE INDEX (sprint -w 의 migration 000032) dedup 으로 audit 손실 회피. P2-E `keycloak_operations.md §8.6.2` Expiration row wording 정정 — '짧아도 무관' → '운영 outage tolerance + recovery time 보다 길게 (권장 7d 이상)'. 신규 test 4건 — `keycloak_event_puller_test.go` 의 LoadCursor_NotFound_PersistsSeed (P1-B seed 영구화 검증) + SkipOnlyPage_AdvancesCursor (P1-A skip type 만 들어와도 cursor advance) + HashUserEvent_DistinguishesByClientID (P2-D client/sessionId 다를 때 hash distinct) + HashAdminEvent_DistinguishesByAuthClient (admin hash 도 동일 검증). codex P2 #4 (PR #189 same-ms boundary) 는 sprint -u Stage 3 (`580c0db` `evTime.Before`) 에서 이미 해소 — false positive 코멘트 응답으로 처리. ADR-0019 §6 변경 이력 row 추가 + keycloak_operations.md §11 변경 이력 row 추가. go build + go test 모두 PASS (audit 0.932s). 추적성 영향 N/A (코드 fix + docs wording 정정). | current session |
| 2026-05-19 | sprint `claude/work_260519-x` (PR #192) — **ADR-0019 §5.3 (9) audit event listener Phase 2 PR-E (운영 SOP)** — sprint -e design 의 실 구현 5단계 마지막. `docs/setup/keycloak_operations.md` §8.6 신규 9 sub-section: §8.6.1 활성화 사전 조건 (migration 000031 + 000032 + service account view-events 권한 + Keycloak Events 활성화) / §8.6.2 Keycloak admin console 설정 (User+Admin events Save ON + Expiration 7d + Include representation OFF privacy) / §8.6.3 backend env 3종 운영 권장값 (`DEVHUB_KEYCLOAK_EVENT_LISTENER_ENABLED` / `_INTERVAL` 30~60s / `_MAX_EVENTS` 500) + 활성화 후 startup log 확인 / §8.6.4 Prometheus dashboard 4 panel + PromQL 예시 3건 (rate / max lag / increase) / §8.6.5 알람 3종 (`cursor_lag_high` 600s for 5m / `cursor_lag_critical` 3600s for 10m / `pull_error_rate` 5건/5분) + JWKS metric 동시 가시화 권장 / §8.6.6 audit_logs dedup 동작 확인 SQL 3건 (최근 emit + 중복 검사 + cursor 위치) / §8.6.7 트러블슈팅 5 케이스 (audit row 없음 / cursor lag / 중복 등장 / pull error / unknown action 빈번) + 대응 절차 / §8.6.8 disable·rollback 절차 (env false + cursor 보존, migration rollback 주의) / §8.6.9 sub-carve 3 (SPI push 전환 / cold storage archival / dashboard JSON 자산). §9.2 audit log 통합 carve → resolved 표기로 갱신 + §10 잔여 carve out 표의 audit 항목 strikethrough + Phase 2 PR-E resolved 표기. §11 변경 이력 row 추가. ADR-0019 §5.3 (9) row + 변경 이력 row 추가 — **§5.3 (9) Phase 2 모든 carve (PR-B~PR-E) resolved**. 추적성 영향 N/A (docs-only, code/schema 변경 없음). | current session |
| 2026-05-19 | sprint `claude/work_260519-w` (PR #191) — **ADR-0019 §5.3 (9) audit event listener Phase 2 PR-D (store-level dedup)** — sprint -e design 의 실 구현 5단계 중 3번째 (PR-B~PR-D 모든 backend resolved). `backend-core/migrations/000032_audit_logs_source_event_id.{up,down}.sql` 신규 — `audit_logs.source_event_id TEXT` 컬럼 + partial UNIQUE INDEX `audit_logs_source_event_id_uniq` (source_type, source_event_id) WHERE source_event_id IS NOT NULL. partial 이유: 기존 audit_logs row (commands flow / Kratos hook / Gitea webhook) 는 source_event_id NULL 이라 unique 제약 영향 없음, Keycloak event listener 처럼 deterministic id 부여 emitter 만 dedup 대상. `internal/domain/domain.go` `AuditLog` struct 에 `SourceEventID string` 필드 추가. `internal/store/audit_logs.go` `CreateAuditLog` INSERT 가 `ON CONFLICT (source_type, source_event_id) WHERE source_event_id IS NOT NULL DO NOTHING` + ErrNoRows 분기 시 `getAuditLogBySourceEventID` lookup helper 로 기존 row 반환 (caller 가 idempotency 의식 없이 사용 가능). `ListAuditLogs` SELECT 도 `COALESCE(source_event_id, '')` 포함. `internal/audit/keycloak_event_puller.go` `AuditEmitter` 시그니처 확장 — `(ctx, action, targetType, targetID, sourceEventID, payload)`. `pullUserEvents` / `pullAdminEvents` 가 자체 SHA256 evHash 를 sourceEventID 로 전달. `backend-core/main.go` `buildKeycloakEventAuditEmitter` 가 `entry.SourceEventID = sourceEventID` set. 기존 audit/integration_test 의 emitter callback signature 정정 (6 호출 site) + sourceEventID 가 nonempty + tick1 LOGIN ≠ tick3 LOGOUT hash 검증 추가. 신규 store integration test 2건 `audit_logs_dedup_integration_test.go` (DEVHUB_TEST_DB_URL skip pattern): (1) 동일 (source_type, source_event_id) INSERT 2회 → audit_logs 1개 row + 기존 row 반환 + ActorLogin/Payload 변경 무시, (2) 빈 source_event_id INSERT 2회 → partial WHERE 가 NULL row 제외 → 2개 row 정상. go build + go test 모두 PASS (audit / store / domain / httpapi). ADR-0019 §5.3 (9) row + 변경 이력 row 추가 — **§5.3 (9) Phase 2 backend (PR-B~PR-D) 모든 carve resolved**. 잔여 = PR-E keycloak_operations.md §8.6 운영 SOP. | current session |
| 2026-05-19 | sprint `claude/work_260519-v` (PR #190) — **ADR-0019 §5.3 (9) audit event listener Phase 2 PR-C (wire + metric + integration test)** — sprint -e design 의 실 구현 5단계 중 두 번째. `backend-core/internal/domain/domain.go` 의 `AuditSourceType` enum 에 `AuditSourceKeycloakEvent = "keycloak_event"` 신규 (audit_logs `source_type` 정합). `backend-core/internal/audit/metrics.go` 신규 — DREQ + HomeLab adapters metric 패턴 정합 (`sync.Once init` + `registerCollector` + observe helpers). Prometheus metric 3종 — `devhub_keycloak_events_processed_total` CounterVec[kind=user/admin, action] + `devhub_keycloak_event_cursor_lag_seconds` GaugeVec[cursor_key=keycloak.events/keycloak.events.admin] + `devhub_keycloak_event_pull_errors_total` CounterVec[kind=user/admin]. `internal/audit/keycloak_admin_adapter.go` 신규 — `HTTPAPIUserEvent` + `HTTPAPIAdminEvent` (httpapi 측 nested `AuthDetails` 의 평탄화 mirror) + `HTTPAPIEventLister` interface + `NewHTTPAPIEventListerAdapter` (audit ← httpapi 순방향 의존 유지). `backend-core/main.go` cron loop wire — `DEVHUB_KEYCLOAK_EVENT_LISTENER_ENABLED` gate + `kratosAdmin.(*httpapi.KeycloakAdminClient)` 검증 + `keycloakAdminEventLister` thin wrapper (httpapi.KeycloakAdminClient → audit.HTTPAPIEventLister, `AuthDetails` 평탄화) + `buildKeycloakEventAuditEmitter` (actor login resolver — payload `user_id` 우선 / fallback `auth_user_id` / fallback `system:keycloak-event`, best-effort like DREQ intake token cron) + goroutine `RunKeycloakEventPuller` 기동. `backend-core/internal/config/config.go` 신규 3 env — `DEVHUB_KEYCLOAK_EVENT_LISTENER_ENABLED` (bool gate) + `_INTERVAL` (default 30s) + `_MAX_EVENTS` (default 500). `internal/audit/keycloak_event_puller.go` 의 `pullOnce` 정정 — 한쪽 (user 또는 admin) error 가 다른 쪽 emit 을 막지 않게 firstErr 패턴 + `ObservePullError(kind)` emit. 매 emit 직후 `ObserveEventProcessed(kind, action)` + cursor advance/no-advance 직후 `ObserveCursorLag(cursor_key, lagSeconds)` 호출. 신규 test 5건 — `keycloak_event_integration_test.go` 1건 (3-tick end-to-end — tick1 user LOGIN + admin USER:CREATE emit / tick2 dateFrom inclusive boundary dedup / tick3 신규 LOGOUT emit + payload key 검증 + cursor advance 검증) + `metrics_test.go` 4건 (InitMetrics idempotent / counter increment / gauge set overwrite / pull-error counter). `go build ./...` + `go test ./internal/audit/... ./internal/httpapi/... ./internal/store/... ./internal/config/...` 모두 PASS. **잔여 carve = PR-D `audit_logs` schema dedup UNIQUE constraint (source_event_id) + PR-E [keycloak_operations.md](../setup/keycloak_operations.md) §8.6 운영 SOP**. ADR-0019 §5.3 (9) row + 변경 이력 row 추가. | current session |
| 2026-05-20 | sprint `claude/work_260519-ad` (PR TBD) — **Kratos 잔재 residual cleanup (ADR-0019 정합)** — PR #167 (Keycloak-only refactor) 머지 후 backend 에 wire 안 된 채로 잔존하던 Kratos 흐름을 일괄 정리. (1) **backend 삭제** (11 파일): `account_password.go` + `kratos_login_client.go` + `kratos_settings_client.go` + `kratos_session_cache.go` + `kratos_admin_client.go` + `password_auth_types.go` + 각 `_test.go`. `/api/v1/account/password` endpoint + `PasswordAuthClient` interface + `IDPSessionCache` + `IdentityAdminClient` (Kratos admin impl) 모두 제거. (2) **router.go / permissions.go**: `RouterConfig.KratosLogin` + `RouterConfig.IDPSessionCache` field + lazy init + `/account/password` route + RBAC bypass entry 일괄 제거. (3) **identity_resolver.go 신규** (kratos_identity_resolver.go 의 generic 부분 보존): `resolveIdPSubject` method + `ErrIdentityNotFound` (keycloak_admin_client.go 로 이동). test 는 `git mv` 로 history 보존. (4) **identity_admin_mock_test.go 신규** (test-only): `MockIdentityAdmin` fake 를 production 파일 (삭제된 kratos_admin_client.go) 에서 분리. (5) **frontend 정리**: `account/page.tsx` 의 password form 전체 제거 + Keycloak Account Console (`${OIDC_ISSUER_URL}/account/`) redirect link 로 대체. `account.service.ts` 의 `updateMyPassword` + `SettingsFlowError` + `SettingsFlowErrorCode` + `mapApiErrorToSettingsFlowError` 제거 (admin 메서드 5개는 보존). `account.service.test.ts` 삭제 (전체가 password test). `tests/e2e/account.spec.ts` 의 password round-trip test 제거 + TC-ACC-KEYCLOAK-CONSOLE-01 (외부 link 검증) 신규. (6) **변수명 / 주석 generic 화**: main.go `kratosAdmin` → `idpAdmin` (KeycloakAdminClient 인스턴스 변수), `kratosID` → `idpID`. main_test.go `kratosAdminFake` → `idpAdminFake`. domain.go `AuditSourceKratos` enum 은 historical audit_logs row decode 위해 **보존** + comment 만 ADR-0001 superseded 명시. accounts_admin.go 의 audit action 이름 (`account.issue.kratos_failed` / `account.create.kratos_identity` / `account.status.kratos_state` / `account.delete.kratos`) 도 historical DB row 정합 위해 **보존**, comment 만 generic. 응답 string ("kratos identity not found" → "idp identity not found"), log prefix (`[kratos-cache]` → `[idp-cache]`), audit details key `kratos_id` 는 보존 (DB historical). request_context.go / me.go / organization.go / users_units.go 의 Kratos 잔재 주석 generic 화. (7) **endpoints.ts**: dead export `KRATOS_ADMIN_URL_SERVER` 제거 (frontend code 어디서도 import 안 됨). e2e global-setup.ts + fixtures.ts 의 Kratos admin API 직접 호출은 sprint -m (PR #181) design `docs/infrastructure/keycloak-idp/e2e_migration.md` 따른 별도 carve 로 보존. (8) **검증**: backend `go build` + `go test ./...` 모두 PASS. frontend `npm run build` + `npm test` (8 test files / 34 tests) PASS. 추적성 영향 — IMPL: account_password / kratos_login_client / kratos_settings_client / kratos_session_cache / kratos_admin_client / password_auth_types 제거 (대체 = Keycloak Account Console 외부 redirect). API: `/api/v1/account/password` deprecated (제거). TC: TC-ACC-01/02/03 삭제 + TC-ACC-PROFILE-01 (유지) + TC-ACC-KEYCLOAK-CONSOLE-01 (신규). | current session |
| 2026-05-20 | sprint `gemini/work_260520-a-209-accounts-cleanup` — **ADR-0020 sub-carve B (frontend)** — `account.service.ts` (86 lines) 파일 삭제 + `MemberTable.tsx` 의 admin actions ('Issue Account' / 'Force Reset Password' / 'Revoke Account') + 관련 modal/ActionMenu 로직 제거 (104 lines) + `AdminSettingsUsersPage.tsx` 에 system_admin 안내 banner + Keycloak Admin Console 외부 link 추가 + `endpoints.ts` 의 `getKCAdminConsoleUrl()` helper 신규 (OIDC issuer URL 에서 realm 추출 + `/admin/{realm}/console/` 구성) + e2e `admin-users-crud.spec.ts` 의 `TC-USR-CRUD-03` (system_admin 액션 3종 메뉴 노출 test) 제거. 추적성 영향: API-25 `/api/v1/accounts/*` deprecated (재확인). TC-USR-CRUD-03 폐기. backend `go test` + frontend `npx tsc --noEmit` + Playwright CI all green. **sub-carve B-frontend = Gemini 영역 done** — ADR-0020 §4.1 sub-carve B (frontend) row finalize. | current session |
| 2026-05-21 | sprint `gemini/housekeeping-260520-status-update` — **housekeeping #7** — main flat memory (state/handoff/backlog) 갱신. 금일 8 PR (#245~#253) 진척 사항 반영 및 ADR-0020 sub-carve B-frontend/F 종결 명문화. | current session |
| 2026-05-20 | sprint `claude/work_260520-q-215-hrdb-cancel` (issue #215 close, not planned) — **HRDB ETL cron 폐기 정합**. 사용자 결정 (외부 Keycloak 시나리오 채택, HR ↔ Keycloak sync 책임이 외부 IdP 팀으로 이관) 따라 정합 작업: (1) `scripts/hrdb_etl_sync.sh` header 에 deprecation banner 추가 (대체 흐름 = sub-carve C event listener). (2) `docs/infrastructure/keycloak-idp/offboarding_immediacy.md` 의 §3.1 Phase 1 cron 부분 deprecation banner. (3) `docs/adr/0019-keycloak-only-idp.md` §5.3 (7) row 를 'design resolved' → 'decision shift' 로 갱신 + 변경 이력 row 추가. (4) `docs/planning/release_v1_roadmap.md` §3.2 P1-4 strikethrough + §4.2 v1.1 milestone P1-4 제외 정정. 추적성 영향 — IMPL: scripts/hrdb_etl_sync.sh deprecated (historical reference 보존). API: 변경 없음 (외부 Keycloak 책임). v1.0 release gate 잔여 2건 (#210 + #214). | current session |
| 2026-05-21 | sprint `gemini/work_260520-b-210-ui-polish` (PR #248) — **E2E selector fix + UI Polish verification**. E2E test failures on `account profile` and `sign out` dropdown items resolved by updating Playwright locators to use `menuitem` instead of `button` to match the native accessibility tree (`role="menuitem"`). All Playwright tests passed. | current session |
| 2026-05-26 | sprint `codex/work_260526-b-project-registration-management-enhancement` (본 PR) — **DREQ 의뢰 접수 알림 및 하이브리드 프로젝트 생성 연계 기획/설계/구현 고도화**. `project_creation_dreq_notification_concept.md`를 active로 정식 개편, `requirements.md` 및 `system_usecases.md`에 실시간 DREQ 알림 배지 및 Promote 프리필 연계 조항 추가(`REQ-FR-DREQ-012..013`, `UC-DREQ-11..12`). `traceability/report.md` 에 DREQ 도메인 매핑 테이블 갱신 및 신규 E2E 테스트 `TC-DREQ-NOTI-01..03` 매핑 추가. | current session |
| 2026-06-02 | sprint `main` (RBAC 고도화 문서 동기화) — `docs/domain/rbac-permissions/requirements.md` 보완에 따라 추적성 정합 갱신: REQ 범위를 `REQ-RBAC-001..009` → `REQ-RBAC-001..016`으로 확장 반영 (신규 `REQ-RBAC-010A`, `012A`, `015`, `016` 포함). §2.1 Requirements 인덱스, §3.1 Core 도메인 `rbac-permissions` row, Phase 4 재구성 주석의 임시 ID 범위를 동일 값으로 동기화. | current session |
| 2026-06-02 | sprint `main` (RBAC 구현 준비) — RBAC 고도화 후속 추적성 검토 반영: UC 범위를 `UC-RBAC-01..07`으로 확장 동기화, rbac-permissions 도메인 TC 카탈로그 `docs/domain/rbac-permissions/test_cases.md` 신규 발급(ROLE-SYNC/DRIFT/ROW-READ/LOGOUT/CODE/TRACE 포함), API/Architecture 문서를 Two-Dimensional RBAC 기준으로 갱신하고 구현 준비 문서 `docs/planning/rbac-hardening-implementation-readiness-20260602.md` 추가. | current session |
| 2026-06-02 | sprint `main` (RBAC + DREQ E2E 정합) — 로컬 macOS E2E 재검증 중 발견된 DREQ cleanup warning 제거. `frontend/tests/e2e/dev-requests.spec.ts` 의 best-effort cleanup fetch 를 backend API 절대경로(`/api/v1/dev-request-tokens/:id`) 기준으로 정규화했고, traceability 누락이던 `TC-DREQ-PROMOTE-PROJ-01` 를 `docs/domain/dev-request/test_cases.md` 및 §2.6/§3 dev-request row 에 동기화. | current session |
| 2026-06-05 | sprint `gemini/work_260605-b-debug-request-button` — 의뢰 수신 기능 테스트를 위한 디버그 전용 DREQ 생성 버튼(Header) 및 백엔드 미들웨어(RequireIntakeToken) 인증 우회 추가. 추적성 영향: N/A (개발/디버그 전용 편의 기능). | current session |
