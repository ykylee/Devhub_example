-- 000048: DOWN — No-op. Up migration is also no-op since initial schema
-- (baseline reset #477) already uses platform naming natively.

-- No-op. Initial schema (baseline reset #477) already uses platform naming natively.
SELECT 1;
